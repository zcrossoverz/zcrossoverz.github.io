/* INCIDENTS — Database. Schema: xem i1. */

INCIDENTS.register({
  id: "inc-pool-exhaustion", sev: "P1", domain: "Database", skills: ["data-pool", "spr-tx"], time: "20 phút",
  title: "Giờ cao điểm: mọi endpoint timeout, DB lại đang... rảnh",
  teaser: "HikariCP kêu cứu, 147 request xếp hàng chờ connection. CPU database: 15%. Ai đang giam connection?",
  alert: "<b>[Sentry]</b> 12:31 — <code>order-service</code> lỗi hàng loạt: <code>SQLTransientConnectionException: HikariPool-1 - Connection is not available, request timed out after 30000ms</code>. Bắt đầu ~12:15, trùng khung giờ trưa. Release gần nhất (hôm qua): tích hợp <b>đối tác giao hàng GHTK</b> vào luồng tạo đơn.",
  artifacts: [
    { label: "📈 Hikari metrics", body: "<pre>hikaricp.connections            = 20   (maximumPoolSize)\nhikaricp.connections.active     = <span class='hl'>20</span>   (100% đang bị mượn)\nhikaricp.connections.idle       = 0\nhikaricp.connections.pending    = <span class='hl'>147</span>  (thread đang xếp hàng chờ mượn)\nhikaricp.connections.usage (avg thời gian mượn 1 connection):\n   hôm kia:  38 ms\n   hôm nay:  <span class='hl'>4.720 ms</span>  ← connection bị giữ lâu gấp ~120 lần\n\nDatabase (Postgres): CPU 15% · active queries: 4 · slow query log: trống</pre>" },
    { label: "🧵 Thread dump", body: "<pre>\"http-nio-8080-exec-12\" RUNNABLE                       ← 20 thread giống thế này\n   at java.net.SocketInputStream.socketRead0(Native Method)\n   at org.apache.http.impl.io...readLine(...)\n   at com.corp.shipping.<span class='hl'>GhtkClient.createShipment</span>(GhtkClient.java:52)\n   at com.corp.order.OrderService.<span class='hl'>createOrder</span>(OrderService.java:71)\n   at ...$Proxy142.createOrder(Unknown Source)             ← proxy @Transactional\n\n\"http-nio-8080-exec-30\" TIMED_WAITING                   ← ~150 thread giống thế này\n   at com.zaxxer.hikari.pool.HikariPool.<span class='hl'>getConnection</span>(HikariPool.java:180)\n   at ...</pre><p style='font-size:12.5px;color:var(--text-muted)'>20 thread RUNNABLE trong call HTTP tới GHTK; ~150 thread chờ mượn connection.</p>" },
    { label: "📄 Code luồng tạo đơn", body: "<pre>@Transactional                                        // ← mở transaction = MƯỢN connection\npublic OrderResult createOrder(OrderRequest req) {\n    Order order = orderRepo.save(buildOrder(req));    // dùng DB: hợp lý\n    inventoryRepo.reserve(req.getItems());            // dùng DB: hợp lý\n    <span class='hl'>ShipmentInfo ship = ghtkClient.createShipment(order);</span>  // HTTP 3-8s, KHÔNG timeout!\n    order.setTrackingCode(ship.getCode());\n    return toResult(order);                           // commit ở đây → connection mới được trả\n}</pre>" }
  ],
  steps: [
    { q: "Metrics nào cho manh mối ĐẮT nhất về bản chất sự cố?",
      opts: [
        { t: "Thời gian mượn connection trung bình nhảy 38ms → 4.720ms — connection không thiếu, nó đang bị GIỮ quá lâu", s: 2, why: "Pool cạn có hai họ nguyên nhân: mượn nhiều hơn (tải tăng) hoặc giữ lâu hơn. Usage time ×120 + DB rảnh chỉ thẳng họ thứ hai — và khớp luôn với release 'tích hợp GHTK' hôm qua." },
        { t: "pending = 147 — cần tăng maximumPoolSize lên gấp đôi ngay", s: 0, why: "147 người chờ là HẬU QUẢ. Khi connection bị giữ 4.7s/lượt, pool 40 hay 80 cũng sẽ cạn theo (chỉ chậm hơn vài phút) — và DB phải gánh thêm connection thật. Đọc con số nào là nguyên nhân, con số nào là nạn nhân." },
        { t: "DB CPU 15% — cần bật slow query log chi tiết để tìm query chậm", s: 1, why: "Hướng kiểm hợp lệ nhưng bằng chứng ĐÃ có: slow query log trống, active queries = 4. DB đang thanh minh 'không phải tôi' khá rõ — đi tiếp hướng này là lịch sự với DB nhưng phí thời gian vàng." },
        { t: "Trùng giờ trưa — nguyên nhân là traffic cao, chờ qua giờ cao điểm", s: 0, why: "Giờ trưa hôm kia cũng cao mà usage chỉ 38ms. Traffic là MỒI LỬA làm lộ vấn đề, không phải vấn đề. Đổ cho tải mà không giải thích được delta so với hôm kia là chẩn đoán lười." }
      ] },
    { q: "Thread dump: 20 thread RUNNABLE ở <code>GhtkClient.createShipment</code> bên trong <code>OrderService.createOrder</code> có <code>@Transactional</code>. Suy luận đúng?",
      opts: [
        { t: "Mỗi request tạo đơn mượn 1 connection từ lúc mở transaction và chỉ trả khi commit — mà giữa chừng nó đứng chờ HTTP GHTK 3-8s (không timeout) → 20 connection bị giam trong các call HTTP", s: 2, why: "Đây là mắt xích chốt: @Transactional = giữ connection trọn vòng đời method. Nhét network call vào giữa nghĩa là tốc độ trả connection của bạn = tốc độ của đối tác. 20 connection ÷ 4.7s/lượt ≈ 4 đơn/giây là trần — đúng con số đang thấy." },
        { t: "GhtkClient bị leak connection HTTP riêng của nó", s: 1, why: "Có thể tồn tại song song (client HTTP cũng cần pool + timeout riêng) nhưng không giải thích được vì sao POOL DB cạn — hai loại connection khác nhau. Giữ giả thuyết này ở hàng chờ, mắt xích chính đã rõ hơn." },
        { t: "Proxy @Transactional bị lỗi tạo transaction lồng nhau", s: 0, why: "Dump không có dấu hiệu nested transaction, và propagation mặc định REQUIRED tham gia transaction sẵn có chứ không lồng. Đổ cho 'phép thuật proxy hỏng' khi chưa có bằng chứng là né trách nhiệm thiết kế." },
        { t: "20 thread RUNNABLE chứng tỏ code đang chạy tốt, chỉ là chậm", s: 0, why: "RUNNABLE tại socketRead0 = 'đang đợi byte từ mạng' — nó chiếm connection + thread mà không tiến triển. Trạng thái thread phải đọc KÈM stack frame, không đọc mỗi nhãn." }
      ] }
  ],
  cause: { q: "Nguyên nhân gốc?",
    opts: [
      { t: "Call HTTP chậm (không timeout) nằm BÊN TRONG @Transactional — connection DB bị giam suốt thời gian chờ đối tác; giờ trưa đông đơn → 20 connection giam hết → pool cạn, mọi endpoint cần DB chết chùm", ok: true, why: "Bài học transaction số 1: transaction chỉ được bọc THAO TÁC DB, càng ngắn càng tốt. Mọi thứ chậm-và-không-DB (HTTP, gửi mail, tính toán nặng) phải nằm ngoài. Không timeout làm mọi thứ tệ thêm một bậc: trần chờ = vô hạn." },
      { t: "maximumPoolSize = 20 quá nhỏ cho lượng user hiện tại", ok: false, why: "Hôm kia cũng 20 connection phục vụ ngon lành (usage 38ms → throughput lý thuyết ~500 req/s). Kích thước pool chưa bao giờ là biến số thay đổi giữa hai ngày — code mới chèn HTTP vào transaction mới là." },
      { t: "GHTK API quá chậm — lỗi phía đối tác, chờ họ sửa", ok: false, why: "Đối tác chậm là ĐIỀU KIỆN MÔI TRƯỜNG phải thiết kế cho nó (timeout, tách transaction, hàng đợi) — không phải nguyên nhân gốc của việc pool của BẠN cạn. 'Chờ họ sửa' = outsource số phận hệ thống mình." },
      { t: "Postgres không đủ tài nguyên xử lý transaction dài", ok: false, why: "DB CPU 15%, 4 active query — nó đang ngồi chơi. Transaction 'dài' ở đây dài vì CHỜ ở app, DB chỉ giữ hộ một connection idle-in-transaction. Bằng chứng minh oan cho DB ngay từ artifact đầu." }
    ] },
  fix: { q: "Sửa thế nào cho đúng tầng?",
    opts: [
      { t: "Tách call GHTK ra khỏi transaction: commit đơn trước (trạng thái PENDING_SHIPMENT) → gọi GHTK ngoài transaction có connect/read timeout → update tracking code trong transaction MỚI, ngắn; kèm retry/outbox cho bước gọi đối tác", s: 2, why: "Transaction ngắn lại còn vài chục ms, connection quay vòng nhanh, pool 20 lại thừa thãi. Trạng thái trung gian PENDING là cái giá nhỏ và trung thực — nghiệp vụ giao hàng vốn dĩ bất đồng bộ." },
      { t: "Tăng maximumPoolSize lên 100 + tăng connectionTimeout lên 60s", s: 0, why: "100 connection bị giam 5s/cái vẫn cạn ở tải cao hơn chút; DB gánh 100 session; còn timeout 60s nghĩa là user chờ 60s mới nhận lỗi. Cả hai núm vặn đều chỉnh HẬU QUẢ, để nguyên cỗ máy sản xuất vấn đề." },
      { t: "Thêm read timeout 5s cho GhtkClient, giữ nguyên vị trí trong transaction", s: 1, why: "Bịt được kịch bản tệ nhất (treo vô hạn) — đáng làm NGAY như hotfix. Nhưng transaction vẫn dài 5s khi GHTK chậm → pool vẫn nghẹt ở tải cao. Đây là bước 1 tốt, chưa phải lời giải." },
      { t: "Chuyển sang gọi GHTK bằng thread riêng bên trong transaction cho song song", s: 0, why: "Transaction vẫn phải CHỜ kết quả trước khi commit (join) → connection vẫn bị giam y nguyên, giờ cộng thêm phức tạp thread. Chuyển chỗ chờ không xoá việc chờ — phải chuyển RANH GIỚI transaction." }
    ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> release tích hợp GHTK đặt HTTP call (3-8s, không timeout) vào giữa @Transactional → thời gian giữ connection ×120 → 20 connection giam trong call HTTP giờ trưa → 147 thread xếp hàng, mọi endpoint cần DB timeout → DB ngồi xem với CPU 15%.</p><p><b>Ba bài học mang đi:</b> (1) <em>@Transactional = mượn connection tới khi commit</em> — soi mọi thứ chậm nằm trong nó (HTTP, mail, file). (2) Chữ ký chẩn đoán: <em>pool cạn + DB nhàn + usage time tăng vọt</em> = connection bị giam, không phải thiếu. (3) Mọi network call phải có <em>timeout</em>; bật <code>leakDetectionThreshold</code> của Hikari để bắt kẻ mượn dai từ sớm.</p>",
  prove: "<p>Tái hiện bằng 1 test Spring Boot (~15 phút):</p><pre>// application.yml (test):  spring.datasource.hikari.maximum-pool-size: 2\n@Service class DemoService {\n  @Transactional\n  public void orderWithHttpInside() throws Exception {\n      repo.save(new Order());\n      Thread.sleep(5000);            // gia lap call GHTK cham\n  }\n}\n// Test: ban 5 luong goi orderWithHttpInside() dong thoi\n// → luong 3+ vang: \"Connection is not available, request timed out\"</pre><p><b>Cách xác minh:</b> (1) với pool=2 và 5 luồng, các luồng sau nhận đúng lỗi HikariPool như production; (2) chuyển <code>Thread.sleep</code> RA NGOÀI method @Transactional (tách 2 transaction) rồi chạy lại — hết lỗi dù vẫn 5 luồng; (3) bật <code>logging.level.com.zaxxer.hikari=DEBUG</code> để nhìn active/idle nhảy theo thời gian thật.</p>",
  refs: [["HikariCP – pool sizing & leak detection", "https://github.com/brettwooldridge/HikariCP"], ["Vlad Mihalcea – transaction boundaries", "https://vladmihalcea.com/spring-transaction-best-practices/"]]
});

INCIDENTS.register({
  id: "inc-db-deadlock", sev: "P2", domain: "Database", skills: ["data-tx"], time: "15–20 phút",
  title: "23:00 mỗi đêm: 'deadlock detected' rải rác trong log",
  teaser: "Batch job đối soát và API hoàn tiền cùng đụng 2 bảng. Postgres tự phá deadlock nhưng lỗi cứ lặp lại từng đêm.",
  alert: "<b>[Log-based alert]</b> 23:04 — <code>reconcile-job</code> và <code>refund-api</code> cùng văng lỗi lặp lại: <code>org.postgresql.util.PSQLException: ERROR: deadlock detected</code>. Tự xuất hiện từ khi job đối soát được đổi lịch về 23:00 (trùng khung khách hoàn tiền cuối ngày).",
  artifacts: [
    { label: "📜 Postgres log", body: "<pre>23:04:11 ERROR:  deadlock detected\n23:04:11 DETAIL:\n  Process <span class='hl'>4211</span> waits for ShareLock on transaction 88123;\n           blocked by process <span class='hl'>4788</span>.\n  Process <span class='hl'>4788</span> waits for ShareLock on transaction 88101;\n           blocked by process <span class='hl'>4211</span>.\n  Process 4211: <span class='hl'>UPDATE wallets SET balance = balance + $1 WHERE user_id = $2</span>\n                (ứng dụng: reconcile-job)\n  Process 4788: <span class='hl'>UPDATE ledger  SET status = 'REFUNDED' WHERE tx_id = $1</span>\n                (ứng dụng: refund-api)\n23:04:11 HINT:  See server log for query details.\n23:04:11 STATEMENT:  UPDATE wallets ...</pre>" },
    { label: "📄 Code hai luồng", body: "<pre>// reconcile-job (23:00 hang dem) — voi MOI giao dich lech:\n@Transactional\nvoid fixDiscrepancy(Discrepancy d) {\n    ledgerRepo.markResolved(d.txId());        // ① khoá dòng LEDGER truoc\n    walletRepo.adjustBalance(d.userId(), d.delta()); // ② roi khoá dòng WALLETS\n}\n\n// refund-api (user bam hoan tien):\n@Transactional\nvoid refund(RefundRequest r) {\n    walletRepo.adjustBalance(r.userId(), r.amount()); // ① khoá dòng WALLETS truoc\n    ledgerRepo.markRefunded(r.txId());                // ② roi khoá dòng LEDGER\n}</pre><p style='font-size:12.5px;color:var(--text-muted)'>Hai luồng đụng cùng cặp (wallets, ledger) của cùng user — theo thứ tự NGƯỢC nhau.</p>" }
  ],
  steps: [
    { q: "Đọc log Postgres: process 4211 chờ 4788, 4788 chờ 4211. Bước khai thác bằng chứng đúng nhất tiếp theo?",
      opts: [
        { t: "Lấy 2 câu STATEMENT trong log, lần về 2 đoạn code tương ứng và liệt kê THỨ TỰ các dòng/bảng mà mỗi transaction khoá", s: 2, why: "Deadlock là bài toán THỨ TỰ. Log đã cho 2 nhân chứng + 2 câu khai — việc của bạn là dựng lại trình tự khoá của từng bên. Làm đúng bước này thì nguyên nhân tự lộ, không cần đoán." },
        { t: "Bật log_lock_waits và deadlock_timeout chi tiết hơn rồi chờ đêm sau", s: 1, why: "Thu thêm bằng chứng là phản xạ tốt khi log hiện tại MỎNG — nhưng log này đã đủ dày (đủ 2 câu lệnh, 2 process). Chờ thêm một đêm là trả giá một đêm lỗi cho dữ liệu mình đã có." },
        { t: "Tăng deadlock_timeout để Postgres đợi lâu hơn trước khi tuyên bố deadlock", s: 0, why: "Deadlock THẬT thì đợi thêm bao lâu cũng không tự gỡ — vòng chờ là mãi mãi. Tăng timeout chỉ làm nạn nhân giữ khoá lâu hơn, kéo theo nhiều transaction khác kẹt sau lưng." },
        { t: "Chuyển isolation level lên SERIALIZABLE cho an toàn", s: 0, why: "SERIALIZABLE không chống deadlock (thêm xung đột phải retry là khác); và 'nâng isolation khi gặp lỗi lock' là phản xạ sai phổ biến — bài này là thứ tự khoá, không phải mức cô lập." }
      ] }
  ],
  cause: { q: "Nguyên nhân gốc?",
    opts: [
      { t: "Hai luồng khoá cùng cặp tài nguyên theo thứ tự NGƯỢC nhau: job đi ledger→wallets, API đi wallets→ledger — trùng user + trùng khung giờ là thành vòng chờ", ok: true, why: "Điều kiện đủ của deadlock: khoá giữ + chờ vòng tròn. Thứ tự ngược trên cùng cặp dòng chính là cách tạo vòng ổn định nhất. Đổi lịch job về 23:00 chỉ là que diêm — xăng đã đổ sẵn trong code từ trước." },
      { t: "Postgres yếu phần xử lý khoá, nên đổi sang DB khác", ok: false, why: "Postgres phát hiện vòng chờ và hy sinh một nạn nhân trong ~1s — đó là hành vi ĐÚNG chuẩn mọi RDBMS. MySQL/Oracle gặp thứ tự khoá ngược cũng deadlock y hệt. Đổi DB mang theo đúng bug này sang nhà mới." },
      { t: "Batch job chạy quá nhanh, cần thêm sleep giữa các bản ghi", ok: false, why: "Sleep làm HẸP xác suất trùng nhịp chứ không xoá vòng chờ — và làm job chậm đi vô ích. Giải pháp xác suất cho bài toán cấu trúc luôn thua vào đêm đông đơn nhất." },
      { t: "Thiếu index làm UPDATE khoá nhiều dòng hơn cần thiết", ok: false, why: "Giả thuyết nghe có nghề (thiếu index gây lock lan rộng là chuyện thật!) nhưng bằng chứng không ủng hộ: cả hai UPDATE đều theo khoá chính/unique (user_id, tx_id) — khoá đúng 1 dòng. Đừng để kiến thức đúng đè lên bằng chứng." }
    ] },
  fix: { q: "Sửa gốc rễ + phòng tái diễn?",
    opts: [
      { t: "Thống nhất MỘT thứ tự khoá toàn codebase (vd luôn wallets trước, ledger sau — ghi thành quy ước trong tài liệu); đồng thời app bắt mã lỗi deadlock (40P01) và retry transaction như lỗi tạm thời", s: 2, why: "Không có vòng nếu mọi người leo thang cùng chiều — sửa một trong hai method là vòng biến mất. Retry là tầng bảo hiểm cho các ca hiếm còn lại (deadlock vẫn có thể đến từ gap lock, FK...). Quy ước phải VIẾT RA, không nằm trong đầu ai đó." },
      { t: "Cho reconcile-job chạy 03:00 thay vì 23:00 để tránh giờ hoàn tiền", s: 1, why: "Giảm tần suất ngay tối nay — hotfix chấp nhận được. Nhưng thứ tự khoá ngược vẫn nằm trong code: khách hoàn tiền lúc 03:01, hoặc job chạy bù ban ngày, là chuyện cũ tái diễn. Băng keo cần kèm vé sửa gốc." },
      { t: "Bọc cả hai luồng bằng distributed lock Redis 'mỗi lúc một người'", s: 0, why: "Serialize toàn bộ hoàn tiền + đối soát qua một khoá toàn cục = đổi deadlock lấy nghẽn cổ chai và thêm một hệ thống phải vận hành. Dùng búa tạ phân tán cho bài toán có lời giải một-dòng-đổi-thứ-tự là kiến trúc quá tay." },
      { t: "Bỏ @Transactional ở một trong hai method để không còn khoá", s: 0, why: "Hết khoá thì hết deadlock... và hết luôn tính nguyên tử: ledger đánh dấu REFUNDED mà ví chưa cộng tiền nếu crash giữa chừng — bạn vừa đổi lỗi-có-tiếng-động lấy sai-dữ-liệu-im-lặng, một trade tồi tệ." }
    ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> hai code path đụng cặp (wallets, ledger) theo thứ tự ngược → đổi lịch job làm hai luồng chạy trùng khung giờ → cùng user bị đụng từ hai phía → vòng chờ → Postgres giết một nạn nhân mỗi lần → lỗi rải rác hằng đêm.</p><p><b>Ba bài học mang đi:</b> (1) Log deadlock của DB là <em>hồ sơ vụ án hoàn chỉnh</em> — có nạn nhân, nghi phạm và lời khai; kỹ năng là dựng lại thứ tự khoá. (2) Quy ước <em>thứ tự khoá thống nhất</em> (theo bảng, rồi theo khoá tăng dần khi update hàng loạt) là thuốc đặc trị. (3) Deadlock error là lỗi <em>tạm thời theo thiết kế</em> — code production phải retry nó, đừng ném thẳng 500 cho user.</p>",
  prove: "<p>Tự tạo deadlock bằng 2 cửa sổ psql (~5 phút):</p><pre>-- chuan bi:\nCREATE TABLE t(id int primary key, v int); INSERT INTO t VALUES (1,0),(2,0);\n\n-- Cửa sổ A:                      | -- Cửa sổ B:\nBEGIN;                            | BEGIN;\nUPDATE t SET v=1 WHERE id=1;      | UPDATE t SET v=1 WHERE id=2;\n-- (id=1 đã bị A khoá)            | -- (id=2 đã bị B khoá)\nUPDATE t SET v=1 WHERE id=2;      | UPDATE t SET v=1 WHERE id=1;\n-- A treo chờ B...                | -- B chờ A → sau ~1s:\n                                  | ERROR:  deadlock detected</pre><p><b>Cách xác minh:</b> một bên nhận đúng <code>deadlock detected</code> (nạn nhân bị rollback), bên kia chạy tiếp bình thường. Lặp lại nhưng cho cả hai cửa sổ UPDATE <em>cùng thứ tự id=1 rồi id=2</em>: chỉ còn chờ tuần tự, không bao giờ deadlock — chứng minh bằng tay luật 'khoá cùng chiều'.</p>",
  refs: [["PostgreSQL – deadlocks", "https://www.postgresql.org/docs/current/explicit-locking.html#LOCKING-DEADLOCKS"], ["Postgres error 40P01", "https://www.postgresql.org/docs/current/errcodes-appendix.html"]]
});

INCIDENTS.register({
  id: "inc-n-plus-one", sev: "P2", domain: "Database / JPA", skills: ["spr-jpa", "data-sql-tuning"], time: "15–20 phút",
  title: "Trang đơn hàng của khách lớn mất 9 giây — index nào cũng có rồi",
  teaser: "APM nói mỗi query chỉ 3ms. Nhưng có… 1.203 query cho một lần bấm F5. Hibernate đang làm gì sau lưng bạn?",
  alert: "<b>[APM Alert]</b> 14:20 — <code>GET /admin/customers/{id}/orders</code> p95 = <b>9.2s</b> cho các khách hàng lớn (&gt;500 đơn). Khách nhỏ vẫn nhanh. DBA xác nhận: không có slow query nào trong log (ngưỡng 100ms).",
  artifacts: [
    { label: "🔍 APM trace (1 request)", body: "<pre>GET /admin/customers/8842/orders                total: <span class='hl'>9.214 ms</span>\n ├─ OrderController.list                          9.213 ms\n │   ├─ SELECT ... FROM orders WHERE customer_id=?      3.1 ms   (601 rows)\n │   ├─ SELECT ... FROM order_items WHERE order_id=?    2.9 ms\n │   ├─ SELECT ... FROM order_items WHERE order_id=?    3.0 ms\n │   ├─ SELECT ... FROM order_items WHERE order_id=?    2.8 ms\n │   │   ... <span class='hl'>(lặp 601 lần — mỗi order một câu)</span>\n │   └─ SELECT ... FROM users WHERE id=?  × 601         ~2.7 ms/câu\n └─ Jackson serialize                              214 ms\nTổng số câu SQL: <span class='hl'>1.203</span> · trung bình 2.9ms/câu · KHÔNG câu nào 'chậm'</pre>" },
    { label: "📄 Entity + Controller", body: "<pre>@Entity class Order {\n    @ManyToOne(fetch = FetchType.LAZY)  User customer;\n    @OneToMany(mappedBy = \"order\", fetch = FetchType.LAZY)\n    List&lt;OrderItem&gt; items;\n}\n\n@GetMapping(\"/admin/customers/{id}/orders\")\nList&lt;OrderDto&gt; list(@PathVariable Long id) {\n    return orderRepo.findByCustomerId(id).stream()      // 1 query: orders\n        .map(o -&gt; new OrderDto(\n            o.getId(),\n            <span class='hl'>o.getItems().size()</span>,                        // +1 query MỖI order\n            <span class='hl'>o.getCustomer().getName()</span>,                   // +1 query MỖI order\n            o.getTotal()))\n        .toList();\n}</pre>" }
  ],
  steps: [
    { q: "Nghịch lý 'trang 9 giây nhưng không query nào chậm' — kết luận đúng từ trace?",
      opts: [
        { t: "Vấn đề là SỐ LƯỢNG round-trip: 1.203 câu × ~3ms (phần lớn là đường đi mạng + overhead mỗi câu) = 9s — mẫu 1 + N + N của lazy loading trong vòng lặp", s: 2, why: "Slow query log chỉ bắt câu ĐƠN LẺ chậm — nó mù với 'nghìn câu nhanh'. Trace APM đếm số span mới lộ bệnh. Nhận diện mẫu '1 câu list + N câu con lặp' là kỹ năng đọc trace quan trọng bậc nhất với JPA." },
        { t: "Jackson serialize 214ms là điểm nghẽn cần tối ưu", s: 0, why: "214ms / 9.214ms ≈ 2% tổng thời gian. Tối ưu chỗ chiếm 2% thì kịch trần nhanh thêm 2%. Luôn tấn công thành phần chiếm tỉ trọng lớn nhất trong trace — ở đây là 1.202 câu SQL còn lại." },
        { t: "DB overload vì bị 1.203 query — cần scale DB", s: 1, why: "1.203 câu/request nhân với nhiều admin đồng thời ĐÚNG là tải vô ích lên DB — quan sát hợp lệ. Nhưng DB hiện xử lý mỗi câu trong 3ms (khoẻ); scale DB để phục vụ tốt hơn một pattern lãng phí là mua thêm ghế cho một cuộc họp không cần diễn ra." },
        { t: "Thiếu index trên order_items.order_id", s: 0, why: "Mỗi câu con chạy 2.9ms và DBA không thấy slow query — index đang hoạt động tốt. Đây là bẫy của câu hỏi: bài N+1 KHÔNG chữa được bằng index, vì từng câu vốn đã nhanh." }
      ] },
    { q: "Vì sao code 'trông vô tội' này sinh ra 1.202 query thừa?",
      opts: [
        { t: "items và customer là LAZY: repo chỉ load orders; mỗi lần vòng lặp chạm o.getItems()/o.getCustomer(), Hibernate âm thầm bắn thêm một query cho ĐÚNG order đó", s: 2, why: "Lazy = 'hẹn khi chạm mới load' — đúng đắn để tránh load thừa, nhưng CHẠM TRONG VÒNG LẶP là kích hoạt N lần. ORM giấu SQL đi làm code sạch, và cũng giấu luôn cái giá — phải bật show-sql/APM để nhìn xuyên." },
        { t: "Vì dùng stream() thay cho vòng for truyền thống", s: 0, why: "stream hay for đều chạm getter như nhau — số query y hệt. Đổ cho cú pháp là điều tra theo trang phục, không theo hành vi." },
        { t: "Vì thiếu @Transactional nên mỗi getter mở transaction mới", s: 1, why: "Có mảnh sự thật (mỗi lazy-load ngoài transaction có thể mượn connection riêng — thêm overhead), nhưng KHÔNG phải nguồn của con số 1.202: có @Transactional bao ngoài thì vẫn đủ N+N câu, chỉ chung connection. Số câu đến từ pattern truy cập, không từ annotation." },
        { t: "Bug của Hibernate 6, nâng version là hết", s: 0, why:"N+1 là HỆ QUẢ THIẾT KẾ của lazy loading — tồn tại từ Hibernate 1 tới nay và trong mọi ORM có lazy. Thứ cần nâng cấp là cách fetch của code, không phải version." }
      ] }
  ],
  cause: { q: "Chốt nguyên nhân gốc:",
    opts: [
      { t: "N+1 query: truy cập quan hệ LAZY (items, customer) bên trong vòng lặp trên list 601 order → 1 + 601 + 601 câu; từng câu nhanh nên slow-query-log mù, tổng thời gian chết vì round-trip", ok: true, why: "Công thức nhận dạng: thời gian ∝ số bản ghi cha (khách to mới chậm — khớp triệu chứng), trace toàn câu lặp giống nhau khác mỗi tham số. Đây là lỗi hiệu năng số 1 của mọi codebase JPA." },
      { t: "FetchType.LAZY là cấu hình sai, mặc định phải để EAGER", ok: false, why: "Ngược lại: LAZY là mặc định ĐÚNG (và là default của @OneToMany). EAGER toàn cục nghĩa là MỌI nơi load Order đều gánh items+customer kể cả không dùng — đổi N+1 có chọn lọc lấy over-fetching toàn hệ thống." },
      { t: "Admin page cần dữ liệu quá nhiều, 9s là hợp lý với 601 đơn", ok: false, why: "601 đơn + items + tên khách lấy được trong 1-3 câu SQL tử tế (~50-100ms cả serialize). '9s là bản chất dữ liệu lớn' là đầu hàng sớm 100 lần so với giới hạn thật." },
      { t: "Connection pool quá nhỏ làm các query xếp hàng", ok: false, why: "Trace cho thấy các câu chạy TUẦN TỰ trong một request một connection — không có chờ pool. Pool là nghi phạm quen mặt nhưng vô can trong vụ này; bằng chứng ngoại phạm nằm ngay trong trace." }
    ] },
  fix: { q: "Sửa đúng cách?",
    opts: [
      { t: "Fetch đúng hình dạng dữ liệu cần: JOIN FETCH / @EntityGraph (orders + items + customer trong 1-2 câu), hoặc thẳng tay hơn — query projection DTO đúng các cột màn hình cần; verify lại bằng đếm query trong test", s: 2, why: "Nguyên tắc: nói cho ORM biết TRƯỚC hình dạng dữ liệu sẽ dùng. Với màn hình đọc-only, DTO projection còn tốt hơn entity (không dirty-check, không lazy mìn). Test đếm query (vd assert ≤3 câu) chống tái phát khi người sau sửa code." },
      { t: "Bật spring.jpa.properties.hibernate.default_batch_fetch_size=100", s: 1, why: "Batch fetching gom N câu thành N/100 câu IN (...) — cải thiện 10-20 lần mà không sửa code, đáng biết như thuốc giảm đau diện rộng. Nhưng vẫn là 'load kiểu lười có gom' — hình dạng fetch vẫn không được thiết kế, và 7 câu vẫn nhiều hơn 2 câu." },
      { t: "Đổi hai quan hệ sang FetchType.EAGER", s: 0, why: "Chữa màn hình này bằng cách bắt MỌI màn hình khác trả giá (Order xuất hiện ở đâu cũng kéo items+customer). EAGER trên collection còn dễ sinh cartesian product khi join nhiều nhánh. Đây là fix lan truyền tác dụng phụ tệ nhất trong danh sách." },
      { t: "Cache kết quả endpoint này bằng Redis TTL 5 phút", s: 0, why: "Admin cần dữ liệu tươi khi xử lý khiếu nại; và lần miss đầu vẫn 9s. Cache đắp lên một query pattern sai là giấu nợ kỹ thuật dưới thảm — nợ vẫn tính lãi ở mọi chỗ khác dùng pattern này." }
    ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> vòng lặp chạm getter LAZY trên 601 order → Hibernate bắn 1.202 câu con → mỗi câu 3ms nhân số lượng thành 9s → slow-query-log không thấy gì vì không câu nào vượt ngưỡng → 'DB khoẻ mà trang chậm'.</p><p><b>Ba bài học mang đi:</b> (1) Nghịch lý <em>'không query nào chậm nhưng trang chậm'</em> = nghĩ ngay N+1; công cụ nhìn ra là APM trace/show-sql đếm SỐ CÂU, không phải slow query log. (2) LAZY đúng, nhưng <em>hình dạng fetch phải khai báo theo use-case</em> (JOIN FETCH/EntityGraph/DTO). (3) Chốt chống tái phát bằng <em>test đếm query</em> — hiệu năng cũng cần regression test.</p>",
  prove: "<p>Tự chứng kiến N+1 (~15 phút với project Spring Boot bất kỳ có quan hệ OneToMany):</p><pre># application.yml\nspring.jpa.show-sql: true\nlogging.level.org.hibernate.SQL: debug\n\n# Test:\n1) Seed 50 order, moi order 3 item\n2) Goi endpoint list co cham o.getItems() → dem dong 'select' trong log: <span class='hl'>51 cau</span>\n3) Doi repo thanh:  @Query(\"select o from Order o join fetch o.items where ...\")\n   → chay lai: <span class='hl'>1 cau</span></pre><p><b>Cách xác minh:</b> số dòng SELECT trong log giảm từ 51 → 1; thời gian response giảm tương ứng. Nâng cao: viết test tự động với <code>hibernate.generate_statistics=true</code> và assert <code>getQueryExecutionCount() &lt;= 2</code> — đây chính là 'test hiệu năng dạng unit' dùng được trong CI.</p>",
  refs: [["Vlad Mihalcea – N+1 query problem", "https://vladmihalcea.com/n-plus-1-query-problem/"], ["Spring Data – EntityGraph", "https://docs.spring.io/spring-data/jpa/reference/jpa/entity-graph.html"]]
});
