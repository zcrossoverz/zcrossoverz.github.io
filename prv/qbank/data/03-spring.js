/* ============================================================
   QBANK DATA — Spring   (prefix id: spr-)
   Trạng thái: REWRITTEN — đáp án viết bằng ngôn ngữ đơn giản cho junior,
   giải thích thuật ngữ cặn kẽ, không giả định biết trước.
   Thêm câu mới: copy 1 khối { ... }, dán ngay TRƯỚC dòng đánh dấu DATA_END
   (gần cuối file), đổi id tăng dần (spr-048, spr-049...).
   Schema + quy tắc viết đáp án: xem qbank/GUIDE.md
   ============================================================ */
QB.register({
  group: "Spring",
  order: 3,
  prefix: "spr",
  status: "rewritten",
  questions: [
/* DATA_START */

{ id:"spr-001", t:"Spring Core", lv:"junior", core:true,
  tags:["bean","applicationcontext","container","spring-core"],
  q:"Bean là gì? ApplicationContext (IoC container) là gì? Spring khởi động thì chuyện gì xảy ra?",
  a:"<p>Ba khái niệm nền của cả hệ sinh thái Spring — hiểu chúng thì mọi annotation về sau đều có chỗ bám:</p><p><strong>Bean</strong> = một object <em>do Spring tạo ra và quản lý</em> thay bạn. Nó chẳng có gì thần bí — vẫn là object Java bình thường, khác mỗi chỗ: bạn không <code>new</code> nó, Spring new giùm, giữ giùm, và đưa cho ai cần.</p><p><strong>ApplicationContext</strong> (hay gọi là <strong>IoC container</strong>) = 'cái kho + bộ máy' quản lý toàn bộ bean: biết cần tạo những bean nào, tạo theo thứ tự nào (bean A cần bean B thì B phải có trước), ghép nối (inject) chúng vào nhau, và đưa ra khi được hỏi (<code>context.getBean(...)</code> — dù thực tế bạn hiếm khi gọi tay, vì đã có DI).</p><p><strong>Khi app Spring Boot khởi động</strong>, tuần tự dễ hình dung:</p><ol><li><strong>Quét (component scan):</strong> tìm các class gắn <code>@Component/@Service/@Repository/@Controller</code>... từ package gốc trở xuống — lập 'danh sách những gì cần tạo' (bean definition).</li><li><strong>Tạo và ghép:</strong> new từng bean theo đúng thứ tự phụ thuộc, tiêm dependency vào nhau (DI), chạy các bước khởi tạo (<code>@PostConstruct</code>...).</li><li><strong>Sẵn sàng:</strong> mọi bean nằm trong context, web server nhúng bật lên nhận request. Lỗi thiếu bean/lặp vòng được phát hiện <em>ngay lúc khởi động</em> — fail sớm còn hơn nổ giữa giờ cao điểm.</li></ol><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> ApplicationContext như <em>bếp trưởng của nhà hàng</em>: đọc menu (bean definitions), chuẩn bị sẵn mọi nguyên liệu và dụng cụ (tạo bean), món nào cần nước sốt nào thì gắn sẵn với nhau (DI). Đầu bếp (code của bạn) chỉ việc nấu, không phải chạy đi mua đồ (new) giữa chừng.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'object nào cũng là bean à?' — KHÔNG: chỉ những gì bạn <em>khai cho Spring</em> (annotation/@Bean) mới là bean; DTO, entity bạn tự new trong logic vẫn là object thường. Và mặc định mỗi bean chỉ có <strong>một instance duy nhất</strong> (singleton scope) dùng chung toàn app — gốc rễ của câu 'bean có thread-safe không' phía dưới.</div>",
  refs:[["Spring – IoC container","https://docs.spring.io/spring-framework/reference/core/beans/basics.html"]] },

{ id:"spr-002", legacy:34, t:"Spring Core", lv:"junior", core:true,
  tags:["ioc","dependency-injection","di","spring-core"],
  q:"IoC (Inversion of Control) và DI (Dependency Injection) là gì? Lợi ích thực tế?",
  a:"<p>Bắt đầu từ đời sống không có Spring:</p><pre><code class='language-java'>class OrderService {\n    private EmailService email = new EmailService(cauHinhSmtp);  // tự tạo lấy\n}</code></pre><p>Trông vô hại, nhưng: OrderService <em>bị dính cứng</em> vào một cách tạo EmailService cụ thể — muốn thay bằng SmsService? Sửa code. Muốn test mà không gửi mail thật? Không nhét bản giả vào được. Mười class cùng tự new EmailService = mười chỗ sửa khi đổi cấu hình.</p><p><strong>IoC (đảo ngược điều khiển)</strong> = đảo lại quyền quyết định: class <em>không tự tạo</em> thứ nó cần nữa mà <strong>khai báo</strong> 'tôi cần một EmailService' — việc tạo ra và đưa vào là của container. Quyền điều khiển chuyển từ code của bạn sang khung (framework) — nên gọi là 'đảo ngược'.</p><p><strong>DI (tiêm phụ thuộc)</strong> = cách cụ thể để thực hiện IoC: container 'tiêm' dependency vào qua constructor (phổ biến nhất) / setter / field:</p><pre><code class='language-java'>@Service\nclass OrderService {\n    private final NotifyService notify;          // chỉ biết interface\n    OrderService(NotifyService notify) {         // Spring tiêm bản cài đặt vào đây\n        this.notify = notify;\n    }\n}</code></pre><p><strong>Lợi ích sờ được:</strong> (1) <strong>test dễ</strong> — constructor nhận gì thì test đưa mock vào đó, không cần khởi động cả app; (2) <strong>thay thế linh hoạt</strong> — đổi cài đặt (Email → Sms, thật → giả lập) không đụng class dùng nó; (3) <strong>ghép nối tập trung</strong> — cấu hình 'ai dùng cái gì' nằm một chỗ thay vì rải trong new khắp nơi.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Không IoC = đầu bếp tự đi chợ, tự nuôi gà lấy trứng — đổi nhà cung cấp là đảo lộn cả bếp. IoC/DI = bếp chỉ ghi 'cần 10 trứng loại 1' (khai báo dependency), bộ phận thu mua (container) lo nguồn hàng và giao tận nơi — mai đổi nhà cung cấp, bếp không cần biết.</div><div class='qb-gotcha'><b>⚠ Phân biệt cho chuẩn:</b> IoC là <em>nguyên lý</em> (ai nắm quyền điều khiển), DI là <em>kỹ thuật</em> hiện thực nó (tiêm qua constructor/setter). Trả lời 'IoC và DI là một' bị trừ nhẹ; trả lời được 'DI là một cách làm IoC, event/callback cũng là dạng IoC' là điểm cộng.</div>",
  refs:[["Spring – Dependency injection","https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html"],["Martin Fowler – IoC containers","https://martinfowler.com/articles/injection.html"]] },

{ id:"spr-003", legacy:35, t:"Spring Core", lv:"junior", core:false,
  tags:["constructor-injection","field-injection","autowired"],
  q:"Constructor injection và field injection — nên dùng cái nào, vì sao?",
  a:"<p>Ba kiểu tiêm dependency:</p><pre><code class='language-java'>// ① Field injection — gọn nhưng nhiều vấn đề\n@Autowired private EmailService email;\n\n// ② Setter injection — cho dependency tuỳ chọn/đổi được\n@Autowired void setEmail(EmailService e) { this.email = e; }\n\n// ③ Constructor injection — khuyến nghị chính thức\nprivate final EmailService email;\nOrderService(EmailService email) { this.email = email; }</code></pre><p><strong>Vì sao constructor injection thắng:</strong></p><ul><li><strong>Field là <code>final</code> được</strong> — gán một lần lúc tạo, không ai đổi được nữa, và <em>không bao giờ null</em>: object sinh ra là đủ đồ nghề, không có trạng thái 'tạo rồi mà chưa tiêm xong'.</li><li><strong>Trung thực về độ phức tạp:</strong> class cần 7 dependency thì constructor 7 tham số — nhìn phát biết class này ôm quá nhiều việc (tín hiệu tách nhỏ). Field injection giấu biến 7 dòng <code>@Autowired</code> trông 'gọn' — che mất mùi thiết kế.</li><li><strong>Test không cần Spring:</strong> <code>new OrderService(mockEmail)</code> — xong. Field injection thì field private không có cách gán tử tế, phải dùng reflection hoặc kéo cả Spring context vào test — chậm và lằng nhằng.</li><li>Bắt <strong>circular dependency lộ diện ngay khi khởi động</strong> (A cần B cần A → nổ sớm, tốt!) thay vì âm thầm hoạt động nửa vời.</li></ul><p>Bonus: từ Spring 4.3, class có <em>đúng một constructor</em> thì <strong>khỏi cần @Autowired</strong> — Spring tự hiểu. Kết hợp Lombok <code>@RequiredArgsConstructor</code> (tự sinh constructor cho các field final) thì code gọn hơn cả field injection.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Constructor injection như kiểm tra đủ đồ <em>trước khi lên máy bay</em> — thiếu hộ chiếu là không cất cánh (thiếu bean là fail lúc khởi động). Field injection như lên máy bay rồi mới lục túi — phát hiện thiếu giữa trời (NullPointerException lúc runtime).</div><div class='qb-gotcha'><b>⚠ Câu bẫy:</b> 'field injection tiện thế sao Spring không khuyến khích?' — nói được 3 ý: final/không null, test không cần container, lộ mùi thiết kế. Và biết ngoại lệ thực dụng: test class hoặc config đặc thù đôi khi vẫn field injection cho nhanh — biết luật rồi mới được phá luật có chủ đích.</div>",
  refs:[["Spring – Constructor-based DI","https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html#beans-constructor-injection"]] },

{ id:"spr-004", t:"Spring Core", lv:"junior", core:true,
  tags:["spring-boot","starter","embedded-server"],
  q:"Spring Boot khác Spring Framework chỗ nào? Vì sao ai cũng dùng Boot?",
  a:"<p>Hai thứ <strong>không phải đối thủ</strong> — Boot xây <em>chồng lên</em> Framework:</p><p><strong>Spring Framework</strong> = phần lõi: IoC container, DI, Spring MVC, transaction, AOP... Cực mạnh nhưng thời 'Spring chay' bạn phải <em>tự cấu hình mọi thứ</em>: khai DataSource, EntityManager, DispatcherServlet, đóng file WAR deploy vào Tomcat cài riêng — dự án mới mất vài ngày chỉ để 'chạy được Hello World'.</p><p><strong>Spring Boot</strong> = tầng 'khởi động nhanh' đóng gói các quyết định hợp lý sẵn, ba trụ cột:</p><ul><li><strong>Starter dependency:</strong> <code>spring-boot-starter-web</code> kéo về nguyên combo đã kiểm chứng hợp nhau (Spring MVC + Jackson + Tomcat + validation) — khỏi tự lắp ráp version từng viên.</li><li><strong>Auto-configuration:</strong> nhìn classpath mà tự cấu hình — thấy driver PostgreSQL + thông tin kết nối là tự dựng DataSource, thấy starter-web là tự dựng DispatcherServlet (chi tiết ở câu auto-config). Quy tắc 'convention over configuration': theo mặc định thì khỏi khai gì, muốn khác thì ghi đè.</li><li><strong>Embedded server:</strong> Tomcat nằm <em>trong</em> file jar — <code>java -jar app.jar</code> là chạy, không cần cài server ngoài; chuẩn không khí container/Docker.</li></ul><p>Kèm quà: Actuator (health/metrics), cấu hình theo profile, DevTools... Nói gọn: <strong>Framework cho sức mạnh, Boot cho tốc độ khởi đầu và vận hành</strong> — 'ai cũng dùng Boot' vì nó biến vài ngày setup thành vài phút.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Spring Framework như bộ linh kiện PC rời cao cấp — ráp được cỗ máy đúng ý nhưng phải tự chọn từng món tương thích. Spring Boot như laptop cấu hình sẵn của cùng hãng — mở hộp là dùng, vẫn cho phép nâng RAM đổi SSD (override config) khi cần.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'Boot thay thế Spring à?' — sai, Boot <em>là</em> Spring + lớp tự động hoá; code @Service/@Autowired bạn viết chính là Spring Framework thuần. Câu nối hay gặp: 'jar của Boot khác war truyền thống gì?' — jar tự chứa server (self-contained), war cần server ngoài chứa nó.</div>",
  refs:[["Spring Boot – Reference","https://docs.spring.io/spring-boot/index.html"]] },

{ id:"spr-005", legacy:149, t:"Spring Core", lv:"middle", core:true,
  tags:["component","bean","configuration"],
  q:"<code>@Component</code> và <code>@Bean</code> khác nhau ra sao? Khi nào bắt buộc dùng <code>@Bean</code>?",
  a:"<p>Cùng đích đến — đăng ký một bean vào container — nhưng hai cửa khác nhau:</p><p><strong><code>@Component</code> — dán lên CLASS, để Spring tự quét.</strong> (Cùng họ: <code>@Service</code>, <code>@Repository</code>, <code>@Controller</code> — bản chất là @Component có thêm ngữ nghĩa.) Điều kiện: class đó phải <em>của bạn</em> (bạn sửa được source) và nằm trong vùng component scan.</p><p><strong><code>@Bean</code> — dán lên METHOD trong class <code>@Configuration</code>, bạn TỰ TAY tạo object:</strong></p><pre><code class='language-java'>@Configuration\nclass AppConfig {\n    @Bean\n    ObjectMapper objectMapper() {                      // class của Jackson — không sửa source được\n        return new ObjectMapper().registerModule(new JavaTimeModule());\n    }\n    @Bean\n    RestClient shopeeClient(@Value(\"${shopee.url}\") String url) {\n        return RestClient.builder().baseUrl(url).build();   // cần logic dựng phức tạp\n    }\n}</code></pre><p><strong>Bắt buộc dùng @Bean khi:</strong> (1) class đến từ <strong>thư viện bên thứ ba</strong> — không dán @Component lên source của người ta được; (2) việc tạo cần <strong>logic/điều kiện</strong> (đọc config, if-else chọn implementation, builder nhiều bước); (3) muốn đăng ký <strong>nhiều bean cùng kiểu</strong> với tên khác nhau từ cùng một class.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> @Component như nhân viên chính thức có phù hiệu — bảo vệ (component scan) nhìn phù hiệu tự cho vào danh sách. @Bean như khách mời do chính bạn dẫn vào và giới thiệu với lễ tân — dùng cho người 'không thể đeo phù hiệu công ty' (class thư viện ngoài).</div><div class='qb-gotcha'><b>⚠ Chi tiết hay bị xoáy:</b> trong class <code>@Configuration</code>, một method @Bean gọi method @Bean khác sẽ <strong>không tạo object mới</strong> — Spring dùng proxy CGLIB chặn lời gọi và trả về bean sẵn có trong container (giữ đúng singleton). Đặt @Bean trong class thường (@Component — 'lite mode') thì không có phép màu này, gọi chéo là new thật — nguồn bug 'sao có 2 instance' kinh điển.</div>",
  refs:[["Spring – Java-based configuration","https://docs.spring.io/spring-framework/reference/core/beans/java/basic-concepts.html"]] },

{ id:"spr-006", legacy:150, t:"Spring Core", lv:"middle", core:true,
  tags:["qualifier","primary","autowired"],
  q:"Có 2 bean cùng kiểu, inject bị <code>NoUniqueBeanDefinitionException</code>. Xử lý bằng gì?",
  a:"<p>Bối cảnh: một interface — hai bản cài đặt:</p><pre><code class='language-java'>interface NotifyService { void send(String msg); }\n@Service class EmailNotify implements NotifyService { ... }\n@Service class SmsNotify   implements NotifyService { ... }\n\n// Ở đâu đó:  OrderService(NotifyService notify)  ← Spring: 'cái NÀO?!' → nổ</code></pre><p>Spring resolve dependency <strong>theo kiểu</strong> trước; hai ứng viên cùng kiểu là nó không dám đoán. Các cách chỉ định, xếp theo mức dùng:</p><ul><li><strong><code>@Qualifier(\"tenBean\")</code></strong> — chỉ đích danh tại chỗ tiêm: <code>OrderService(@Qualifier(\"emailNotify\") NotifyService n)</code>. (Tên bean mặc định = tên class viết thường chữ đầu.) Rõ ràng, chủ động — dùng nhiều nhất.</li><li><strong><code>@Primary</code></strong> — dán lên một bản: 'khi không ai nói gì thì mặc định là tôi'. Hợp khi có bản chính + bản phụ ít dùng. Cẩn thận: mặc định ngầm nhiều khi khó truy.</li><li><strong>Match theo tên biến</strong> — đặt tên tham số trùng tên bean (<code>NotifyService emailNotify</code>) Spring cũng tự hiểu; nhưng 'thoả thuận ngầm' đổi tên biến là gãy — nên coi là may mắn, đừng coi là thiết kế.</li><li><strong>Tiêm cả danh sách</strong> — <code>List&lt;NotifyService&gt; allNotifiers</code>: nhận MỌI bản cài đặt; pattern đẹp cho plugin/chain (gửi qua mọi kênh, hay chọn kênh lúc runtime theo logic riêng).</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gọi taxi mà công ty có 2 tài xế tên Nam — tổng đài không dám tự chọn (exception). @Qualifier = 'cho tôi Nam Bến Thành'; @Primary = công ty quy định 'nói Nam trống không thì hiểu là Nam Bến Thành'; List = 'cho cả hai anh Nam tới, tôi tự phân việc'.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối:</b> 'chọn implementation theo <em>config môi trường</em> thì sao?' — <code>@Profile(\"dev\")</code>/<code>@ConditionalOnProperty</code>: chỉ đăng ký bean phù hợp môi trường ngay từ đầu — sạch hơn if-else runtime. Kể được cách này là cộng điểm vận hành thực tế.</div>",
  refs:[["Spring – Autowiring / qualifiers","https://docs.spring.io/spring-framework/reference/core/beans/annotation-config/autowired-qualifiers.html"]] },

{ id:"spr-007", legacy:36, t:"Spring Core", lv:"middle", core:true,
  tags:["singleton","thread-safe","state"],
  q:"Vì sao đặt một field mutable trong <code>@Service</code> (singleton) là bug trong web app?",
  a:"<p>Ghép hai sự thật lại là thấy quả bom:</p><ul><li><strong>Sự thật 1:</strong> bean Spring mặc định là <strong>singleton</strong> — TOÀN BỘ ứng dụng chỉ có MỘT instance của <code>OrderService</code>.</li><li><strong>Sự thật 2:</strong> web server xử lý mỗi request trên một thread riêng — 200 request đồng thời = <strong>200 thread cùng đi xuyên qua đúng MỘT object đó</strong>.</li></ul><p>Vậy field của service = <strong>biến dùng chung giữa mọi request</strong>:</p><pre><code class='language-java'>@Service\nclass OrderService {\n    private Order currentOrder;              // ❌ CHUNG cho mọi request!\n    void process(Order o) {\n        this.currentOrder = o;               // request A đặt đơn của A...\n        // ... request B chen vào, ghi đè đơn của B ...\n        validate(currentOrder);              // A đang validate ĐƠN CỦA B — trộn dữ liệu!\n    }\n}</code></pre><p>Đây là <strong>race condition</strong> phiên bản Spring — triệu chứng ngoài đời cực kỳ đáng sợ: <em>user này nhìn thấy dữ liệu của user kia</em>, chỉ xảy ra lúc tải cao, không tái hiện được trên máy dev (một mình một thread).</p><p><strong>Cách đúng:</strong> service phải <strong>stateless</strong> — dữ liệu của một lần xử lý sống trong <em>biến local và tham số</em> (mỗi thread một stack riêng — an toàn tự nhiên): <code>void process(Order o) { validate(o); ... }</code>. Field của bean chỉ dành cho: dependency (final), config bất biến. Cần state chia sẻ thật sự (cache, counter)? Dùng công cụ concurrent (<code>ConcurrentHashMap</code>, <code>AtomicLong</code>) hoặc đẩy ra ngoài (Redis) — có chủ đích, có bảo vệ.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Service singleton như MỘT quầy thu ngân phục vụ cả siêu thị. Biến local = tờ hoá đơn in riêng cho từng khách. Field mutable = thu ngân ghi tạm 'giỏ hàng hiện tại' lên <em>tấm bảng chung sau lưng</em> — hai khách đến cùng lúc là tính tiền giỏ của nhau.</div><div class='qb-gotcha'><b>⚠ Câu bẫy liên quan:</b> 'vậy đổi scope thành prototype/request là xong?' — chữa được triệu chứng nhưng thường là sai hướng (tốn object, giấu vấn đề thiết kế); câu trả lời chuẩn vẫn là <strong>viết service stateless</strong>. Hỏi thêm kinh điển: 'SimpleDateFormat làm field static được không?' — không, nó không thread-safe; dùng <code>DateTimeFormatter</code> (immutable) của java.time.</div>",
  refs:[["Spring – Bean scopes","https://docs.spring.io/spring-framework/reference/core/beans/factory-scopes.html"]] },

{ id:"spr-008", legacy:152, t:"Spring Core", lv:"middle", core:false,
  tags:["bean-scope","request","session","prototype"],
  q:"Ngoài singleton và prototype, có scope nào khác? <code>request</code> và <code>session</code> scope dùng khi nào?",
  a:"<p>Scope = 'một bean sống bao lâu và ai được dùng chung'. Toàn cảnh:</p><ul><li><strong>singleton</strong> (mặc định) — một instance cho cả app. 99% service/repository nên ở đây (stateless mà — chia sẻ vô hại, tiết kiệm).</li><li><strong>prototype</strong> — <em>mỗi lần xin là một instance mới</em>. Cho object CÓ state riêng theo lần dùng (builder, phiên tính toán). Lưu ý lớn: Spring tạo xong là <em>phủi tay</em> — không quản lý destroy callback của prototype.</li><li>Riêng web app có thêm: <strong>request</strong> — một instance <em>mỗi HTTP request</em> (chứa context của request: trace id, thông tin user đang gọi — thay cho ThreadLocal thủ công); <strong>session</strong> — một instance <em>mỗi phiên user</em> (giỏ hàng, preferences — ngày càng ít dùng vì app hiện đại chuộng stateless + Redis); <strong>application</strong> — một cho cả ServletContext; <strong>websocket</strong> — theo phiên WebSocket.</li></ul><p><strong>Cái bẫy ghép scope (scoped proxy):</strong> tiêm bean scope ngắn (request) vào bean scope dài (singleton) — singleton tạo MỘT lần lúc khởi động, lúc đó lấy đâu ra 'request hiện tại'? Giải pháp Spring: <code>@Scope(value = \"request\", proxyMode = ScopedProxyMode.TARGET_CLASS)</code> — tiêm vào singleton một <strong>proxy</strong>; mỗi lần gọi method, proxy tự tra 'request đang chạy là gì' và chuyển lời gọi tới instance đúng của request đó.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> singleton = máy pha cà phê chung cả văn phòng; prototype = cốc giấy — mỗi người lấy cái mới; request = khay ăn phát khi vào căng-tin, ra cửa trả; session = tủ đồ cá nhân thuê theo tháng. Scoped proxy = ô 'khay của khách hiện tại' — nhân viên (singleton) cứ với tay vào ô đó, hệ thống tự đảm bảo đó là khay của đúng người đang đứng trước mặt.</div><div class='qb-gotcha'><b>⚠ Câu hay hỏi kèm:</b> 'tiêm prototype vào singleton thì mỗi lần gọi có instance mới không?' — KHÔNG mặc định: tiêm một lần lúc dựng singleton là đóng băng luôn instance đó. Muốn 'mới mỗi lần' phải dùng <code>ObjectProvider&lt;T&gt;.getObject()</code> / scoped proxy / <code>@Lookup</code>. Đây là bẫy scope kinh điển nhất.</div>",
  refs:[["Spring – Bean scopes","https://docs.spring.io/spring-framework/reference/core/beans/factory-scopes.html"]] },

{ id:"spr-009", legacy:153, t:"Spring Core", lv:"middle", core:false,
  tags:["bean-lifecycle","postconstruct","predestroy"],
  q:"Thứ tự vòng đời bean: constructor, <code>@Autowired</code>, <code>@PostConstruct</code>, <code>@PreDestroy</code> chạy lúc nào?",
  a:"<p>Một bean đi từ 'chưa có gì' đến 'sẵn sàng phục vụ' theo trình tự cố định — hiểu trình tự là hiểu <em>lúc nào được làm gì</em>:</p><ol><li><strong>Constructor chạy</strong> — dependency tiêm qua <em>constructor</em> đã có mặt tại đây; nhưng dependency tiêm qua <em>field/setter</em> thì CHƯA (object còn chưa dựng xong lấy gì tiêm).</li><li><strong>Tiêm field/setter</strong> — Spring điền nốt các <code>@Autowired</code> còn lại.</li><li><strong>Các Aware callback + BeanPostProcessor (before)</strong> — móc nội bộ của framework (biết tên bean, context...).</li><li><strong><code>@PostConstruct</code></strong> — 'mọi thứ đã tiêm đủ, giờ khởi động': đây là chỗ đúng để validate config, warm cache, mở kết nối. (Sau nó còn <code>InitializingBean.afterPropertiesSet</code> và init-method nếu khai.)</li><li><strong>BeanPostProcessor (after)</strong> — QUAN TRỌNG: đây là lúc bean có thể bị <strong>bọc proxy</strong> (@Transactional, @Async, security...). Nghĩa là trong constructor/@PostConstruct, proxy CHƯA chắc tồn tại.</li><li><strong>Phục vụ</strong> — nằm trong context, được tiêm vào nơi khác, nhận request.</li><li><strong><code>@PreDestroy</code></strong> — lúc app shutdown tử tế: đóng kết nối, flush buffer, nhả tài nguyên.</li></ol><p><strong>Hai hệ quả thực chiến từ trình tự này:</strong> (a) <em>đừng làm việc nặng/gọi dependency trong constructor</em> khi dùng field injection — chúng còn null (một lý do nữa cho constructor injection); (b) <em>gọi method @Transactional của CHÍNH MÌNH từ @PostConstruct</em> có thể chưa có transaction — vì proxy bọc sau bước đó (liên quan câu @Transactional không tác dụng).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như mở nhà hàng: xây xong khung (constructor) → lắp bếp, bàn ghế (tiêm dependency) → tổng duyệt, bật thử mọi thiết bị (@PostConstruct) → gắn hệ thống camera + POS của chuỗi (bọc proxy) → khai trương (phục vụ) → cuối cùng, đóng cửa đúng quy trình: tắt gas, khoá kho (@PreDestroy).</div><div class='qb-gotcha'><b>⚠ Câu bẫy hay gặp:</b> 'khác nhau giữa @PostConstruct và constructor?' — constructor lúc dependency (field-inject) chưa có + proxy chưa bọc; @PostConstruct đảm bảo tiêm đủ rồi. Và @PreDestroy KHÔNG chạy cho bean <em>prototype</em> (Spring không quản phần đời sau của prototype) — chi tiết ít người biết, nói ra là ghi điểm.</div>",
  refs:[["Spring – Lifecycle callbacks","https://docs.spring.io/spring-framework/reference/core/beans/factory-nature.html"]] },

{ id:"spr-010", legacy:151, t:"Spring Core", lv:"middle", core:false,
  tags:["circular-dependency","design","lazy"],
  q:"Circular dependency (A cần B, B cần A) trong Spring xảy ra khi nào và xử lý thế nào?",
  a:"<p><strong>Tình huống:</strong> <code>OrderService</code> cần <code>PaymentService</code>, mà <code>PaymentService</code> lại cần <code>OrderService</code>. Spring phải tạo cái nào trước? Tạo A cần có B, tạo B cần có A — gà và trứng.</p><p><strong>Chuyện gì xảy ra:</strong> với <strong>constructor injection</strong>, không có cách nào dựng được object đầu tiên (constructor đòi đủ tham số ngay) → Spring ném <code>BeanCurrentlyInCreationException</code> <em>ngay lúc khởi động</em> kèm sơ đồ vòng: ┌ A → B → A ┘. (Spring Boot mới còn tắt luôn cả 'đường lách' setter-injection mặc định — cho thấy quan điểm chính chủ: vòng là lỗi thiết kế, đừng lách.)</p><p><strong>Xử lý ĐÚNG — coi nó là mùi thiết kế và gỡ vòng:</strong></p><ul><li><strong>Tách phần chung ra bean thứ ba:</strong> thường A và B gọi nhau vì cả hai cùng ôm một mẩu logic chung → tách mẩu đó thành <code>C</code>; A → C ← B, hết vòng. Đây là lời giải đẹp nhất trong đa số case.</li><li><strong>Đảo một chiều thành sự kiện:</strong> Payment xong không 'gọi ngược' OrderService nữa mà publish <code>PaymentCompletedEvent</code>; OrderService lắng nghe (<code>@EventListener</code>). Hai bên hết biết mặt nhau.</li><li><strong>Xem lại ranh giới:</strong> hai class gọi nhau chằng chịt thường là <em>một khối bị cắt sai chỗ</em> — cân nhắc gộp hoặc cắt lại theo nghiệp vụ.</li></ul><p><strong>Xử lý TẠM (biết để trả lời, hạn chế dùng):</strong> <code>@Lazy</code> trên một bên — tiêm proxy 'hẹn khi nào gọi mới tra bean thật', phá vòng lúc khởi động nhưng <em>vòng logic vẫn nguyên</em>; setter injection tương tự. Cả hai là thuốc giảm đau, không phải thuốc chữa.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hai phòng ban mà quy trình của phòng nào cũng 'chờ phòng kia ký trước' — không hồ sơ nào chạy được. Giải pháp thật là sửa quy trình (tách bước chung, hoặc chuyển sang 'thông báo — ai liên quan tự xử lý' như event), không phải thuê người đứng giữa chuyển giấy hộ (@Lazy).</div><div class='qb-gotcha'><b>⚠ Trả lời ăn điểm:</b> đừng dừng ở '@Lazy là hết lỗi' — interviewer chờ bạn nói 'lỗi này là <em>tín hiệu thiết kế</em>, em sẽ gỡ vòng bằng tách bean/event, @Lazy chỉ là tạm'. Phân biệt được 'làm cho chạy' và 'làm cho đúng' chính là ranh giới junior/middle.</div>",
  refs:[["Spring – Circular dependencies","https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html#beans-dependency-resolution"]] },

{ id:"spr-011", legacy:38, t:"Spring Boot", lv:"middle", core:false,
  tags:["auto-configuration","conditional","starter"],
  q:"Spring Boot 'auto-configuration' hoạt động thế nào? Vì sao chỉ thêm dependency là chạy được?",
  a:"<p>Phép màu 'thêm starter vào pom.xml là có ngay DataSource/WebServer' có cơ chế rất đời thường — ba mảnh ghép:</p><p><strong>1. Danh sách ứng viên:</strong> mỗi thư viện auto-config (như <code>spring-boot-autoconfigure</code>) chứa file khai báo <code>...AutoConfiguration.imports</code> liệt kê hàng trăm class cấu hình ứng viên: <code>DataSourceAutoConfiguration</code>, <code>WebMvcAutoConfiguration</code>, <code>KafkaAutoConfiguration</code>... Boot khởi động là đọc danh sách này.</p><p><strong>2. Điều kiện từng ứng viên — trái tim cơ chế:</strong> mỗi class cấu hình gắn các <code>@Conditional...</code>:</p><ul><li><code>@ConditionalOnClass(DataSource.class)</code> — 'chỉ kích hoạt nếu classpath CÓ class này' → đây là lý do <em>thêm dependency là bật tính năng</em>: bạn kéo driver postgres vào → class của nó xuất hiện trên classpath → điều kiện thoả → cấu hình DataSource được kích hoạt.</li><li><code>@ConditionalOnMissingBean(DataSource.class)</code> — 'chỉ tạo nếu user CHƯA tự khai bean này' → đây là lý do <em>bạn luôn ghi đè được</em>: tự khai @Bean DataSource là bản auto lùi bước. Quy tắc vàng: <strong>cấu hình của user luôn thắng</strong>.</li><li><code>@ConditionalOnProperty(...)</code> — bật/tắt theo property trong application.yml.</li></ul><p><strong>3. Starter chỉ là 'gói combo':</strong> <code>spring-boot-starter-web</code> bản thân gần như KHÔNG có code — nó là pom kéo về đúng bộ dependency (MVC + Tomcat + Jackson), để bước 2 'thấy class' mà kích hoạt các cấu hình tương ứng.</p><p><strong>Debug khi 'sao nó không tự cấu hình?':</strong> chạy với <code>--debug</code> → Boot in <em>Condition Evaluation Report</em>: từng auto-config matched/not matched kèm lý do — đọc báo cáo này thay vì đoán.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như khách sạn thông minh: danh sách dịch vụ có sẵn (imports), mỗi dịch vụ có điều kiện kích hoạt — 'thấy khách mang giày chạy bộ thì chuẩn bị bản đồ đường chạy' (OnClass), 'trừ khi khách đã có HDV riêng' (OnMissingBean). Bạn không gọi dịch vụ nào cả — hành lý của bạn (classpath) nói thay.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối kinh điển:</b> 'muốn tắt một auto-config?' — <code>@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)</code> hoặc property <code>spring.autoconfigure.exclude</code>. Và nhớ nhấn: auto-config chỉ là <em>mặc định thông minh</em>, mọi thứ ghi đè được — trả lời thế cho thấy bạn không coi Boot là hộp đen.</div>",
  refs:[["Spring Boot – Auto-configuration","https://docs.spring.io/spring-boot/reference/using/auto-configuration.html"]] },

{ id:"spr-012", legacy:39, t:"Spring Boot", lv:"junior", core:false,
  tags:["configuration-properties","profiles","config"],
  q:"<code>@Value</code> và <code>@ConfigurationProperties</code> khác gì? Profiles để làm gì?",
  a:"<p><strong>Bối cảnh:</strong> app cần config (URL dịch vụ, timeout, feature flag...) nằm trong <code>application.yml</code> — hai cách đưa vào code:</p><p><strong><code>@Value</code> — tiêm lẻ từng giá trị:</strong> <code>@Value(\"${app.timeout:30}\")</code> (sau dấu <code>:</code> là mặc định). Hợp khi cần 1-2 giá trị đơn giản. Nhược khi config phình: rải chuỗi <code>${...}</code> khắp codebase (gõ sai key chỉ phát hiện lúc chạy), không gom nhóm, khó validate.</p><p><strong><code>@ConfigurationProperties</code> — map cả CỤM config vào một object có kiểu:</strong></p><pre><code class='language-java'>@ConfigurationProperties(prefix = \"app.payment\")\nrecord PaymentProps(\n    @NotBlank String baseUrl,      // validate được bằng Bean Validation!\n    Duration timeout,               // tự đổi kiểu xịn: '30s' → Duration\n    List&lt;String&gt; allowedMethods\n) {}\n// application.yml:  app.payment: { base-url: ..., timeout: 30s, ... }</code></pre><p>Ưu: gom nhóm có cấu trúc, <strong>type-safe</strong>, validate lúc khởi động (config sai là app từ chối chạy — fail sớm), IDE gợi ý key. Quy tắc thực dụng: <em>quá 2-3 giá trị cùng chủ đề → chuyển @ConfigurationProperties</em>.</p><p><strong>Profiles — bộ config theo MÔI TRƯỜNG:</strong> cùng code nhưng dev trỏ DB local, staging trỏ DB test, prod trỏ DB thật. Đặt file <code>application-dev.yml</code>, <code>application-prod.yml</code>; kích hoạt bằng <code>spring.profiles.active=prod</code> (biến môi trường khi deploy) — Boot nạp file gốc rồi <em>đè</em> bằng file profile đang bật. Kèm <code>@Profile(\"dev\")</code> trên bean để bean chỉ tồn tại ở môi trường đó (vd bean fake payment cho dev).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> @Value như xin từng món gia vị lẻ; @ConfigurationProperties như nhận nguyên <em>khay gia vị dán nhãn, được kiểm đủ trước khi vào bếp</em>. Profiles như cùng một công thức nấu nhưng ba bảng nguyên liệu: bản nấu thử, bản đãi khách quen, bản tiệc lớn — chọn bảng bằng một câu lệnh khi vào ca.</div><div class='qb-gotcha'><b>⚠ Điểm cộng vận hành:</b> thứ tự ưu tiên config của Boot (cao đè thấp): tham số dòng lệnh &gt; biến môi trường &gt; application-{profile}.yml &gt; application.yml — nắm được là debug 'sao config không ăn' rất nhanh. Và secret (mật khẩu DB) không nằm trong file yml commit lên git — đi đường biến môi trường/secret manager.</div>",
  refs:[["Spring Boot – Externalized configuration","https://docs.spring.io/spring-boot/reference/features/external-config.html"]] },

{ id:"spr-013", legacy:154, t:"Spring MVC", lv:"middle", core:true,
  tags:["filter","interceptor","aop","spring-mvc"],
  q:"Filter, Interceptor (HandlerInterceptor) và AOP aspect khác nhau ra sao? Thứ tự chạy?",
  a:"<p>Ba 'trạm gác' mà một request đi qua, nằm ở ba tầng khác nhau — chọn sai tầng là code đặt nhầm chỗ:</p><p><strong>Thứ tự từ ngoài vào trong:</strong></p><pre><code class='language-text'>Request → [Filter] → DispatcherServlet → [Interceptor] → [AOP aspect] → Controller method</code></pre><ul><li><strong>Filter</strong> — tầng Servlet, chạy TRƯỚC cả Spring MVC. Nó chỉ thấy request/response 'thô' (chưa biết controller nào sẽ xử lý). Hợp cho việc <em>không cần biết đích đến</em>: xác thực token (Spring Security chính là một chuỗi filter), nén gzip, CORS, ghi access log, chặn IP.</li><li><strong>Interceptor</strong> — tầng Spring MVC, chạy SAU khi DispatcherServlet đã chọn được handler. Nó biết <em>controller/method nào sắp chạy</em> (có HandlerMethod), có 3 móc: <code>preHandle</code> (trước), <code>postHandle</code> (sau khi controller xong, trước render), <code>afterCompletion</code> (xong hẳn, kể cả khi lỗi). Hợp cho: đo thời gian theo endpoint, kiểm tra quyền theo annotation, gắn thông tin user vào context.</li><li><strong>AOP aspect</strong> — tầng <em>bean/method</em>, không dính gì tới web: bọc lời gọi method qua proxy. Hợp cho logic cắt ngang ở mức nghiệp vụ: @Transactional (chính là AOP), retry, cache, log tham số method — và dùng được cho cả method <em>không phải</em> web (scheduler, consumer).</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Vào toà nhà công ty: Filter = bảo vệ cổng (soát mọi người, chưa cần biết bạn gặp ai); Interceptor = lễ tân tầng (biết bạn có hẹn với phòng nào, ghi giờ vào-ra); AOP = trợ lý riêng của từng trưởng phòng (bọc quanh chính cuộc họp, ai gọi trưởng phòng — qua điện thoại hay trực tiếp — đều qua trợ lý).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'lỗi ném từ Filter có bị @RestControllerAdvice bắt không?' — <strong>Không</strong>: advice sống trong thế giới Spring MVC, mà Filter đứng ngoài cổng đó. Đây là lý do lỗi xác thực trong Security filter cần <code>AuthenticationEntryPoint</code> riêng thay vì handler thường — bẫy kinh điển khi làm API error format thống nhất.</div>",
  refs:[["Baeldung – Filters vs Interceptors","https://www.baeldung.com/spring-mvc-handlerinterceptor-vs-filter"]] },

{ id:"spr-014", legacy:155, t:"Spring MVC", lv:"middle", core:true,
  tags:["exception-handling","controlleradvice","spring-mvc"],
  q:"Xử lý exception tập trung cho REST API — dùng gì để không lặp try/catch khắp controller?",
  a:"<p><strong>Vấn đề:</strong> mỗi controller tự try/catch và tự dựng response lỗi → 30 controller là 30 kiểu lỗi khác nhau, FE khóc, code trùng lặp. <strong>Lời giải: gom hết về một chỗ</strong> bằng <code>@RestControllerAdvice</code> + <code>@ExceptionHandler</code>:</p><pre><code class='language-java'>@RestControllerAdvice\nclass ApiExceptionHandler {\n    @ExceptionHandler(OrderNotFoundException.class)\n    ResponseEntity&lt;ApiError&gt; notFound(OrderNotFoundException e) {\n        return ResponseEntity.status(404)\n            .body(new ApiError(\"ORDER_NOT_FOUND\", e.getMessage()));\n    }\n    @ExceptionHandler(MethodArgumentNotValidException.class)   // lỗi @Valid\n    ResponseEntity&lt;ApiError&gt; badInput(MethodArgumentNotValidException e) { ... }  // 400 + field nào sai\n    @ExceptionHandler(Exception.class)                          // lưới cuối\n    ResponseEntity&lt;ApiError&gt; unknown(Exception e) {\n        log.error(\"Unhandled\", e);                              // log ĐẦY ĐỦ ở đây\n        return ResponseEntity.status(500).body(new ApiError(\"INTERNAL\", \"Có lỗi xảy ra\"));  // KHÔNG lộ chi tiết\n    }\n}</code></pre><p><strong>Cơ chế:</strong> exception nổi lên từ controller/service, DispatcherServlet không thấy ai bắt sẽ tra danh sách @ExceptionHandler — chọn handler <em>khớp kiểu cụ thể nhất</em> (con trước cha). Controller từ đó chỉ ném exception ngữ nghĩa (OrderNotFoundException) và trả về happy path — sạch bong.</p><p><strong>Response lỗi nên có cấu trúc thống nhất:</strong> mã lỗi máy-đọc-được (<code>ORDER_NOT_FOUND</code>), thông điệp cho người, chi tiết field (với validation), trace id để dò log. FE xử lý bằng switch theo mã, không parse tiếng Việt.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Thay vì mỗi nhân viên tự trả lời khiếu nại theo cách riêng, công ty có MỘT bộ phận CSKH: mọi khiếu nại đổ về đó, trả lời theo mẫu chuẩn, phân loại theo mã. Nhân viên chỉ việc 'chuyển lên CSKH' (throw).</div><div class='qb-gotcha'><b>⚠ Ba lỗi hay gặp:</b> (1) handler <code>Exception.class</code> trả nguyên <code>e.getMessage()</code>/stack trace cho client — lộ nội bộ, lỗ hổng bảo mật; message 500 phải chung chung, chi tiết nằm trong log. (2) Bắt exception ở service rồi <em>nuốt</em> → advice không bao giờ thấy. (3) Lỗi từ Security filter không qua advice (xem câu Filter/Interceptor) — cần cấu hình riêng.</div>",
  refs:[["Spring – Error Handling","https://spring.io/blog/2013/11/01/exception-handling-in-spring-mvc"]] },

{ id:"spr-015", legacy:156, t:"Spring MVC", lv:"junior", core:false,
  tags:["requestparam","pathvariable","requestbody","spring-mvc"],
  q:"<code>@RequestParam</code>, <code>@PathVariable</code>, <code>@RequestBody</code> lấy dữ liệu từ đâu?",
  a:"<p>Ba annotation 'rút' dữ liệu từ ba ngăn khác nhau của request (đây là phiên bản Spring của câu path/query/body bên nhóm Web):</p><pre><code class='language-java'>//        ┌── @PathVariable: từ ĐƯỜNG DẪN — định danh resource\nGET /orders/{id}?page=2&amp;status=PAID\n//                └── @RequestParam: từ QUERY STRING — lọc, phân trang, tuỳ chọn\n\n@GetMapping(\"/orders/{id}\")\nOrderDto get(@PathVariable Long id,\n             @RequestParam(defaultValue = \"1\") int page,\n             @RequestParam(required = false) String status) { ... }\n\n@PostMapping(\"/orders\")   // @RequestBody: từ BODY — dữ liệu tạo/sửa (JSON → object)\nOrderDto create(@RequestBody @Valid CreateOrderRequest req) { ... }</code></pre><ul><li><strong>@PathVariable</strong> — phần <em>bắt buộc</em> của định danh: thiếu là URL trỏ vào hư không. Kiểu được convert tự động (String → Long), sai kiểu → 400.</li><li><strong>@RequestParam</strong> — tham số <em>tuỳ chọn khi đọc</em>: nên có <code>defaultValue</code> hoặc <code>required = false</code> (mặc định required = true — quên là dính 400 khó hiểu).</li><li><strong>@RequestBody</strong> — Jackson deserialize JSON body vào object. Mỗi method chỉ MỘT @RequestBody (body chỉ đọc được một lần). GET không dùng body.</li></ul><div class='qb-eli5'><b>🌱 Mẹo nhớ:</b> Path = số nhà (thiếu là thư không tới); Query = ghi chú giao hàng (không có cũng giao được); Body = món đồ trong hộp (chỉ có khi gửi đồ — POST/PUT/PATCH).</div><div class='qb-gotcha'><b>⚠ Hay bị hỏi kèm:</b> 'nhiều query param quá thì sao?' — gom vào một object không cần annotation (Spring tự bind từng field từ query param — kiểu này gọi là binding object/@ModelAttribute); code gọn và validate được. Và nhắc được: dữ liệu nhạy cảm không đi query param (lộ trong log/history) là điểm cộng.</div>",
  refs:[["Baeldung – @RequestParam vs @PathVariable","https://www.baeldung.com/spring-request-param"]] },

{ id:"spr-016", legacy:157, t:"Spring MVC", lv:"middle", core:false,
  tags:["validation","valid","bean-validation","spring-mvc"],
  q:"Validate dữ liệu request trong Spring — <code>@Valid</code> hoạt động thế nào? Bắt lỗi validation ở đâu?",
  a:"<p><strong>Nguyên tắc:</strong> dữ liệu từ ngoài vào là KHÔNG ĐÁNG TIN cho tới khi được kiểm — và việc kiểm nên <em>khai báo</em> (annotation) thay vì viết if-else thủ công rải rác.</p><p><strong>Hai mảnh ghép:</strong></p><pre><code class='language-java'>// ① Luật đặt trên DTO (Bean Validation — jakarta.validation):\nrecord CreateOrderRequest(\n    @NotBlank(message = \"Thiếu mã sản phẩm\") String productId,\n    @Min(value = 1, message = \"Số lượng tối thiểu 1\") int quantity,\n    @Email String contactEmail,\n    @Valid ShippingInfo shipping        // ← validate lồng cả object con\n) {}\n\n// ② Kích hoạt tại controller bằng @Valid:\n@PostMapping(\"/orders\")\nOrderDto create(@RequestBody @Valid CreateOrderRequest req) { ... }</code></pre><p>Thiếu <code>@Valid</code> ở tham số là các annotation trên DTO <strong>đứng im như trang trí</strong> — bug 'validate không chạy' số 1.</p><p><strong>Lỗi đi đâu?</strong> Validation fail → Spring ném <code>MethodArgumentNotValidException</code> TRƯỚC khi vào method → bắt tập trung tại @RestControllerAdvice, duyệt <code>e.getBindingResult().getFieldErrors()</code> để trả 400 kèm danh sách <em>field nào sai, vì sao</em> — FE hiển thị đúng ô lỗi.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như mẫu đơn có ô đánh dấu 'bắt buộc, tối đa 50 ký tự' in sẵn (annotation trên DTO) + nhân viên nhận hồ sơ đối chiếu ngay tại quầy (@Valid ở controller) — hồ sơ lỗi bị trả tại chỗ kèm tờ liệt kê thiếu gì, không lọt vào trong rồi mới phát hiện.</div><div class='qb-gotcha'><b>⚠ Phân tầng cho đúng:</b> Bean Validation lo <em>hình thức</em> (rỗng, độ dài, định dạng). Luật <em>nghiệp vụ</em> (tồn kho đủ không, user được phép mua không) thuộc tầng service — đừng nhét vào annotation. Câu nối hay gặp: 'validate @PathVariable/@RequestParam thì sao?' — đặt constraint trực tiếp trên tham số + <code>@Validated</code> trên class; lỗi lúc này là <code>ConstraintViolationException</code> (KHÁC exception ở trên) — advice phải bắt cả hai.</div>",
  refs:[["Baeldung – Validation in Spring Boot","https://www.baeldung.com/spring-boot-bean-validation"]] },

{ id:"spr-017", legacy:159, t:"Spring Boot", lv:"middle", core:false,
  tags:["actuator","graceful-shutdown","kubernetes","spring-boot"],
  q:"Spring Boot Actuator để làm gì? Vì sao cần graceful shutdown khi chạy trên Kubernetes?",
  a:"<p><strong>Actuator</strong> = bộ 'đồng hồ đo và nút vận hành' gắn sẵn vào app, mở qua các endpoint <code>/actuator/*</code>:</p><ul><li><code>/actuator/health</code> — sống hay chết, kèm chi tiết từng dependency (DB, Redis, disk). Quan trọng nhất: K8s dùng nó làm <strong>liveness probe</strong> (treo thì restart) và <strong>readiness probe</strong> (chưa sẵn sàng thì đừng đổ traffic vào) — Boot tách sẵn 2 group <code>/health/liveness</code> và <code>/health/readiness</code>.</li><li><code>/actuator/metrics</code> + Prometheus endpoint — JVM memory, GC, HTTP latency, Hikari pool... nguồn số liệu cho Grafana.</li><li><code>/actuator/info</code>, <code>/env</code>, <code>/loggers</code> (đổi log level runtime — cứu tinh khi debug production), <code>/threaddump</code>, <code>/heapdump</code>.</li></ul><p>Lưu ý an ninh: chỉ expose thứ cần (<code>management.endpoints.web.exposure.include</code>), các endpoint nhạy cảm (env, heapdump) phải chặn từ ngoài — để hở là tự tặng dữ liệu cho attacker.</p><p><strong>Graceful shutdown — vì sao sống còn trên K8s:</strong> K8s tắt pod <em>thường xuyên</em> (rolling deploy, scale down, node drain) bằng cách gửi <code>SIGTERM</code>. Không graceful thì app chết ngang: request đang xử lý đứt giữa chừng, transaction dở dang, connection không đóng — user thấy lỗi mỗi lần deploy. Bật <code>server.shutdown=graceful</code>: nhận SIGTERM → ngừng NHẬN request mới nhưng <strong>xử lý nốt request đang chạy</strong> (trong <code>timeout-per-shutdown-phase</code>, mặc định 30s) → đóng tài nguyên tử tế → thoát.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Actuator như bảng đồng hồ + cổng chẩn đoán OBD của ô tô. Graceful shutdown như quán ăn đến giờ đóng cửa: ngừng nhận khách mới nhưng phục vụ nốt bàn đang ăn — không phải đèn tắt cái rụp đuổi khách đang cầm đũa.</div><div class='qb-gotcha'><b>⚠ Chi tiết ăn điểm phỏng vấn:</b> chuỗi tắt chuẩn trên K8s: pod bị đánh dấu Terminating → readiness fail → LB ngừng đổ traffic → SIGTERM → app xử lý nốt → thoát trước <code>terminationGracePeriodSeconds</code> (quá hạn là SIGKILL). Bug kinh điển: grace period của K8s (30s) NGẮN HƠN thời gian xử lý request dài nhất → vẫn bị chém. Và nhớ: consumer Kafka cũng cần graceful (commit offset trước khi chết — xem sự cố duplicate message).</div>",
  refs:[["Spring Boot – Actuator","https://docs.spring.io/spring-boot/reference/actuator/index.html"],["Spring Boot – Graceful shutdown","https://docs.spring.io/spring-boot/reference/web/graceful-shutdown.html"]] },

{ id:"spr-018", legacy:37, t:"Transaction", lv:"middle", core:true,
  tags:["transactional","proxy","self-invocation","spring-transaction"],
  q:"<code>@Transactional</code> đặt đúng mà không có tác dụng. 3 nguyên nhân kinh điển là gì?",
  a:"<p>Trước hết phải hiểu <strong>@Transactional hoạt động bằng gì</strong>: Spring không sửa code của bạn — nó bọc bean trong một <strong>proxy</strong>. Ai gọi method sẽ thực ra gọi vào proxy: proxy mở transaction → gọi method thật → commit/rollback. Mọi 'sự cố câm lặng' đều xoay quanh việc <em>lời gọi không đi qua proxy</em> hoặc <em>proxy quyết định không rollback</em>:</p><p><strong>1. Self-invocation — gọi chính mình qua <code>this</code>:</strong></p><pre><code class='language-java'>public void importFile() {        // không có transaction\n    this.saveBatch();             // ❌ gọi THẲNG object thật, né proxy\n}\n@Transactional\npublic void saveBatch() { ... }   // annotation thành trang trí</code></pre><p><em>Sửa:</em> tách <code>saveBatch</code> sang bean khác (đúng đắn nhất), hoặc tự tiêm proxy của chính mình (ObjectProvider) rồi gọi qua đó.</p><p><strong>2. Method không public</strong> (với proxy mặc định): proxy chỉ chặn được lời gọi public — private/protected/package method gắn @Transactional là vô hiệu, không lỗi không cảnh báo.</p><p><strong>3. Exception không thuộc diện rollback:</strong> mặc định chỉ rollback với <code>RuntimeException</code>/<code>Error</code>. Ném checked exception, hoặc <strong>catch nuốt exception ngay trong method</strong> → proxy thấy method kết thúc 'êm đẹp' → COMMIT. (Chi tiết ở câu rollback rules.)</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Proxy như thư ký ngồi TRƯỚC cửa phòng sếp — ai đến đều được ghi sổ (mở transaction). Nhưng sếp tự nói với chính mình trong phòng (this.method) thì thư ký ngoài cửa không biết gì; và người vào bằng cửa sau (non-public) cũng vậy.</div><div class='qb-gotcha'><b>⚠ Cách tự kiểm nhanh:</b> bật log <code>logging.level.org.springframework.transaction.interceptor=TRACE</code> — thấy dòng 'Getting transaction for...' quanh method nghĩa là transaction có chạy. Debug bằng bằng chứng, đừng nhìn annotation rồi đoán. Câu nối hay gặp: 'stack trace có <code>$Proxy142</code> nghĩa là gì?' — đó chính là dấu vân tay của proxy.</div>",
  refs:[["Spring – Transaction annotations","https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/annotations.html"]] },

{ id:"spr-019", legacy:40, t:"Transaction", lv:"middle", core:true,
  tags:["propagation","requires-new","spring-transaction"],
  q:"<code>REQUIRED</code> và <code>REQUIRES_NEW</code> khác nhau ra sao? Vì sao ghi audit log thường dùng <code>REQUIRES_NEW</code>?",
  a:"<p><strong>Propagation</strong> trả lời câu hỏi: method @Transactional được gọi khi <em>đang có sẵn một transaction</em> — thì tham gia chung hay tách riêng?</p><ul><li><strong>REQUIRED (mặc định):</strong> có sẵn thì <em>nhập hội</em>; chưa có thì mở mới. Nhập hội nghĩa là <strong>chung số phận</strong>: một người rollback là cả hội rollback — đúng ý muốn trong đa số nghiệp vụ (trừ kho + tạo đơn phải cùng sống cùng chết).</li><li><strong>REQUIRES_NEW:</strong> <em>tạm treo</em> transaction hiện tại, mở một transaction MỚI độc lập (connection riêng!), commit/rollback xong mới quay lại cái cũ. Hai bên <strong>không chung số phận</strong>.</li></ul><p><strong>Vì sao audit log dùng REQUIRES_NEW:</strong> yêu cầu nghiệp vụ là 'đã CỐ GẮNG chuyển tiền thì phải có vết, kể cả khi chuyển thất bại'. Nếu ghi log bằng REQUIRED (chung transaction), nghiệp vụ rollback sẽ <em>cuốn luôn dòng log</em> — mất vết đúng ca cần vết nhất. REQUIRES_NEW cho dòng log một 'mạng riêng': nghiệp vụ chết, log vẫn đã commit.</p><pre><code class='language-java'>@Transactional                       // giao dịch chính\npublic void transfer(...) {\n    auditService.logAttempt(...);    // ← @Transactional(propagation = REQUIRES_NEW)\n    ... nghiệp vụ có thể ném exception → rollback, nhưng logAttempt ĐÃ commit\n}</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> REQUIRED như ghi chung một tờ biên bản — xé là mất hết. REQUIRES_NEW như camera an ninh chạy nguồn điện riêng: thương vụ đổ bể thì băng ghi hình vẫn còn.</div><div class='qb-gotcha'><b>⚠ Hai bẫy của REQUIRES_NEW:</b> (1) nó <strong>mượn thêm một connection</strong> trong lúc connection cũ vẫn bị giữ — lạm dụng trong luồng cao tải là nhân đôi áp lực pool (liên hệ sự cố pool cạn); (2) gọi <em>trong cùng bean</em> vẫn dính bẫy self-invocation — propagation chỉ ăn khi đi qua proxy. Và biết thêm NESTED (savepoint — rollback bộ phận, cùng connection) là điểm cộng.</div>",
  refs:[["Spring – Transaction propagation","https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html"]] },

{ id:"spr-020", legacy:41, t:"Transaction", lv:"middle", core:true,
  tags:["rollback","checked-exception","spring-transaction"],
  q:"Ném một checked exception mà transaction vẫn commit (không rollback). Vì sao? Fix?",
  a:"<p><strong>Luật mặc định của Spring</strong> (thừa kế từ thời EJB): chỉ rollback khi method thoát bằng <strong>unchecked exception</strong> (RuntimeException, Error). <strong>Checked exception</strong> (IOException, SQLException, custom Exception) được coi là 'kết quả nghiệp vụ đã lường trước — người gọi tự xử' → proxy vẫn <strong>COMMIT</strong>.</p><p>Hệ quả thực tế: method update 2 bảng, bảng thứ hai ném <code>MyBusinessException extends Exception</code> → bảng thứ nhất VẪN được commit → dữ liệu nửa vời, đúng thứ transaction sinh ra để chống.</p><p><strong>Ba cách fix, chọn theo triết lý đội:</strong></p><pre><code class='language-java'>// ① Khai rõ diện rollback (giữ checked exception):\n@Transactional(rollbackFor = Exception.class)\n\n// ② Đổi hệ exception nghiệp vụ sang unchecked (hướng hiện đại, đỡ khai đi khai lại):\nclass MyBusinessException extends RuntimeException { ... }\n\n// ③ Rollback theo quyết định runtime (khi buộc phải catch):\nTransactionAspectSupport.currentTransactionStatus().setRollbackOnly();</code></pre><p><strong>Và bẫy anh em cùng mẹ:</strong> catch exception <em>nuốt luôn trong method</em> — dù là RuntimeException — thì proxy thấy method return bình thường → commit. Muốn vừa catch (để log/dịch lỗi) vừa rollback: ném tiếp, hoặc setRollbackOnly.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Proxy như công chứng viên chỉ huỷ hợp đồng khi thấy bạn <em>hét lên bỏ ngang</em> (unchecked). Bạn nói khẽ 'có vấn đề nhỏ' (checked) hoặc im lặng nuốt bực (catch) — công chứng viên hiểu là 'vẫn ký nhé' và đóng dấu.</div><div class='qb-gotcha'><b>⚠ Câu kiểm tra hiểu sâu:</b> 'transaction bị đánh dấu rollback-only bởi method con (REQUIRED lồng nhau), method cha catch exception rồi cố commit — chuyện gì xảy ra?' → <code>UnexpectedRollbackException</code>: cả transaction vẫn rollback, cha còn bị bất ngờ. Bài học: trong một transaction chung, con đã đánh dấu chết là cha không cứu được — muốn con chết riêng phải REQUIRES_NEW.</div>",
  refs:[["Spring – Rolling back","https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/rolling-back.html"]] },

{ id:"spr-021", legacy:158, t:"Transaction", lv:"middle", core:false,
  tags:["readonly","flush","spring-transaction"],
  q:"<code>@Transactional(readOnly = true)</code> thực sự có lợi ích gì, hay chỉ là 'đánh dấu cho đẹp'?",
  a:"<p>Không phải trang trí — nó bật ba tầng tối ưu thật:</p><ul><li><strong>Hibernate tắt dirty checking:</strong> transaction thường, Hibernate chụp snapshot MỌI entity load lên để cuối transaction so xem có gì thay đổi cần UPDATE (xem câu dirty checking). readOnly = true đặt <code>FlushMode.MANUAL</code> — khỏi chụp, khỏi so. Load 10.000 entity để xuất báo cáo: tiết kiệm cả bộ nhớ snapshot lẫn CPU so sánh — khác biệt đo được.</li><li><strong>Gợi ý cho JDBC/DB:</strong> <code>Connection.setReadOnly(true)</code> — driver/DB có thể tối ưu (Postgres đặt transaction READ ONLY, chặn luôn lệnh ghi lạc vào).</li><li><strong>Định tuyến read replica:</strong> trong kiến trúc primary–replica, đây là 'tín hiệu' để router (hoặc LazyConnectionDataSourceProxy) đẩy query sang replica — trụ cột của tách đọc/ghi.</li></ul><p>Và một lợi ích không đo bằng ms: <strong>tuyên bố ý định</strong>. Người đọc code (và reviewer) biết method này không ghi — ai thêm lệnh ghi vào sau này sẽ gặp lỗi ngay thay vì âm thầm chạy.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như mượn sách 'chỉ đọc tại chỗ': thủ thư khỏi photo trang gốc để đối chiếu khi trả (bỏ dirty checking), và bạn được dẫn sang phòng đọc rộng hơn (replica) thay vì chen ở quầy mượn-trả (primary).</div><div class='qb-gotcha'><b>⚠ Bẫy hay gặp:</b> (1) readOnly = true mà vẫn gọi <code>save()</code> — tuỳ stack sẽ lỗi hoặc <em>âm thầm không flush</em> (thay đổi biến mất không dấu vết — tệ hơn cả lỗi). (2) Nó KHÔNG phải rào bảo mật — chỉ là tối ưu + hint; chặn quyền ghi thật sự nằm ở phân quyền DB. (3) Đặt trên class rồi override từng method ghi bằng @Transactional thường — pattern gọn cho repository/service đọc-nhiều.</div>",
  refs:[["Vlad Mihalcea – readOnly transaction","https://vladmihalcea.com/spring-read-only-transaction-hibernate-optimization/"]] },

{ id:"spr-022", legacy:43, t:"Transaction", lv:"middle", core:true,
  tags:["isolation","dirty-read","spring-transaction"],
  q:"Kể 4 mức isolation và mỗi mức chặn được anomaly nào? Set isolation trong Spring thế nào?",
  a:"<p>(Phần lý thuyết gốc nằm ở nhóm Database — câu isolation level. Ở đây là góc nhìn Spring + bảng tra nhanh.)</p><p><strong>Ba anomaly:</strong> dirty read (đọc dữ liệu CHƯA commit của người khác), non-repeatable read (đọc lại cùng dòng ra giá trị khác), phantom read (chạy lại cùng query thấy dòng MỚI xuất hiện).</p><p><strong>Bảng tra:</strong></p><table><tr><th>Isolation</th><th>Dirty</th><th>Non-repeatable</th><th>Phantom</th></tr><tr><td>READ_UNCOMMITTED</td><td>có thể</td><td>có thể</td><td>có thể</td></tr><tr><td>READ_COMMITTED</td><td><strong>chặn</strong></td><td>có thể</td><td>có thể</td></tr><tr><td>REPEATABLE_READ</td><td><strong>chặn</strong></td><td><strong>chặn</strong></td><td>có thể*</td></tr><tr><td>SERIALIZABLE</td><td><strong>chặn</strong></td><td><strong>chặn</strong></td><td><strong>chặn</strong></td></tr></table><p style='font-size:.85em'>*Theo chuẩn SQL; thực tế InnoDB (gap lock) và Postgres (snapshot) ở mức này chặn được phần lớn phantom.</p><p><strong>Trong Spring:</strong> <code>@Transactional(isolation = Isolation.REPEATABLE_READ)</code>. Mặc định <code>Isolation.DEFAULT</code> = <em>theo DB</em> (Postgres/Oracle: READ_COMMITTED; MySQL: REPEATABLE_READ) — tức cùng một code chạy hai DB có thể hành xử khác nhau, điểm hay quên khi migrate DB.</p><div class='qb-eli5'><b>🌱 Chọn thế nào cho thực dụng:</b> giữ mặc định READ_COMMITTED cho 95% nghiệp vụ; chỗ cần đọc-ổn-định trong một giao dịch dài (báo cáo nhất quán) → REPEATABLE_READ; bất biến phức tạp kiểu 'tổng ≥ X' → SERIALIZABLE + code retry. Nâng isolation = trả giá throughput — nâng có địa chỉ, đừng nâng toàn cục.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn quen thuộc:</b> 'nâng isolation có chữa được bài trừ kho quá bán không?' — KHÔNG với check-then-act ở READ_COMMITTED (xem câu diagnostic db): cái cần là điều kiện nguyên tử trong UPDATE / khoá / optimistic lock. Isolation không phải thuốc chữa race nghiệp vụ.</div>",
  refs:[["PostgreSQL – Transaction isolation","https://www.postgresql.org/docs/current/transaction-iso.html"],["Spring – Isolation","https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/annotations.html"]] },

{ id:"spr-023", legacy:42, t:"Transaction", lv:"senior", core:false,
  tags:["transactional-event","outbox","kafka","spring-transaction"],
  q:"Publish sự kiện Kafka ngay BÊN TRONG method <code>@Transactional</code> — rủi ro gì? Cách đúng?",
  a:"<p><strong>Rủi ro — hai hệ thống, hai số phận:</strong> Kafka không tham gia transaction DB. Publish nằm giữa method nghĩa là gửi <em>trước khi commit</em>:</p><ul><li><strong>Event 'ma':</strong> publish xong → transaction ROLLBACK → consumer đã nhận event về một đơn hàng <em>không tồn tại</em>, đi xử lý (gửi mail, trừ kho) trên hư không.</li><li>Chiều ngược cũng hỏng: commit xong mới publish mà publish fail → đơn có thật nhưng <em>không ai biết</em> — event mất.</li></ul><p><strong>Nấc 1 — <code>@TransactionalEventListener(phase = AFTER_COMMIT)</code>:</strong> method chỉ publish một ApplicationEvent nội bộ; listener gửi Kafka <em>SAU khi commit thành công</em>. Diệt được event 'ma'. Nhưng chưa trọn: app crash ngay sau commit, trước khi kịp gửi → <strong>vẫn mất event</strong> (không có retry bền).</p><p><strong>Nấc 2 — Outbox pattern (chuẩn khi cần bảo đảm):</strong> trong CÙNG transaction, ghi nghiệp vụ + ghi event vào bảng <code>outbox</code> — một DB, nguyên tử sẵn. Một relay (polling/Debezium CDC) đọc outbox gửi Kafka, đánh dấu đã gửi, <em>retry được</em> khi fail. Không ma, không mất; giá phải trả: event trễ chút (eventual) + consumer vẫn phải idempotent (relay retry sinh trùng — xem sự cố duplicate message).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Publish trong transaction như hô to 'đơn chốt rồi nhé!' TRƯỚC khi ký hợp đồng — hợp đồng đổ là cả chợ đã hành động theo tin vịt. Outbox như ghi thông báo vào SỔ CÔNG TY cùng lúc ký (một nét bút, một số phận), rồi nhân viên truyền tin cứ theo sổ mà loan — loan hụt hôm nay mai loan lại.</div><div class='qb-gotcha'><b>⚠ Câu đo độ trưởng thành:</b> 'AFTER_COMMIT đủ chưa?' — trả lời phân tầng: đủ cho event 'nice-to-have' (cache invalidate, notification phụ); KHÔNG đủ cho event nghiệp vụ phải-đến (thanh toán, đối soát) → outbox. Biết đặt câu hỏi 'event này mất được không?' trước khi chọn công cụ — đó mới là điểm senior của câu này.</div>",
  refs:[["Spring – Transaction-bound events","https://docs.spring.io/spring-framework/reference/data-access/transaction/event.html"],["microservices.io – Outbox","https://microservices.io/patterns/data/transactional-outbox.html"]] },

{ id:"spr-024", legacy:44, t:"Security", lv:"middle", core:true,
  tags:["jwt","authentication","authorization","spring-security"],
  q:"JWT gồm mấy phần? Phân biệt authentication và authorization. Vì sao KHÔNG để dữ liệu nhạy cảm trong payload?",
  a:"<p>(Nền tảng chi tiết ở nhóm Nền tảng & Web — câu JWT và câu AuthN/AuthZ. Đây là bản tổng hợp góc Spring Security.)</p><p><strong>JWT = 3 phần nối bằng dấu chấm:</strong> <code>header.payload.signature</code> — header (thuật toán ký), payload (các claim: <code>sub</code>, <code>exp</code>, roles...), signature (ký trên hai phần kia bằng secret/private key). Server verify chữ ký là tin được nội dung mà <em>không cần lưu session</em> — stateless.</p><p><strong>AuthN vs AuthZ trong Spring Security:</strong></p><ul><li><strong>Authentication — bạn LÀ AI:</strong> filter đọc token → verify chữ ký + exp → dựng object <code>Authentication</code> đặt vào <code>SecurityContext</code>. Trượt bước này → <strong>401</strong>.</li><li><strong>Authorization — bạn ĐƯỢC LÀM GÌ:</strong> so quyền của Authentication với yêu cầu của endpoint — rule <code>requestMatchers(...).hasRole(\"ADMIN\")</code> hoặc <code>@PreAuthorize(\"hasRole('ADMIN')\")</code> trên method. Trượt → <strong>403</strong>.</li></ul><p><strong>Vì sao payload không được chứa đồ nhạy cảm:</strong> payload chỉ được <strong>encode base64</strong> — ai cầm token đều decode đọc được (dán vào jwt.io là thấy hết). Chữ ký chống <em>SỬA</em>, không chống <em>ĐỌC</em>. Token lại nằm ở client, lọt vào log, được copy đi khắp nơi → coi payload là công khai. Chỉ để claim tối thiểu: sub, roles, exp; dữ liệu thật tra ở server bằng sub.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> JWT như thẻ ra vào có mộc chống giả: bảo vệ soi mộc để biết thẻ THẬT (authN), rồi nhìn màu thẻ để biết bạn được vào tầng nào (authZ). Nội dung thẻ thì ai nhặt được cũng đọc — nên không ai in số tài khoản lên thẻ.</div><div class='qb-gotcha'><b>⚠ Chuỗi hỏi xoáy quen thuộc:</b> 'verify chữ ký xong là xong chưa?' — chưa: còn phải check <code>exp</code>, và lý tưởng là <code>iss</code>/<code>aud</code>; thuật toán phải bị PIN cứng (từ chối token tự khai <code>alg: none</code> — lỗ hổng lịch sử nổi tiếng). Nói được cả 401-vs-403 map vào authN-vs-authZ là tròn câu.</div>",
  refs:[["jwt.io – Introduction","https://jwt.io/introduction"],["Spring Security – Authorization","https://docs.spring.io/spring-security/reference/servlet/authorization/index.html"]] },

{ id:"spr-025", legacy:45, t:"Security", lv:"middle", core:false,
  tags:["filter-chain","security-context","spring-security"],
  q:"Spring Security 'filter chain' là gì? Authentication xảy ra ở đâu trước khi tới controller?",
  a:"<p><strong>Toàn bộ Spring Security là một CHUỖI servlet filter</strong> đứng chắn trước DispatcherServlet — request phải 'sống sót' qua chuỗi này rồi mới thấy mặt controller. Mỗi filter một nhiệm vụ, thứ tự cố định có chủ đích:</p><pre><code class='language-text'>Request\n  → SecurityContextHolderFilter     (chuẩn bị chỗ chứa danh tính cho request này)\n  → CorsFilter / CsrfFilter          (các lớp phòng thủ chung)\n  → JwtAuthFilter / UsernamePassword...  (ĐỌC credential → verify → dựng Authentication)\n  → ExceptionTranslationFilter       (dịch lỗi security → 401/403 đúng chuẩn)\n  → AuthorizationFilter              (so quyền với rule của endpoint — cho qua hay 403)\n  → DispatcherServlet → Controller</code></pre><p><strong>Authentication diễn ra ở tầng filter</strong> (không phải controller): với JWT, một filter (thường tự viết hoặc oauth2-resource-server) lấy token từ header <code>Authorization: Bearer ...</code>, verify, dựng <code>Authentication</code> bỏ vào <strong><code>SecurityContextHolder</code></strong> — 'túi danh tính' gắn theo thread của request. Từ đó về sau, mọi nơi (AuthorizationFilter, @PreAuthorize, controller lấy principal) đều đọc từ túi này.</p><p>Cấu hình hiện đại = khai một bean <code>SecurityFilterChain</code> (kiểu lambda DSL) — có thể có <em>nhiều</em> chain cho các nhóm URL khác nhau (vd chain riêng cho <code>/api/**</code> stateless, chain khác cho trang admin dùng session).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như chuỗi trạm kiểm soát sân bay theo đúng thứ tự: soi hành lý chung (CORS/CSRF) → đối chiếu hộ chiếu, phát thẻ danh tính (authentication filter + SecurityContext) → cổng ra máy bay kiểm thẻ có đúng chuyến không (authorization). Máy bay (controller) không tự soát vé — mọi việc xong trước khi bạn bước lên.</div><div class='qb-gotcha'><b>⚠ Hai điểm hay bị hỏi:</b> (1) Lỗi 401/403 từ chain này <strong>không đi qua @RestControllerAdvice</strong> (advice nằm sau DispatcherServlet) — muốn body lỗi thống nhất phải cấu hình <code>AuthenticationEntryPoint</code> (401) và <code>AccessDeniedHandler</code> (403). (2) SecurityContext mặc định gắn ThreadLocal — code async/thread khác là 'mất danh tính' nếu không truyền context (DelegatingSecurityContext...).</div>",
  refs:[["Spring Security – Architecture","https://docs.spring.io/spring-security/reference/servlet/architecture.html"]] },

{ id:"spr-026", legacy:47, t:"Security", lv:"middle", core:false,
  tags:["cors","allowcredentials","spring-security"],
  q:"Vì sao <code>allowedOrigins(\"*\")</code> kèm <code>allowCredentials(true)</code> lại lỗi? Cấu hình CORS đúng trong Spring?",
  a:"<p>(CORS là gì — xem câu ở nhóm Nền tảng & Web. Đây là phần cấu hình + cái bẫy nổi tiếng.)</p><p><strong>Vì sao cặp <code>*</code> + credentials bị cấm:</strong> <code>allowCredentials(true)</code> nghĩa là trình duyệt được gửi kèm <em>cookie/thông tin đăng nhập</em> trong request cross-origin. Nếu đồng thời cho phép <em>MỌI origin</em> (*), thì bất kỳ trang web nào trên Internet cũng có thể dùng JS gọi API của bạn <strong>bằng cookie đăng nhập của nạn nhân</strong> và đọc kết quả — chính xác kiểu tấn công mà Same-Origin Policy sinh ra để chặn. Spec CORS cấm thẳng tổ hợp này: trình duyệt từ chối khi thấy <code>Access-Control-Allow-Origin: *</code> đi cùng <code>Allow-Credentials: true</code>, còn Spring thì ném <code>IllegalArgumentException</code> ngay khi khởi động — fail sớm hộ bạn.</p><p><strong>Cấu hình đúng:</strong> liệt kê origin <em>đích danh</em> (hoặc <code>allowedOriginPatterns</code> cho subdomain):</p><pre><code class='language-java'>@Bean\nCorsConfigurationSource corsConfigurationSource() {\n    CorsConfiguration c = new CorsConfiguration();\n    c.setAllowedOrigins(List.of(\"https://app.mycorp.vn\"));   // đích danh, KHÔNG *\n    c.setAllowedMethods(List.of(\"GET\",\"POST\",\"PUT\",\"DELETE\"));\n    c.setAllowedHeaders(List.of(\"Authorization\",\"Content-Type\"));\n    c.setAllowCredentials(true);\n    ...\n}\n// và http.cors(...) trong SecurityFilterChain — để preflight OPTIONS đi qua Security</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> allowCredentials = cho khách dùng <em>thẻ thành viên của người khác đang lưu trong ví</em>; allowedOrigins(*) = cho MỌI người lạ vào quầy. Gộp hai cái = ai cũng tiêu được thẻ của bạn — nên luật cấm ghép là hợp lý, không phải Spring khó tính.</div><div class='qb-gotcha'><b>⚠ Ghi chú thực chiến:</b> API dùng Bearer token trong header (không cookie) thì thường KHÔNG cần allowCredentials — để false và mọi thứ đơn giản hẳn. Và cấu hình CORS ở MỘT chỗ (bean trên) — rải @CrossOrigin lên từng controller là mầm mống 'chỗ chạy chỗ không'.</div>",
  refs:[["MDN – CORS","https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS"],["Spring – CORS","https://docs.spring.io/spring-framework/reference/web/webmvc-cors.html"]] },

{ id:"spr-027", legacy:46, t:"Security", lv:"senior", core:false,
  tags:["jwt","logout","refresh-token","revoke"],
  q:"JWT stateless nên 'logout' / thu hồi token rất khó. Nêu các cách và đánh đổi.",
  a:"<p><strong>Gốc vấn đề:</strong> sức mạnh của JWT — server không lưu gì — chính là điểm yếu khi cần thu hồi: token đã phát ra sẽ <em>hợp lệ tới đúng giây exp</em>, server chẳng có sổ nào để gạch tên. User bấm logout, kẻ trộm token vẫn dùng được tới hết hạn. Các lời giải, mỗi cái bán đi một phần 'stateless':</p><ul><li><strong>① Access token sống NGẮN + refresh token sống dài (chuẩn ngành):</strong> access 5–15 phút — trộm được cũng chỉ dùng vài phút; refresh token <em>có lưu server</em> (DB/Redis) nên thu hồi được: logout = xoá refresh token → hết đường xin access mới. Cửa sổ rủi ro = TTL của access. Đánh đổi: thêm luồng refresh ở client, thêm bảng lưu — chấp nhận rộng rãi.</li><li><strong>② Denylist (blacklist) token/jti trong Redis:</strong> logout thì ghi <code>jti</code> (id của token) vào danh sách cấm với TTL = thời gian còn lại; mọi request verify xong phải <em>check thêm Redis</em>. Thu hồi tức thì — nhưng mỗi request tốn một lượt tra: đã 'bán' một nửa chữ stateless. Hợp khi yêu cầu 'logout phải ăn ngay' (ngân hàng, admin).</li><li><strong>③ Đổi khoá ký / version theo user:</strong> claim <code>ver</code> trong token so với version lưu theo user — 'logout mọi thiết bị' = tăng version. Thu hồi theo user chứ không theo token lẻ; vẫn cần một lượt tra (cache được).</li></ul><p><strong>Khung trả lời trưởng thành:</strong> chọn theo <em>chi phí của token sống sót thêm N phút</em>: app đọc tin tức — nấc ① là đủ; thao tác tiền/quyền cao — ① + ② cho các endpoint nhạy cảm. Nói 'JWT không logout được' là sai; nói 'thu hồi miễn phí' cũng sai — chỉ có mua lại state một cách có chọn lọc.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> JWT như vé xem phim đã in: rạp không giữ danh sách nên vé in rồi là vào được tới giờ chiếu. Muốn 'huỷ vé' phải chọn: in vé hạn 10 phút và quầy đổi vé liên tục (①), hoặc dán bảng số vé bị huỷ ở cửa soát (② — soát viên phải liếc bảng mỗi lần).</div><div class='qb-gotcha'><b>⚠ Chi tiết ăn điểm:</b> refresh token nên <strong>rotate</strong> (mỗi lần dùng cấp cái mới, cái cũ vô hiệu) + phát hiện reuse (refresh token cũ bị dùng lại = có kẻ trộm → thu hồi cả chuỗi). Đây là khuyến nghị OAuth2 hiện đại — nhắc được là vượt mặt bằng.</div>",
  refs:[["Auth0 – Revoke tokens","https://auth0.com/docs/secure/tokens/revoke-tokens"],["OAuth 2.0 Security BCP","https://datatracker.ietf.org/doc/html/draft-ietf-oauth-security-topics"]] },

{ id:"spr-028", legacy:48, t:"Spring Data JPA", lv:"middle", core:true,
  tags:["n-plus-one","fetch-join","entitygraph","spring-data-jpa"],
  q:"Vấn đề N+1 query là gì? Kể các cách fix và khi nào chọn cách nào.",
  a:"<p><strong>N+1</strong> = load danh sách N bản ghi bằng 1 query, rồi khi duyệt từng bản ghi lại chạm vào quan hệ LAZY → Hibernate âm thầm bắn <em>thêm 1 query cho MỖI bản ghi</em>. Tổng: 1 + N câu. 500 đơn hàng = 501 câu — từng câu nhanh (3ms) nên slow-query-log <strong>mù</strong>, nhưng cộng lại vài giây (đã mổ xẻ trọn vẹn trong Incident Lab — sự cố N+1).</p><p><strong>Nhận diện:</strong> bật <code>show-sql</code>/APM thấy hàng loạt câu GIỐNG NHAU chỉ khác tham số; thời gian trang tỉ lệ thuận số bản ghi.</p><p><strong>Bộ cách fix — chọn theo tình huống:</strong></p><ul><li><strong>JOIN FETCH</strong> (JPQL): <code>select o from Order o join fetch o.items where ...</code> — 1 câu lấy cả cha lẫn con. Chọn khi: một use-case cụ thể cần đúng quan hệ đó. Cẩn thận: fetch join 2 collection song song → cartesian product; kèm phân trang bị Hibernate kéo hết về memory rồi cắt (cảnh báo HHH000104).</li><li><strong>@EntityGraph</strong> trên repository method — khai 'load kèm gì' mà không viết JPQL; cùng bản chất fetch join, gọn cho derived query.</li><li><strong>DTO projection</strong> — query thẳng các cột màn hình cần, bỏ qua entity: nhanh nhất, không dirty checking, không mìn lazy. <em>Mặc định tốt nhất cho màn hình đọc.</em></li><li><strong><code>default_batch_fetch_size</code></strong> (vd 100) — thuốc giảm đau diện rộng: gom N câu lẻ thành N/100 câu <code>IN (...)</code>. Chọn khi codebase cũ nhiều điểm N+1, chưa sửa từng chỗ được. Không thay được thiết kế fetch đúng.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Đi chợ mua đồ cho 30 món ăn: N+1 = 30 chuyến xe, mỗi chuyến mua 1 món. Fetch join = 1 chuyến có danh sách đầy đủ. Batch size = gom 30 chuyến thành 3 chuyến, mỗi chuyến 10 món — đỡ hơn hẳn nhưng vẫn không bằng lên danh sách từ đầu.</div><div class='qb-gotcha'><b>⚠ Chốt chống tái phát:</b> viết test đếm query (bật <code>hibernate.generate_statistics</code>, assert <code>getPrepareStatementCount() &lt;= k</code>) — hiệu năng cũng cần regression test; người sau thêm một <code>.getItems()</code> vào vòng lặp là CI đỏ ngay thay vì production chậm sau 3 tháng.</div>",
  refs:[["Vlad Mihalcea – N+1","https://vladmihalcea.com/n-plus-1-query-problem/"],["Spring Data – EntityGraph","https://docs.spring.io/spring-data/jpa/reference/jpa/entity-graph.html"]] },

{ id:"spr-029", legacy:49, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["lazyinitializationexception","osiv","dto","spring-data-jpa"],
  q:"<code>LazyInitializationException</code> xảy ra khi nào? Cách fix đúng (và cách fix sai)?",
  a:"<p><strong>Khi nào nổ:</strong> bạn chạm vào một quan hệ LAZY (proxy chưa nạp) <em>sau khi persistence context (session) đã đóng</em>. Kịch bản kinh điển: service có @Transactional load entity → return → transaction đóng, session đóng → controller/Jackson serialize chạm <code>order.getItems()</code> → proxy không còn session đứng sau để query → <code>LazyInitializationException: could not initialize proxy — no Session</code>.</p><p><strong>Bản chất là lỗi RANH GIỚI:</strong> entity 'sống' gắn với session; lôi nó ra ngoài vùng có session rồi mới đòi nạp thêm là đòi hỏi vô lý. Fix đúng = quyết định <em>dữ liệu cần gì</em> NGAY trong vùng có session:</p><ul><li>✅ <strong>Map sang DTO trong service</strong> (trong transaction): controller chỉ nhận DTO 'chết' đầy đủ dữ liệu — sạch tầng, hết mìn. Cách nên là mặc định.</li><li>✅ <strong>Fetch join / @EntityGraph</strong> cho use-case cần quan hệ đó — nạp đủ trước khi ra khỏi vùng.</li></ul><p><strong>Các cách fix SAI (nhưng phổ biến):</strong></p><ul><li>❌ <strong>Bật EAGER toàn cục</strong> — chữa chỗ này bằng cách bắt MỌI chỗ khác over-fetch (xem câu FetchType).</li><li>❌ <strong>Open Session In View (OSIV)</strong> — giữ session mở tới hết request: exception biến mất nhưng đổi bằng N+1 rình rập ở tầng view + connection bị giữ suốt render + che mất lỗi thiết kế. Spring Boot bật mặc định (spring.jpa.open-in-view=true) và còn in cảnh báo — hệ nghiêm túc nên tắt và sửa đúng cách.</li><li>❌ <code>Hibernate.initialize()</code> rải khắp nơi — chữa triệu chứng từng điểm, code rác dần.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Entity lazy như cuốn sách mượn 'đọc tại thư viện' — ra khỏi cửa (session đóng) mới giở trang chưa photo là bị chặn. Fix đúng: photo đủ trang cần TRƯỚC khi ra (DTO/fetch join). Fix sai: bắt thư viện mở cửa 24/7 đi theo bạn (OSIV).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối kinh điển:</b> 'sao ở máy em chạy được mà lên server lỗi?' — vì local đang bật OSIV còn cấu hình server tắt (hoặc code path khác đi qua async thread — session không theo thread mới). Trả lời được 'OSIV là lý do nó *tình cờ* chạy' chứng tỏ hiểu cơ chế thật.</div>",
  refs:[["Baeldung – LazyInitializationException","https://www.baeldung.com/hibernate-initialize-proxy-exception"],["Vlad Mihalcea – OSIV anti-pattern","https://vladmihalcea.com/the-open-session-in-view-anti-pattern/"]] },

{ id:"spr-030", legacy:50, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["dirty-checking","flush","persistence-context","spring-data-jpa"],
  q:"Vì sao sửa field của entity trong <code>@Transactional</code> mà không gọi <code>save()</code> vẫn được lưu xuống DB?",
  a:"<p>Đây là tính năng <strong>dirty checking</strong> — và hiểu nó là hiểu nửa Hibernate:</p><p><strong>Cơ chế:</strong> khi load một entity, persistence context không chỉ giữ entity mà còn chụp một <strong>snapshot</strong> trạng thái lúc load. Đến thời điểm <strong>flush</strong> (thường là ngay trước commit), Hibernate so entity hiện tại với snapshot — field nào lệch là tự sinh câu <code>UPDATE</code>. Bạn không gọi save; bản thân entity <em>đang được quản lý (managed)</em> nên mọi thay đổi được 'ghi sổ' tự động:</p><pre><code class='language-java'>@Transactional\npublic void doiTen(Long id, String ten) {\n    User u = userRepo.findById(id).orElseThrow();  // u = MANAGED, có snapshot\n    u.setTen(ten);                                  // chỉ set — không save()\n}   // flush trước commit: so snapshot → sinh UPDATE users SET ten = ?</code></pre><p><strong>Hệ quả cần khắc cốt:</strong></p><ul><li><code>save()</code> với entity managed là <em>thừa</em> (vô hại nhưng thể hiện chưa hiểu cơ chế).</li><li><strong>Chiều nguy hiểm:</strong> sửa entity managed 'chỉ để tính toán tạm' — không định lưu — nhưng nó VẪN bị UPDATE xuống DB. Muốn dữ liệu không đổi: đừng sửa entity managed, hoặc dùng DTO.</li><li>Ngoài transaction/session, entity là <em>detached</em> — sửa thoải mái, không ai lưu (lúc này mới cần merge/save).</li><li>Chi phí: mỗi entity managed tốn snapshot + công so sánh khi flush — lý do <code>readOnly = true</code> tắt nó cho luồng đọc (xem câu readOnly).</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Khách sạn chụp ảnh minibar lúc bạn nhận phòng (snapshot). Trả phòng (commit), nhân viên so ảnh với hiện trạng — thiếu lon nào tự tính tiền lon đó. Bạn không cần 'khai báo đã uống' (save) — và cũng đừng 'mượn lon ra ngắm rồi để lệch chỗ': ảnh so lệch là bị tính tiền (update ngoài ý muốn).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối:</b> 'flush và commit khác gì?' — flush = ĐẨY các câu SQL xuống DB (chưa chốt); commit = chốt vĩnh viễn. Flush có thể xảy ra sớm hơn bạn nghĩ (trước một query JPQL đụng bảng liên quan — để query thấy dữ liệu mới nhất). Chi tiết ở câu flush.</div>",
  refs:[["Vlad Mihalcea – Dirty checking","https://vladmihalcea.com/the-anatomy-of-hibernate-dirty-checking/"]] },

{ id:"spr-031", legacy:51, t:"Spring Data JPA", lv:"senior", core:true,
  tags:["optimistic-lock","pessimistic-lock","version","spring-data-jpa"],
  q:"Optimistic lock và Pessimistic lock trong JPA — dùng thế nào (@Version, @Lock) và chọn cái nào khi nào?",
  a:"<p>(Nguyên lý hai trường phái — xem câu cùng tên bên nhóm Database. Đây là cách 'bấm nút' trong JPA + điểm riêng của tầng ORM.)</p><p><strong>Optimistic — <code>@Version</code>:</strong></p><pre><code class='language-java'>@Entity class Product {\n    @Id Long id;\n    @Version Long version;    // ← chỉ cần thế này\n}\n// Hibernate tự sinh:  UPDATE product SET ..., version = 8 WHERE id = ? AND version = 7\n// 0 dòng bị ảnh hưởng → ném OptimisticLockingFailureException</code></pre><p>Không khoá gì khi đọc; xung đột bị phát hiện lúc ghi. App phải <strong>xử lý exception có chủ đích</strong>: cho user reload + báo 'dữ liệu đã bị người khác sửa', hoặc retry tự động với dữ liệu mới (cẩn thận retry đè mất ý định người trước).</p><p><strong>Pessimistic — <code>@Lock</code> trên repository method:</strong></p><pre><code class='language-java'>@Lock(LockModeType.PESSIMISTIC_WRITE)     // → SELECT ... FOR UPDATE\n@Query(\"select p from Product p where p.id = :id\")\nOptional&lt;Product&gt; findByIdForUpdate(Long id);</code></pre><p>Khoá dòng ngay khi đọc — người khác đụng dòng này phải chờ tới khi transaction bạn xong. CHỈ có nghĩa bên trong transaction; kèm rủi ro chờ lâu/deadlock — đặt lock timeout.</p><p><strong>Chọn:</strong> đụng độ hiếm (form sửa hồ sơ, cấu hình) → optimistic (mặc định nên có @Version trên entity nghiệp vụ quan trọng — rẻ, không tốn khoá). Đụng độ dày trên tài nguyên nóng (trừ kho flash sale, số dư) → pessimistic hoặc UPDATE điều kiện nguyên tử.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> @Version như số phiên bản trên Google Doc: hai người cùng sửa offline, ai sync sau bị báo 'bản đã đổi, xem lại'. @Lock như mượn sổ gốc khỏi tủ — người sau đứng chờ trả sổ.</div><div class='qb-gotcha'><b>⚠ Bẫy riêng của tầng JPA:</b> optimistic lock chỉ phát nổ lúc <strong>flush/commit</strong> — nghĩa là mọi logic TRƯỚC đó đã chạy trên dữ liệu cũ; đừng đặt side-effect (gửi mail, gọi API) trước điểm ghi. Và version chỉ tăng khi <em>entity đó</em> đổi — bảo vệ bất biến trải trên nhiều bảng thì @Version một bảng không đủ (nhớ lại write skew).</div>",
  refs:[["Baeldung – JPA optimistic locking","https://www.baeldung.com/jpa-optimistic-locking"],["Vlad Mihalcea – Optimistic vs pessimistic","https://vladmihalcea.com/optimistic-vs-pessimistic-locking/"]] },

{ id:"spr-032", legacy:160, t:"Spring Data JPA", lv:"middle", core:true,
  tags:["entity-lifecycle","managed","detached","spring-data-jpa"],
  q:"4 trạng thái vòng đời của entity JPA (transient / managed / detached / removed) là gì?",
  a:"<p>Mọi hành vi 'kỳ lạ' của JPA (tự lưu không cần save, LazyInitializationException, merge làm gì...) đều giải thích được bằng câu hỏi: <strong>entity này đang ở trạng thái nào so với persistence context?</strong></p><ul><li><strong>Transient (mới sinh):</strong> vừa <code>new Order()</code> — chỉ là object Java thường, persistence context không biết, DB không có. Không ai theo dõi nó.</li><li><strong>Managed (được quản lý):</strong> entity nằm TRONG persistence context — do <code>findById</code> load lên, hoặc sau khi <code>persist/save</code>. Đặc quyền: được <strong>dirty checking</strong> (sửa là tự UPDATE khi flush), lazy load hoạt động, cùng id trong cùng context là <em>cùng một object</em> (identity map).</li><li><strong>Detached (rời chuồng):</strong> từng managed nhưng context đã đóng (transaction kết thúc) hoặc bị <code>detach/clear</code>. Object còn đó, dữ liệu còn đó, nhưng: sửa <em>không ai lưu</em>, chạm lazy <em>nổ</em>. Muốn 'nhập chuồng lại' → <code>merge</code> (trả về bản managed MỚI — bản cũ vẫn detached, bẫy hay gặp!).</li><li><strong>Removed (án tử):</strong> đã gọi <code>remove/delete</code> khi đang managed — bị đánh dấu, câu DELETE chạy lúc flush.</li></ul><pre><code class='language-text'>new → [persist/save] → MANAGED → [tx đóng] → DETACHED → [merge] → MANAGED\n                          │\n                       [remove] → REMOVED → [flush] → hết đời</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bệnh nhân trong bệnh viện: transient = người ngoài đường (chưa hồ sơ); managed = đang nội trú — mọi thay đổi được y tá ghi sổ tự động (dirty checking); detached = đã xuất viện — tự thay băng ở nhà không ai ghi sổ; removed = hồ sơ đã ký huỷ chờ xử lý.</div><div class='qb-gotcha'><b>⚠ Vì sao phải thuộc bảng này:</b> 90% câu hỏi JPA khó là trạng thái trá hình — 'sao sửa không lưu?' (detached), 'sao không save vẫn lưu?' (managed), 'sao merge xong sửa bản cũ không ăn?' (merge trả bản mới), 'sao equals hai lần load khác nhau?' (khác context = khác object). Trả lời bằng trạng thái là trúng tim mọi biến thể.</div>",
  refs:[["Vlad Mihalcea – Entity states","https://vladmihalcea.com/jpa-persist-and-merge/"]] },

{ id:"spr-033", legacy:161, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["persist","merge","save","saveandflush"],
  q:"<code>persist</code>, <code>merge</code>, <code>save</code>, <code>saveAndFlush</code> khác nhau ra sao?",
  a:"<p>Bốn cái tên, hai tầng API — hiểu tầng là hết rối:</p><p><strong>Tầng JPA (EntityManager) — hai thao tác gốc:</strong></p><ul><li><strong><code>persist(entity)</code></strong> — 'nhận đứa TRANSIENT này vào quản lý': entity trở thành managed, INSERT sẽ chạy lúc flush. Gọi trên entity đã có id/detached → lỗi. Không trả về gì — chính object bạn đưa vào trở thành managed.</li><li><strong><code>merge(entity)</code></strong> — 'lấy dữ liệu của bản DETACHED này đắp vào bản managed': load (hoặc tìm trong context) bản managed cùng id, copy field sang, <strong>trả về bản managed</strong>. Nhớ kỹ: object bạn truyền vào VẪN detached — làm việc tiếp phải dùng <em>kết quả trả về</em>, không phải bản cũ (bug kinh điển).</li></ul><p><strong>Tầng Spring Data (<code>save</code>) — vỏ tiện dụng:</strong> <code>save(e)</code> = <em>if (e mới) persist else merge</em> — phán 'mới' bằng id null (hoặc @Version null). Tiện, nhưng che mất ngữ nghĩa: save một entity detached là chạy merge — tức có thể phát sinh thêm một câu SELECT để load bản managed trước khi update.</p><p><strong><code>saveAndFlush</code></strong> = save + ép <strong>flush ngay</strong> (đẩy SQL xuống DB tại chỗ, thay vì đợi trước commit). Dùng khi cần: thấy lỗi constraint NGAY tại dòng này, cần DB sinh giá trị để dùng tiếp, hoặc query native ngay sau đó phải thấy dữ liệu. Đừng rắc bừa — flush sớm liên tục là mất lợi ích gom lệnh của Hibernate.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> persist = đăng ký nhập học cho học sinh MỚI. merge = học sinh cũ mang bảng điểm photo về trường — trường chép vào sổ gốc và bạn phải làm việc với SỔ GỐC từ giờ, tờ photo vẫn chỉ là photo. save = cô văn thư tự đoán bạn thuộc ca nào. saveAndFlush = 'ghi sổ NGAY trước mặt tôi', không đợi cuối ngày.</div><div class='qb-gotcha'><b>⚠ Câu bẫy hay gặp:</b> 'save có ghi xuống DB ngay không?' — KHÔNG chắc: save chỉ đưa entity vào context; SQL thật chạy lúc flush (thường trước commit). Thấy log INSERT muộn hơn dòng save là bình thường — hiểu điều này thì hết ngạc nhiên vì 'thứ tự SQL không giống thứ tự code'.</div>",
  refs:[["Vlad Mihalcea – persist vs merge","https://vladmihalcea.com/jpa-persist-and-merge/"]] },

{ id:"spr-034", legacy:162, t:"Spring Data JPA", lv:"middle", core:true,
  tags:["cascade","orphanremoval","spring-data-jpa"],
  q:"Cascade và <code>orphanRemoval</code> là gì? <code>CascadeType.REMOVE</code> khác <code>orphanRemoval=true</code> ở đâu?",
  a:"<p><strong>Cascade = lan thao tác từ CHA sang CON:</strong> khi bạn persist/merge/remove entity cha, các quan hệ khai <code>cascade</code> được hưởng ứng theo. Vd <code>@OneToMany(cascade = CascadeType.PERSIST)</code>: lưu Order là các OrderItem trong list tự được lưu — khỏi lặp từng đứa. Các type: PERSIST, MERGE, REMOVE, ALL... Quy tắc chọn: cascade chỉ hợp với quan hệ <strong>sở hữu trọn vẹn</strong> (item là 'ruột' của order); đừng bao giờ cascade REMOVE trên quan hệ tới entity dùng chung (xoá Order kéo chết Product thì thảm hoạ).</p><p><strong>REMOVE vs orphanRemoval — khác nhau ở NGUỒN kích hoạt:</strong></p><ul><li><strong><code>CascadeType.REMOVE</code>:</strong> con chỉ chết khi <em>CHA bị remove</em>. Bạn <code>order.getItems().remove(item)</code> — item rời list nhưng KHÔNG bị xoá DB (chỉ mất liên kết, hoặc chẳng có gì xảy ra tuỳ mapping).</li><li><strong><code>orphanRemoval = true</code>:</strong> con chết khi <em>mất cha</em> — tức bị <strong>gỡ khỏi collection</strong> (hoặc set null phía con). <code>order.getItems().remove(item)</code> → item thành 'mồ côi' → Hibernate DELETE nó lúc flush. Ngữ nghĩa: 'con không thể tồn tại thiếu cha'.</li></ul><pre><code class='language-java'>@OneToMany(mappedBy = \"order\", cascade = CascadeType.ALL, orphanRemoval = true)\nList&lt;OrderItem&gt; items;   // combo chuẩn cho quan hệ 'ruột thịt': cha chết con chết,\n                          // con rời khỏi cha cũng chết</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Cascade REMOVE = dọn cả căn hộ thì đồ trong hộ bị dọn theo. orphanRemoval = món đồ bị CHỦ VỨT KHỎI NHÀ (gỡ khỏi list) thì đội dọn rác tự thu — dù căn hộ vẫn còn đó.</div><div class='qb-gotcha'><b>⚠ Hai lưu ý ăn điểm:</b> (1) cascade của JPA là cơ chế <strong>tầng ứng dụng</strong> — hoàn toàn KHÁC và không thay thế <code>ON DELETE CASCADE</code> của DB (câu bên nhóm Database): xoá bằng native query/tool ngoài là cascade JPA không chạy. (2) Với orphanRemoval, thao tác đúng là <em>sửa collection</em> (add/remove trên list hiện có), đừng <code>setItems(newList)</code> thay nguyên list — Hibernate có thể hiểu thành 'mồ côi cả đàn' + lỗi 'collection no longer referenced'.</div>",
  refs:[["Baeldung – Cascade & orphanRemoval","https://www.baeldung.com/jpa-cascade-types"],["Vlad Mihalcea – orphanRemoval","https://vladmihalcea.com/orphanremoval-jpa-hibernate/"]] },

{ id:"spr-035", legacy:163, t:"Spring Data JPA", lv:"middle", core:true,
  tags:["fetchtype","eager","lazy","spring-data-jpa"],
  q:"FetchType mặc định của <code>@ManyToOne</code> và <code>@OneToMany</code> là gì? Vì sao EAGER thường nguy hiểm?",
  a:"<p><strong>Mặc định của JPA (phải thuộc lòng):</strong></p><ul><li><code>@ManyToOne</code>, <code>@OneToOne</code> → <strong>EAGER</strong> (quan hệ 'một phía' — spec cho là rẻ)</li><li><code>@OneToMany</code>, <code>@ManyToMany</code> → <strong>LAZY</strong> (collection — mặc định đúng đắn)</li></ul><p>Nghĩa là <em>chính mặc định của @ManyToOne đã là một cái bẫy</em> — đa số hướng dẫn hiện đại khuyên ghi đè hết về LAZY.</p><p><strong>Vì sao EAGER nguy hiểm:</strong> nó là lời hứa 'LUÔN load kèm' — áp cho <em>mọi</em> nơi entity được load, kể cả nơi không cần:</p><ul><li><strong>Over-fetching mọi ngả:</strong> màn hình chỉ cần tên đơn hàng nhưng Order.customer là EAGER → mỗi lần load Order là kéo theo User (+ những gì User EAGER tiếp — dây chuyền).</li><li><strong>EAGER ≠ hết N+1, nhiều khi còn TỆ hơn:</strong> query list bằng JPQL, Hibernate vẫn có thể nạp quan hệ EAGER bằng <em>N câu phụ</em> — bạn dính N+1 mà <em>không có quyền từ chối</em>.</li><li><strong>Không rút lại được theo use-case:</strong> LAZY thì chỗ nào cần cứ fetch join chỗ đó (opt-in); EAGER là án chung thân — muốn 'không load' phải lách rất xấu.</li></ul><p><strong>Chiến lược chuẩn:</strong> mọi quan hệ để <code>LAZY</code>; use-case nào cần gì thì khai tường minh bằng <strong>fetch join / @EntityGraph / DTO projection</strong>. Kiểm soát fetch theo <em>use-case</em>, không theo <em>mapping</em>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> EAGER như đăng ký 'mỗi lần mua gì cũng giao kèm combo gia đình' — tiện đúng một bữa, còn lại è cổ nhận đồ thừa và không huỷ được. LAZY = mua lẻ, hôm nào tiệc thì đặt combo (fetch join) cho đúng hôm đó.</div><div class='qb-gotcha'><b>⚠ Câu bẫy phỏng vấn:</b> 'đổi hết sang EAGER là hết LazyInitializationException đúng không?' — hết exception thật, nhưng bạn vừa đổi một lỗi ỒN ÀO (dễ thấy, dễ sửa) lấy suy hao hiệu năng ÂM THẦM ở mọi nơi. Đổi bệnh cấp tính lấy bệnh mãn tính không phải là chữa.</div>",
  refs:[["Vlad Mihalcea – EAGER is a code smell","https://vladmihalcea.com/eager-fetching-is-a-code-smell/"]] },

{ id:"spr-036", legacy:164, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["derived-query","jpql","native-query","spring-data-jpa"],
  q:"Derived query method, <code>@Query</code> (JPQL) và native query — chọn cái nào khi nào?",
  a:"<p>Ba nấc 'viết query' của Spring Data, đổi <em>tiện lợi</em> lấy <em>quyền kiểm soát</em>:</p><ul><li><strong>① Derived query — đặt tên method, Spring tự sinh SQL:</strong> <code>findByStatusAndCreatedAtAfter(status, date)</code>. Không viết một chữ query; compile-time an toàn (đổi tên field là lỗi build). Giới hạn lộ rõ khi điều kiện phức: tên method thành tràng giang <code>findByStatusInAndCustomer_VipTrueAndTotalGreaterThan...</code> — không ai đọc nổi. <em>Luật thực dụng: quá ~2-3 điều kiện là chuyển nấc ②.</em></li><li><strong>② <code>@Query</code> với JPQL:</strong> viết query trên <em>entity và field</em> (không phải bảng/cột) — vẫn hưởng tính di động DB + fetch join được + đọc rõ ràng. Đây là <strong>mặc định tốt</strong> cho query có logic. Kèm <code>@Modifying</code> cho UPDATE/DELETE hàng loạt (lưu ý: bỏ qua persistence context — entity đang managed không biết dữ liệu vừa đổi, cần clearAutomatically khi cần).</li><li><strong>③ Native query (<code>nativeQuery = true</code>):</strong> SQL thô — dùng khi cần đặc sản của DB mà JPQL không nói được: window function, CTE/WITH, hint, JSON operator, UPSERT... Trả giá: dính chặt dialect, đổi tên cột là lỗi runtime (không ai check hộ), map kết quả phức tạp hơn. <em>Dùng có chủ đích, kèm test chạy trên DB thật (Testcontainers).</em></li></ul><p>Nấc ngoài: query động nhiều filter tuỳ chọn → <strong>Specification / Querydsl</strong>, đừng nối chuỗi JPQL bằng tay (vừa xấu vừa rủi ro).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Derived = gọi món theo tên trong menu (nhanh, không tuỳ biến); JPQL = dặn bếp bằng ngôn ngữ chung của chuỗi nhà hàng (linh hoạt, chi nhánh nào cũng hiểu); native = vào tận bếp tự nấu món địa phương (ngon độc bản, nhưng sang chi nhánh khác công thức có thể không dùng được).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'vì sao derived query dài là code smell?' — vì tên method là <em>chữ ký công khai</em>: logic query bị nêm vào tên, đổi logic là đổi tên là sửa mọi call-site. Và điểm cộng: kể được rằng cả ba nấc đều <strong>bind parameter an toàn</strong> (chống SQL injection) — chỉ khi tự NỐI CHUỖI query mới chết.</div>",
  refs:[["Spring Data JPA – Query methods","https://docs.spring.io/spring-data/jpa/reference/jpa/query-methods.html"]] },

{ id:"spr-037", legacy:165, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["flush","persistence-context","write-behind","spring-data-jpa"],
  q:"Hibernate 'flush' là gì? Nó xảy ra lúc nào (không phải lúc bạn nghĩ)?",
  a:"<p><strong>Flush = đồng bộ persistence context xuống DB:</strong> Hibernate gom mọi thay đổi (entity mới persist, dirty checking phát hiện sửa, remove) và <strong>bắn các câu INSERT/UPDATE/DELETE</strong> xuống connection. Lưu ý ranh giới: flush <em>đẩy</em> SQL đi nhưng <strong>chưa commit</strong> — dữ liệu chỉ chốt vĩnh viễn khi transaction commit; rollback vẫn kéo lại được.</p><p>Vì sao không ghi ngay từng lệnh? Hibernate cố tình <strong>trì hoãn (write-behind)</strong> để: gom lệnh (batching), bớt round-trip, và cho dirty checking chỉ chạy một lần. Code của bạn 'set' thoải mái — sổ sách chỉ được chép xuống DB ở vài thời điểm:</p><p><strong>Ba thời điểm flush tự động (chế độ AUTO mặc định):</strong></p><ul><li><strong>Trước khi commit</strong> — điểm ai cũng biết.</li><li><strong>TRƯỚC một query JPQL/Criteria đụng tới bảng liên quan</strong> — điểm gây bất ngờ nhất: bạn vừa sửa entity Order chưa commit, rồi chạy <code>select count(o) from Order o</code> → Hibernate flush TRƯỚC để query nhìn thấy thay đổi của chính bạn (read-your-writes trong transaction). Hệ quả: câu UPDATE xuất hiện trong log ở vị trí 'lạ', sớm hơn commit rất nhiều.</li><li>Khi gọi tay <code>flush()</code> / <code>saveAndFlush()</code>.</li></ul><p>(Chi tiết thêm: native query mặc định cũng kích flush; và flush theo <em>thứ tự loại lệnh</em> của Hibernate — insert trước update, v.v. — không phải thứ tự code của bạn.)</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bạn ghi nợ khách vào giấy nhớ (persistence context) để cuối buổi chép vào sổ cái (flush trước commit). Nhưng nếu GIỮA buổi có ai hỏi 'tổng nợ hiện tại?' (query), bạn buộc phải chép giấy nhớ vào sổ TRƯỚC rồi mới cộng — không thì con số thiếu chính phần bạn vừa ghi.</div><div class='qb-gotcha'><b>⚠ Vì sao đáng biết kỹ:</b> (1) giải mã log 'SQL chạy không đúng thứ tự code' — flush order là của Hibernate; (2) hiểu vì sao query giữa transaction có thể 'kích' một loạt UPDATE (đôi khi là gánh nặng hiệu năng — FlushMode.COMMIT cho luồng đặc thù); (3) phân biệt được flush/commit là nền của câu saveAndFlush và readOnly (FlushMode.MANUAL).</div>",
  refs:[["Vlad Mihalcea – Flush order","https://vladmihalcea.com/how-do-jpa-and-hibernate-define-the-flush-order/"]] },

{ id:"spr-038", legacy:166, t:"Spring Data JPA", lv:"middle", core:true,
  tags:["equals","hashcode","entity","spring-data-jpa"],
  q:"Viết <code>equals()</code>/<code>hashCode()</code> cho JPA entity thế nào cho đúng? Vì sao KHÔNG dùng bộ field như class thường?",
  a:"<p>Contract equals/hashCode (câu bên Core Java) gặp hai đặc thù của entity là thành bài toán riêng:</p><p><strong>Vấn đề 1 — id sinh sau:</strong> entity mới <code>new</code> có <code>id = null</code>; id chỉ xuất hiện SAU persist/flush. Nếu equals/hashCode dựa trên id: bỏ entity mới vào HashSet (hash theo id=null) → persist xong id đổi → <strong>hashCode đổi</strong> → entity 'lạc trôi' trong Set — contains ra false, đồ vẫn trong Set mà không tìm thấy (đúng bẫy 'sửa key sau khi put').</p><p><strong>Vấn đề 2 — dùng hết field thì tệ hơn:</strong> field nghiệp vụ đổi liên tục (dirty checking mà) → equals bất ổn định; so entity với <em>proxy lazy</em> còn kích query. Copy equals của record/POJO sang entity là sai bài.</p><p><strong>Các lời giải chuẩn (chọn 1):</strong></p><ul><li><strong>Natural/business key bất biến</strong> (mã đơn, email, ISBN) — nếu nghiệp vụ CÓ một khoá không đổi: equals/hashCode theo nó. Đẹp nhất nhưng không phải bảng nào cũng có.</li><li><strong>Kiểu Vlad Mihalcea (phổ biến nhất):</strong> <code>hashCode()</code> trả <strong>hằng số</strong> (vd <code>getClass().hashCode()</code>); <code>equals</code> = cùng class và <code>id != null &amp;&amp; id.equals(other.id)</code> (hai entity id null coi là KHÁC nhau). Hằng số nghe 'phản Big-O' nhưng đúng contract tuyệt đối: hashCode không bao giờ đổi theo vòng đời — an toàn trong Set/Map; entity trong một Set hiếm khi nhiều đến mức collision thành vấn đề thật.</li><li><strong>UUID gán từ constructor</strong> (id do app sinh, có ngay từ lúc new) — hết luôn vấn đề id-sinh-sau; đổi lại các trade-off của UUID làm khoá (nhớ câu clustered index).</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Entity mới như em bé chưa có số định danh — lấy 'số CCCD' làm danh tính thì lúc chưa cấp và lúc cấp xong là hai 'người' khác nhau trong sổ. Giải pháp hằng số = xếp mọi hồ sơ vào MỘT ngăn, phân biệt bằng đối chiếu trực tiếp — chậm hơn tí, không bao giờ lạc.</div><div class='qb-gotcha'><b>⚠ Điểm hay bị vặn:</b> 'hashCode hằng số làm HashSet thành list, chậm chứ?' — đúng về lý thuyết O(n) trong Set; nhưng entity set trong một context thường vài chục phần tử, và cái giá đó mua được <em>đúng đắn tuyệt đối</em> — trade-off có ý thức. Kể thêm được 'so sánh phải dùng getClass()/instanceof cẩn thận vì Hibernate proxy là SUBCLASS' là điểm cộng sâu.</div>",
  refs:[["Vlad Mihalcea – equals/hashCode entity","https://vladmihalcea.com/how-to-implement-equals-and-hashcode-using-the-jpa-entity-identifier/"]] },

{ id:"spr-039", legacy:167, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["pageable","pagination","count-query","spring-data-jpa"],
  q:"Phân trang trong Spring Data: <code>Pageable</code>/<code>Page</code> hoạt động thế nào? Chi phí ẩn là gì?",
  a:"<p><strong>Cách dùng:</strong> repository nhận <code>Pageable</code>, trả <code>Page&lt;T&gt;</code>:</p><pre><code class='language-java'>Page&lt;Order&gt; page = orderRepo.findByStatus(\"PAID\",\n        PageRequest.of(2, 20, Sort.by(\"createdAt\").descending()));\n// Spring sinh:  SELECT ... ORDER BY created_at DESC LIMIT 20 OFFSET 40\n//        VÀ:    SELECT COUNT(*) ...   ← chi phí ẩn số 1</code></pre><p><strong>Chi phí ẩn phải biết:</strong></p><ul><li><strong>Count query đi kèm MỖI lần gọi:</strong> <code>Page</code> cần totalElements để tính tổng trang → thêm một câu COUNT trên toàn bộ điều kiện — với bảng lớn, COUNT có khi <em>đắt hơn</em> câu lấy dữ liệu. Không cần tổng trang (UI chỉ có nút 'sau')? Dùng <strong><code>Slice&lt;T&gt;</code></strong> — lấy size+1 dòng để biết 'còn trang sau không', khỏi COUNT. Danh sách kiểu cuộn vô tận: Slice là mặc định đúng.</li><li><strong>OFFSET sâu vẫn chậm:</strong> Pageable dùng LIMIT/OFFSET — trang 5000 phải đếm-bỏ 100.000 dòng (bài keyset bên nhóm Database áp dụng nguyên xi). Spring Data có <code>ScrollPosition</code>/keyset scrolling ở bản mới, hoặc tự viết điều kiện <code>WHERE (created_at, id) &lt; ...</code>.</li><li><strong>Sort theo field chưa index</strong> → filesort trên tập lớn; cột trong Sort phải nằm trong tính toán index như mọi ORDER BY khác.</li><li><strong>Phân trang + fetch join collection:</strong> Hibernate cảnh báo HHH000104 — kéo TOÀN BỘ về memory rồi cắt trang. Kết hợp phân trang với load quan hệ phải đi hai bước (page id trước, fetch theo id sau) hoặc DTO.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Page như mục lục ghi 'tổng 1.203 trang' — muốn in con số đó thư viện phải ĐẾM HẾT kho mỗi lần bạn hỏi (count query). Slice chỉ trả lời 'còn nữa không?' — liếc thêm đúng một cuốn là biết.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối:</b> 'API trả totalPages có đáng giá COUNT mỗi request?' — câu trả lời senior: cache count ngắn hạn / trả count ước lượng / bỏ total và đổi UI sang cursor — <em>đàm phán yêu cầu sản phẩm bằng chi phí kỹ thuật</em>, thay vì gồng gánh mặc định.</div>",
  refs:[["Spring Data – Paging & sorting","https://docs.spring.io/spring-data/jpa/reference/repositories/query-methods-details.html"]] },

{ id:"spr-040", legacy:168, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["mappedby","owning-side","bidirectional","spring-data-jpa"],
  q:"Quan hệ hai chiều (bidirectional): <code>mappedBy</code> và 'owning side' nghĩa là gì?",
  a:"<p><strong>Vấn đề gốc:</strong> trong DB, quan hệ Order–OrderItem chỉ có <em>MỘT</em> chỗ lưu: cột khoá ngoại <code>order_id</code> trên bảng con. Nhưng trong Java bạn muốn đi hai chiều: <code>item.getOrder()</code> VÀ <code>order.getItems()</code> — hai field mô tả <em>cùng một sự thật</em>. Hibernate cần biết: khi hai phía nói khác nhau, <strong>tin ai?</strong></p><ul><li><strong>Owning side (phía sở hữu)</strong> = phía có khoá ngoại = <code>@ManyToOne Order order</code> trên OrderItem. <strong>Chỉ thay đổi ở phía này mới được ghi xuống DB.</strong></li><li><strong>Inverse side</strong> = phía khai <code>mappedBy</code>: <code>@OneToMany(mappedBy = \"order\")</code> trên Order — nghĩa đen: 'quan hệ này đã được map bởi field <code>order</code> bên kia, tôi chỉ là tấm gương đọc'. Thêm/bớt phần tử ở list này <em>mà quên set phía kia</em> → DB không đổi gì.</li></ul><pre><code class='language-java'>// Bug kinh điển: chỉ sửa inverse side\norder.getItems().add(item);        // ❌ DB không có gì thay đổi!\n// Đúng bài: sửa owning side (và đồng bộ cả hai cho object graph nhất quán)\npublic void addItem(OrderItem item) {   // helper method trên Order\n    items.add(item);                     // gương\n    item.setOrder(this);                 // ← sự thật (owning side)\n}</code></pre><p>Quên <code>mappedBy</code> thì sao? Hibernate tưởng là HAI quan hệ độc lập → sinh thêm <strong>bảng join thừa</strong> <code>order_items_item</code> — schema rác mà nhiều người không hiểu từ đâu ra.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hộ khẩu chỉ ghi 'con thuộc hộ nào' TRÊN HỒ SƠ NGƯỜI CON (khoá ngoại — owning side). Danh sách con dán ở nhà cha mẹ (list bên Order) chỉ là bản chép tay cho tiện nhìn — sửa bản chép tay mà không sửa hồ sơ gốc thì nhà nước (DB) không công nhận.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'có cần hai chiều không?' — KHÔNG mặc định: chỉ làm bidirectional khi thật sự hay truy cập từ phía cha (và luôn kèm helper đồng bộ hai phía). Một chiều @ManyToOne đơn giản hơn, ít bẫy hơn — thêm chiều ngược là thêm trách nhiệm giữ nhất quán, không phải thêm 'cho đủ bộ'.</div>",
  refs:[["Baeldung – Hibernate One to Many","https://www.baeldung.com/hibernate-one-to-many"],["Vlad Mihalcea – mappedBy","https://vladmihalcea.com/jpa-hibernate-synchronize-bidirectional-entity-associations/"]] },

{ id:"spr-041", legacy:169, t:"Spring Data JPA", lv:"middle", core:false,
  tags:["manytomany","join-table","spring-data-jpa"],
  q:"Vì sao nhiều người tránh <code>@ManyToMany</code> và thay bằng entity trung gian?",
  a:"<p><code>@ManyToMany</code> (Student ↔ Course) để Hibernate tự tạo <strong>bảng join ẩn</strong> chỉ gồm 2 khoá ngoại. Trông gọn — cho tới khi đời thật gõ cửa:</p><ul><li><strong>Nghiệp vụ luôn mọc thêm thuộc tính cho QUAN HỆ:</strong> ngày ghi danh, điểm, trạng thái... Các cột này thuộc về <em>mối quan hệ</em> chứ không thuộc Student hay Course — mà bảng join ẩn thì <strong>không thêm cột được</strong> (bạn không kiểm soát nó). Đến lúc cần là đập đi làm lại mapping + migrate dữ liệu.</li><li><strong>Thao tác collection đắt bất ngờ:</strong> với <code>List</code>, remove một phần tử có thể khiến Hibernate <strong>DELETE sạch các dòng join của entity đó rồi INSERT lại</strong> phần còn lại — bảng join lớn là thảm hoạ nhỏ mỗi lần sửa. (Dùng <code>Set</code> đỡ hơn nhiều.)</li><li>Cascade REMOVE trên ManyToMany là <strong>cấm kỵ</strong> — xoá Student kéo chết Course dùng chung; và các giới hạn phân trang/fetch của collection to.</li></ul><p><strong>Lời giải trưởng thành: thăng cấp quan hệ thành entity thật</strong> — <code>Enrollment</code> với 2 @ManyToOne (student, course) + các cột riêng (enrolledAt, grade...):</p><pre><code class='language-text'>Student 1—n Enrollment n—1 Course     // bảng join giờ là entity chính danh:\n// thêm cột thoải mái, query trực tiếp (\"ai ghi danh tháng này?\"),\n// index/unique(student_id, course_id) tự kiểm soát, phân trang bình thường</code></pre><p>@ManyToMany chỉ nên giữ cho quan hệ <em>thuần gắn nhãn, chắc chắn không bao giờ có thuộc tính</em> (vd bài viết ↔ tag) — và kể cả khi đó, dùng Set.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bảng join ẩn như tấm bảng ghim 'ai học lớp nào' ngoài hành lang — chỉ ghim được tên. Khi cần ghi thêm điểm và ngày nộp học phí, bạn cần SỔ GHI DANH tử tế (entity Enrollment) — mỗi dòng một mối quan hệ, muốn ghi chú gì cũng có chỗ.</div><div class='qb-gotcha'><b>⚠ Trả lời cho khéo:</b> đừng nói '@ManyToMany là sai' — nói 'nó đúng cho quan hệ không thuộc tính; nhưng kinh nghiệm cho thấy quan hệ nghiệp vụ RỒI SẼ có thuộc tính, nên em mặc định thiết kế entity trung gian từ đầu cho khỏi migrate đau'. Đó là câu trả lời của người đã từng migrate.</div>",
  refs:[["Baeldung – Many to Many","https://www.baeldung.com/jpa-many-to-many"],["Vlad Mihalcea – ManyToMany best way","https://vladmihalcea.com/the-best-way-to-use-the-manytomany-annotation-with-jpa-and-hibernate/"]] },

{ id:"spr-042", legacy:170, t:"Spring Data JPA", lv:"senior", core:false,
  tags:["batch-insert","jdbc-batch","hibernate","performance"],
  q:"Insert 100.000 bản ghi qua Hibernate rất chậm. Tối ưu thế nào?",
  a:"<p><strong>Vì sao mặc định chậm — ba tội chồng nhau:</strong> (1) Hibernate gửi <strong>từng câu INSERT một</strong> — 100k round-trip mạng; (2) mọi entity persist đều <strong>ở lại persistence context</strong> — context phình 100k object, dirty checking quét cả đàn mỗi lần flush → càng về sau càng lết + nguy cơ OOM; (3) nếu id dùng <code>GenerationType.IDENTITY</code> — Hibernate <strong>tắt luôn JDBC batching</strong> (phải INSERT ngay để lấy id DB sinh, không gom được).</p><p><strong>Bộ chỉnh chuẩn (theo thứ tự tác động):</strong></p><pre><code class='language-text'>spring.jpa.properties.hibernate.jdbc.batch_size=50      # gom 50 INSERT/chuyến\nspring.jpa.properties.hibernate.order_inserts=true       # xếp lệnh cùng bảng cạnh nhau để gom được\nspring.jpa.properties.hibernate.order_updates=true\n# Postgres JDBC: thêm reWriteBatchedInserts=true vào URL — gộp batch thành multi-values INSERT</code></pre><ul><li><strong>Id đổi sang SEQUENCE</strong> + allocationSize (vd 50): Hibernate xin trước một dải id, tự gán → batching sống lại. (IDENTITY và batch insert là cặp không đội trời chung — điểm hay bị hỏi nhất của câu này.)</li><li><strong>Chẻ nhỏ + xả context theo chu kỳ:</strong> mỗi <code>batch_size</code> bản ghi thì <code>flush(); clear();</code> — context luôn nhỏ, tốc độ ổn định từ đầu tới cuối, khỏi OOM. Kèm transaction theo chunk (10-50k/chunk) thay vì một transaction khổng lồ.</li></ul><p><strong>Và câu hỏi senior thật sự: có nên dùng Hibernate cho việc này không?</strong> Nạp dữ liệu thuần khối lượng lớn (migration, import file) → <code>JdbcTemplate.batchUpdate</code> nhanh hơn hẳn (bỏ toàn bộ máy móc entity), hoặc thẳng tay <code>COPY</code> của Postgres — nhanh hơn nữa cả chục lần. ORM cho nghiệp vụ, bulk-load cho công cụ bulk.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Mặc định = chuyển nhà bằng cách cầm TỪNG cái ly chạy một chuyến, và mỗi chuyến về lại kiểm kê toàn bộ đồ đã chuyển (context phình). Tối ưu = đóng thùng 50 món/chuyến (batch), chuyển xong thùng nào gạch sổ thùng đó (flush+clear). Còn chuyển cả kho hàng? Thuê xe container (COPY/JDBC batch) — đừng dùng xe máy dù đã đóng thùng.</div><div class='qb-gotcha'><b>⚠ Bẫy đo đạc:</b> bật <code>batch_size</code> rồi tưởng xong — hãy VERIFY: log của Hibernate không cho thấy batch gộp; dùng datasource-proxy/p6spy đếm câu thật, hoặc thống kê <code>Statistics.getPrepareStatementCount()</code>. 'Đã cấu hình' và 'đang chạy batch thật' là hai chuyện — IDENTITY âm thầm vô hiệu tất cả là ca kinh điển.</div>",
  refs:[["Vlad Mihalcea – Batch insert/update","https://vladmihalcea.com/how-to-batch-insert-and-update-statements-with-hibernate/"]] },

{ id:"spr-043", legacy:52, t:"WebFlux", lv:"senior", core:false,
  tags:["webflux","reactive","event-loop","blocking","spring-webflux"],
  q:"Khi nào NÊN và KHÔNG nên dùng Spring WebFlux (reactive)? Bẫy 'blocking' là gì?",
  a:"<p><strong>Khác biệt gốc:</strong> Spring MVC = mô hình <em>thread-per-request</em> — mỗi request chiếm 1 thread từ đầu tới cuối, thread đứng CHỜ mỗi lần gọi DB/API. WebFlux = <em>event loop</em> — vài thread (~số core) phục vụ tất cả: gặp IO thì 'gửi yêu cầu rồi buông', quay sang việc khác, dữ liệu về thì nhặt tiếp qua callback (Mono/Flux). Thread không bao giờ đứng chờ → vài thread cân được chục nghìn kết nối đồng thời.</p><p><strong>NÊN dùng khi:</strong> hệ <strong>IO-bound với concurrency rất cao</strong> — API gateway/BFF gọi nhiều dịch vụ, streaming/SSE/WebSocket hàng vạn kết nối, proxy tới downstream chậm. Điều kiện đi kèm bắt buộc: <em>toàn tuyến phải non-blocking</em> — R2DBC cho DB, WebClient cho HTTP, driver reactive cho Redis/Mongo.</p><p><strong>KHÔNG nên khi:</strong> app CRUD thường với vài trăm concurrent — MVC + pool 200 thread thừa sức, mà code MVC <em>dễ viết, dễ đọc, dễ debug</em> hơn hẳn (stack trace reactive là mê cung); đội chưa quen reactive (chi phí học + bug khó lần rất thật); phụ thuộc thư viện blocking không có bản reactive (JPA/JDBC truyền thống!). <strong>Java 21 còn đổi cả bài toán:</strong> virtual threads cho phép GIỮ code blocking dễ đọc mà đạt phần lớn lợi ích chịu tải — lý do cân nhắc WebFlux cho 'throughput thuần' ngày càng mỏng; WebFlux còn lại thế mạnh ở streaming và backpressure.</p><p><strong>Bẫy blocking — tội nặng nhất của thế giới reactive:</strong> event loop chỉ có ~số-core thread; MỘT lời gọi blocking (JDBC, Thread.sleep, HttpClient thường, thậm chí log ra đĩa chậm) trên thread event loop là <strong>chặn cả nghìn request đang chờ thread đó</strong> — app reactive chậm hơn cả MVC. Phát hiện: <strong>BlockHound</strong> (ném lỗi khi có blocking call trên thread cấm) chạy trong test; bất đắc dĩ phải blocking → đẩy sang <code>Schedulers.boundedElastic()</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> MVC như nhà hàng mỗi bàn một bồi bàn đứng chờ suốt bữa — đông khách là hết bồi. WebFlux như 3 bồi bàn siêu tốc phục vụ 100 bàn: ghi order xong đi ngay, món xong bếp bấm chuông. Bẫy blocking = một bồi bàn đứng tán chuyện 5 phút ở một bàn — 30 bàn của anh ta đói theo.</div><div class='qb-gotcha'><b>⚠ Câu trả lời trưởng thành khi được hỏi 'sao không dùng reactive cho xịn?':</b> 'reactive mua thêm khả năng chịu tải IO bằng độ phức tạp code + debug; hệ của em nghẽn ở DB query chứ không ở số thread, nên em tối ưu query và pool trước — và với Java 21, virtual threads cho phần lớn lợi ích đó mà không đổi mô hình lập trình.' Trả lời bằng trade-off theo bối cảnh, không theo mốt.</div>",
  refs:[["Spring – WebFlux","https://docs.spring.io/spring-framework/reference/web/webflux.html"],["BlockHound","https://github.com/reactor/BlockHound"]] },

{ id:"spr-044", t:"Spring Core", lv:"middle", core:true,
  tags:["spring-core","configuration","proxy","bean"],
  q:"Trong class <code>@Configuration</code>, method <code>@Bean</code> này gọi method <code>@Bean</code> kia — có tạo ra 2 instance không? Spring làm gì sau lưng?",
  a:"<p>Tình huống cụ thể:</p><pre><code class='language-java'>@Configuration\nclass AppConfig {\n    @Bean DataSource dataSource() { return new HikariDataSource(cfg); }\n    @Bean JdbcTemplate jdbc()     { return new JdbcTemplate(dataSource()); }  // gọi thẳng method trên\n    @Bean AuditDao audit()        { return new AuditDao(dataSource()); }      // gọi lần nữa!\n}</code></pre><p>Nhìn theo Java thuần: <code>dataSource()</code> bị gọi 2 lần → 2 connection pool → thảm hoạ tài nguyên. Nhưng thực tế <strong>vẫn chỉ có MỘT instance</strong>. Vì sao?</p><p><strong>Spring không dùng class AppConfig của bạn trực tiếp</strong> — nó tạo một <strong>subclass CGLIB</strong> lúc khởi động. Mỗi method <code>@Bean</code> trong subclass bị override: trước khi chạy thân method, nó <em>hỏi container trước</em> — 'bean này có trong kho chưa?'. Có rồi thì trả bản trong kho, chưa có mới chạy code của bạn rồi cất vào kho. Lời gọi <code>dataSource()</code> thứ hai vì thế không new gì cả — nó bị chặn và trả lại singleton.</p><p><strong>Chỗ này lộ ra ở đâu:</strong> (1) <code>@Configuration(proxyBeanMethods = false)</code> — TẮT cơ chế proxy để khởi động nhanh hơn (Spring Boot dùng nhiều trong auto-configuration nội bộ); lúc đó gọi chéo method @Bean là tạo instance mới thật — muốn dùng chung phải khai dependency qua <em>tham số method</em> thay vì gọi chéo. (2) Class <code>@Configuration</code> không được <code>final</code> (CGLIB cần kế thừa để override). (3) <code>@Component</code> + <code>@Bean</code> ('lite mode') cũng KHÔNG có proxy — gọi chéo là double instance âm thầm.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Method @Bean như <em>gọi món qua bồi bàn</em>: bạn gọi 'cho ly cà phê' hai lần, bồi bàn (proxy CGLIB) biết ly đầu đã pha xong đang ở quầy nên mang lại đúng ly đó, không bảo bếp pha ly mới. Tắt proxy (lite mode) = tự vào bếp pha — gọi hai lần là hai ly thật.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'vậy đổi sang @Component có @Bean bên trong thì sao?' — lite mode, không proxy, gọi chéo tạo instance mới: bug thật ngoài production khi ai đó 'dọn code' đổi annotation. Cách viết an toàn mọi chế độ: <em>truyền dependency qua tham số</em> <code>@Bean JdbcTemplate jdbc(DataSource ds)</code> — container tự tiêm, khỏi gọi chéo.</div>",
  refs:[["Spring – Basic concepts: @Bean and @Configuration","https://docs.spring.io/spring-framework/reference/core/beans/java/basic-concepts.html"],["Spring – proxyBeanMethods javadoc","https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/annotation/Configuration.html"]] },

{ id:"spr-045", t:"WebFlux", lv:"senior", core:false,
  tags:["spring-webflux","backpressure","reactive","flux"],
  q:"Backpressure trong WebFlux/Reactor là gì? Không có nó thì hỏng kiểu gì, và Flux xử lý ra sao khi consumer chậm hơn producer?",
  a:"<p><strong>Bài toán gốc:</strong> nguồn dữ liệu bơm nhanh (DB trả 1 triệu dòng, service upstream đẩy event liên tục) nhưng phía nhận xử lý chậm (ghi từng dòng ra client mạng yếu). Không có cơ chế hãm thì dữ liệu dồn ứ ở giữa — buffer phình → OOM, hoặc drop vô kiểm soát. Đây chính là lý do Reactive Streams ra đời chuẩn 4 interface (Publisher/Subscriber/Subscription/Processor) mà linh hồn là <code>request(n)</code>.</p><p><strong>Backpressure = phía tiêu thụ CHỦ ĐỘNG khai số lượng nó kham được:</strong> subscriber gọi <code>request(n)</code> — publisher chỉ được phát tối đa n phần tử rồi phải chờ lượt request tiếp. Dòng chảy do người uống điều khiển, không phải người rót. Đây là khác biệt bản chất với callback/listener thường (bị đẩy vô điều kiện).</p><p><strong>Khi consumer chậm, Flux cho chọn chiến lược qua các operator <code>onBackpressureXxx</code>:</strong> <code>buffer</code> (xếp hàng chờ — an toàn tới khi hết RAM, nên giới hạn kích thước), <code>drop</code> (bỏ phần tử mới khi đầy — hợp dữ liệu 'chỉ cần bản mới nhất' như giá/telemetry), <code>latest</code> (giữ đúng bản mới nhất), hoặc mặc định nổ <code>OverflowException</code> — fail rõ ràng còn hơn chết âm thầm. Chọn chiến lược là quyết định NGHIỆP VỤ (mất dữ liệu được không?), không phải chi tiết kỹ thuật.</p><p><strong>Điểm hay bị bỏ sót:</strong> backpressure chỉ chạy hết tuyến khi TOÀN chuỗi tôn trọng nó. Nguồn không hãm được (WebSocket đẩy tới, Kafka consumer poll theo nhịp riêng, <code>Flux.interval</code>) thì phải chèn operator chiến lược ngay sau nguồn; và <code>publishOn/flatMap</code> có prefetch buffer riêng (mặc định 256) — 'sao nó lấy 256 cái một lần?' chính là prefetch, không phải bug.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như <em>băng chuyền sushi</em>: khách (consumer) lấy đĩa theo tốc độ ăn — bếp chỉ đặt thêm khi thấy băng chuyền vơi (request(n)). Nhà hàng không backpressure = bếp cứ chất đĩa bất kể — băng chuyền tràn, đĩa rơi (OOM/drop). Chọn onBackpressureDrop = cho phép bỏ đĩa cũ; buffer = kê thêm bàn chứa tạm — nhưng bàn nào cũng có giới hạn.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'MVC có backpressure không?' — có dạng thô sơ tự nhiên: thread-per-request + pool đầy = chặn/từ chối ngay từ cổng (blocking chính là cái hãm). Điểm cộng lớn: nói được backpressure là lý do WebFlux vẫn giữ giá trị ở bài toán <em>streaming</em> ngay cả khi virtual threads (Java 21) đã lấy mất lợi thế 'chịu tải IO' — vthreads không giải bài dòng-chảy-nhanh-hơn-người-nhận.</div>",
  refs:[["Reactive Streams spec","https://www.reactive-streams.org/"],["Project Reactor – Which operator (backpressure)","https://projectreactor.io/docs/core/release/reference/#which.errors"]] },

{ id:"spr-046", t:"Spring Data JPA", lv:"middle", core:true,
  tags:["spring-data-jpa","jpa","hibernate","jdbc"],
  q:"JPA, Hibernate, Spring Data JPA, JDBC — bốn thứ này quan hệ với nhau thế nào? Hibernate có thay thế JDBC không?",
  a:"<p><strong>Trả lời gọn: JPA là CHUẨN (bộ interface), Hibernate là bản CÀI ĐẶT chuẩn đó, Spring Data JPA là tầng repository đứng TRÊN JPA để giảm code lặp, và JDBC là đường ống DƯỚI CÙNG mà mọi thứ cuối cùng đều đi qua. Hibernate không thay JDBC — nó sinh SQL rồi vẫn GỌI JDBC để gửi xuống DB.</strong></p><p>Xếp tầng từ trên xuống, kèm đúng vai trò từng tầng:</p><pre><code class='language-text'>Spring Data JPA   → repository abstraction: extends JpaRepository là có CRUD,\n                    sinh query từ tên method (findByEmailAndStatus...)\nJPA (Jakarta)     → SPEC: định nghĩa @Entity, EntityManager, JPQL — 'bản vẽ', không chạy được một mình\nHibernate         → IMPLEMENTATION của bản vẽ: persistence context, dirty checking,\n                    sinh SQL từ JPQL, cache L1 — 'cỗ máy thật'\nJDBC              → API thấp nhất: Connection, PreparedStatement, ResultSet + driver của từng DB\nDatabase</code></pre><p><strong>Vì sao tách spec khỏi implementation:</strong> code của bạn import <code>jakarta.persistence.*</code> (chuẩn) chứ không import <code>org.hibernate.*</code> — về lý thuyết đổi Hibernate lấy EclipseLink không sửa code. Thực dụng hơn: hiểu tầng giúp <em>đọc lỗi ra đúng chỗ</em> — <code>LazyInitializationException</code> là chuyện persistence context (tầng JPA/Hibernate), <code>SQLSyntaxErrorException</code> là câu SQL sinh ra (nhìn SQL log), <code>CannotGetJdbcConnectionException</code> là pool/kết nối (tầng JDBC) — ba tầng lỗi, ba hướng debug hoàn toàn khác nhau.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gọi món ở nhà hàng: Spring Data JPA là <em>menu đặt sẵn combo</em> (chọn tên là ra món), JPA là <em>bộ tiêu chuẩn công thức</em>, Hibernate là <em>đội bếp nấu theo công thức</em>, JDBC là <em>hệ thống bếp ga-ống nước</em> — bếp trưởng giỏi cỡ nào thì món ăn cuối cùng vẫn phải nấu qua bếp ga. Hỏi 'Hibernate có thay JDBC không' giống hỏi 'đầu bếp có thay cái bếp không'.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn (bộ ba câu xoáy VN rất chuộng):</b> 'Spring Data JPA có phải Hibernate không?' — không, nó đứng trên và gọi xuống qua chuẩn JPA. 'JPA có phải framework không?' — không, là spec; không có provider (Hibernate/EclipseLink) thì JPA không chạy được. 'Khi nào bỏ ORM viết tay?' — bulk load/report phức tạp → native query, <code>JdbcTemplate</code>, hoặc COPY của Postgres (xem câu batch insert spr-042): ORM cho nghiệp vụ, đồ thấp tầng cho việc nặng cơ bắp.</div>",
  refs:[["Jakarta Persistence spec","https://jakarta.ee/specifications/persistence/"],["Spring Data JPA – Reference","https://docs.spring.io/spring-data/jpa/reference/jpa.html"]] },

{ id:"spr-047", t:"Spring Transaction", lv:"middle", core:true,
  tags:["spring-transaction","transactional","spring-data-jpa"],
  q:"Method service KHÔNG gắn <code>@Transactional</code> mà gọi <code>repository.save()</code> vẫn chạy được — vì sao? Vậy lúc nào nổ <code>TransactionRequiredException</code>, và cái bẫy thật sự nằm ở đâu?",
  a:"<p><strong>Trả lời gọn: save() vẫn chạy vì <code>SimpleJpaRepository</code> (class cài sẵn đứng sau mọi JpaRepository) đã có <code>@Transactional</code> trên các method ghi — mỗi lệnh save tự mở và commit MỘT transaction riêng của nó. Chạy được ≠ đúng: nhiều save liên tiếp là nhiều transaction RỜI NHAU, lệnh sau fail thì lệnh trước KHÔNG rollback.</strong></p><p>Ba tình huống cần phân biệt rạch ròi:</p><ul><li><strong>save()/delete() trần:</strong> chạy — transaction ngầm của SimpleJpaRepository bọc đúng MỘT lệnh đó. Gọi 3 lần save trong một method service không @Transactional = 3 biên lai riêng: cái thứ 3 ném exception thì 2 cái trước ĐÃ COMMIT — dữ liệu nửa vời, và đây là bug data-integrity im lặng chứ không có stack trace nào chỉ cho bạn.</li><li><strong><code>@Modifying @Query</code> (UPDATE/DELETE JPQL) hoặc <code>EntityManager</code> trực tiếp, ngoài transaction:</strong> nổ ngay <code>TransactionRequiredException: Executing an update/delete query</code> — các đường này KHÔNG có transaction cài sẵn, JPA từ chối ghi ngoài transaction.</li><li><strong>Sửa entity đã managed mà không save lại:</strong> trong @Transactional thì dirty checking tự UPDATE lúc commit; NGOÀI transaction thì sửa xong... không có gì xảy ra cả — 'sao setter rồi mà DB không đổi' chính là ca này.</li></ul><p><strong>Kết luận thực chiến:</strong> nghiệp vụ nhiều bước ghi (tạo đơn + trừ kho + ghi log) LUÔN bọc <code>@Transactional</code> ở method service — 'cùng sống cùng chết' phải do bạn tuyên bố, đừng dựa vào transaction ngầm từng-lệnh của repository.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Mỗi save trần là một lần <em>quẹt thẻ tách hoá đơn riêng</em>: mua 3 món quẹt 3 lần, món thứ 3 lỗi thì 2 món trước vẫn đã trừ tiền — muốn trả phải 'hoàn tác' bằng tay. <code>@Transactional</code> bao ngoài = gom cả giỏ vào MỘT hoá đơn: bất kỳ món nào lỗi, huỷ nguyên giỏ, tiền chưa mất.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> interviewer sẽ xoáy tiếp: 'vậy @Transactional đặt ở service hay repository?' — service, vì RANH GIỚI nghiệp vụ nằm ở đó (repository chỉ biết từng lệnh, không biết chúng thuộc chung một câu chuyện). Và nhớ hai bẫy họ hàng đã học: self-invocation không kích hoạt proxy (gọi nội bộ là @Transactional câm), và exception bị catch nuốt thì transaction không rollback. Bộ ba này đi chung trong 90% câu hỏi transaction thực tế.</div>",
  refs:[["Spring Data JPA – Transactionality","https://docs.spring.io/spring-data/jpa/reference/jpa/transactions.html"],["SimpleJpaRepository source","https://github.com/spring-projects/spring-data-jpa/blob/main/spring-data-jpa/src/main/java/org/springframework/data/jpa/repository/support/SimpleJpaRepository.java"]] },

{ id:"spr-048", t:"Spring Boot", lv:"middle", core:false,
  tags:["async","threadpool","spring-boot"],
  q:"<code>@Async</code> trong Spring hoạt động thế nào? Thread lấy từ đâu, và 4 cái bẫy production của nó là gì?",
  a:"<p><strong>Trả lời gọn: <code>@Async</code> chạy method trên thread pool riêng qua cơ chế proxy — caller trả về ngay không chờ. Spring Boot tự cấu hình một <code>ThreadPoolTaskExecutor</code> (mặc định: 8 thread thường trực, hàng đợi KHÔNG GIỚI HẠN) — và chính các giá trị mặc định này là nguồn của phần lớn sự cố.</strong></p><p><strong>Cơ chế:</strong> bean có method @Async được bọc proxy (giống @Transactional): lời gọi từ NGOÀI vào bị chặn lại, nộp task vào executor, trả về ngay (void hoặc <code>CompletableFuture</code>). Thread thực thi lấy từ pool của Boot (cấu hình qua <code>spring.task.execution.*</code>), dùng xong trả về pool — không tạo/huỷ thread mỗi lần.</p><p><strong>4 bẫy production, theo tần suất gặp:</strong></p><ul><li><strong>Queue mặc định không đáy:</strong> Boot mặc định core 8 thread, queue capacity = <code>Integer.MAX_VALUE</code> → pool KHÔNG BAO GIỜ mở quá 8 thread (queue nuốt hết trước), task dồn ứ âm thầm: latency tăng dần, heap phình, OOM lúc tải cao. Production phải đặt core/max/queue hữu hạn + <code>RejectedExecutionHandler</code> (CallerRunsPolicy làm van xả tự nhiên).</li><li><strong>Self-invocation:</strong> method cùng class gọi <code>this.asyncMethod()</code> — không qua proxy → chạy ĐỒNG BỘ như thường, không lỗi không cảnh báo. Y hệt bẫy @Transactional.</li><li><strong>Exception bị nuốt:</strong> @Async trả <code>void</code> mà ném exception → không ai nhận, mặc định chỉ một dòng log dễ trôi — nghiệp vụ 'gửi email' chết im lặng hàng tuần. Trả <code>CompletableFuture</code> (exception nằm trong future, caller xử được) hoặc khai <code>AsyncUncaughtExceptionHandler</code>.</li><li><strong>Context không đi theo thread:</strong> transaction, SecurityContext, MDC (traceId trong log) KHÔNG tự truyền sang thread pool — @Async bên trong @Transactional là hai transaction (hoặc không transaction), log mất dấu vết. Cần TaskDecorator để copy MDC/SecurityContext nếu muốn giữ.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> @Async như <em>khay 'việc cần làm' đưa cho tổ trợ lý 8 người</em>: bạn thả việc vào rồi đi tiếp. Bẫy nằm ở chỗ cái khay mặc định <em>sâu không đáy</em> — việc chất nghìn tờ không ai kêu; trợ lý làm hỏng cũng <em>không chạy lại báo bạn</em> (exception nuốt); và tự tay mình làm việc của mình (self-invocation) thì đâu có qua khay.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'đã có @Async sao còn cần cấu hình ThreadPoolTaskExecutor?' — trả lời bằng bộ mặc định nguy hiểm ở trên là trúng tim. Điểm cộng phiên bản: Boot 3.2+ có <code>spring.threads.virtual.enabled=true</code> — @Async chạy trên virtual threads (Java 21), bài toán 'sizing pool cho IO-bound' gần như biến mất, nhưng 3 bẫy còn lại (self-invocation, exception, context) VẪN NGUYÊN — virtual thread không cứu được lỗi thiết kế.</div>",
  refs:[["Spring – @Async","https://docs.spring.io/spring-framework/reference/integration/scheduling.html#scheduling-annotation-support-async"],["Spring Boot – Task execution properties","https://docs.spring.io/spring-boot/reference/features/task-execution-and-scheduling.html"]] },

{ id:"spr-049", t:"Spring Boot", lv:"junior", core:true,
  tags:["spring-boot","auto-configuration","component-scan"],
  q:"<code>@SpringBootApplication</code> thực chất gồm những gì? Vì sao để class ngoài package gốc là bean 'biến mất'?",
  a:"<p><strong>Trả lời gọn: @SpringBootApplication = 3 annotation gộp lại: <code>@Configuration</code> (class này khai báo bean được) + <code>@EnableAutoConfiguration</code> (bật tự cấu hình theo classpath) + <code>@ComponentScan</code> (quét bean từ package hiện tại TRỞ XUỐNG). Vế sau của câu hỏi chính là hệ quả của chữ 'trở xuống'.</strong></p><p><strong>Tách từng mảnh để hiểu việc nó làm lúc khởi động:</strong></p><ul><li><code>@ComponentScan</code> — quét tìm <code>@Component/@Service/@Repository/@Controller</code> bắt đầu từ package CHỨA class main, đi xuống các package con. Không khai gì thêm thì phạm vi quét = 'từ đây trở xuống'.</li><li><code>@EnableAutoConfiguration</code> — phép màu của Boot: nhìn classpath rồi tự dựng hạ tầng. Thấy <code>spring-boot-starter-web</code> → dựng Tomcat nhúng + DispatcherServlet; thấy driver Postgres + spring-data-jpa → dựng DataSource, EntityManagerFactory. Mỗi auto-config đều có điều kiện <code>@ConditionalOnClass/@ConditionalOnMissingBean</code> — nghĩa là bạn tự khai bean riêng thì Boot tự LÙI, không đè của bạn.</li><li><code>@Configuration</code> — cho phép chính class main khai <code>@Bean</code> khi cần.</li></ul><p><strong>Bug kinh điển của người mới:</strong> class main nằm ở <code>com.shop.app</code>, nhưng service mới viết đặt ở <code>com.shop2.util</code> (hoặc <code>com.shoputil</code> — KHÔNG phải package con!) → component scan không với tới → bean không được tạo → <code>NoSuchBeanDefinitionException</code> hoặc controller 404 mà 'code nhìn đâu có sai'. Sửa: đưa class về dưới package gốc (chuẩn nhất), hoặc khai tường minh <code>@ComponentScan(basePackages = {...})</code> / <code>scanBasePackages</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Class main như <em>trạm phát sóng wifi đặt ở tầng trệt</em>: sóng phủ từ vị trí đặt máy toả xuống các phòng bên dưới nó (package con). Đặt thiết bị ở toà nhà bên cạnh (package ngang hàng) thì ngoài vùng phủ — thiết bị vẫn tồn tại, chỉ là <em>không ai kết nối tới nó</em> (bean không được tạo).</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'auto-configuration có tắt/ghi đè được không?' — được: <code>exclude = DataSourceAutoConfiguration.class</code> để tắt hẳn, hoặc đơn giản tự khai bean cùng loại là Boot nhường (@ConditionalOnMissingBean). Câu nối hay gặp: 'starter là gì?' — gói dependency gom sẵn theo chủ đề để auto-config có nguyên liệu mà dò. Trả lời được chuỗi starter → classpath → conditional → bean là hiểu Boot thật chứ không xài mù.</div>",
  refs:[["Spring Boot – Using @SpringBootApplication","https://docs.spring.io/spring-boot/reference/using/using-the-springbootapplication-annotation.html"],["Spring Boot – Auto-configuration","https://docs.spring.io/spring-boot/reference/using/auto-configuration.html"]] },

{ id:"spr-050", t:"Spring MVC", lv:"junior", core:true,
  tags:["spring-mvc","rest","controller"],
  q:"<code>@Controller</code> và <code>@RestController</code> khác nhau thế nào? <code>@ResponseBody</code> thật sự làm gì?",
  a:"<p><strong>Trả lời gọn: @RestController = @Controller + @ResponseBody dán sẵn lên MỌI method. Khác biệt nằm ở cách hiểu GIÁ TRỊ TRẢ VỀ của method: @Controller coi String trả về là TÊN VIEW cần render (đi tìm template HTML); @ResponseBody coi giá trị trả về là DỮ LIỆU cần serialize (thường thành JSON) ghi thẳng vào HTTP response body.</strong></p><p><strong>Cùng một method, hai số phận:</strong></p><pre><code class='language-java'>@Controller\nclass PageController {\n    @GetMapping(\"/hello\")\n    String hello() { return \"hello\"; }   // đi tìm template tên 'hello' (hello.html)\n                                          // không có template engine → lỗi 404/500 khó hiểu\n}\n\n@RestController\nclass ApiController {\n    @GetMapping(\"/api/users/1\")\n    UserDto get() { return new UserDto(\"Nam\"); }  // Jackson serialize → {\"name\":\"Nam\"}\n}</code></pre><p><strong>Cơ chế bên dưới đáng nói:</strong> khi có @ResponseBody, Spring đưa giá trị trả về qua bộ <code>HttpMessageConverter</code> — với JSON là Jackson (có sẵn trong starter-web). Đây chính là chỗ nối sang câu JavaBean: Jackson serialize dựa trên getter/quy ước bean — DTO thiếu getter là JSON trả về <em>rỗng</em> <code>{}</code> không báo lỗi gì (bug junior gặp hoài). Chiều ngược lại, <code>@RequestBody</code> dùng đúng bộ converter đó để đọc JSON vào object.</p><p><strong>Chọn cái nào:</strong> app render HTML server-side (Thymeleaf) → @Controller; API cho frontend/mobile (đa số dự án hiện nay) → @RestController. Trộn được: @Controller + @ResponseBody trên method lẻ.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> @Controller như <em>lễ tân chỉ đường</em>: bạn hỏi, cô ấy đưa bạn <em>tên phòng</em> ('phòng hello') rồi hệ thống dẫn bạn tới căn phòng trang trí sẵn (template). @RestController như <em>quầy giao đồ mang đi</em>: hỏi gì nhận thẳng <em>món đồ đóng hộp</em> (JSON) — không phòng ốc gì cả, nhận rồi tự dùng.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> câu xoáy quen thuộc: 'API trả về JSON nhưng một field cứ bị thiếu — nghi đâu trước?' — getter của field đó (Jackson đi theo getter, không theo field), rồi mới tới @JsonIgnore/naming. Và 'vì sao trả entity JPA thẳng ra @RestController là ý tồi?' — lộ cấu trúc DB, dính lazy loading nổ <code>LazyInitializationException</code> khi serialize ngoài transaction, vòng tham chiếu hai chiều làm Jackson đệ quy vô hạn — luôn map sang DTO.</div>",
  refs:[["Spring – @RestController","https://docs.spring.io/spring-framework/reference/web/webmvc-controller/ann-restcontroller.html"],["Spring – HttpMessageConverter","https://docs.spring.io/spring-framework/reference/web/webmvc/message-converters.html"]] },

{ id:"spr-051", t:"Spring MVC", lv:"junior", core:false,
  tags:["resttemplate","webclient","spring-boot"],
  q:"Service A cần gọi HTTP sang service B: RestTemplate, WebClient hay RestClient? Và vì sao 'quên cấu hình timeout' là bug nghiêm trọng nhất của việc gọi service khác?",
  a:"<p><strong>Trả lời gọn: RestTemplate — client blocking kinh điển, vẫn dùng tốt nhưng ở chế độ bảo trì; WebClient — client của thế giới reactive (non-blocking), dùng được cả trong app MVC thường; RestClient (Spring 6.1+) — API hiện đại kiểu fluent cho code blocking, là lựa chọn mặc định cho dự án mới không-reactive. Nhưng chọn client nào cũng THUA câu hỏi thứ hai: timeout đặt chưa?</strong></p><p><strong>So nhanh ba client:</strong></p><ul><li><code>RestTemplate</code>: đồng bộ, API kiểu template cũ (<code>getForObject</code>, <code>exchange</code>). Không bị 'deprecated' như đồn — nhưng Spring tuyên bố chỉ bảo trì, tính năng mới không vào đây.</li><li><code>RestClient</code>: cùng mô hình blocking nhưng API fluent dễ đọc (<code>restClient.get().uri(...).retrieve().body(UserDto.class)</code>) — dự án Boot 3.2+ mới nên chọn cái này.</li><li><code>WebClient</code>: non-blocking, trả <code>Mono/Flux</code> — bắt buộc nếu app WebFlux; app MVC thường vẫn dùng được (block() ở cuối) nhưng đừng rước reactive vào chỉ để gọi một API.</li></ul><p><strong>Vì sao timeout là chuyện sống còn (không phải 'cấu hình cho có'):</strong> mặc định connect/read timeout của các client này là <em>không giới hạn hoặc rất dài</em>. Service B chậm 30 giây → thread của A đứng CHỜ 30 giây → tải cao là toàn bộ thread pool của A bị giam bởi B → A chết theo B dù code A không có dòng nào sai. Đây là <strong>cascading failure</strong> — sự cố lan dây chuyền mà bài Resilience đã dạy; cái phanh đầu tiên và rẻ nhất chính là: <em>connect timeout ~1-3s, read timeout theo SLA của B (vd 5s), cộng retry có backoff cho lỗi tạm thời và circuit breaker cho lỗi kéo dài</em>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gọi service khác không đặt timeout như <em>gọi điện thoại không có giới hạn chờ</em>: đầu kia không nhấc máy, bạn cứ ôm ống nghe mãi — một người chờ thì kỳ, cả tổng đài 200 nhân viên (thread pool) cùng ôm máy chờ một số bận thì công ty tê liệt. Timeout = quy định 'đổ chuông 5 giây không nghe thì cúp, làm việc khác'.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'RestTemplate bị deprecated rồi đúng không?' — sai, nó ở maintenance mode; nói đúng trạng thái + biết RestClient là bản kế nhiệm blocking là điểm cộng cập nhật. Câu xoáy tiếp: 'timeout đặt bao nhiêu?' — không có số đúng vạn năng: connect ngắn (mạng nội bộ 1-3s), read theo SLA của downstream + ngân sách deadline của chính request đang phục vụ (đừng để tổng thời gian chờ dài hơn thời gian client chịu đợi bạn).</div>",
  refs:[["Spring – REST clients (RestClient, WebClient, RestTemplate)","https://docs.spring.io/spring-framework/reference/integration/rest-clients.html"]] },

{ id:"spr-052", t:"Spring MVC", lv:"junior", core:true,
  tags:["controlleradvice","exceptionhandler","spring-mvc"],
  q:"Xử lý lỗi tập trung bằng <code>@ControllerAdvice</code> + <code>@ExceptionHandler</code> hoạt động thế nào? Vì sao không try-catch trong từng controller?",
  a:"<p><strong>Trả lời gọn: @ControllerAdvice là 'trạm bắt exception toàn cục' cho mọi controller — exception ném ra từ controller/service sẽ được DispatcherServlet chuyển tới method @ExceptionHandler khớp kiểu, nơi bạn đổi nó thành HTTP response chuẩn. Một chỗ duy nhất quyết định 'lỗi trông như thế nào' cho toàn bộ API.</strong></p><p><strong>Cái đau nó chữa:</strong> không có nó, mỗi controller tự try-catch → ba controller trả ba format lỗi khác nhau (chỗ trả text, chỗ trả JSON tự chế, chỗ để nguyên stack trace 500 lộ nội tạng) — client khổ, security khổ, và logic 'dịch exception → status code' bị copy-paste khắp nơi. Tập trung lại một chỗ:</p><pre><code class='language-java'>@RestControllerAdvice   // = @ControllerAdvice + @ResponseBody\nclass GlobalExceptionHandler {\n\n    @ExceptionHandler(OrderNotFoundException.class)\n    @ResponseStatus(HttpStatus.NOT_FOUND)                    // 404\n    ErrorResponse notFound(OrderNotFoundException e) {\n        return new ErrorResponse(\"ORDER_NOT_FOUND\", e.getMessage());\n    }\n\n    @ExceptionHandler(MethodArgumentNotValidException.class) // lỗi @Valid\n    @ResponseStatus(HttpStatus.BAD_REQUEST)                  // 400 + danh sách field sai\n    ErrorResponse invalid(MethodArgumentNotValidException e) { ... }\n\n    @ExceptionHandler(Exception.class)                       // lưới cuối: lỗi không lường trước\n    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)        // 500 — log đầy đủ, trả message CHUNG CHUNG\n    ErrorResponse unexpected(Exception e) { ... }\n}</code></pre><p><strong>Cách chọn handler:</strong> Spring khớp exception với handler <em>cụ thể nhất</em> theo cây kế thừa (OrderNotFoundException trúng handler riêng của nó trước khi rơi xuống Exception.class). Kết hợp chuẩn: nghiệp vụ ném <em>custom exception có ý nghĩa</em> (OrderNotFoundException, InsufficientStockException) — advice dịch chúng thành (status code + mã lỗi máy-đọc-được + message người-đọc-được), khớp với chuẩn error contract của API (câu dg-api-03).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như <em>quầy chăm sóc khách hàng duy nhất của siêu thị</em>: sự cố ở bất kỳ quầy nào cũng dồn về một nơi xử lý theo đúng một quy trình, giọng điệu nhất quán — thay vì mỗi nhân viên tự xin lỗi một kiểu, người thì kể tuốt tuồn tuột chuyện nội bộ (stack trace), người thì im lặng (nuốt lỗi).</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> hai lỗ hổng hay bị hỏi: (1) handler <code>Exception.class</code> lưới-cuối trả message chung chung nhưng phải LOG chi tiết kèm traceId — trả chi tiết cho client là lộ nội tạng, nuốt luôn không log là mù khi điều tra; (2) @ControllerAdvice chỉ bắt exception nổ trong LUỒNG XỬ LÝ request MVC — lỗi trong filter (trước khi vào DispatcherServlet, vd lỗi JWT ở security filter) hay trong @Async/@Scheduled KHÔNG rơi vào đây — mỗi chỗ có cơ chế riêng. Biết ranh giới của công cụ là điểm phân biệt với người chỉ chép mẫu.</div>",
  refs:[["Spring – @ExceptionHandler","https://docs.spring.io/spring-framework/reference/web/webmvc-controller/ann-exceptionhandler.html"],["Spring – Error Responses (ProblemDetail)","https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-ann-rest-exceptions.html"]] },

/* DATA_END */
] });
