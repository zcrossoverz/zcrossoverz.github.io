/* ============================================================
   QBANK DATA — Testing   (prefix id: test-)
   Trạng thái: REWRITTEN (2026-07-04) — đáp án cho junior ~1 năm, giải thích cặn kẽ,
   mỗi câu có ví dụ đời thường + bẫy phỏng vấn + nguồn. KHÔNG ví dụ banking.
   Thêm câu mới: copy 1 khối { ... }, dán ngay TRƯỚC dòng đánh dấu DATA_END
   (gần cuối file), đổi id tăng dần. Schema + quy tắc viết: qbank/GUIDE.md
   ============================================================ */
QB.register({
  group: "Testing",
  order: 10,
  prefix: "test",
  status: "rewritten",
  questions: [
/* DATA_START */

{ id:"test-001", legacy:118, t:"Chiến lược test", lv:"junior", core:true,
  tags:["testing-strategy","test-pyramid"],
  q:"Test pyramid là gì? Vì sao nên nhiều unit test, ít end-to-end test?",
  a:"<p>Kim tự tháp là <strong>gợi ý về tỉ lệ</strong> các loại test: <strong>nhiều unit test</strong> ở đáy → <strong>ít integration test</strong> ở giữa → <strong>rất ít end-to-end (E2E) test</strong> ở đỉnh.</p><p>Lý do nằm ở ba thứ mỗi loại đánh đổi: <em>tốc độ, chi phí, độ ổn định</em>. Unit test chỉ chạy một mảnh code trong bộ nhớ nên <strong>nhanh (mili-giây), rẻ, và khi đỏ thì chỉ thẳng vào chỗ hỏng</strong>. E2E test dựng cả hệ thống (web + DB + service ngoài) rồi bấm như người dùng thật — <strong>chậm (giây tới phút), đắt, và hay 'flaky'</strong>: đỏ vì mạng chập chờn hay dữ liệu bẩn chứ không phải vì code sai. Khi một E2E đỏ, bạn phải dò cả chuỗi mới biết lỗi ở tầng nào.</p><p>Nên phủ phần lớn logic bằng unit test, dùng integration test cho ranh giới quan trọng (DB, API), và chỉ để lại vài E2E cho những luồng sống-còn nhất.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như kiểm tra một chiếc xe hơi trước khi giao: bạn kiểm <em>từng con ốc, từng cảm biến</em> (unit — nhanh, rẻ, làm hàng nghìn lần) là chính; chỉ <em>lái thử nguyên xe trên cao tốc</em> (E2E — tốn xăng, tốn giờ, phụ thuộc thời tiết) vài lần cho những kịch bản quan trọng.</div><div class='qb-gotcha'><b>⚠ Anti-pattern hay bị hỏi:</b> 'ice-cream cone' — kim tự tháp <em>lộn ngược</em>: nhiều E2E, ít unit. Hậu quả: CI chạy hàng chục phút, đỏ vặt liên tục, đội mất niềm tin vào test và bắt đầu bỏ qua kết quả đỏ. Nói được cái tên này + hậu quả là ăn điểm.</div>",
  refs:[["Martin Fowler – Test Pyramid","https://martinfowler.com/articles/practical-test-pyramid.html"]] },

{ id:"test-002", legacy:119, t:"Chiến lược test", lv:"middle", core:true,
  tags:["testing-strategy","brittle-test"],
  q:"Vì sao nên test <em>behavior</em> chứ không test <em>implementation</em>? 'Brittle test' là gì?",
  a:"<p>Test <strong>behavior</strong> = kiểm 'với input này, kết quả quan sát được từ bên ngoài là gì' — giá trị trả về, thay đổi trạng thái, hoặc tương tác thật sự quan trọng với cộng tác viên (ví dụ 'có gọi gửi thông báo không'). Test <strong>implementation</strong> = kiểm nội bộ: gọi đúng private method X, đúng thứ tự, mock sạch mọi thứ bên trong.</p><p><strong>Brittle test</strong> (test giòn) là test <em>đỏ mỗi khi bạn refactor dù hành vi không hề đổi</em>. Gốc rễ thường là bám vào chi tiết cài đặt hoặc over-mocking. Nó biến bộ test từ 'lưới an toàn' thành 'gông cùm': đổi tên biến, tách hàm cho gọn hơn cũng làm cả chục test đỏ, khiến người ta ngại refactor — mà ngại refactor thì code mục dần.</p><p>Nguyên tắc: test qua <strong>API công khai</strong> của unit, mock ở <strong>ranh giới ra thế giới ngoài</strong> (DB, HTTP, hàng đợi), không mock các mảnh nội bộ do chính bạn tách ra.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Chấm một đầu bếp nên nếm <em>món ăn ra đĩa</em> (behavior), không phải bắt bẻ 'phải cầm dao tay trái, đảo chảo đúng 12 lần' (implementation). Đổi cách nấu mà món vẫn ngon thì không được trừ điểm.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'vậy đừng mock à?' — không. Mock <em>tương tác ra ngoài</em> (gửi mail, ghi DB) là đúng và cần; cái sai là mock <em>chi tiết nội bộ</em>. Phân biệt được 'ranh giới nên mock' vs 'nội bộ không nên mock' là dấu hiệu người viết test lâu năm.</div>",
  refs:[["Martin Fowler – Practical Test Pyramid","https://martinfowler.com/articles/practical-test-pyramid.html"]] },

{ id:"test-003", legacy:120, t:"Integration test", lv:"middle", core:false,
  tags:["testing-strategy","testcontainers"],
  q:"Integration test tầng DB nên dùng H2 (in-memory) hay Testcontainers? Trade-off?",
  a:"<p><strong>H2</strong> là DB Java in-memory: bật cực nhanh, không cần Docker. Nhược điểm chí mạng: nó <em>không phải</em> DB production. Kiểu dữ liệu, dialect SQL, cách index hoạt động, hành vi khoá — đều khác Postgres/Oracle. Kết quả kinh điển: <strong>test xanh nhưng production đỏ</strong>, vì cú pháp hay đặc thù DB thật không bao giờ được kiểm.</p><p><strong>Testcontainers</strong> khởi một container chạy <em>chính DB thật</em> (Postgres, Oracle, MySQL...) trong lúc test, rồi tự dọn. Bạn kiểm trên đúng thứ sẽ chạy production — bắt được lỗi dialect, lỗi migration Flyway/Liquibase, lỗi index. Đổi lại: chậm hơn (kéo image, khởi container) và CI phải có Docker.</p><p>Kim chỉ nam thực dụng: dự án nghiêm túc dùng <strong>Testcontainers cho integration test tầng DB</strong>; H2 chỉ hợp cho vài smoke test siêu nhanh khi bạn <em>chắc</em> không đụng đặc thù DB.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Diễn tập chữa cháy trong <em>chính toà nhà bạn ở</em> (Testcontainers) khác hẳn tập trên <em>bản đồ giấy</em> (H2). Bản đồ nhanh và gọn, nhưng hôm cháy thật mới biết cầu thang thoát hiểm ngoài đời khác bản vẽ.</div><div class='qb-gotcha'><b>⚠ Bẫy tối ưu:</b> 'Testcontainers chậm quá' — mẹo là <em>tái dùng container</em> giữa các test (singleton container / reusable mode) thay vì dựng lại mỗi class, và chỉ dùng nó ở tầng integration, không phải mọi unit test.</div>",
  refs:[["Testcontainers","https://testcontainers.com/"],["Baeldung – DB Integration Tests","https://www.baeldung.com/spring-boot-testcontainers-integration-test"]] },

{ id:"test-004", legacy:121, t:"Performance testing", lv:"middle", core:false,
  tags:["performance-testing","load-test"],
  q:"Phân biệt load, stress, spike, soak test — mỗi loại trả lời câu hỏi gì?",
  a:"<p>Bốn loại này khác nhau ở <em>hình dạng của tải</em> bạn bơm vào, và mỗi hình dạng trả lời một câu hỏi khác nhau.</p><p><strong>Load test</strong> chạy ở đúng mức tải <em>kỳ vọng</em> (ví dụ lượng user giờ cao điểm bình thường) để trả lời: 'hệ có đạt SLA về latency/throughput ở tải thật không?'. <strong>Stress test</strong> tăng tải <em>vượt giới hạn</em> cho tới khi hỏng, để trả lời: 'điểm gãy ở đâu, và khi gãy thì hệ suy thoái nhẹ nhàng hay đổ sập dây chuyền?'. <strong>Spike test</strong> bơm tải <em>tăng vọt đột ngột</em> rồi rút (kiểu flash sale mở cửa) để trả lời: 'auto-scaling và hàng đợi có chịu nổi cú sốc không, hay chết trong 30 giây đầu?'. <strong>Soak (endurance) test</strong> giữ tải vừa phải <em>trong nhiều giờ</em> để lộ những thứ chỉ hiện ra theo thời gian: memory leak, đầy disk log, rò connection pool.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Kiểm một cây cầu: cho đúng lượng xe thiết kế qua (load), chất thêm tới khi nứt (stress), cho cả đoàn xe ào qua cùng lúc (spike), và để xe chạy liên tục nhiều ngày xem có mỏi kim loại không (soak).</div><div class='qb-gotcha'><b>⚠ Cách đọc kết quả (hay bị hỏi tiếp):</b> đừng nhìn throughput hay latency <em>trung bình</em> — nó giấu nỗi đau. Nhìn <strong>p95/p99</strong> và <strong>error rate theo thời gian</strong>. Trung bình 80ms nghe êm, nhưng p99 = 4 giây nghĩa là 1% người dùng đang rất khổ.</div>",
  refs:[["Grafana k6 – Test types","https://grafana.com/load-testing/types-of-load-testing/"]] },

{ id:"test-005", t:"Test doubles", lv:"middle", core:true,
  tags:["testing-strategy","test-double","mock"],
  q:"Kể các loại 'test double': dummy, stub, spy, mock, fake — khác nhau chỗ nào?",
  a:"<p>'Test double' là thuật ngữ chung (như 'diễn viên đóng thế') cho mọi object <em>giả</em> thay cho cộng tác viên thật trong test. Martin Fowler chia 5 loại theo <strong>mức độ nó tham gia vào bài test</strong>:</p><div class='qb-term'><b>📖 Từ khó:</b> <strong>SUT</strong> = System Under Test, tức mảnh code bạn đang kiểm; các double đứng quanh nó để cô lập nó ra.</div><ul><li><strong>Dummy</strong>: chỉ để lấp chỗ tham số, không bao giờ được dùng thật (ví dụ truyền một object rỗng cho đủ constructor).</li><li><strong>Stub</strong>: trả về câu trả lời <em>đóng sẵn</em> khi được gọi ('gọi getGia() thì luôn trả 100'). Dùng để <em>đẩy input</em> vào SUT.</li><li><strong>Spy</strong>: như stub nhưng còn <em>ghi lại</em> nó đã bị gọi thế nào (mấy lần, tham số gì) để bạn kiểm sau.</li><li><strong>Mock</strong>: được <em>lập trình kỳ vọng trước</em> ('phải được gọi đúng 1 lần với tham số X'); test đỏ nếu kỳ vọng không thoả. Dùng để kiểm <em>tương tác</em>.</li><li><strong>Fake</strong>: có cài đặt <em>chạy được thật</em> nhưng rút gọn — kinh điển là repository lưu bằng HashMap thay cho DB thật.</li></ul><p>Ranh giới quan trọng: <strong>stub/fake giúp kiểm STATE</strong> (kết quả cuối ra sao), còn <strong>mock/spy giúp kiểm INTERACTION</strong> (có gọi đúng thứ cần gọi không). Chọn nhầm trục là nguồn của test giòn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Quay phim hành động: <em>dummy</em> là hình nộm ngồi ghế sau cho đủ người; <em>stub</em> là diễn viên lồng tiếng đọc sẵn một câu; <em>spy</em> là đóng thế nhưng có người ghi lại đã nhào lộn mấy lần; <em>mock</em> là hợp đồng 'phải té đúng 1 lần'; <em>fake</em> là xe mô hình chạy được nhưng không phải xe thật.</div><div class='qb-gotcha'><b>⚠ Bẫy thuật ngữ:</b> đời thường ai cũng gọi tất cả là 'mock' (vì Mockito). Biết được 'thứ tôi vừa tạo bằng when().thenReturn() thực chất là <em>stub</em>, chỉ thành mock khi tôi verify()' là điểm cộng lớn — đúng tinh thần bài 'Mocks Aren't Stubs' của Fowler.</div>",
  refs:[["Martin Fowler – Mocks Aren't Stubs","https://martinfowler.com/articles/mocksArentStubs.html"],["Martin Fowler – Test Double","https://martinfowler.com/bliki/TestDouble.html"]] },

{ id:"test-006", t:"Mockito", lv:"junior", core:true,
  tags:["testing-strategy","mockito"],
  q:"Mockito căn bản: <code>@Mock</code>, <code>@InjectMocks</code>, <code>when().thenReturn()</code>, <code>verify()</code> làm gì?",
  a:"<p>Mockito là thư viện tạo test double phổ biến nhất cho Java. Bốn mảnh bạn gặp mỗi ngày:</p><pre><code class='language-java'>@ExtendWith(MockitoExtension.class)\nclass OrderServiceTest {\n  @Mock OrderRepository repo;        // tạo một bản giả của repo\n  @InjectMocks OrderService service; // new OrderService, tiêm các @Mock vào\n\n  @Test void tinh_tong() {\n    when(repo.findById(1L)).thenReturn(Optional.of(new Order(1L, 100)));\n    int tong = service.tong(1L);\n    assertThat(tong).isEqualTo(100);\n    verify(repo).findById(1L);        // xác nhận repo đã bị gọi\n  }\n}</code></pre><p><strong><code>@Mock</code></strong> tạo object giả — mọi method mặc định trả 'rỗng' (null, 0, empty). <strong><code>@InjectMocks</code></strong> tạo SUT thật rồi <em>tự tiêm</em> các mock vào qua constructor. <strong><code>when(...).thenReturn(...)</code></strong> dạy mock trả gì khi bị gọi (đây là dựng <em>stub</em>). <strong><code>verify(...)</code></strong> kiểm mock <em>có</em> bị gọi như mong đợi (đây mới là dùng nó như <em>mock</em> thật sự).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Tập kịch mà bạn đơn của bệnh viện chưa xây xong: bạn thuê <em>diễn viên đóng vai bác sĩ</em> (@Mock), dặn trước 'ai hỏi thì trả kết quả bình thường' (when/thenReturn), rồi sau buổi tập kiểm 'diễn viên có nói đúng thoại không' (verify). Bạn tập được luồng mà không cần bệnh viện thật.</div><div class='qb-gotcha'><b>⚠ Bẫy hay gặp:</b> 'UnnecessaryStubbingException' — bạn when() một thứ rồi không dùng tới; Mockito (strict mode mặc định từ v3) báo lỗi để bạn dọn stub thừa. Và nhớ: <em>@Mock chỉ giả một lớp</em>; nếu test cần cả Spring context (transaction, security) thì đó là integration test, không phải chỗ của Mockito thuần.</div>",
  refs:[["Mockito – Javadoc","https://javadoc.io/doc/org.mockito/mockito-core/latest/org/mockito/Mockito.html"],["Baeldung – Mockito Series","https://www.baeldung.com/mockito-series"]] },

{ id:"test-007", t:"Mockito", lv:"middle", core:false,
  tags:["testing-strategy","mockito"],
  q:"Khi nào dùng <code>doReturn()</code> thay <code>when()</code>? <code>ArgumentCaptor</code> để làm gì? Vì sao 'đừng mock cái bạn không sở hữu'?",
  a:"<p><strong><code>when(x).thenReturn()</code> vs <code>doReturn().when(x)</code>:</strong> cú pháp <code>when(mock.method())</code> phải <em>gọi thật</em> method để Mockito bắt. Với <strong>spy</strong> (object thật được bọc) điều đó chạy code thật một lần — nguy hiểm nếu method có side-effect hoặc ném exception. Với method <strong>void</strong> thì <code>when()</code> còn không biên dịch được. Cả hai ca đó dùng <code>doReturn(...).when(spy).method()</code> hoặc <code>doThrow(...)</code> / <code>doNothing()</code> — chúng <em>không</em> gọi method thật.</p><p><strong><code>ArgumentCaptor</code></strong> tóm lấy <em>tham số thật</em> mà mock nhận được, để bạn soi nó. Hữu ích khi code tạo một object phức tạp bên trong rồi trao cho dependency — bạn 'chộp' object đó ra và assert từng field:</p><pre><code class='language-java'>ArgumentCaptor&lt;Email&gt; cap = ArgumentCaptor.forClass(Email.class);\nverify(mailer).send(cap.capture());\nassertThat(cap.getValue().subject()).contains('Xin chao');</code></pre><p><strong>'Đừng mock cái bạn không sở hữu':</strong> đừng mock trực tiếp thư viện/SDK bên thứ ba (client của hãng khác). Vì mock <em>đóng băng giả định của bạn</em> về cách nó hoạt động — hôm nó nâng version đổi hành vi, test vẫn xanh nhưng production vỡ. Thay vào đó, bọc nó sau một <em>interface của chính bạn</em> rồi mock interface đó.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bạn không tự đóng vai <em>nhà mạng viễn thông</em> để tập (cái bạn không sở hữu); bạn dựng một 'quầy tiếp nhận' của riêng mình trước nhà mạng, và tập với quầy đó. Nhà mạng đổi quy trình thì chỉ quầy của bạn cần chỉnh.</div><div class='qb-gotcha'><b>⚠ Bẫy:</b> lạm dụng ArgumentCaptor để kiểm mọi tham số nội bộ = quay lại test implementation (giòn). Dùng nó cho <em>dữ liệu thật sự quan trọng đi ra ngoài</em>, không phải để soi mói mọi lời gọi.</div>",
  refs:[["Mockito – doReturn/Answer","https://javadoc.io/doc/org.mockito/mockito-core/latest/org/mockito/Mockito.html"],["Baeldung – ArgumentCaptor","https://www.baeldung.com/mockito-argumentcaptor"]] },

{ id:"test-008", t:"Spring test", lv:"middle", core:true,
  tags:["testing-strategy","spring-test"],
  q:"<code>@SpringBootTest</code> khác các 'slice test' (<code>@WebMvcTest</code>, <code>@DataJpaTest</code>) thế nào? Vì sao slice nhanh hơn nhiều?",
  a:"<p><strong><code>@SpringBootTest</code></strong> khởi <em>toàn bộ</em> application context — mọi bean, mọi auto-config, có thể cả web server nhúng. Rất sát thật, nhưng <strong>nặng và chậm</strong>: mỗi context load tốn giây, và nếu cấu hình test khác nhau, Spring còn dựng nhiều context.</p><p><strong>Slice test</strong> chỉ dựng <em>một lát cắt</em> của app:</p><ul><li><strong><code>@WebMvcTest</code></strong> chỉ nạp tầng web (controller, filter, Jackson) và <em>không</em> nạp service/DB — bạn <code>@MockBean</code> service phía dưới. Test controller: mapping URL, validation, mã lỗi, JSON.</li><li><strong><code>@DataJpaTest</code></strong> chỉ nạp tầng JPA (repository, EntityManager) + một DB test, và mặc định <em>rollback</em> mỗi test. Kiểm câu query, entity mapping, ràng buộc.</li><li><strong><code>@JsonTest</code></strong> chỉ kiểm serialize/deserialize JSON.</li></ul><p>Slice nhanh vì nó bỏ qua phần lớn context — ít bean phải khởi tạo hơn. Đúng tinh thần kim tự tháp: nhiều test hẹp-và-nhanh, ít test rộng-và-chậm.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Kiểm một căn hộ: <code>@SpringBootTest</code> là bật <em>điện nước gas thang máy toàn toà nhà</em> rồi mới thử; slice test là chỉ mở nước để thử vòi, chỉ bật điện để thử đèn — nhanh hơn nhiều vì không phải khởi động cả toà.</div><div class='qb-gotcha'><b>⚠ Bẫy hiệu năng của cả bộ test:</b> Spring <em>cache context</em> theo cấu hình. Mỗi biến thể <code>@MockBean</code> / <code>@TestPropertySource</code> khác nhau tạo một context mới không tái dùng được — vô tình làm CI phình thời gian. Giữ cấu hình test đồng nhất để tận dụng cache là mẹo tối ưu ít người biết.</div>",
  refs:[["Spring Boot – Testing","https://docs.spring.io/spring-boot/reference/testing/index.html"],["Baeldung – @DataJpaTest","https://www.baeldung.com/junit-datajpatest-repository"]] },

{ id:"test-009", t:"Spring test", lv:"middle", core:false,
  tags:["testing-strategy","spring-test"],
  q:"Test một REST controller: <code>MockMvc</code> và <code>@MockBean</code> đóng vai gì?",
  a:"<p><strong><code>MockMvc</code></strong> cho phép 'bắn' request giả vào tầng web <em>mà không cần mở cổng mạng thật</em> — nó chạy qua đúng DispatcherServlet, filter, validation, message converter, nhưng trong bộ nhớ nên nhanh. Bạn kiểm status code, header, thân JSON:</p><pre><code class='language-java'>mockMvc.perform(get('/orders/1'))\n   .andExpect(status().isOk())\n   .andExpect(jsonPath('$.total').value(100));</code></pre><p><strong><code>@MockBean</code></strong> thay một bean thật trong context bằng một mock Mockito. Trong <code>@WebMvcTest</code>, service tầng dưới <em>không</em> được nạp, nên bạn <code>@MockBean OrderService</code> và dạy nó trả dữ liệu — nhờ đó test tập trung vào <em>hợp đồng HTTP</em> của controller, tách khỏi logic nghiệp vụ.</p><p>Với app reactive (WebFlux) thì đối trọng là <strong><code>WebTestClient</code></strong>; muốn test qua HTTP thật đầu-cuối thì dùng <code>@SpringBootTest(webEnvironment=RANDOM_PORT)</code> + <code>TestRestTemplate</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> MockMvc như <em>diễn tập tiếp khách ngay trong sảnh</em> — vẫn đi qua cửa an ninh, quầy lễ tân, đúng quy trình, nhưng 'khách' là người của mình đóng, không phải mở cửa cho phố xá ùa vào (mạng thật).</div><div class='qb-gotcha'><b>⚠ Phân biệt cho chuẩn:</b> <code>@MockBean</code> đặt mock <em>vào Spring context</em> (dùng khi có context); <code>@Mock</code> của Mockito chỉ là object rời (dùng cho unit test không Spring). Trả lời lẫn hai cái là mất điểm.</div>",
  refs:[["Spring – MockMvc","https://docs.spring.io/spring-framework/reference/testing/spring-mvc-test-framework.html"],["Baeldung – Testing with @MockBean","https://www.baeldung.com/java-spring-mockito-mock-mockbean"]] },

{ id:"test-010", t:"Chất lượng test", lv:"middle", core:true,
  tags:["testing-strategy","flaky-test"],
  q:"'Flaky test' là gì? Kể các nguyên nhân thường gặp và cách xử lý.",
  a:"<p><strong>Flaky test</strong> là test khi chạy thì xanh, chạy lại (không đổi code) lại đỏ — kết quả <em>không xác định</em>. Nó độc hại vì phá niềm tin: một khi đội quen 'chạy lại là xanh', họ bắt đầu bỏ qua cả những lần đỏ thật.</p><p>Các thủ phạm quen mặt: <strong>(1) Thời gian</strong> — dùng <code>Thread.sleep</code> chờ việc async, máy CI chậm hơn thì hụt giờ. <strong>(2) Phụ thuộc thứ tự</strong> — test A để lại dữ liệu/trạng thái tĩnh làm test B đổi kết quả. <strong>(3) Trạng thái chia sẻ</strong> — cùng ghi một bảng DB, một biến static, một file. <strong>(4) Ngẫu nhiên</strong> — random không seed, hoặc phụ thuộc thứ tự HashMap. <strong>(5) Múi giờ / locale / ngày hệ thống</strong> — dùng <code>LocalDate.now()</code> trực tiếp. <strong>(6) Đồng thời</strong> — race condition thật trong code lộ ra ngẫu nhiên.</p><p>Cách chữa: chờ async bằng <strong>Awaitility</strong> (poll tới điều kiện) thay vì ngủ cứng; làm mỗi test <strong>tự cô lập dữ liệu</strong> (rollback / dọn sạch); <strong>tiêm <code>Clock</code></strong> thay vì gọi now(); seed random; cố định timezone. Đừng 'chữa' bằng cách <code>@Retry</code> — đó là giấu bệnh.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như cái công tắc đèn lúc bật được lúc không — bạn không sửa bằng cách 'bấm vài lần cho ăn', mà phải tìm dây lỏng. Retry một flaky test chính là 'bấm vài lần cho ăn'.</div><div class='qb-gotcha'><b>⚠ Bẫy tư duy quan trọng:</b> đôi khi flaky test đang <em>tố cáo một bug thật</em> — một race condition trong code production chứ không phải lỗi test. Trước khi đổ cho 'test dở', hãy hỏi 'liệu chính hệ thống có bị race không?'.</div>",
  refs:[["Google Testing Blog – Flaky Tests","https://testing.googleblog.com/2016/05/flaky-tests-at-google-and-how-we.html"],["Awaitility","https://github.com/awaitility/awaitility"]] },

{ id:"test-011", t:"Phương pháp", lv:"middle", core:false,
  tags:["testing-strategy","tdd"],
  q:"TDD (red-green-refactor) là gì? Lợi ích thật sự và khi nào nó không hợp?",
  a:"<p><strong>TDD (Test-Driven Development)</strong> đảo ngược thứ tự quen thuộc: <em>viết test trước, code sau</em>, theo vòng lặp ngắn:</p><p><strong>🔴 Red</strong> — viết một test cho hành vi <em>chưa</em> có; chạy, nó đỏ (đúng như mong đợi, vì code chưa viết). <strong>🟢 Green</strong> — viết <em>ít nhất</em> code cho test xanh, kể cả cách xấu tạm thời. <strong>🔵 Refactor</strong> — giờ đã có lưới an toàn, dọn lại code cho sạch mà không sợ hỏng hành vi. Lặp lại từng bước nhỏ.</p><p>Lợi ích thật (không phải 'nhiều test'): nó ép bạn <strong>nghĩ về hành vi và API trước khi cắm đầu vào cài đặt</strong>; kết quả tự nhiên là code <em>dễ test</em> (ít phụ thuộc cứng); và bạn luôn có bộ test bám sát ý định. Test trở thành 'đặc tả chạy được'.</p><p>Khi nó kém hợp: lúc bạn đang <em>dò đường</em> (spike/prototype, chưa biết mình muốn gì), hoặc bài toán mà 'đúng' khó phát biểu trước (giao diện, tinh chỉnh thuật toán). Ở đó khám phá trước, viết test sau hợp lý hơn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như <em>viết đề bài kiểm tra trước rồi mới ôn</em> đúng phần đó: bạn buộc phải làm rõ 'thế nào là đạt' trước khi bắt tay, nên không lan man học thừa.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'TDD có đảm bảo ít bug không?' — nói cân bằng: TDD cải thiện <em>thiết kế và độ bao phủ ý định</em>, nhưng không thay được integration/E2E test và không bắt được lỗi bạn không nghĩ tới. Tán dương hay chê bai cực đoan đều bị coi là non.</div>",
  refs:[["Martin Fowler – TestDrivenDevelopment","https://martinfowler.com/bliki/TestDrivenDevelopment.html"]] },

{ id:"test-012", t:"Chất lượng test", lv:"senior", core:false,
  tags:["testing-strategy","code-coverage"],
  q:"Code coverage đo gì? Vì sao 100% coverage KHÔNG phải mục tiêu? 'Mutation testing' là gì?",
  a:"<p><strong>Code coverage</strong> đo <em>bao nhiêu phần code bị test chạy qua</em>. Line coverage đếm dòng được thực thi; <strong>branch coverage</strong> (chặt hơn) đếm mỗi nhánh if/else, mỗi lối rẽ đã đi cả hai chiều chưa. Nó hữu ích để <em>tìm vùng chưa ai test</em>.</p><p>Vì sao 100% không phải đích: coverage chỉ nói code <em>được chạy</em>, <strong>không</strong> nói nó <em>được kiểm</em>. Một test gọi hàm rồi <em>không assert gì</em> vẫn cho 100% coverage — mà chẳng bảo vệ điều gì. Chạy theo con số 100% đẻ ra test rỗng, test cho getter/setter, và cảm giác an toàn giả. Coverage thấp là tín hiệu xấu đáng tin; coverage cao <em>không</em> là bằng chứng tốt.</p><p><strong>Mutation testing</strong> (công cụ Java: PIT) kiểm chất lượng test một cách thông minh hơn: nó cố ý <em>gây đột biến</em> code — đổi <code>&gt;</code> thành <code>&gt;=</code>, đổi <code>+</code> thành <code>-</code>, bỏ một lời gọi — rồi chạy test. Nếu test <em>vẫn xanh</em> dù code đã bị phá, nghĩa là test đó không thật sự kiểm gì ('mutant sống sót'). Tỉ lệ 'giết được mutant' đo sức mạnh thật của bộ test, chứ không phải chỉ số dòng chạy qua.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Coverage như đếm 'đã đi qua bao nhiêu phòng trong nhà'; mutation testing như 'thử lén dời đồ trong phòng xem người gác có phát hiện không'. Đi qua phòng mà mắt nhắm thì đâu tính là canh.</div><div class='qb-gotcha'><b>⚠ Cách nói chín chắn:</b> đặt coverage làm <em>ngưỡng sàn</em> chống tụt (ví dụ không cho giảm dưới mức hiện tại), đừng đặt làm <em>đích trần</em> 100%. Nhắc được mutation testing là dấu hiệu senior thật.</div>",
  refs:[["Martin Fowler – TestCoverage","https://martinfowler.com/bliki/TestCoverage.html"],["PIT – Mutation Testing","https://pitest.org/"]] },

{ id:"test-013", t:"JUnit 5", lv:"junior", core:true,
  tags:["testing-strategy","junit"],
  q:"JUnit 5: <code>@BeforeEach</code> vs <code>@BeforeAll</code>, <code>@ParameterizedTest</code>, <code>assertThrows</code> — và vì sao nên dùng AssertJ?",
  a:"<p><strong><code>@BeforeEach</code></strong> chạy <em>trước mỗi</em> test method — dùng để dựng lại trạng thái sạch cho từng ca, tránh test này ảnh hưởng test kia. <strong><code>@BeforeAll</code></strong> chạy <em>một lần</em> trước tất cả (phải static theo mặc định) — dùng cho thứ đắt và bất biến (mở một container, nạp file lớn). Nhầm hai cái là nguồn của test phụ thuộc lẫn nhau.</p><p><strong><code>@ParameterizedTest</code></strong> chạy <em>cùng một test với nhiều bộ dữ liệu</em>, thay vì copy-paste chục method gần giống nhau:</p><pre><code class='language-java'>@ParameterizedTest\n@ValueSource(ints = {0, -1, -999})\nvoid so_am_bi_tu_choi(int x) {\n  assertThatThrownBy(() -&gt; validate(x)).isInstanceOf(IllegalArgumentException.class);\n}</code></pre><p><strong><code>assertThrows</code></strong> (hoặc <code>assertThatThrownBy</code> của AssertJ) kiểm rằng một đoạn code <em>ném đúng exception</em> mong đợi — test cho đường lỗi cũng quan trọng như đường thành công.</p><p><strong>AssertJ</strong> cho assertion 'fluent' dễ đọc và báo lỗi rõ hơn: <code>assertThat(list).hasSize(3).contains('a')</code> đọc như tiếng Anh, và khi đỏ nó in ra cả giá trị thực tế — đỡ phải đoán.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> <code>@ParameterizedTest</code> như một khuôn bánh đổ được nhiều mẻ nhân khác nhau — một công thức, nhiều biến thể, khỏi viết lại lò mỗi lần.</div><div class='qb-gotcha'><b>⚠ Bẫy version:</b> JUnit 5 (Jupiter) khác JUnit 4 về tên annotation — <code>@BeforeEach</code>/<code>@BeforeAll</code> thay cho <code>@Before</code>/<code>@BeforeClass</code>, và không còn <code>@RunWith</code> mà là <code>@ExtendWith</code>. Trộn hai đời trong một class là lỗi hay gặp khi migrate.</div>",
  refs:[["JUnit 5 User Guide","https://junit.org/junit5/docs/current/user-guide/"],["AssertJ","https://assertj.github.io/doc/"]] },

{ id:"test-014", t:"Microservices testing", lv:"senior", core:false,
  tags:["testing-strategy","contract-test"],
  q:"Vì sao microservices cần 'contract testing' (Pact / Spring Cloud Contract)? Nó giải bài toán gì mà integration test thường bỏ sót?",
  a:"<p>Khi service A gọi service B qua HTTP, câu hỏi đáng sợ là: 'nếu team B lặng lẽ đổi format response, liệu A có vỡ trên production không?'. Hai cách test quen thuộc đều đuối: mock B trong test của A thì <em>mock có thể lỗi thời</em> so với B thật; còn dựng cả A+B thật (E2E) thì <em>chậm, giòn, khó phủ hết ca</em> và không rõ lỗi thuộc bên nào.</p><p><strong>Contract testing</strong> biến 'hợp đồng' giữa hai bên thành thứ <em>kiểm được tự động ở cả hai phía</em>. Bên <strong>consumer</strong> (A) khai kỳ vọng 'tôi gửi request thế này, mong nhận response thế kia' → sinh ra một <em>contract</em>. Bên <strong>provider</strong> (B) chạy test <em>đối chiếu chính contract đó</em> với code thật của mình. Nếu B đổi API làm gãy kỳ vọng của A, <strong>build của B đỏ ngay</strong> — bắt được lỗi tương thích <em>trước khi</em> deploy, không cần chạy hai service cùng lúc.</p><p>Pact là công cụ phổ biến (consumer-driven); Spring Cloud Contract là lựa chọn trong hệ Spring. Điểm cốt lõi: mỗi bên vẫn test <em>độc lập, nhanh</em>, nhưng cái contract chung đảm bảo hai bên không lệch nhau.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như <em>bản vẽ kích thước ổ cắm và phích cắm</em>: nhà sản xuất phích và nhà sản xuất ổ ở hai nơi, không cần gặp nhau, chỉ cần cả hai kiểm sản phẩm của mình khớp <em>bản vẽ chuẩn</em> là cắm vừa. Đổi kích thước mà quên báo là xưởng kia phát hiện ngay khi kiểm.</div><div class='qb-gotcha'><b>⚠ Bẫy khái niệm:</b> contract test <em>không thay</em> integration/E2E test — nó chỉ đảm bảo <em>hình dạng giao tiếp</em> khớp nhau, không đảm bảo logic nghiệp vụ đầu-cuối đúng. Nói 'có contract test rồi thì khỏi E2E' là sai.</div>",
  refs:[["Pact – Contract Testing","https://docs.pact.io/"],["Spring Cloud Contract","https://spring.io/projects/spring-cloud-contract"]] },

{ id:"test-015", t:"Spring test", lv:"middle", core:false,
  tags:["testing-strategy","test-isolation"],
  q:"<code>@Transactional</code> trong test giúp cô lập dữ liệu thế nào? Cạm bẫy 'test xanh giả' của nó là gì?",
  a:"<p>Đặt <strong><code>@Transactional</code></strong> lên một test (hoặc dùng <code>@DataJpaTest</code>) khiến Spring mở một transaction trước test và <strong>rollback nó khi test xong</strong>. Nhờ vậy mỗi test bắt đầu trên DB sạch, không để lại rác cho test sau — giải bài toán 'cô lập dữ liệu' một cách gọn gàng, khỏi phải xoá bảng tay.</p><p>Nhưng chính cái rollback đó giăng bẫy: nó <em>che giấu những lỗi chỉ lộ ra khi commit thật</em>. Ràng buộc DB được kiểm lúc commit (deferred constraint), trigger, hoặc hành vi flush của Hibernate có thể không chạy trong test rollback → <strong>test xanh nhưng production đỏ lúc commit</strong>. Ngoài ra, vì mọi thứ nằm trong <em>một</em> transaction, bạn không thấy được bug về <em>ranh giới transaction</em> thật của code (ví dụ hai transaction riêng lẽ ra phải tách).</p><p>Cách phòng: với những luồng nhạy cảm về commit, viết thêm test <em>không</em> rollback (tự dọn dữ liệu sau), hoặc ép flush (<code>saveAndFlush</code>) để lỗi ràng buộc hiện ra sớm. Và luôn để mỗi test <em>tự chuẩn bị dữ liệu của mình</em> (qua <code>@Sql</code> hoặc builder), không dựa vào dữ liệu test khác để lại.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như <em>tập thuyết trình rồi xoá sạch bảng</em> trước người sau — gọn cho từng lượt, nhưng nếu lỗi chỉ xảy ra <em>lúc nộp bài thật</em> (commit) thì buổi tập luôn-xoá này không bao giờ phát hiện ra.</div><div class='qb-gotcha'><b>⚠ Bẫy phụ thuộc thứ tự:</b> nếu test của bạn <em>cần</em> dữ liệu do test khác tạo, là bạn đã có một quả bom hẹn giờ — đổi thứ tự chạy (hay chạy song song) là đỏ. Test tốt phải chạy đúng dù <em>đứng một mình</em>.</div>",
  refs:[["Spring – Testing Transactions","https://docs.spring.io/spring-framework/reference/testing/testcontext-framework/tx.html"]] },

{ id:"test-016", t:"Chất lượng test", lv:"middle", core:false,
  tags:["testing-strategy","async-test"],
  q:"Test code bất đồng bộ / đa luồng: vì sao <code>Thread.sleep</code> là phản mẫu, và dùng gì thay thế?",
  a:"<p>Khi code chạy việc gì đó <em>ở luồng khác</em> (gửi event, xử lý @Async, ghi qua hàng đợi), test phải <em>chờ</em> kết quả xuất hiện. Cám dỗ đầu tiên là <code>Thread.sleep(500)</code> rồi assert. Đây là phản mẫu vì bạn phải chọn giữa hai cái tệ: ngủ <em>quá ngắn</em> → máy CI chậm hơn thì kết quả chưa kịp có, test <strong>flaky</strong>; ngủ <em>quá dài</em> → an toàn hơn nhưng cả bộ test <strong>chậm dần</strong> theo mỗi lần thêm sleep.</p><p>Cách đúng là <strong>chờ theo điều kiện, không chờ theo đồng hồ</strong>. <strong>Awaitility</strong> poll liên tục tới khi điều kiện đúng (hoặc hết hạn tối đa):</p><pre><code class='language-java'>await().atMost(2, SECONDS)\n       .untilAsserted(() -&gt; assertThat(repo.count()).isEqualTo(1));</code></pre><p>Nó trả về <em>ngay khi</em> điều kiện thoả (thường vài chục ms) nên nhanh, và không đỏ oan trên máy chậm vì có trần 2 giây. Khi bạn kiểm soát được điểm đồng bộ, <strong><code>CountDownLatch</code></strong> hay <code>CompletableFuture.get(timeout)</code> còn chắc hơn: chờ đúng tín hiệu 'xong việc' thay vì đoán.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Chờ ấm nước sôi: <code>Thread.sleep</code> là 'đặt hẹn giờ 3 phút rồi quay lại' — bếp yếu thì nước chưa sôi, bếp mạnh thì phí thời gian. Awaitility là 'ngó chừng, sôi phát là rót ngay' — nhanh và không hụt.</div><div class='qb-gotcha'><b>⚠ Bẫy sâu hơn:</b> test đa luồng đỏ ngẫu nhiên có thể đang <em>tố cáo race condition thật</em> trong code, không phải lỗi test. Với logic đồng thời hiểm, cân nhắc công cụ chuyên như jcstress thay vì test JUnit thường.</div>",
  refs:[["Awaitility","https://github.com/awaitility/awaitility"],["Baeldung – Testing async","https://www.baeldung.com/java-testing-multithreaded"]] }

/* DATA_END */
] });
