/* ============================================================
   QBANK DATA — Core Java   (prefix id: java-)
   Trạng thái: REWRITTEN — đáp án viết bằng ngôn ngữ đơn giản cho junior,
   giải thích thuật ngữ cặn kẽ, không giả định biết trước.
   Thêm câu mới: copy 1 khối { ... }, dán ngay TRƯỚC dòng đánh dấu DATA_END
   (gần cuối file), đổi id tăng dần (java-035, java-036...).
   Schema + quy tắc viết đáp án: xem qbank/GUIDE.md
   ============================================================ */
QB.register({
  group: "Core Java",
  order: 2,
  prefix: "java",
  status: "rewritten",
  questions: [
/* DATA_START */

{ id:"java-001", t:"Java cơ bản", lv:"junior", core:true,
  tags:["jvm","jdk","bytecode"],
  q:"JDK, JRE, JVM khác nhau thế nào? Vì sao Java 'viết một lần, chạy mọi nơi'?",
  a:"<p>Ba chữ viết tắt này lồng nhau như búp bê Nga — đi từ trong ra ngoài:</p><ul><li><strong>JVM</strong> (Java Virtual Machine — máy ảo Java): 'cỗ máy' thực thi chương trình Java. Nó đọc <strong>bytecode</strong> (file <code>.class</code>) và dịch thành lệnh máy cho đúng hệ điều hành đang chạy.</li><li><strong>JRE</strong> (Java Runtime Environment): JVM + các thư viện chuẩn (String, List, IO...). Đủ để <em>chạy</em> chương trình Java, nhưng không <em>viết</em> được.</li><li><strong>JDK</strong> (Java Development Kit): JRE + đồ nghề cho lập trình viên — quan trọng nhất là <code>javac</code> (trình biên dịch) và các công cụ debug/theo dõi. Dev cài JDK; máy chỉ chạy app thì JRE là đủ.</li></ul><p><strong>'Write once, run anywhere' hoạt động thế nào?</strong> Ngôn ngữ như C++ biên dịch thẳng ra lệnh máy của từng hệ điều hành — muốn chạy nơi khác phải biên dịch lại. Java đi đường vòng thông minh: <code>javac</code> biên dịch code của bạn ra <strong>bytecode trung gian</strong>, giống nhau trên mọi máy. Phần 'phiên dịch theo địa phương' do JVM của từng nền tảng lo (có JVM cho Windows, Linux, macOS...). Bạn ship một file <code>.jar</code>, ai có JVM là chạy được.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bytecode như bản nhạc — viết một lần theo ký hiệu chuẩn quốc tế. JVM như nghệ sĩ ở mỗi nước: đọc cùng bản nhạc và chơi được trên nhạc cụ địa phương của họ.</div><div class='qb-gotcha'><b>⚠ Điểm cộng:</b> JVM không chỉ 'phiên dịch từng câu' (interpret) — nó có <strong>JIT compiler</strong> (Just-In-Time): đoạn code nào chạy lặp đi lặp lại nhiều ('hot spot') sẽ được biên dịch hẳn ra lệnh máy và tối ưu mạnh lúc chạy. Vì thế Java sau khi 'nóng máy' nhanh hơn lúc mới khởi động — nhắc được JIT là vượt mức thuộc bài.</div>",
  refs:[["Oracle – JDK, JRE, JVM","https://docs.oracle.com/javase/8/docs/index.html"],["Baeldung – JVM vs JRE vs JDK","https://www.baeldung.com/jvm-vs-jre-vs-jdk"]] },

{ id:"java-002", t:"Java cơ bản", lv:"junior", core:true,
  tags:["pass-by-value","reference","memory"],
  q:"Java là pass-by-value hay pass-by-reference? (bẫy kinh điển)",
  a:"<p>Trả lời chắc nịch: <strong>Java LUÔN LUÔN là pass-by-value</strong> — truyền tham số là truyền <em>bản sao</em> của giá trị. Không có ngoại lệ. Sự bối rối nằm ở chỗ: <em>giá trị đó là cái gì</em> tuỳ loại biến.</p><ul><li>Biến <strong>kiểu nguyên thuỷ</strong> (int, double...): giá trị là chính con số → hàm nhận <em>bản sao con số</em>. Sửa trong hàm không ảnh hưởng bên ngoài.</li><li>Biến <strong>kiểu object</strong>: giá trị của biến là <strong>reference</strong> — 'tờ giấy ghi địa chỉ' của object trên heap. Hàm nhận <em>bản sao tờ giấy địa chỉ</em>, nhưng hai tờ giấy ghi <em>cùng một địa chỉ nhà</em>.</li></ul><p>Từ đó suy ra hai hành vi khiến người mới rối:</p><pre><code class='language-java'>void doiTen(User u) { u.setName(\"Bình\"); }   // ① CÓ tác dụng ra ngoài\nvoid gan(User u)    { u = new User(\"Chi\"); } // ② KHÔNG có tác dụng</code></pre><p>① đi theo địa chỉ đến <em>căn nhà thật</em> và sửa đồ trong nhà → bên ngoài thấy thay đổi. ② chỉ <em>viết đè địa chỉ mới lên bản sao tờ giấy</em> của hàm — tờ giấy gốc bên ngoài vẫn ghi địa chỉ cũ, object cũ không suy suyển.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Bạn photo tờ giấy ghi địa chỉ nhà mình đưa thợ sửa ống nước. Thợ đến đúng nhà sửa (①) — nhà bạn thay đổi thật. Nhưng nếu thợ gạch địa chỉ trên <em>bản photo</em> và ghi nhà khác (②), tờ gốc của bạn đâu có đổi.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> Nói 'primitive là pass-by-value còn object là pass-by-reference' là <strong>SAI</strong> và interviewer chờ đúng câu sai này. Pass-by-reference thật (như C++) cho phép hàm gán <code>u = new User()</code> và thay đổi biến gốc bên ngoài — Java không làm được điều đó. Java copy reference, không truyền reference.</div>",
  refs:[["Baeldung – Pass-by-value","https://www.baeldung.com/java-pass-by-value-or-pass-by-reference"]] },

{ id:"java-003", t:"Java cơ bản", lv:"junior", core:true,
  tags:["primitive","wrapper","autoboxing","integer-cache"],
  q:"Primitive và wrapper class khác gì? Autoboxing là gì và có bẫy nào?",
  a:"<p>Java có hai 'họ' kiểu dữ liệu:</p><ul><li><strong>Primitive (nguyên thuỷ)</strong> — 8 kiểu: <code>int, long, double, boolean, byte, short, float, char</code>. Là <em>giá trị trần trụi</em> nằm gọn trong biến: nhẹ, nhanh, không bao giờ null.</li><li><strong>Wrapper (vỏ bọc)</strong> — <code>Integer, Long, Double, Boolean...</code>: object bọc quanh giá trị. Vì là object nên: được dùng trong generics (<code>List&lt;Integer&gt;</code> — List không nhận primitive), có method tiện ích (<code>Integer.parseInt</code>), và <em>có thể null</em>.</li></ul><p><strong>Autoboxing / unboxing</strong>: Java tự đổi qua lại giữa hai họ cho bạn — <code>Integer x = 5;</code> (tự bọc — boxing), <code>int y = x;</code> (tự mở — unboxing). Tiện, nhưng có hai bẫy:</p><p><strong>Bẫy 1 — NullPointerException khi unbox:</strong> <code>Integer soLuong = null; int n = soLuong;</code> → nổ NPE, vì Java phải gọi <code>soLuong.intValue()</code> trên null. Rất hay gặp khi map dữ liệu từ DB (cột nullable) sang primitive.</p><p><strong>Bẫy 2 — so sánh wrapper bằng <code>==</code>:</strong> JVM cache sẵn object Integer từ <strong>-128 đến 127</strong>. Trong khoảng này <code>==</code> tình cờ đúng (cùng object cache), ra ngoài là sai:</p><pre><code class='language-java'>Integer a = 127, b = 127;  a == b  // true  (dùng chung cache)\nInteger c = 128, d = 128;  c == d  // false (hai object riêng!)</code></pre><p>→ So sánh wrapper luôn dùng <code>.equals()</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Primitive như tiền mặt cầm tay — gọn, tiêu ngay. Wrapper như tiền bỏ trong ví có ngăn giấy tờ — cồng kềnh hơn nhưng 'gắn thêm được thông tin' và ví có thể... rỗng (null).</div><div class='qb-gotcha'><b>⚠ Điểm cộng:</b> giải thích được VÌ SAO có cache -128..127 (các số nhỏ dùng cực nhiều, cache để đỡ tạo object trùng lặp) và vì sao bug '== đúng lúc test số nhỏ, sai ở production số to' cực kỳ khó lần.</div>",
  refs:[["Oracle – Autoboxing","https://docs.oracle.com/javase/tutorial/java/data/autoboxing.html"]] },

{ id:"java-004", t:"Java cơ bản", lv:"junior", core:false,
  tags:["static","final","keyword"],
  q:"<code>static</code> và <code>final</code> nghĩa là gì trong từng ngữ cảnh (biến, method, class)?",
  a:"<p>Hai keyword độc lập nhau (và hay đi cùng nhau trong <code>static final</code>) — hiểu theo từng chỗ đứng:</p><p><strong><code>static</code> = 'thuộc về class, không thuộc về từng object'.</strong></p><ul><li><strong>Biến static</strong>: cả class chỉ có <em>một bản duy nhất</em>, mọi object nhìn chung — vd <code>static int soUserDangOnline</code>. (Cẩn thận: state chia sẻ = mầm bug đa luồng.)</li><li><strong>Method static</strong>: gọi thẳng qua tên class (<code>Math.max(a, b)</code>), không cần tạo object. Vì không gắn với object nào, bên trong nó <em>không dùng được <code>this</code></em> và không đụng được biến thường của object.</li><li><strong>Class static</strong>: chỉ áp dụng cho class lồng bên trong class khác (nested class) — nghĩa là nested class đó không cần object của class ngoài để tồn tại.</li></ul><p><strong><code>final</code> = 'chốt rồi, không đổi nữa'.</strong></p><ul><li><strong>Biến final</strong>: gán một lần duy nhất. Lưu ý tinh tế: với object, final khoá <em>tờ giấy địa chỉ</em> chứ không khoá <em>căn nhà</em> — <code>final List&lt;String&gt; ds</code> vẫn <code>ds.add(...)</code> được, chỉ không gán <code>ds = listKhac</code> được.</li><li><strong>Method final</strong>: class con không được override.</li><li><strong>Class final</strong>: không ai kế thừa được — <code>String</code> là final, một phần lý do nó immutable đáng tin.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> static như <em>bảng thông báo chung của toà nhà</em> — một bảng, mọi căn hộ đọc chung; biến thường như tủ lạnh riêng từng nhà. final như <em>khắc lên bia đá</em> — viết một lần, không sửa.</div><div class='qb-gotcha'><b>⚠ Câu bẫy hay gặp:</b> 'static method có override được không?' — <strong>Không</strong>. Class con khai báo method static trùng tên chỉ là <em>che khuất</em> (method hiding): method được gọi quyết định lúc <em>biên dịch</em> theo kiểu khai báo của biến, không có tính đa hình lúc chạy như override thật.</div>",
  refs:[["Oracle – Class variables","https://docs.oracle.com/javase/tutorial/java/javaOO/classvars.html"]] },

{ id:"java-005", legacy:8, t:"OOP", lv:"junior", core:true,
  tags:["oop","encapsulation","polymorphism"],
  q:"4 tính chất OOP là gì? Giải thích sao cho không như trả bài?",
  a:"<p>Học thuộc 4 cái tên thì ai cũng thuộc — interviewer muốn nghe bạn giải thích <em>vấn đề mỗi tính chất giải quyết</em>. Lấy ví dụ xuyên suốt: hệ thống có các loại tài khoản người dùng.</p><p><strong>1. Encapsulation (đóng gói) — 'đừng cho ai thò tay vào ruột'.</strong> Dữ liệu (field) để <code>private</code>, chỉ mở ra các method public có kiểm soát. Vd: không cho ai gán thẳng <code>user.email = \"xyz\"</code>, mà buộc đi qua <code>user.doiEmail(email)</code> — nơi có validate + ghi log. Lợi ích: state không bị sửa lung tung, muốn tìm chỗ đổi email chỉ có một cửa.</p><p><strong>2. Abstraction (trừu tượng hoá) — 'chỉ cần biết làm được gì, khỏi biết làm thế nào'.</strong> Người dùng class chỉ thấy <code>thanhToan(donHang)</code>, không cần biết bên trong gọi cổng nào, retry ra sao. Che chi tiết để đầu óc người gọi nhẹ, và chi tiết đổi được mà không ai bị ảnh hưởng.</p><p><strong>3. Inheritance (kế thừa) — 'con dùng lại và mở rộng của cha'.</strong> <code>TaiKhoanVip extends TaiKhoan</code>: dùng lại toàn bộ logic chung, thêm/thay phần riêng. Dùng khi quan hệ thực sự là 'LÀ MỘT loại' (Vip <em>là một</em> tài khoản).</p><p><strong>4. Polymorphism (đa hình) — 'cùng một lời gọi, mỗi loại phản ứng kiểu của nó'.</strong> Code chỉ viết <code>taiKhoan.tinhPhi()</code>; lúc chạy, object thật là Vip thì chạy công thức Vip, là thường thì công thức thường. Nhờ nó, thêm loại tài khoản mới <em>không phải sửa</em> đống <code>if-else</code> ở mọi nơi.</p><div class='qb-eli5'><b>🌱 Nhớ nhanh:</b> đóng gói = giấu ruột; trừu tượng = giấu cách làm; kế thừa = con nối nghiệp cha; đa hình = một hiệu lệnh, nhiều kiểu thực hiện.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'Encapsulation khác Abstraction chỗ nào?' — đóng gói giấu <em>dữ liệu</em> (chống sửa bậy, hướng về an toàn state); trừu tượng giấu <em>độ phức tạp của cách làm</em> (hướng về thiết kế giao diện sử dụng). Một cái bảo vệ bên trong, một cái đơn giản hoá bề mặt.</div>",
  refs:[["Baeldung – OOP in Java","https://www.baeldung.com/java-oop"]] },

{ id:"java-006", legacy:9, t:"OOP", lv:"junior", core:true,
  tags:["oop","overloading","overriding","polymorphism"],
  q:"Overloading và Overriding khác nhau ra sao?",
  a:"<p>Hai từ giống nhau đến khó chịu nhưng là hai cơ chế khác hẳn:</p><p><strong>Overloading (nạp chồng) — cùng tên, khác 'chữ ký', cùng một class.</strong> Nhiều method trùng tên nhưng khác tham số (khác kiểu/số lượng):</p><pre><code class='language-java'>void in(int x)            { ... }\nvoid in(String s)         { ... }\nvoid in(String s, int n)  { ... }</code></pre><p>Compiler nhìn <em>tham số lúc gọi</em> để chọn bản nào — quyết định xong ngay <strong>lúc biên dịch</strong> (compile-time). Mục đích: một hành động, nhiều kiểu đầu vào, đỡ phải đặt tên <code>inInt</code>, <code>inString</code>.</p><p><strong>Overriding (ghi đè) — class con viết lại method của cha, giữ nguyên chữ ký.</strong></p><pre><code class='language-java'>class TaiKhoan     { double tinhPhi() { return 10; } }\nclass TaiKhoanVip extends TaiKhoan { @Override double tinhPhi() { return 0; } }</code></pre><p>Bản nào chạy được quyết định <strong>lúc chạy</strong> (runtime), theo <em>object thật</em> chứ không theo kiểu khai báo: <code>TaiKhoan tk = new TaiKhoanVip(); tk.tinhPhi()</code> → chạy bản Vip, trả 0. Đây chính là trái tim của đa hình.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Overloading như tổng đài có nhiều số máy nhánh cùng tên phòng — gọi kèm 'nhánh 1' hay 'nhánh 2' là nối đúng người ngay từ lúc bấm số. Overriding như chi nhánh địa phương tự thay cách phục vụ món của công ty mẹ — khách gọi món quen, chi nhánh nào phục vụ theo kiểu chi nhánh đó.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> (1) Overload mà chỉ khác <em>kiểu trả về</em> → không biên dịch được (kiểu trả về không thuộc chữ ký). (2) Override có luật: không được thu hẹp quyền truy cập (public → private là fail), không ném checked exception rộng hơn cha. (3) Luôn dán <code>@Override</code> — gõ sai tên method thì compiler la ngay thay vì âm thầm thành method mới.</div>",
  refs:[["Baeldung – Overload vs Override","https://www.baeldung.com/java-method-overload-override"]] },

{ id:"java-007", legacy:10, t:"OOP", lv:"junior", core:false,
  tags:["oop","interface","abstract-class"],
  q:"Khi nào dùng interface, khi nào dùng abstract class?",
  a:"<p>Cả hai đều là 'khuôn' không tự đứng một mình được (không <code>new</code> trực tiếp) — khác nhau ở thứ chúng chia sẻ cho class bên dưới:</p><p><strong>Interface — chia sẻ HỢP ĐỒNG.</strong> Nó chỉ tuyên bố 'ai ký hợp đồng này phải làm được các việc sau', không giữ state (không có field thường). Sức mạnh lớn nhất: một class <strong>implement được nhiều interface</strong>, và các class <em>chẳng họ hàng gì nhau</em> vẫn ký chung được một hợp đồng — <code>Comparable</code> được cả String, Integer, class của bạn implement.</p><p><strong>Abstract class — chia sẻ MÁU MỦ.</strong> Là một class 'chưa hoàn thiện': có field (state), có method viết sẵn dùng chung, và chừa vài method abstract cho con tự điền. Giới hạn: mỗi class chỉ <strong>extends được một</strong> abstract class. Hợp khi các class con thực sự cùng một họ và cần dùng chung cả dữ liệu lẫn logic — vd <code>BaseEntity</code> chứa sẵn <code>id, createdAt</code>.</p><p><strong>Luật chọn nhanh:</strong> mô tả <em>khả năng</em> mà nhiều loại khác nhau cùng có (so sánh được, đóng được, gửi thông báo được) → interface. Mô tả <em>một họ</em> có code và dữ liệu chung → abstract class. Phân vân → ưu tiên interface (ràng buộc lỏng hơn, không 'đốt' suất kế thừa duy nhất của class người ta).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Interface như <em>chứng chỉ hành nghề</em> — ai học đủ đều thi được, một người lấy nhiều chứng chỉ. Abstract class như <em>gia sản cha để lại</em> — có đồ dùng chung sẵn (state, code) nhưng mỗi người chỉ một dòng họ.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối từ Java 8:</b> 'Interface có default method rồi (code viết sẵn được), sao còn cần abstract class?' — vì interface vẫn <strong>không có state</strong> (không field instance, không constructor). Abstract class vẫn là lựa chọn duy nhất khi cần chia sẻ <em>dữ liệu</em> và vòng đời khởi tạo chung.</div>",
  refs:[["Oracle – Abstract vs Interface","https://docs.oracle.com/javase/tutorial/java/IandI/abstract.html"]] },

{ id:"java-008", legacy:11, t:"OOP", lv:"junior", core:true,
  tags:["equals","reference","integer-cache"],
  q:"<code>==</code> và <code>.equals()</code> khác nhau ra sao trong Java?",
  a:"<p>Một câu phân biệt được người hiểu bộ nhớ Java với người học vẹt:</p><p><strong><code>==</code> so sánh giá trị nằm TRONG biến.</strong> Với primitive, trong biến là con số → so số, chuẩn. Với object, trong biến là <em>reference (địa chỉ)</em> → <code>==</code> hỏi: 'hai biến có trỏ về <strong>cùng một object</strong> không?' — nó không hề nhìn nội dung.</p><p><strong><code>.equals()</code> so sánh theo LUẬT do class định nghĩa.</strong> Mặc định (kế thừa từ Object) nó cũng so địa chỉ như <code>==</code>; nhưng các class như String, Integer, LocalDate đã <em>override</em> để so <strong>nội dung</strong>. Class của bạn muốn so nội dung thì phải tự override.</p><pre><code class='language-java'>String a = new String(\"abc\");\nString b = new String(\"abc\");\na == b        // false — hai object riêng trên heap\na.equals(b)   // true  — nội dung như nhau</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> <code>==</code> hỏi 'hai tờ giấy có ghi cùng một địa chỉ nhà không?'. <code>.equals()</code> hỏi 'hai căn nhà có giống hệt nhau bên trong không?'. Hai nhà giống hệt nhau vẫn là hai địa chỉ khác nhau.</div><div class='qb-gotcha'><b>⚠ Hai bẫy kinh điển:</b> (1) <strong>String literal</strong>: <code>String a = \"abc\"; String b = \"abc\"; a == b</code> ra <code>true</code> — vì cả hai cùng trỏ vào một bản trong <em>String pool</em> (xem câu String). Đúng 'tình cờ', đừng dựa vào. (2) <strong>Integer cache</strong>: <code>Integer 127 == 127</code> true nhưng <code>128 == 128</code> false. Kết luận thực chiến: so sánh object LUÔN dùng <code>.equals()</code> (hoặc <code>Objects.equals(a, b)</code> để an toàn null).</div>",
  refs:[["Baeldung – == vs equals","https://www.baeldung.com/java-equals-method-operator-difference"]] },

{ id:"java-009", t:"OOP", lv:"junior", core:true,
  tags:["equals","hashcode","hashmap","contract"],
  q:"Vì sao override <code>equals()</code> thì PHẢI override <code>hashCode()</code>? Chuyện gì xảy ra nếu quên?",
  a:"<p>Vì hai method này bị ràng bởi một <strong>bản giao kèo (contract)</strong> mà các collection dạng hash (HashMap, HashSet) tin tưởng tuyệt đối: <em>hai object equals nhau thì hashCode PHẢI bằng nhau</em>. (Chiều ngược lại không bắt buộc — hai object cùng hashCode có thể khác nhau, gọi là collision.)</p><p><strong>Vì sao contract này quan trọng?</strong> HashMap tìm đồ theo 2 bước: bước 1 dùng <code>hashCode()</code> để nhảy thẳng đến 'ngăn' (bucket) chứa đồ; bước 2 dùng <code>equals()</code> so từng món trong ngăn. Tức là: <em>hashCode quyết định tìm Ở ĐÂU, equals quyết định có ĐÚNG NÓ không</em>.</p><p><strong>Quên override hashCode thì sao?</strong> Class của bạn dùng hashCode mặc định (dựa trên địa chỉ object) — hai object 'bằng nhau' theo equals nhưng nằm ở hai ngăn khác nhau:</p><pre><code class='language-java'>Set&lt;User&gt; set = new HashSet&lt;&gt;();\nset.add(new User(\"an@x.com\"));\nset.contains(new User(\"an@x.com\"))  // false?! — nó tìm sai ngăn\n// HashSet giờ chứa được 2 user 'giống hệt nhau' — mất luôn tính duy nhất</code></pre><p>Bug kiểu này <em>không văng exception</em> — chỉ âm thầm sai: chứa trùng, tìm không thấy, cache miss vô lý. Cực khó lần.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gửi đồ ở tủ khoá theo số điện thoại: nhân viên tính 'số tủ' từ SĐT của bạn (hashCode) rồi mở tủ so mặt (equals). Nếu mỗi lần bạn đến, máy tính ra <em>số tủ khác nhau</em> dù cùng SĐT — đồ của bạn coi như mất, dù nó vẫn nằm đâu đó trong kho.</div><div class='qb-gotcha'><b>⚠ Thực chiến:</b> (1) Luôn generate cặp equals/hashCode từ <em>cùng một bộ field</em> (IDE, Lombok <code>@EqualsAndHashCode</code>, hoặc dùng <code>record</code> — tự có sẵn đúng chuẩn). (2) Bẫy hiểm: dùng object làm key trong HashMap rồi <em>sửa field tham gia hashCode</em> → key 'lạc trôi' sang ngăn khác, get không ra nữa. Key nên là immutable.</div>",
  refs:[["Baeldung – equals & hashCode contract","https://www.baeldung.com/java-equals-hashcode-contracts"]] },

{ id:"java-010", t:"String", lv:"junior", core:true,
  tags:["string","immutable","string-pool","stringbuilder"],
  q:"Vì sao String immutable? String pool là gì? Khi nào dùng StringBuilder?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Immutable</strong> (bất biến) = object tạo ra rồi thì <em>không sửa được nữa</em>; mọi thao tác 'sửa' thật ra tạo object mới. <code>s.toUpperCase()</code> không đổi <code>s</code> — nó trả về String mới.</div><p><strong>Vì sao Java thiết kế String bất biến?</strong> Ba lý do chính:</p><ul><li><strong>An toàn</strong>: String được truyền khắp nơi (tên user, đường dẫn file, câu SQL). Nếu sửa được, code nhận String có thể lén đổi nội dung sau khi đã kiểm tra — lỗ hổng bảo mật.</li><li><strong>Dùng chung được (String pool)</strong>: vì không ai sửa được, nhiều biến trỏ chung một bản là vô hại. JVM tận dụng: các chuỗi literal (<code>\"abc\"</code> viết thẳng trong code) được gom vào một kho chung gọi là <strong>String pool</strong> — hai chỗ cùng viết <code>\"abc\"</code> sẽ trỏ vào <em>cùng một object</em>, tiết kiệm bộ nhớ. (Đây là lý do <code>\"abc\" == \"abc\"</code> tình cờ true, còn <code>new String(\"abc\")</code> thì cố tình tạo bản mới ngoài pool nên == false.)</li><li><strong>Cache hashCode</strong>: nội dung không đổi → hashCode tính một lần dùng mãi → String làm key HashMap rất nhanh và an toàn.</li></ul><p><strong>Mặt trái và StringBuilder:</strong> nối chuỗi trong vòng lặp bằng <code>+=</code> là thảm hoạ ngầm — mỗi vòng tạo một String trung gian mới (vòng 1000 tạo cả nghìn object rác):</p><pre><code class='language-java'>String s = \"\";\nfor (var x : ds) s += x;              // ❌ O(n²), xả rác\n\nStringBuilder sb = new StringBuilder();\nfor (var x : ds) sb.append(x);         // ✅ sửa tại chỗ trên một buffer\nString kq = sb.toString();</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> String như <em>ảnh đã in</em> — muốn khác đi phải in tấm mới; nhờ vậy treo một tấm cho cả nhà xem chung không sợ ai vẽ bậy (pool). StringBuilder như <em>bảng trắng</em> — viết xoá thoải mái, xong việc mới chụp lại thành ảnh (toString).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối:</b> 'StringBuilder vs StringBuffer?' — giống hệt nhau về API, StringBuffer có synchronized (thread-safe) nên chậm hơn; nó là đồ cổ, thực tế gần như luôn dùng StringBuilder (một chuỗi hiếm khi được nhiều thread cùng build). Điểm cộng nữa: <code>+</code> nối chuỗi <em>một dòng đơn lẻ</em> thì compiler tự tối ưu rồi — chỉ trong vòng lặp mới phải tự dùng StringBuilder.</div>",
  refs:[["Baeldung – Why String is immutable","https://www.baeldung.com/java-string-immutable"]] },

{ id:"java-011", t:"Collections", lv:"junior", core:true,
  tags:["collections","list","set","map"],
  q:"List, Set, Map khác nhau thế nào? Cho ví dụ chọn cái nào khi nào.",
  a:"<p>Ba 'kiểu hộp đựng' phủ 95% nhu cầu hằng ngày — chọn theo <em>luật chơi của dữ liệu</em>:</p><ul><li><strong>List — danh sách CÓ THỨ TỰ, CHO PHÉP TRÙNG.</strong> Từng phần tử có chỉ số (index), lấy theo vị trí được. Dùng khi thứ tự có ý nghĩa: các dòng trong đơn hàng, lịch sử thao tác, kết quả đã sort. Mặc định: <code>ArrayList</code>.</li><li><strong>Set — tập hợp KHÔNG TRÙNG, thường không quan tâm thứ tự.</strong> Add phần tử 'đã có' (theo equals/hashCode) thì bị lờ đi. Dùng khi cần tính duy nhất hoặc kiểm tra tồn tại nhanh: tập user id đã gửi thông báo, tập từ khoá cấm. Mặc định: <code>HashSet</code>; cần giữ thứ tự thêm vào → <code>LinkedHashSet</code>; cần sắp xếp sẵn → <code>TreeSet</code>.</li><li><strong>Map — bảng tra CẶP KHOÁ → GIÁ TRỊ, khoá không trùng.</strong> Không phải 'danh sách phần tử' mà là 'danh bạ': đưa khoá, nhận giá trị trong O(1). Dùng khi tra cứu theo định danh: userId → User, mã sản phẩm → tồn kho. Mặc định: <code>HashMap</code>.</li></ul><p><strong>Phản xạ chọn nhanh khi phỏng vấn</strong>: nghe 'không được trùng' → Set; nghe 'tra theo id/mã' → Map; nghe 'giữ thứ tự, có phần tử thứ i' → List. Nói thêm được biến thể (Linked/Tree/Concurrent) khi bị hỏi sâu.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> List = hàng người xếp trước quầy (có vị trí, hai người trùng tên vẫn đứng được). Set = danh sách khách mời (mỗi tên một lần, thứ tự chẳng quan trọng). Map = danh bạ điện thoại (tên → số, mỗi tên một dòng).</div><div class='qb-gotcha'><b>⚠ Bẫy hay gặp:</b> (1) Set và Map key <em>sống chết nhờ equals/hashCode</em> — class tự viết mà quên override là 'trùng vẫn lọt' (xem câu contract). (2) HashMap/HashSet <strong>không đảm bảo thứ tự</strong>, thậm chí thứ tự có thể đổi giữa các lần chạy — test in ra 'đúng thứ tự' chỉ là ăn may.</div>",
  refs:[["Oracle – Collections trail","https://docs.oracle.com/javase/tutorial/collections/interfaces/index.html"]] },

{ id:"java-012", legacy:24, t:"Collections", lv:"junior", core:true,
  tags:["collections","arraylist","linkedlist","big-o"],
  q:"<code>ArrayList</code> và <code>LinkedList</code> khác nhau ra sao? Thực tế nên dùng cái nào?",
  a:"<p>Hai cách cài đặt cùng một interface <code>List</code>, khác nhau ở cấu trúc bên dưới:</p><p><strong>ArrayList — mảng co giãn.</strong> Phần tử nằm <em>liền kề nhau</em> trong một mảng. Lấy phần tử thứ i (<code>get(i)</code>) là phép tính địa chỉ → <strong>tức thì O(1)</strong>. Chèn/xoá ở <em>giữa</em> phải dịch cả khúc đuôi sang một ô → O(n). Mảng đầy thì tự tạo mảng to gấp rưỡi và copy sang (thi thoảng mới xảy ra, tính trung bình add cuối vẫn coi như O(1)).</p><p><strong>LinkedList — chuỗi mắt xích.</strong> Mỗi phần tử là một nút (node) rời rạc, giữ con trỏ tới nút trước/sau. Chèn/xoá <em>tại vị trí đang đứng</em> chỉ là nối lại vài con trỏ → O(1). Nhưng muốn <em>đến</em> phần tử thứ i phải lần theo từng mắt xích → <strong>get(i) là O(n)</strong>.</p><p><strong>Thực tế: gần như luôn dùng ArrayList.</strong> Không chỉ vì Big-O: mảng nằm liền kề nên cực thân thiện với CPU cache (đọc phần tử này, mấy phần tử cạnh đã được kéo sẵn vào cache); LinkedList mỗi node là một object rải rác trên heap — mỗi bước nhảy là một lần 'đi xa', cộng thêm tốn bộ nhớ cho 2 con trỏ mỗi node. Benchmark thực tế LinkedList thua cả ở nhiều kịch bản 'trên giấy nó phải thắng'.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> ArrayList như dãy ghế đánh số trong rạp — 'ghế 57' là đi thẳng tới; nhưng chen thêm một ghế vào giữa thì cả hàng phải xê dịch. LinkedList như trò nắm tay thành vòng — chen người mới vào cạnh mình chỉ cần buông tay nắm lại, nhưng muốn tìm người thứ 57 phải đếm lần lượt từ đầu.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'LinkedList chèn giữa O(1) mà?' — chỉ đúng khi <em>đã đứng sẵn tại chỗ đó</em> (qua Iterator). Còn <code>list.add(500000, x)</code> vẫn phải lần tới vị trí 500000 trước — O(n) rồi mới O(1). Nói được ý này + ý CPU cache là ăn trọn câu này.</div>",
  refs:[["Baeldung – ArrayList vs LinkedList","https://www.baeldung.com/java-arraylist-linkedlist"]] },

{ id:"java-013", legacy:25, t:"Collections", lv:"middle", core:true,
  tags:["hashmap","hashcode","collision","treeify"],
  q:"<code>HashMap</code> hoạt động bên trong thế nào? Collision và 'treeify' là gì?",
  a:"<p>Trước tiên nhớ mục tiêu của HashMap: <code>get(key)</code> nhanh gần như tức thì dù chứa cả triệu phần tử. Nó làm được nhờ cấu trúc: <strong>một mảng các 'ngăn' (bucket)</strong>.</p><p><strong>Khi <code>put(key, value)</code>:</strong></p><ol><li>Gọi <code>key.hashCode()</code> → ra một số nguyên.</li><li>'Nén' số đó về chỉ số ngăn: <code>index = hash &amp; (soNgan - 1)</code> — vd 16 ngăn thì ra 0..15.</li><li>Bỏ cặp (key, value) vào ngăn đó.</li></ol><p><code>get(key)</code> đi đúng con đường ấy: tính lại index → nhảy thẳng tới ngăn → so <code>equals()</code> với từng key trong ngăn. Không phải duyệt cả map — đó là phép màu O(1).</p><p><strong>Collision (va chạm):</strong> hai key khác nhau ra cùng chỉ số ngăn — chuyện bình thường (ngăn ít, key nhiều). Các cặp trong cùng ngăn được xâu thành <strong>danh sách liên kết</strong>; tìm trong ngăn là so equals lần lượt. Ngăn nào bị nhồi quá <strong>8 phần tử</strong> (và map đủ lớn), Java 8+ chuyển danh sách đó thành <strong>cây đỏ-đen</strong> ('treeify') → tìm trong ngăn từ O(n) xuống O(log n), chống kịch bản bị nhồi độc ý (hash-flooding).</p><p><strong>Resize:</strong> số phần tử vượt <code>soNgan × load factor (0.75)</code> → tạo mảng ngăn gấp đôi, chia lại toàn bộ phần tử (rehash) — tốn kém, nên biết trước kích thước thì khởi tạo <code>new HashMap&lt;&gt;(dungLuong)</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gửi xe theo số cuối biển số: biển 4437 → ô số 7, lấy xe nhảy thẳng ô 7 so biển (equals). Nhiều xe cùng đuôi 7 (collision) thì trong ô xếp thành hàng; hàng dài quá thì lắp giá đỡ nhiều tầng có đánh chỉ mục (treeify).</div><div class='qb-gotcha'><b>⚠ Chuỗi hỏi xoáy quen thuộc:</b> 'hashCode tệ (luôn trả 1) thì sao?' — mọi thứ dồn 1 ngăn, HashMap thoái hoá thành danh sách O(n). 'Key bị sửa field sau khi put?' — hash tính lại ra ngăn khác, get không thấy nữa → key phải immutable. Hai câu này chính là lý do người ta bắt bạn học ruột HashMap.</div>",
  refs:[["Baeldung – HashMap internals","https://www.baeldung.com/java-hashmap-advanced"]] },

{ id:"java-014", legacy:26, t:"Collections", lv:"middle", core:false,
  tags:["concurrenthashmap","thread-safe","collections"],
  q:"<code>HashMap</code>, <code>Hashtable</code> và <code>ConcurrentHashMap</code> — chọn cái nào cho đa luồng?",
  a:"<p>Ba lựa chọn, ba thời đại:</p><ul><li><strong>HashMap</strong> — nhanh nhất, <em>không</em> thread-safe. Nhiều thread cùng ghi có thể mất dữ liệu hoặc hỏng cấu trúc bên trong. Dùng khi map chỉ sống trong một thread/một request.</li><li><strong>Hashtable</strong> — đồ cổ từ Java 1.0: thread-safe kiểu thô sơ — <em>khoá toàn bộ map</em> cho mọi thao tác, kể cả đọc. Trăm thread xếp hàng qua một cánh cửa → nghẽn. Gần như không còn lý do để dùng.</li><li><strong>ConcurrentHashMap</strong> — thread-safe hiện đại: đọc <em>không cần khoá</em>, ghi chỉ khoá <em>đúng ngăn (bucket) đang đụng</em> chứ không khoá cả map → nhiều thread thao tác song song thoải mái. Kèm các thao tác nguyên tử cực hữu ích: <code>putIfAbsent</code>, <code>computeIfAbsent</code>, <code>merge</code>.</li></ul><p><strong>Bẫy tư duy quan trọng nhất:</strong> thread-safe từng thao tác ≠ thread-safe cho <em>chuỗi</em> thao tác. Đoạn này vẫn race dù dùng ConcurrentHashMap:</p><pre><code class='language-java'>if (!map.containsKey(k)) {   // thread khác chen vào giữa 2 dòng!\n    map.put(k, taoMoi());\n}\n// ✅ đúng: gộp kiểm tra + ghi thành MỘT thao tác nguyên tử\nmap.computeIfAbsent(k, key -&gt; taoMoi());</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hashtable như siêu thị chỉ mở một quầy tính tiền cho tất cả. ConcurrentHashMap như siêu thị nhiều quầy — ai mua gì ra quầy khu đó, chỉ ai cùng quầy mới phải chờ nhau.</div><div class='qb-gotcha'><b>⚠ Chi tiết hay bị hỏi:</b> ConcurrentHashMap <strong>không cho key/value null</strong> — vì <code>get(k)</code> trả null sẽ không phân biệt được 'không có key' với 'có key, value là null' trong môi trường đa luồng (không thể check-rồi-get an toàn). HashMap cho 1 key null vì đơn luồng thì check được.</div>",
  refs:[["Baeldung – ConcurrentHashMap","https://www.baeldung.com/java-concurrent-map"]] },

{ id:"java-015", legacy:27, t:"Collections", lv:"middle", core:false,
  tags:["iterator","fail-fast","collections"],
  q:"<code>ConcurrentModificationException</code> xảy ra khi nào? Cách tránh?",
  a:"<p>Exception này nổ khi bạn <strong>vừa duyệt vừa sửa cấu trúc</strong> của collection — cụ thể: đang lặp bằng for-each (bên dưới là Iterator) mà gọi <code>list.add()</code>/<code>list.remove()</code> trực tiếp:</p><pre><code class='language-java'>for (User u : users) {\n    if (u.inactive()) users.remove(u);   // 💥 ConcurrentModificationException\n}</code></pre><p><strong>Cơ chế:</strong> collection giữ một bộ đếm <code>modCount</code> — đếm số lần cấu trúc bị thay đổi (thêm/xoá phần tử; sửa <em>nội dung</em> một phần tử không tính). Iterator chụp giá trị này lúc bắt đầu; mỗi bước lặp nó so lại — thấy lệch nghĩa là 'có ai sửa sau lưng tôi' → ném exception <strong>ngay lập tức</strong>. Triết lý này gọi là <strong>fail-fast</strong>: thà chết to tiếng còn hơn âm thầm duyệt sai (bỏ sót/lặp đôi phần tử).</p><p><strong>Các cách sửa đúng:</strong></p><ul><li><strong>Xoá qua chính Iterator</strong>: <code>it.remove()</code> — cách duy nhất Iterator công nhận (nó tự cập nhật modCount).</li><li><strong>Gọn nhất</strong>: <code>users.removeIf(u -&gt; u.inactive());</code></li><li>Hoặc gom thứ cần xoá vào list riêng, duyệt xong <code>users.removeAll(danhSachXoa)</code>.</li><li>Đa luồng thật sự (thread khác sửa trong lúc mình duyệt): dùng collection concurrent (<code>ConcurrentHashMap</code>, <code>CopyOnWriteArrayList</code>) — chúng không fail-fast.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Đang điểm danh giữa buổi mà học sinh tự ý ra vào lớp thì sổ điểm danh loạn — giáo viên khó tính (fail-fast) sẽ dừng ngay và bắt điểm danh lại, thay vì đọc tiếp cho xong rồi sai.</div><div class='qb-gotcha'><b>⚠ Bẫy tên gọi:</b> tên có chữ 'Concurrent' nhưng <em>không cần đa luồng</em> mới dính — một thread vừa lặp vừa remove là đủ nổ. Và fail-fast là <em>best-effort</em> (cố gắng phát hiện, không cam kết 100%) — đừng dùng nó làm cơ chế logic.</div>",
  refs:[["Baeldung – ConcurrentModificationException","https://www.baeldung.com/java-concurrentmodificationexception"]] },

{ id:"java-016", t:"Collections", lv:"junior", core:false,
  tags:["comparable","comparator","sort"],
  q:"<code>Comparable</code> khác <code>Comparator</code> thế nào? Sort một List theo nhiều tiêu chí ra sao?",
  a:"<p>Cả hai đều trả lời câu 'ai đứng trước ai', khác ở chỗ <em>luật so sánh nằm ở đâu</em>:</p><p><strong><code>Comparable</code> — luật nằm TRONG class, là 'thứ tự tự nhiên'.</strong> Class tự implement <code>compareTo(other)</code>: 'so TÔI với người kia'. Mỗi class chỉ có <em>một</em> thứ tự tự nhiên — String theo alphabet, Integer theo giá trị. Dùng khi class có một cách sắp xếp hiển nhiên: <code>Collections.sort(list)</code> chạy thẳng không cần nói gì thêm.</p><p><strong><code>Comparator</code> — luật nằm NGOÀI class, viết bao nhiêu bản tuỳ thích.</strong> Là 'trọng tài' độc lập: <code>compare(a, b)</code>. Dùng khi: class không phải của bạn để sửa, hoặc cần <em>nhiều</em> kiểu sắp xếp khác nhau tuỳ màn hình (theo tên, theo lương, theo ngày vào).</p><p>Từ Java 8, viết Comparator dễ chịu hẳn nhờ các hàm dựng sẵn:</p><pre><code class='language-java'>// Sort nhân viên: lương giảm dần, lương bằng nhau thì theo tên A→Z\nlist.sort(\n    Comparator.comparing(NhanVien::getLuong).reversed()\n              .thenComparing(NhanVien::getTen)\n);</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Comparable như chiều cao — mỗi người 'tự mang' một giá trị so sánh mặc định khi xếp hàng. Comparator như người trọng tài cầm danh sách tiêu chí riêng: hôm nay xếp theo tuổi, mai theo tên — người trong hàng không phải thay đổi gì.</div><div class='qb-gotcha'><b>⚠ Hai điểm hay ăn hỏi:</b> (1) Quy ước giá trị trả về: âm = đứng trước, 0 = ngang nhau, dương = đứng sau; viết <code>a.getGia() - b.getGia()</code> kiểu trừ số int có thể <em>tràn số</em> → dùng <code>Integer.compare(a, b)</code>. (2) Luật so sánh nên <em>nhất quán với equals</em> (compareTo == 0 ⟺ equals true) — lệch nhau thì TreeSet/TreeMap (dùng compareTo để phân biệt phần tử) sẽ 'nuốt' phần tử mà HashSet coi là khác nhau.</div>",
  refs:[["Baeldung – Comparator vs Comparable","https://www.baeldung.com/java-comparator-comparable"]] },

{ id:"java-017", t:"Java 8+", lv:"junior", core:true,
  tags:["lambda","functional-interface","java8"],
  q:"Lambda là gì? Functional interface là gì? Vì sao Java cần chúng?",
  a:"<p><strong>Bài toán trước Java 8:</strong> muốn truyền 'một đoạn hành vi' cho method khác (vd truyền luật so sánh cho sort) phải bọc nó trong cả một class — anonymous class 5 dòng chỉ để chở 1 dòng logic:</p><pre><code class='language-java'>list.sort(new Comparator&lt;User&gt;() {          // Java 7: rườm rà\n    public int compare(User a, User b) {\n        return a.getTen().compareTo(b.getTen());\n    }\n});\nlist.sort((a, b) -&gt; a.getTen().compareTo(b.getTen()));   // Java 8: lambda</code></pre><p><strong>Lambda</strong> là cú pháp gọn để viết 'một khối hành vi' như một giá trị: <code>(thamSo) -&gt; bieuThuc</code>. Nó truyền được vào method, gán được vào biến — code hành vi trở thành dữ liệu.</p><p><strong>Functional interface</strong> là 'chỗ đậu' cho lambda: interface có <strong>đúng MỘT method trừu tượng</strong> (đánh dấu <code>@FunctionalInterface</code>). Vì chỉ có một method, compiler nhìn lambda là hiểu ngay nó cài method nào. Bộ có sẵn hay dùng: <code>Function&lt;T,R&gt;</code> (nhận T trả R), <code>Predicate&lt;T&gt;</code> (nhận T trả true/false — dùng trong filter), <code>Consumer&lt;T&gt;</code> (nhận T không trả gì — forEach), <code>Supplier&lt;T&gt;</code> (không nhận gì, sản xuất T).</p><p><strong>Method reference</strong> là dạng gọn thêm khi lambda chỉ gọi đúng một method có sẵn: <code>u -&gt; u.getTen()</code> viết thành <code>User::getTen</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Trước kia muốn nhờ ai làm việc gì phải viết cả bản hợp đồng lao động (class). Lambda như tờ giấy nhắn 'lấy hộ tôi cái tên' — dán thẳng vào việc cần làm. Functional interface là cái khay quy định tờ giấy nhắn dạng nào đặt vào đâu.</div><div class='qb-gotcha'><b>⚠ Chi tiết hay bị xoáy:</b> lambda dùng được biến local bên ngoài nhưng biến đó phải <em>effectively final</em> (không bị gán lại sau đó). Lý do: lambda có thể chạy sau/ở thread khác, Java <em>copy giá trị</em> biến vào lambda — cho phép biến đổi thay đổi sẽ tạo ảo giác hai bên cùng nhìn một biến.</div>",
  refs:[["Oracle – Lambda expressions","https://docs.oracle.com/javase/tutorial/java/javaOO/lambdaexpressions.html"]] },

{ id:"java-018", legacy:30, t:"Java 8+", lv:"junior", core:true,
  tags:["stream","lazy","java8"],
  q:"Vì sao <code>stream().filter().map()</code> không chạy gì cho tới khi gặp <code>collect()</code>?",
  a:"<p>Vì Stream được thiết kế <strong>lazy (lười biếng có chủ đích)</strong>. Các thao tác trên stream chia hai loại:</p><ul><li><strong>Thao tác trung gian</strong> (<code>filter</code>, <code>map</code>, <code>sorted</code>, <code>distinct</code>...): trả về một Stream khác. Gọi chúng chỉ là <em>ghi thêm một bước vào bản kế hoạch</em> — chưa có phần tử nào được xử lý.</li><li><strong>Thao tác kết thúc (terminal)</strong> (<code>collect</code>, <code>forEach</code>, <code>count</code>, <code>findFirst</code>...): phát lệnh 'chạy!'. Lúc này cả dây chuyền mới thực thi.</li></ul><p><strong>Lười để làm gì? Hai lợi ích thật:</strong></p><p>1 — <strong>Chạy một lượt, không tạo tập trung gian.</strong> Không lười thì <code>filter</code> tạo một list tạm, <code>map</code> tạo tiếp list tạm nữa. Lười thì từng phần tử <em>chảy xuyên suốt cả ống</em>: phần tử 1 qua filter → qua map → vào kết quả, rồi mới tới phần tử 2.</p><p>2 — <strong>Tắt sớm (short-circuit).</strong> Vì xử lý từng phần tử một, gặp <code>findFirst()</code> hay <code>limit(10)</code> là dừng ngay khi đủ — danh sách 1 triệu phần tử có khi chỉ đụng vài chục:</p><pre><code class='language-java'>users.stream()\n     .filter(u -&gt; u.getTuoi() &gt; 30)   // không chạy trước...\n     .map(User::getTen)                // ...không chạy trước...\n     .findFirst();                     // 'chạy!' — và dừng ngay khi có 1 kết quả</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Stream như đầu bếp nhận order: bạn dặn 'rau bỏ cọng (filter), thái nhỏ (map)' — dặn xong bếp vẫn khoanh tay. Đến khi bạn hô 'lên món cho 1 người (findFirst)' bếp mới làm, và chỉ nhặt <em>đúng lượng rau đủ một đĩa</em>, không sơ chế cả kho.</div><div class='qb-gotcha'><b>⚠ Bẫy sinh ra từ tính lười:</b> (1) Viết pipeline mà quên terminal → <em>không chạy gì cả</em>, im lặng không báo lỗi — bug ngớ ngẩn mà ai cũng dính một lần. (2) Stream đã chạy terminal là 'dùng xong vứt' — gọi tiếp lần hai nổ <code>IllegalStateException</code>. (3) <code>peek()</code> để debug cũng chỉ chạy khi có terminal.</div>",
  refs:[["Baeldung – Java Streams","https://www.baeldung.com/java-8-streams-introduction"]] },

{ id:"java-019", legacy:31, t:"Java 8+", lv:"middle", core:true,
  tags:["optional","null-safety","java8"],
  q:"<code>Optional.orElse()</code> và <code>orElseGet()</code> khác nhau ở đâu? Khi nào dùng sai gây bug?",
  a:"<p>Nhắc lại một câu về <code>Optional</code>: là cái 'hộp' có thể có hoặc không có giá trị, sinh ra để thay việc trả về null — buộc người gọi <em>nhìn thấy và xử lý</em> trường hợp rỗng thay vì quên rồi dính NullPointerException.</p><p>Hai cách lấy giá trị kèm phương án dự phòng, nhìn giống hệt nhau:</p><ul><li><strong><code>orElse(giaTriDuPhong)</code></strong> — tham số là <em>một giá trị</em>. Mà giá trị thì phải được <strong>tính xong trước khi truyền vào</strong> (luật đánh giá tham số của Java). Nghĩa là: biểu thức dự phòng <strong>LUÔN chạy</strong>, kể cả khi Optional có sẵn giá trị và bản dự phòng bị vứt đi.</li><li><strong><code>orElseGet(supplier)</code></strong> — tham số là <em>công thức tạo giá trị</em> (lambda <code>Supplier</code>). Truyền công thức thì chưa nấu — chỉ khi Optional <strong>thật sự rỗng</strong> công thức mới được chạy.</li></ul><p><strong>Bug thực tế trông thế này:</strong></p><pre><code class='language-java'>User u = timTrongCache(id)\n    .orElse(taiTuDatabase(id));     // ❌ query DB MỖI LẦN, kể cả khi cache có!\n\nUser u = timTrongCache(id)\n    .orElseGet(() -&gt; taiTuDatabase(id));  // ✅ chỉ query khi cache trượt</code></pre><p>Hệ thống vẫn chạy <em>đúng kết quả</em> — chỉ âm thầm gánh gấp đôi tải DB. Loại bug 'đúng mà chậm' này rất khó phát hiện bằng test chức năng.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> orElse như nấu sẵn nồi lẩu dự phòng cho bữa tiệc <em>dù khách đã báo có đồ ăn</em> — không ai ăn thì đổ đi. orElseGet như dặn đầu bếp 'thiếu đồ thì hẵng nấu'.</div><div class='qb-gotcha'><b>⚠ Quy tắc thực dụng:</b> dự phòng là <em>hằng số rẻ tiền</em> (<code>orElse(0)</code>, <code>orElse(\"\")</code>) — dùng orElse cho dễ đọc. Dự phòng có <em>chi phí</em> (query, gọi API, new object nặng) — bắt buộc orElseGet. Còn 'rỗng thì ném lỗi' → <code>orElseThrow()</code>.</div>",
  refs:[["Baeldung – orElse vs orElseGet","https://www.baeldung.com/java-optional-or-else-vs-or-else-get"]] },

{ id:"java-020", t:"Java 8+", lv:"middle", core:false,
  tags:["java-version","record","var","lts"],
  q:"Từ Java 8 lên 17/21 có gì mới đáng kể? (var, record, text block, sealed...)",
  a:"<p>Câu này để kiểm tra bạn có 'sống' cùng ngôn ngữ không. Nói theo cụm tính năng, kèm phiên bản chính:</p><ul><li><strong><code>var</code> (Java 10)</strong> — khai báo biến local để compiler tự suy kiểu: <code>var danhSach = new ArrayList&lt;String&gt;();</code>. Chỉ là <em>đỡ gõ</em>, vẫn kiểu tĩnh chặt chẽ, không phải dynamic như JS.</li><li><strong>Text block (Java 15)</strong> — chuỗi nhiều dòng bằng <code>\"\"\"</code>: viết JSON/SQL trong code không còn phải cộng chuỗi và escape \\\" nữa.</li><li><strong><code>record</code> (Java 16)</strong> — khai báo class-chở-dữ-liệu trong một dòng: <code>record Diem(int x, int y) {}</code> tự sinh constructor, getter, <code>equals/hashCode/toString</code> chuẩn, field final (immutable). Thay cho 50 dòng boilerplate hoặc Lombok trong nhiều chỗ — cực hợp làm DTO.</li><li><strong><code>sealed</code> (Java 17)</strong> — class/interface 'niêm phong': khai báo rõ <em>chỉ những class nào</em> được kế thừa (<code>permits</code>). Kết hợp với <strong>pattern matching cho switch (Java 21)</strong>: switch theo kiểu object, compiler kiểm tra <em>đủ hết các nhánh</em> — thiếu case là báo lỗi biên dịch.</li><li><strong>Pattern matching instanceof (Java 16)</strong> — <code>if (obj instanceof User u)</code>: check kiểu và ép kiểu gộp một, hết thời <code>(User) obj</code> lặp lại.</li><li><strong>Virtual threads (Java 21)</strong> — thread siêu nhẹ do JVM quản lý, tạo cả triệu cái được; code blocking kiểu cũ mà chịu tải như async (đáng đọc thêm nếu phỏng vấn vị trí có Java 21).</li></ul><div class='qb-term'><b>📖 Từ khó:</b> <strong>LTS</strong> (Long-Term Support) = phiên bản được hỗ trợ lâu dài, doanh nghiệp chỉ dùng các bản này: <strong>8, 11, 17, 21</strong>. Các bản giữa (9, 10, 12...) là bản 'nếm thử' 6 tháng.</div><div class='qb-eli5'><b>🌱 Mẹo trả lời:</b> đừng đọc thuộc lòng cả list — chọn 2-3 cái bạn <em>dùng thật</em> và nói nó thay đổi code hằng ngày ra sao (vd 'record giúp em bỏ hẳn Lombok cho DTO').</div><div class='qb-gotcha'><b>⚠ Bẫy:</b> <code>var</code> không dùng được cho field, tham số method, kiểu trả về — chỉ biến local có gán ngay. Và record không có setter — muốn 'đổi' phải tạo record mới (immutable là tính năng, không phải thiếu sót).</div>",
  refs:[["OpenJDK – JDK feature releases","https://openjdk.org/projects/jdk/"]] },

{ id:"java-021", legacy:32, t:"Exception & Logging", lv:"junior", core:true,
  tags:["exception","checked","unchecked"],
  q:"Checked và unchecked exception khác nhau ra sao? Khi nào nên bắt (catch), khi nào ném (throw)?",
  a:"<p>Mọi exception trong Java mọc từ cây: <code>Throwable</code> → <code>Exception</code> và <code>Error</code>. Trong nhánh Exception có một nhánh con đặc biệt: <code>RuntimeException</code>. Ranh giới checked/unchecked nằm đúng chỗ đó:</p><p><strong>Checked exception</strong> (con của Exception, <em>ngoài</em> nhánh Runtime — vd <code>IOException</code>, <code>SQLException</code>): compiler <strong>bắt buộc</strong> bạn xử lý — hoặc <code>try/catch</code>, hoặc khai báo <code>throws</code> đẩy lên trên. Dùng cho lỗi <em>lường trước được và người gọi có thể làm gì đó</em>: file không tồn tại, mạng đứt — retry, báo user, dùng phương án B.</p><p><strong>Unchecked exception</strong> (<code>RuntimeException</code> và con cháu — <code>NullPointerException</code>, <code>IllegalArgumentException</code>...): compiler không ép. Dùng cho <strong>lỗi lập trình</strong> — truyền tham số bậy, quên check null. Không ép catch vì: bug thì phải <em>sửa code</em>, không phải try/catch che đi.</p><p>(<code>Error</code> — OutOfMemoryError, StackOverflowError — là chuyện JVM hấp hối, đừng bắt.)</p><p><strong>Catch hay throw? Quy tắc vàng: chỉ bắt khi bạn XỬ LÝ ĐƯỢC gì đó có nghĩa</strong> (retry, fallback, dịch thành thông báo cho user, bổ sung ngữ cảnh rồi ném tiếp). Không xử lý được → cho nó nổi lên tầng trên, nơi có <em>global exception handler</em> (trong Spring là <code>@ControllerAdvice</code>) log đầy đủ và trả response lỗi chuẩn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Checked như thời tiết xấu — biết trước có thể xảy ra, buộc phải có kế hoạch (mang ô). Unchecked như tự vấp chân mình — không ai bắt bạn 'lên kế hoạch vấp chân', mà phải tập đi cho đàng hoàng (sửa bug).</div><div class='qb-gotcha'><b>⚠ Anti-pattern kể ra là ăn điểm:</b> (1) <em>Nuốt lỗi</em>: <code>catch (Exception e) {}</code> im re — lỗi biến mất không dấu vết, debug production như mò kim. (2) <em>Log rồi ném tiếp</em> cùng một exception → lỗi bị log đôi ba lần, loạn log. (3) Bắt <code>Exception</code> quá rộng — vơ nhầm cả bug lập trình vào nhánh 'retry'. Xu hướng hiện đại (Spring cũng vậy): nghiêng về unchecked + handler tập trung, vì checked lan <code>throws</code> khắp chữ ký method.</div>",
  refs:[["Oracle – Exceptions","https://docs.oracle.com/javase/tutorial/essential/exceptions/"]] },

{ id:"java-022", t:"Exception & Logging", lv:"junior", core:false,
  tags:["try-with-resources","autocloseable","resource-leak"],
  q:"try-with-resources là gì? Vì sao tốt hơn <code>finally</code> khi đóng resource?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Resource (tài nguyên)</strong> ở đây = thứ mở ra thì <em>phải đóng lại</em>: file, kết nối DB, socket mạng. Quên đóng = <strong>resource leak</strong>: hệ thống cạn dần file descriptor/connection rồi sập — kiểu bug 'chạy êm 3 ngày rồi chết'.</div><p><strong>Thời chưa có nó (Java 6):</strong> đóng resource trong <code>finally</code>, và đoạn code chuẩn chỉnh trông phát sợ:</p><pre><code class='language-java'>BufferedReader br = null;\ntry {\n    br = new BufferedReader(new FileReader(\"data.txt\"));\n    return br.readLine();\n} finally {\n    if (br != null) {\n        try { br.close(); } catch (IOException e) { /* lỗi lúc đóng thì làm gì đây? */ }\n    }\n}</code></pre><p><strong>try-with-resources (Java 7+):</strong> khai resource trong ngoặc tròn của try — hết block, Java <strong>tự gọi <code>close()</code></strong>, kể cả khi có exception, đóng theo thứ tự ngược lúc mở:</p><pre><code class='language-java'>try (var br = new BufferedReader(new FileReader(\"data.txt\"))) {\n    return br.readLine();\n}   // br tự đóng ở đây, mọi ngả đường thoát ra đều đóng</code></pre><p>Điều kiện duy nhất: class đó implement <code>AutoCloseable</code> — file, stream, connection, PreparedStatement... đều có sẵn.</p><p><strong>Điểm ăn tiền ít người biết — suppressed exception:</strong> nếu code trong try nổ lỗi A, và lúc đóng resource nổ thêm lỗi B: bản finally cũ lỗi B <em>đè mất</em> lỗi A (mất dấu nguyên nhân gốc); try-with-resources giữ A làm lỗi chính, đính B vào làm 'suppressed' — <code>e.getSuppressed()</code> xem được cả hai.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như phòng khách sạn có cửa tự khoá — bước ra là khoá, kể cả khi bạn chạy ra vì chuông cháy (exception). Kiểu finally cũ là tự nhớ khoá cửa tay: bình thường thì nhớ, lúc hoảng loạn là quên.</div><div class='qb-gotcha'><b>⚠ Bẫy thực chiến:</b> tạo resource <em>bên ngoài</em> try rồi mới đưa vào là mất bảo hiểm một đoạn; và với connection pool, <code>close()</code> của Connection thực chất <em>trả connection về pool</em> — quên đóng là cạn pool, app treo chờ connection: triệu chứng kinh điển của leak.</div>",
  refs:[["Oracle – try-with-resources","https://docs.oracle.com/javase/tutorial/essential/exceptions/tryResourceClose.html"]] },

{ id:"java-023", legacy:33, t:"Exception & Logging", lv:"middle", core:false,
  tags:["logging","log-level","production"],
  q:"Các mức log (TRACE→ERROR) dùng khi nào? Tuyệt đối KHÔNG log những gì?",
  a:"<p>Log level là 'độ khẩn' của thông điệp — chọn đúng mức thì lúc sự cố mở log ra đọc được, chọn sai thì log vừa ồn vừa thiếu:</p><ul><li><strong>ERROR</strong> — việc đã <em>hỏng</em> và cần người xử lý: thanh toán thất bại không tự phục hồi, gọi service phụ thuộc lỗi hết cả retry. Nguyên tắc: ERROR là thứ đáng đánh thức on-call — cái gì không đáng thì đừng gắn ERROR.</li><li><strong>WARN</strong> — bất thường nhưng hệ thống <em>vẫn chạy</em>: retry thành công ở lần 2, dữ liệu thiếu field phụ đã dùng mặc định, sắp chạm ngưỡng (pool 90%).</li><li><strong>INFO</strong> — các <em>mốc nghiệp vụ</em> đủ để kể lại câu chuyện: nhận đơn #123, xử lý xong sau 1.2s, gọi service X. Đây là mức bật ở production.</li><li><strong>DEBUG</strong> — chi tiết cho dev dò lỗi: tham số vào/ra, quyết định rẽ nhánh. Tắt ở production, bật tạm khi điều tra.</li><li><strong>TRACE</strong> — soi từng bước nhỏ nhất (từng vòng lặp, từng byte) — hiếm dùng.</li></ul><p><strong>Tuyệt đối không log (dù ở level nào):</strong> password, token/API key, số thẻ, OTP, dữ liệu cá nhân nhạy cảm (số CMND/CCCD, số tài khoản) — log là nơi rò rỉ dữ liệu kinh điển vì ai cũng đọc được và nó được ship đi khắp nơi (file, hệ thống tập trung, vendor). Cần dấu vết thì log dạng che: <code>token=eyJh...****</code>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Level như còi báo trong bệnh viện: ERROR = code blue toàn viện; WARN = gọi y tá kiểm tra; INFO = ghi sổ theo dõi định kỳ; DEBUG = máy đo gắn thêm khi nghi ngờ. Ai cũng bấm code blue cho mọi chuyện thì chẳng ai chạy nữa.</div><div class='qb-gotcha'><b>⚠ Điểm cộng phỏng vấn:</b> log kèm <strong>correlation/trace id</strong> (mã theo request xuyên các service) để lần theo một request qua cả hệ thống; log <em>có cấu trúc</em> (JSON, key=value) để máy tìm được thay vì chỉ người đọc. Và đừng log trong vòng lặp nóng — tự tay biến log thành bottleneck hiệu năng.</div>",
  refs:[["12 Factor – Logs","https://12factor.net/logs"]] },

{ id:"java-024", legacy:16, t:"Concurrency", lv:"junior", core:true,
  tags:["thread","process","runnable","callable"],
  q:"Thread và Process khác nhau ra sao? <code>Runnable</code> và <code>Callable</code> khác gì?",
  a:"<p><strong>Process (tiến trình)</strong> = một chương trình đang chạy, được hệ điều hành cấp <em>vùng nhớ riêng biệt</em>. Chrome và IntelliJ là hai process — thằng này sập không kéo thằng kia, và chúng không đọc được bộ nhớ của nhau. Muốn nói chuyện phải qua kênh 'chính thức' (file, socket, pipe) — chậm và lích kích.</p><p><strong>Thread (luồng)</strong> = một dòng thực thi <em>bên trong</em> process. Một process có nhiều thread, và điểm mấu chốt: các thread <strong>dùng chung heap</strong> (vùng nhớ chứa object). Chung nhà nên trao đổi dữ liệu tức thì, tạo thread cũng nhẹ hơn tạo process nhiều — nhưng 'chung đồ' chính là nguồn cơn của mọi đau khổ đa luồng: hai thread cùng sửa một object mà không có kỷ luật → race condition (câu sau).</p><p><strong>Runnable vs Callable</strong> — hai 'tờ mô tả công việc' giao cho thread/thread pool:</p><ul><li><code>Runnable.run()</code> — <em>không trả kết quả, không ném checked exception</em>. Việc kiểu 'chạy đi, xong thì thôi': ghi log, gửi notification.</li><li><code>Callable.call()</code> — <em>trả về giá trị</em> (<code>V</code>) và <em>ném được checked exception</em>. Nộp vào ExecutorService bạn nhận lại một <code>Future&lt;V&gt;</code> — 'phiếu hẹn lấy kết quả': <code>future.get()</code> chờ và nhận kết quả (hoặc nhận exception được gói lại).</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Process như các <em>căn hộ riêng</em> — tường ngăn, đồ ai nấy dùng. Thread như <em>người trong cùng một nhà</em> — chung tủ lạnh: tiện cực kỳ, cho tới khi hai người cùng nấu một cái nồi. Runnable như nhờ 'đổ rác hộ' (không cần hồi âm); Callable như nhờ 'mua đồ hộ' — có phiếu hẹn để nhận hàng (Future).</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối hay gặp:</b> 'Tạo thread bằng extends Thread hay implements Runnable?' — Runnable: tách <em>việc</em> khỏi <em>người chạy</em>, class còn suất extends cho việc khác, và quan trọng nhất là thực tế không ai tự new Thread rải rác — nộp việc vào <strong>thread pool</strong> (ExecutorService) để tái dùng thread và kiểm soát số lượng.</div>",
  refs:[["Baeldung – Runnable vs Callable","https://www.baeldung.com/java-runnable-callable"]] },

{ id:"java-025", legacy:17, t:"Concurrency", lv:"middle", core:true,
  tags:["race-condition","deadlock","thread-safe"],
  q:"Race condition và deadlock khác nhau thế nào? Phát hiện mỗi loại ra sao?",
  a:"<p>Hai 'bệnh' đa luồng kinh điển — một bệnh làm <em>sai</em>, một bệnh làm <em>đứng</em>:</p><p><strong>Race condition — kết quả phụ thuộc vào 'ai nhanh hơn'.</strong> Nhiều thread cùng đọc-sửa-ghi dữ liệu chung không có đồng bộ. Ví dụ gốc rễ: <code>count++</code> không phải một hành động — nó là 3 bước (đọc giá trị → cộng 1 → ghi lại). Hai thread cùng đọc được 5, cùng cộng thành 6, cùng ghi 6 → mất một lượt đếm. <em>Triệu chứng:</em> số liệu thi thoảng sai, bug 'lúc có lúc không', chạy test đơn lẻ thì không bao giờ tái hiện. <em>Phát hiện:</em> soi code tìm state chia sẻ bị ghi không bảo vệ; test tải cao; công cụ như jcstress. Nói thật lòng trong phỏng vấn: race chủ yếu phòng bằng thiết kế (ít state chia sẻ) hơn là bắt bằng tool.</p><p><strong>Deadlock — ôm khoá chờ nhau vòng tròn.</strong> Thread 1 giữ khoá A chờ khoá B; thread 2 giữ B chờ A — cả hai chờ nhau đến vô tận. <em>Triệu chứng:</em> một nhóm chức năng đứng im, CPU lại thảnh thơi (không ai chạy, toàn ngồi chờ). <em>Phát hiện:</em> chộp <strong>thread dump</strong> (<code>jstack &lt;pid&gt;</code>) — JVM in hẳn dòng <code>Found one Java-level deadlock</code> kèm thread nào giữ khoá nào chờ khoá nào.</p><p><strong>Phòng deadlock</strong> gói gọn một câu: <em>mọi nơi lấy các khoá theo CÙNG một thứ tự</em> (luôn A trước B); kèm dùng <code>tryLock</code> có timeout thay vì chờ vô hạn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Race condition = hai người cùng lúc sửa một file Word bản chung mà không khoá — bản lưu cuối đè công người kia. Deadlock = hai người qua cầu hẹp ngược chiều, ai cũng đã bước lên và nhất quyết không lùi.</div><div class='qb-gotcha'><b>⚠ Phân biệt cho sắc:</b> race condition <em>chạy vẫn xong</em> nhưng kết quả sai; deadlock <em>không bao giờ xong</em>. Câu bẫy 'chương trình đứng im nhưng CPU 100%?' — đó thường là <strong>livelock</strong> (hai bên cứ nhường qua nhường lại) hoặc vòng lặp spin, không phải deadlock cổ điển.</div>",
  refs:[["Baeldung – Deadlock","https://www.baeldung.com/java-deadlock-livelock"]] },

{ id:"java-026", legacy:18, t:"Concurrency", lv:"middle", core:true,
  tags:["volatile","synchronized","atomic","visibility"],
  q:"<code>volatile</code>, <code>synchronized</code> và <code>AtomicInteger</code> — chọn cái nào khi nào?",
  a:"<p>Trước khi so, phải tách bạch <strong>hai vấn đề khác nhau</strong> của đa luồng:</p><ul><li><strong>Visibility (nhìn thấy):</strong> thread ghi xong một biến, thread khác <em>có thấy giá trị mới không?</em> Không chắc! — mỗi CPU core có cache riêng, giá trị có thể nằm ở cache của core này mà core kia chưa hay biết.</li><li><strong>Atomicity (trọn vẹn):</strong> thao tác có <em>không thể bị chen ngang</em> không? <code>count++</code> là 3 bước — bị chen giữa là sai (race).</li></ul><p>Giờ xếp ba công cụ theo hai tiêu chí đó:</p><ul><li><strong><code>volatile</code></strong> — chỉ giải quyết <strong>visibility</strong>: ghi là mọi thread thấy ngay, cấm cache 'ỉm' giá trị. KHÔNG cho atomicity — <code>volatile int count; count++</code> vẫn sai như thường. Chỗ dùng chuẩn: <em>cờ trạng thái</em> một-người-ghi-nhiều-người-đọc: <code>volatile boolean running; while (running) {...}</code>.</li><li><strong><code>AtomicInteger</code>/AtomicLong/AtomicReference</strong> — visibility + <strong>atomicity cho MỘT biến</strong>: <code>incrementAndGet()</code> là một hành động trọn vẹn, cài bằng lệnh CPU compare-and-swap (CAS) <em>không cần khoá</em> → nhanh dưới tranh chấp vừa phải. Chỗ dùng: counter, đổi reference nguyên tử.</li><li><strong><code>synchronized</code></strong> — nặng đô nhất: visibility + atomicity cho <strong>cả một khối lệnh nhiều bước trên nhiều biến</strong>. 'Chuyển tiền: trừ A, cộng B' phải là một khối không ai chen — chỉ khoá làm được. Trả giá: thread khác phải xếp hàng.</li></ul><p><strong>Phản xạ chọn:</strong> cờ bật/tắt → volatile. Đếm/cập nhật một biến → Atomic. Bất biến nghiệp vụ trải trên nhiều bước/nhiều biến → synchronized (hoặc ReentrantLock khi cần timeout/fairness).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> volatile = bảng tin ở sảnh: ai dán, mọi người <em>thấy ngay</em> — nhưng hai người cùng dán đè nhau thì bảng không cứu. Atomic = máy phát số thứ tự: bấm là ra số duy nhất, không cần bảo vệ đứng canh. synchronized = phòng họp một chìa khoá: vào làm trọn bộ việc rồi mới trả chìa.</div><div class='qb-gotcha'><b>⚠ Câu bẫy kinh điển:</b> 'volatile có làm count++ đúng không?' — KHÔNG (thiếu atomicity). 'Vậy Atomic thay được synchronized không?' — chỉ khi bất biến gói gọn trong <em>một</em> biến; check-rồi-ghi hai biến trở lên là phải khoá. Trả lời được cặp này là qua bài.</div>",
  refs:[["Baeldung – volatile","https://www.baeldung.com/java-volatile"],["Jenkov – Java Memory Model","https://jenkov.com/tutorials/java-concurrency/java-memory-model.html"]] },

{ id:"java-027", legacy:19, t:"Concurrency", lv:"middle", core:true,
  tags:["threadpool","executor","oom","queue"],
  q:"<code>ThreadPoolExecutor</code> tạo thread mới khi nào? Vì sao <code>newFixedThreadPool</code> có thể gây OOM?",
  a:"<p><strong>Thread pool là gì đã:</strong> tạo thread đắt (mỗi cái ngốn ~1MB stack + công OS quản lý), nên thay vì mỗi việc new một thread, ta nuôi sẵn một 'đội thợ' tái sử dụng. <code>ThreadPoolExecutor</code> có 3 tham số định hình: <code>corePoolSize</code> (quân số thường trực), <code>maximumPoolSize</code> (trần quân số), và <code>workQueue</code> (hàng đợi việc chờ).</p><p><strong>Thứ tự quyết định khi có task mới — điểm hay bị hỏi vặn, và nó KHÔNG như trực giác:</strong></p><ol><li>Quân số hiện tại &lt; <code>corePoolSize</code> → <strong>tạo thread mới</strong> làm luôn.</li><li>Đủ quân thường trực rồi → <strong>xếp task vào queue</strong> — CHƯA tăng quân!</li><li>Queue <em>đầy</em> rồi mà quân &lt; <code>maximumPoolSize</code> → lúc này mới tạo thêm thread 'thời vụ'.</li><li>Queue đầy + đã kịch trần max → <strong>từ chối</strong>, xử theo <code>RejectedExecutionHandler</code> (mặc định ném <code>RejectedExecutionException</code>).</li></ol><p><strong>Vì sao <code>Executors.newFixedThreadPool(10)</code> gây OOM được?</strong> Nhìn ruột nó: core = max = 10, còn queue là <code>LinkedBlockingQueue</code> <strong>KHÔNG GIỚI HẠN</strong>. Theo luật trên, bước 3 không bao giờ xảy ra (queue không bao giờ 'đầy') — nghĩa là khi task đến nhanh hơn 10 thợ làm kịp, queue cứ phình vô hạn, mỗi task chờ chiếm một mẩu heap → <strong>OutOfMemoryError</strong> chết vì 'no bụng việc chờ'. <code>newCachedThreadPool</code> hỏng kiểu ngược lại: max = <em>vô hạn thread</em>.</p><p><strong>Bài học production:</strong> tự dựng <code>ThreadPoolExecutor</code> với queue <em>có giới hạn</em> + chính sách từ chối có chủ đích (vd <code>CallerRunsPolicy</code> — 'hết chỗ thì người nộp việc tự làm', tự nhiên thành phanh giảm tốc).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Quán phở 10 nhân viên (core). Khách đông hơn thì <em>phát số cho xếp hàng</em> (queue) chứ chưa tuyển thêm; hàng dài chạm vạch mới gọi thời vụ (max); vạch cũng tràn thì treo biển 'hết nhận khách' (reject). newFixedThreadPool = quán cho xếp hàng <em>dài vô tận</em> — khách chờ tràn ra chật cứng cả phố (OOM).</div><div class='qb-gotcha'><b>⚠ Câu đo hiểu sâu:</b> 'core đủ 10, queue còn trống, task mới đến — có tạo thread thứ 11 không?' → KHÔNG, vào queue trước; thread 11 chỉ xuất hiện khi queue ĐẦY. Ai trả lời 'tạo tới max rồi mới queue' là chưa từng đọc ruột TPE.</div>",
  refs:[["ThreadPoolExecutor Javadoc","https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/concurrent/ThreadPoolExecutor.html"]] },

{ id:"java-028", legacy:20, t:"Concurrency", lv:"senior", core:false,
  tags:["completablefuture","async","timeout","fallback"],
  q:"Gọi 3 service song song bằng <code>CompletableFuture</code>, 1 cái fail không được kéo sập cả request. Làm sao? <code>thenApply</code> vs <code>thenApplyAsync</code>?",
  a:"<p><strong>Bài toán:</strong> trang chi tiết sản phẩm cần gọi 3 nguồn (giá, tồn kho, đánh giá). Gọi tuần tự = cộng dồn độ trễ; gọi song song nhưng 'buộc chung số phận' = một nguồn phụ chết kéo sập cả trang. Lời giải: <strong>mỗi future tự lo timeout và phương án B của mình</strong> trước khi gom:</p><pre><code class='language-java'>CompletableFuture&lt;Gia&gt; giaF = CompletableFuture\n    .supplyAsync(() -&gt; giaClient.lay(id), pool)\n    .orTimeout(800, TimeUnit.MILLISECONDS)          // quá giờ → tự fail\n    .exceptionally(ex -&gt; { log.warn(...); return Gia.macDinh(); }); // fail → giá trị dự phòng\n// tonKhoF, danhGiaF tương tự — mỗi cái một hạn giờ, một fallback riêng\n\nCompletableFuture.allOf(giaF, tonKhoF, danhGiaF).join();  // chờ cả 3\n// vì từng cái đã 'tự đỡ' exception, allOf không bao giờ nổ vì lỗi lẻ\nreturn new TrangSanPham(giaF.join(), tonKhoF.join(), danhGiaF.join());</code></pre><p>Ý then chốt: <code>exceptionally</code> đặt <em>trước</em> điểm gom biến lỗi thành giá trị lành — 'sập cục bộ, không sập toàn cục' (graceful degradation: trang vẫn hiện, chỗ đánh giá tạm trống).</p><p><strong><code>thenApply</code> vs <code>thenApplyAsync</code>:</strong> cùng là 'xong bước trước thì biến đổi kết quả', khác ở <em>ai chạy đoạn tiếp theo</em>. <code>thenApply</code>: chạy trên thread vừa hoàn thành bước trước (đôi khi là chính thread gọi) — gọn cho biến đổi nhẹ; nhưng nếu hàm nặng, bạn đang <em>chiếm dụng</em> thread của pool async/IO. <code>thenApplyAsync(fn, pool)</code>: bàn giao sang pool chỉ định — cách ly việc nặng, kiểm soát được tài nguyên. Quy tắc: biến đổi rẻ → thenApply; nặng/blocking → thenApplyAsync vào pool riêng.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Ba người đi chợ ba món cho bữa tiệc, mỗi người được dặn riêng: 'quá 15 phút hoặc hết hàng thì mua đồ thay thế rồi VẪN VỀ'. Nhờ vậy bữa tiệc luôn diễn ra — thiếu món xịn thì có món thay, không bao giờ cả bàn ngồi đói chờ một người lạc chợ.</div><div class='qb-gotcha'><b>⚠ Bẫy senior thật sự:</b> (1) Quên chỉ định pool → mặc định dùng <code>ForkJoinPool.commonPool()</code> chung của cả JVM — task blocking IO ném vào đó là bóp cổ cả app (parallel stream cũng dùng pool này!). (2) <code>orTimeout</code> chỉ làm future fail — <em>không huỷ</em> HTTP call bên dưới; timeout thật phải đặt cả ở client (connect/read timeout).</div>",
  refs:[["Baeldung – CompletableFuture","https://www.baeldung.com/java-completablefuture"]] },

{ id:"java-029", legacy:21, t:"Concurrency", lv:"senior", core:true,
  tags:["distributed-lock","synchronized","scale-out","idempotency"],
  q:"Vì sao <code>synchronized</code> KHÔNG đủ để chống xử lý trùng (double-processing) trong production?",
  a:"<p>Câu này kiểm tra bạn có nhìn ra khỏi một máy tính không. Trả lời lõi: <strong><code>synchronized</code> là khoá của MỘT JVM</strong> — nó chặn các thread <em>trong cùng process</em> chen nhau. Nhưng production không chạy một process:</p><ul><li>App được scale ngang: 3 instance sau load balancer. Hai request 'xử lý đơn #42' rơi vào <strong>instance A và instance B</strong> — khoá trong A chỉ các thread của A biết; B vô tư chạy song song → xử lý trùng như thường.</li><li>Chưa kể deploy rolling: bản cũ và mới chạy song song một lúc; hoặc consumer Kafka bị rebalance, message giao lại lần nữa cho máy khác.</li></ul><p><strong>Chống trùng xuyên máy phải dùng thứ mà MỌI instance cùng nhìn thấy:</strong></p><ul><li><strong>Khoá phân tán (distributed lock):</strong> chốt gác đặt ở hệ thống chung — Redis (<code>SET key value NX PX 30000</code> — chỉ một người set được, tự hết hạn), hoặc khoá của DB (<code>SELECT ... FOR UPDATE</code> trên dòng đơn hàng).</li><li><strong>Ràng buộc duy nhất trong DB (unique constraint):</strong> tầng phòng thủ cuối và đáng tin nhất — bảng <code>don_hang_da_xu_ly(order_id UNIQUE)</code>: kẻ đến sau insert trùng bị DB từ chối, transaction rollback. DB làm trọng tài, không cần tin tưởng app.</li><li><strong>Idempotency key</strong> ở tầng API cho request retry (đã nói ở câu HTTP method).</li></ul><p>Thực chiến thường <em>xếp lớp</em>: khoá phân tán để đỡ tốn công tính toán trùng + unique constraint để bảo đảm tuyệt đối.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> synchronized như quy định 'ai lấy hồ sơ phải ghi sổ' <em>của một chi nhánh</em> — hai chi nhánh khác nhau thì sổ ai nấy ghi, vẫn cùng xử lý một hồ sơ. Muốn chặn toàn hệ thống phải có <em>sổ trung tâm</em> (Redis/DB) mà mọi chi nhánh đều phải ghi trước khi làm.</div><div class='qb-gotcha'><b>⚠ Hỏi xoáy tiếp theo:</b> 'khoá Redis hết hạn giữa chừng khi job chạy lâu thì sao?' — người khác lấy được khoá, lại trùng! Đáp án các mức: TTL dài hợp lý + gia hạn định kỳ (watchdog như Redisson), token fencing, và chốt chặn cuối vẫn là unique constraint. Thừa nhận 'khoá phân tán không tuyệt đối, cần idempotent ở tầng ghi' là câu trả lời trưởng thành nhất.</div>",
  refs:[["Redis – Distributed locks","https://redis.io/docs/latest/develop/use/patterns/distributed-locks/"],["Kleppmann – How to do distributed locking","https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html"]] },

{ id:"java-030", t:"JVM & Memory", lv:"junior", core:true,
  tags:["gc","garbage-collection","heap"],
  q:"Garbage Collection là gì? Khi nào một object 'đủ điều kiện' bị dọn? Vì sao GC thỉnh thoảng làm app khựng?",
  a:"<p><strong>GC (Garbage Collection — dọn rác tự động)</strong>: trong C/C++, xin bộ nhớ xong bạn phải tự trả (<code>free</code>) — quên trả là leak, trả hai lần là crash. Java bỏ gánh đó: bạn cứ <code>new</code>, JVM có một 'đội dọn dẹp' định kỳ tìm và thu hồi object không còn ai dùng.</p><p><strong>'Không còn ai dùng' xác định thế nào?</strong> Không phải đếm số reference (đếm kiểu đó bị lừa bởi 2 object trỏ vòng vào nhau). JVM đi từ <strong>GC roots</strong> — các điểm chắc chắn đang sống: biến local trên stack các thread, biến static, thread đang chạy... — rồi lần theo mọi reference như lần theo rễ cây. Object nào <em>không có đường đi nào từ roots tới</em> = unreachable = rác, dù nó vẫn trỏ lằng nhằng vào nhau.</p><p><strong>Vì sao chia thế hệ (generational)?</strong> Quan sát thực nghiệm: <em>đa số object chết trẻ</em> (biến tạm trong request, String trung gian). Nên heap chia <strong>Young Generation</strong> (trẻ — Eden + 2 Survivor) và <strong>Old Generation</strong> (già). Object sinh ở Eden; Minor GC quét Young thường xuyên — nhanh vì hầu hết đã chết, chỉ copy vài kẻ sống sót; sống dai qua nhiều đợt thì được 'thăng cấp' lên Old — nơi quét ít hơn nhưng mỗi lần nặng hơn (Major/Full GC).</p><p><strong>Vì sao app khựng?</strong> Một số pha GC phải <strong>stop-the-world</strong>: tạm dừng TẤT CẢ thread ứng dụng để đội dọn làm việc với bức ảnh bộ nhớ đứng yên. Pause vài ms không sao; Full GC trên heap to có thể đứng cả giây → request đúng lúc đó bị đội latency vọt lên (nhìn trên biểu đồ p99 như răng cưa). GC hiện đại (G1 mặc định, ZGC) chủ yếu là cuộc chiến giảm thời gian đứng hình này.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> GC như dọn nhà hàng cuối ca: bàn nào <em>không còn lần theo được từ danh sách khách đang ngồi</em> (GC roots) là dọn. Khu bàn khách vãng lai (Young) dọn liên tục và nhanh; khu khách ruột ngồi lì (Old) lâu lâu tổng vệ sinh một lần — và lúc tổng vệ sinh thì nhà hàng đóng cửa tạm (stop-the-world).</div><div class='qb-gotcha'><b>⚠ Bẫy hay gặp:</b> (1) <code>System.gc()</code> chỉ là <em>lời gợi ý</em>, JVM có quyền lờ — và gọi nó trong code production là red flag. (2) 'Có GC thì Java hết memory leak?' — KHÔNG: object vẫn <em>reachable</em> mà bạn quên (bỏ vào static Map không bao giờ remove, listener không huỷ đăng ký) thì GC bó tay — xem câu điều tra leak.</div>",
  refs:[["Oracle – GC tuning guide","https://docs.oracle.com/en/java/javase/17/gctuning/introduction-garbage-collection-tuning.html"]] },

{ id:"java-031", legacy:28, t:"JVM & Memory", lv:"middle", core:true,
  tags:["jvm","heap","stack","metaspace"],
  q:"JVM chia bộ nhớ thành những vùng nào? Heap, Stack, Metaspace lưu gì?",
  a:"<p>Ba vùng chính, mỗi vùng một vai và một kiểu lỗi đặc trưng:</p><p><strong>Heap — nhà kho object.</strong> Mọi thứ bạn <code>new</code> đều nằm đây, <em>các thread dùng chung</em>. Đây là địa bàn của GC (chia Young/Old như câu GC). Kích thước chỉnh bằng <code>-Xmx</code> (max) / <code>-Xms</code> (khởi điểm). Hết chỗ mà GC dọn không ra → <code>OutOfMemoryError: Java heap space</code>.</p><p><strong>Stack — sổ tay riêng của TỪNG thread.</strong> Mỗi lần gọi method, thread chồng thêm một <strong>stack frame</strong> chứa: biến local, tham số, vị trí đang chạy. Method return thì frame bị gỡ — sạch sẽ tự nhiên, không cần GC. Lưu ý hay nhầm: biến local kiểu object chỉ giữ <em>reference</em> trên stack, còn object thật vẫn ở heap. Đệ quy không đáy → chồng frame vô tận → <code>StackOverflowError</code>.</p><p><strong>Metaspace — hồ sơ về các class.</strong> Lưu <em>mô tả</em> class (bytecode method, tên field, constant pool...) — không phải object instance. Từ Java 8 nó nằm ở native memory (thay PermGen cũ), tự giãn theo nhu cầu. Phình bất thường thường do lỗi classloader leak (deploy nóng nhiều lần, sinh class động vô tội vạ) → <code>OutOfMemoryError: Metaspace</code>.</p><p>(Kể thêm nếu được hỏi: mỗi thread còn có PC register; native memory cho JIT code cache, direct buffer NIO — ngoài heap nên 'RAM process cao hơn -Xmx' là bình thường.)</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Heap = <em>nhà kho chung</em> cả công ty gửi đồ, có đội dọn kho (GC). Stack = <em>xấp giấy nhớ cá nhân</em> của từng nhân viên — ghi việc đang làm dở, xong việc xé tờ trên cùng. Metaspace = <em>tủ hồ sơ bản thiết kế</em>: mô tả 'sản phẩm loại X gồm những gì' — bản thiết kế một tờ, sản phẩm đóng cả nghìn thùng trong kho.</div><div class='qb-gotcha'><b>⚠ Câu bẫy phân vùng lỗi:</b> OOM heap space = object sống nhiều quá (leak/heap bé); StackOverflow = gọi method lồng sâu quá (đệ quy hỏng); OOM Metaspace = load class nhiều quá. Nghe triệu chứng đoán đúng vùng là kỹ năng debug thật — và nhớ: String literal/pool nằm TRONG heap (từ Java 7), không phải Metaspace.</div>",
  refs:[["Baeldung – Stack vs Heap","https://www.baeldung.com/java-stack-heap"],["Baeldung – Metaspace","https://www.baeldung.com/java-permgen-metaspace"]] },

{ id:"java-032", legacy:29, t:"JVM & Memory", lv:"senior", core:false,
  tags:["memory-leak","heap-dump","mat","debug"],
  q:"Service bị nghi memory leak, RAM tăng dần rồi OOM. Bạn điều tra thế nào?",
  a:"<p>Nguyên tắc số 1: <strong>đi theo bằng chứng, không đoán mò sửa bừa.</strong> Quy trình bài bản:</p><ol><li><strong>Xác nhận có leak thật không:</strong> nhìn biểu đồ heap sau mỗi lần Full GC (qua Grafana/JMX). Heap sau GC <em>cứ cao dần theo thời gian</em> = leak thật. Còn 'RAM process cao' đơn thuần có khi chỉ là heap chưa trả OS hoặc native memory — chưa chắc leak.</li><li><strong>Chộp heap dump — bức ảnh chụp toàn bộ object:</strong> bật sẵn <code>-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=...</code> (chụp đúng lúc chết — quý nhất), hoặc chủ động <code>jmap -dump:live,file=heap.hprof &lt;pid&gt;</code> hai lần cách nhau vài giờ để so sánh.</li><li><strong>Mổ dump bằng Eclipse MAT</strong> (hoặc VisualVM): mở báo cáo <em>Leak Suspects</em>, nhìn <strong>dominator tree</strong> — 'ai đang ghim giữ nhiều bộ nhớ nhất'. Thủ phạm quen mặt: static <code>Map</code>/cache tự chế <em>không có chính sách xoá</em>, collection cứ add không remove, listener/callback đăng ký mà quên huỷ, ThreadLocal không <code>remove()</code> trên thread pool (thread sống mãi → giá trị bám mãi), cache không giới hạn size.</li><li><strong>Đọc đường tham chiếu (path to GC roots)</strong> của nhóm object phình to → thấy chính xác <em>chuỗi reference nào</em> đang giữ chúng sống → về đúng dòng code đó mà sửa (bỏ khỏi map, weak reference, cache có TTL/max size).</li><li><strong>Vá xong phải chứng minh:</strong> chạy tải lại, heap sau GC phẳng là thắng.</li></ol><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Nhà kho cứ đầy dần dù hàng bán đều — kiểm kê chụp ảnh toàn kho (heap dump), xem <em>khu nào chiếm diện tích nhất</em> (dominator tree), lần ra 'ai đứng tên lô hàng đó' (path to GC roots) — hoá ra một phòng ban giữ hàng mẫu từ 2019 không chịu thanh lý (static cache).</div><div class='qb-gotcha'><b>⚠ Chi tiết ghi điểm:</b> (1) <code>jmap -dump:live</code> tự chạy Full GC trước khi chụp — chỉ còn object sống thật, nhưng gây pause: cân nhắc trên node production (chụp node đã tách khỏi LB). (2) Dump có thể chứa dữ liệu nhạy cảm (password trong char[], PII trong object) — bảo mật file dump như bảo mật DB.</div>",
  refs:[["Baeldung – Find memory leaks","https://www.baeldung.com/java-memory-leaks"],["Eclipse MAT","https://eclipse.dev/mat/"]] },

{ id:"java-033", legacy:22, t:"JVM & Memory", lv:"senior", core:true,
  tags:["happens-before","memory-model","visibility","volatile"],
  q:"Thread A set <code>transactionId</code> rồi set cờ <code>isDone=true</code>. Thread B thấy <code>isDone==true</code> nhưng <code>transactionId==null</code>. Vì sao có thể xảy ra?",
  a:"<p>Câu này nghe vô lý với người mới — 'rõ ràng gán transactionId TRƯỚC mà?' — và chính cái vô lý đó là bài học về <strong>Java Memory Model</strong>.</p><p><strong>Sự thật nền tảng:</strong> thứ tự bạn viết code chỉ được cam kết <em>trong nội bộ một thread</em>. Giữa các thread, không có cam kết nào cả trừ khi bạn tạo ra quan hệ <strong>happens-before</strong> — 'mọi ghi chép trước điểm X của thread này, chắc chắn hiện ra với thread kia sau điểm Y'. Không có nó, hai điều 'ma quái' được PHÉP xảy ra:</p><ul><li><strong>Reordering:</strong> compiler JIT và CPU được quyền <em>đảo thứ tự lệnh</em> miễn kết quả trong một thread không đổi — với thread A đơn lẻ, gán cờ trước hay sau chẳng khác gì, nên nó đảo để tối ưu. Thế là ghi <code>isDone=true</code> 'lộ ra' trước khi ghi <code>transactionId</code>.</li><li><strong>Cache CPU:</strong> mỗi core có cache riêng; A ghi vào cache của core mình, B đọc từ cache core khác — thấy cờ mới mà giá trị kia còn bản cũ (null).</li></ul><p><strong>Sửa thế nào — tạo happens-before:</strong> cách gọn nhất cho pattern cờ-báo-hiệu: khai <code>volatile boolean isDone</code>. Luật của volatile: <em>mọi thứ ghi TRƯỚC khi ghi biến volatile</em> sẽ hiện ra đầy đủ với thread nào <em>đọc thấy giá trị volatile mới đó</em> — tức việc gán transactionId được 'kéo theo' cùng gói hàng. Các cách khác cùng tạo happens-before: cùng khoá <code>synchronized</code>, <code>Thread.start()/join()</code>, đặt kết quả qua <code>Future</code>/BlockingQueue/CountDownLatch (các công cụ này đã cài sẵn happens-before cho bạn).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> A dán thông báo 'hồ sơ xong rồi' lên bảng tin, nhưng tờ hồ sơ thì gửi qua đường bưu điện nội bộ — B đọc bảng tin (thấy 'xong') chạy tới ngăn tủ thì hồ sơ chưa tới. volatile giống quy định: 'muốn dán thông báo lên bảng này, phải bỏ đủ hồ sơ vào tủ TRƯỚC, và ai đọc thông báo sẽ thấy đủ tủ'.</div><div class='qb-gotcha'><b>⚠ Nâng độ khó quen thuộc:</b> interviewer sẽ hỏi tiếp 'thay volatile bằng synchronized quanh cả ghi lẫn đọc được không?' — được (khoá tạo happens-before + loại trừ lẫn nhau), nhưng nặng hơn mức cần cho một cờ báo. Và nhớ nói: bug loại này <em>gần như không tái hiện được trong test</em> — nó xuất hiện 1/triệu lần dưới tải — nên phải đúng từ thiết kế, không debug bằng may mắn được.</div>",
  refs:[["JLS 17 – Memory Model","https://docs.oracle.com/javase/specs/jls/se17/html/jls-17.html#jls-17.4"],["Jenkov – happens-before","https://jenkov.com/tutorials/java-concurrency/java-happens-before-guarantee.html"]] },

{ id:"java-034", legacy:23, t:"JVM & Memory", lv:"senior", core:false,
  tags:["singleton","double-checked-locking","volatile","reordering"],
  q:"Double-checked locking để tạo singleton vì sao BẮT BUỘC dùng <code>volatile</code>?",
  a:"<p>Nhắc pattern đã: singleton khởi tạo lười (lazy) trong đa luồng, muốn tránh trả phí khoá mỗi lần gọi nên check hai lớp:</p><pre><code class='language-java'>class Config {\n    private static volatile Config instance;   // ← chữ volatile này là sống còn\n    static Config getInstance() {\n        if (instance == null) {                 // check 1: không khoá — đường nhanh\n            synchronized (Config.class) {\n                if (instance == null) {         // check 2: có khoá — chống lọt khe\n                    instance = new Config();\n                }\n            }\n        }\n        return instance;\n    }\n}</code></pre><p><strong>Điểm chết nằm ở dòng <code>instance = new Config()</code></strong> — nó không phải một hành động, mà là BA: (1) cấp phát vùng nhớ, (2) chạy constructor điền dữ liệu, (3) gán địa chỉ vào <code>instance</code>. JIT/CPU được phép <strong>đảo (2) và (3)</strong> — với thread đang làm thì đảo vậy vô hại. Nhưng thread khác thì thảm hoạ: nó chạy vào <em>check 1 — vốn KHÔNG có khoá</em> — thấy <code>instance != null</code> (bước 3 xong rồi) và dùng ngay một object <strong>constructor chưa chạy xong</strong>: field null, config rỗng. Bug xuất hiện 1/triệu lần, không tái hiện nổi.</p><p><strong><code>volatile</code> vá đúng lỗ này:</strong> ghi volatile cấm reorder kiểu đó — mọi việc khởi tạo phải xong xuôi <em>trước khi</em> phép gán được nhìn thấy; ai đọc thấy <code>instance != null</code> là chắc chắn nhận object hoàn chỉnh (happens-before).</p><p><strong>Nhưng câu trả lời trưởng thành là: đừng tự viết DCL.</strong> Hai cách sạch hơn: <strong>holder class idiom</strong> — <code>static class Holder { static final Config I = new Config(); }</code> — JVM bảo đảm class load lười + thread-safe sẵn, không khoá không volatile; hoặc <strong>enum singleton</strong>. Trong dự án Spring: để container quản bean singleton, tự viết làm gì.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như treo biển 'QUÁN ĐÃ MỞ' trước khi bày xong bàn ghế — khách check-1 (nhìn biển từ xa, không hỏi ai) thấy biển là xông vào ngồi... giữa quán trống. volatile = luật 'bày xong hết mới được treo biển'.</div><div class='qb-gotcha'><b>⚠ Chốt hạ trong phỏng vấn:</b> nhớ nói được 'vì check 1 đọc <em>ngoài</em> khoá nên mới cần volatile' — nếu mọi truy cập đều trong synchronized thì tự khoá đã đủ. Hiểu đúng chỗ này chứng minh bạn hiểu cả hai công cụ, không phải rắc volatile như rắc muối cầu may.</div>",
  refs:[["Baeldung – DCL Singleton","https://www.baeldung.com/java-singleton-double-checked-locking"],["Shipilev – Safe publication","https://shipilev.net/blog/2014/safe-public-construction/"]] },

/* DATA_END */
] });
