/* INCIDENTS — Consumer lag & GC pressure. Schema: xem i1. */

INCIDENTS.register({
  id: "inc-consumer-lag", sev: "P2", domain: "Messaging / Kafka", skills: ["msg-kafka", "resil-perf"], time: "15–20 phút",
  title: "Consumer lag tăng đều từ sáng — thêm consumer không ăn thua",
  teaser: "Sau release 'enrich thêm địa chỉ giao hàng', lag cứ leo dốc tuyến tính. Đội đã thêm consumer thứ 7, thứ 8 — vô dụng. Bảng lag biết tại sao.",
  alert: "<b>[Grafana]</b> 10:30 — consumer group <code>order-events-processor</code> tổng lag <b>310.000</b> message và tăng ~25k/giờ, bắt đầu từ sau release 08:00 (thêm bước enrich địa chỉ giao hàng vào handler). Downstream (dashboard vận đơn) trễ ~50 phút so với thực tế.",
  artifacts: [
    { label: "📊 kafka-consumer-groups --describe", body: "<pre>GROUP                   TOPIC        PARTITION  LAG      CONSUMER-ID\norder-events-processor  order-events  0          51.8k    consumer-1\norder-events-processor  order-events  1          52.1k    consumer-2\norder-events-processor  order-events  2          51.4k    consumer-3\norder-events-processor  order-events  3          52.0k    consumer-4\norder-events-processor  order-events  4          51.2k    consumer-5\norder-events-processor  order-events  5          51.5k    consumer-6\n<span class='hl'>consumer-7, consumer-8: (không được gán partition nào — idle)</span>\n\nProduce rate:  <span class='hl'>~600 msg/s</span> (ổn định, như mọi ngày)\nConsume rate:  <span class='hl'>~190 msg/s</span> (trước release: ~650 msg/s)</pre><p style='font-size:12.5px;color:var(--text-muted)'>Lag ĐỀU trên cả 6 partition — không có partition nào 'nóng' hơn hẳn.</p>" },
    { label: "📄 Handler sau release", body: "<pre>@KafkaListener(topics = \"order-events\", concurrency = \"6\")\npublic void handle(OrderEvent evt) {\n    <span class='hl'>AddressInfo addr = addressClient.lookup(evt.getUserId());   // MỚI: HTTP sync ~25-30ms</span>\n    ShipmentView view = buildView(evt, addr);\n    <span class='hl'>viewRepo.save(view);                                         // INSERT từng dòng ~5ms</span>\n}\n// Trước release: handler chỉ buildView + save, tổng ~4ms/message</pre>" },
    { label: "📈 Metrics handler", body: "<pre>Thời gian xử lý MỖI message (p50):\n  trước release 08:00:   ▂ 4 ms\n  sau release:           <span class='hl'>█ 33 ms</span>  (25ms addressClient + 5ms save + 3ms build)\n\nRebalance log: 2 lần (đúng lúc thêm consumer 7, 8) — sau đó ổn định\nGC / CPU / DB: bình thường · addressClient p99: 45ms (khoẻ, đúng SLA của nó)</pre>" }
  ],
  steps: [
    { q: "Đọc bảng lag: lag ĐỀU trên 6 partition, consumer 7-8 idle, consume rate 190/s &lt; produce rate 600/s. Kết luận đúng nhất?",
      opts: [
        { t: "Nút thắt là THROUGHPUT CỦA HANDLER: 6 consumer × ~30 msg/s (33ms/message) ≈ 190 msg/s — thấp hơn produce rate nên lag tăng vô hạn; thêm consumer quá số partition là thêm người đứng nhìn", s: 2, why: "Số học vận hành cơ bản mà rất ít người chịu làm: throughput trần = số partition × (1000/thời-gian-xử-lý-1-message). Lag ĐỀU mọi partition loại trừ hot key; consumer idle xác nhận trần partition. Mọi dữ kiện chụm về một chỗ: chi phí mỗi message tăng 8 lần sau release." },
        { t: "Cần tăng số partition từ 6 lên 24 ngay để nhận thêm consumer", s: 1, why: "Đúng cơ chế (partition là trần song song) nhưng SAI THỨ TỰ: chưa trả lời 'vì sao mỗi message đắt gấp 8 lần'. Tăng partition là phẫu thuật có hệ quả (không giảm lại được, ảnh hưởng ordering theo key, rebalance) — làm SAU khi đã tối ưu chi phí mỗi message, nếu vẫn thiếu." },
        { t: "Kafka broker chậm, cần thêm broker", s: 0, why: "Produce rate vẫn 600/s ổn định (broker nhận ngon lành), fetch của consumer không báo lỗi/timeout. Lag do consumer XỬ LÝ chậm chứ không phải do broker GIAO chậm — hai việc khác nhau, bằng chứng đã phân xử." },
        { t: "Rebalance liên tục làm consumer group mất ổn định", s: 0, why: "Log ghi đúng 2 lần rebalance — trùng thời điểm đội THÊM consumer (hành vi đúng), sau đó ổn định. Rebalance storm thật sẽ hiện hàng chục dòng revoke/assign lặp lại. Đọc log trước khi gắn nhãn hội chứng." }
      ] },
    { q: "Xác nhận nghi phạm: chi phí 33ms/message đến từ đâu và vì sao nó giết throughput?",
      opts: [
        { t: "Call HTTP sync 25ms cho TỪNG message — mỗi consumer thread dành 75% thời gian ngồi chờ mạng, throughput = 1000/33 ≈ 30 msg/s mỗi partition, kịch trần khi produce 100 msg/s mỗi partition", s: 2, why: "Xử lý tuần tự từng-message-một-call-sync là mẫu 'N+1 phiên bản messaging': từng call nhanh (25ms — đúng SLA của addressClient!), tổng thì chết. Điểm sâu: không ai 'có lỗi' — service kia khoẻ, code mới đúng nghiệp vụ — chỉ có PATTERN tích hợp là sai chỗ." },
        { t: "INSERT từng dòng vào DB là thủ phạm chính", s: 1, why: "Có góp 5ms/message (đáng gom batch — save theo lô sẽ giảm nữa) nhưng phép trừ đơn giản: 33ms tổng - 25ms HTTP = phần DB+build chỉ là 1/4 vấn đề. Tấn công thành phần lớn nhất trước — nguyên tắc tối ưu muôn thuở." },
        { t: "OrderEvent mới to hơn làm deserialize chậm", s: 0, why: "Metrics đã mổ sẵn: 25ms nằm ở addressClient, 5ms ở save. Deserialize nằm trong 3ms 'build'. Đoán thêm nghi phạm khi bảng số liệu đã điểm danh từng ms là bỏ bằng chứng theo cảm giác." },
        { t: "concurrency=6 quá thấp, tăng lên 32 là hết", s: 0, why: "concurrency của listener bị TRẦN bởi số partition được gán — 6 partition thì 32 thread cũng chỉ 6 thread có việc. Đây chính là phiên bản config của lỗi 'thêm consumer 7, 8' mà đội đã thử và thất bại." }
      ] }
  ],
  cause: { q: "Nguyên nhân gốc?",
    opts: [
      { t: "Release thêm call HTTP sync 25ms/message vào handler → chi phí mỗi message ×8 → throughput trần của 6 partition (~190/s) tụt xuống dưới produce rate (600/s) → lag tăng tuyến tính vô hạn; thêm consumer vô dụng vì trần nằm ở partition × chi phí/message", ok: true, why: "Công thức phải khắc: capacity = partitions × (1/cost-per-message). Release làm vế phải giảm 8 lần. Mọi giải pháp đều phải tác động vào MỘT trong hai thừa số đó — mọi thứ khác (thêm consumer thừa, restart, thêm broker) là trang trí." },
      { t: "AddressService quá chậm, cần họ tối ưu xuống dưới 5ms", ok: false, why: "25ms cho một HTTP call có network là TỐT (đúng SLA họ công bố). Vấn đề là bạn gọi nó 600 lần/giây theo kiểu tuần tự từng cái. Sửa cách MÌNH tích hợp (batch/cache/async) nằm trong tay mình ngay hôm nay; đòi người khác nhanh gấp 5 lần thì không." },
      { t: "Kafka không phù hợp cho use case này, nên chuyển RabbitMQ", ok: false, why: "Không có thuộc tính nào của sự cố liên quan tới đặc tính Kafka — RabbitMQ với consumer xử lý 33ms/message cũng nghẽn y hệt (chỉ khác chỗ hàng đợi phình). Đổi công nghệ khi chưa gọi tên được nút thắt là nghi thức cầu mưa." },
      { t: "Thiếu tài nguyên: pod consumer cần thêm CPU/RAM", ok: false, why: "Metrics ghi CPU/GC bình thường — thread đang CHỜ MẠNG chứ không thiếu lực (nhớ mẫu 'CPU thấp + chậm = chờ đợi'). Thêm CPU cho người đang ngồi chờ email không làm email tới nhanh hơn." }
    ] },
  fix: { q: "Hướng xử lý tốt nhất?",
    opts: [
      { t: "Giảm chi phí mỗi message: gom batch (poll 200 message → lookup địa chỉ MỘT call batch/cache theo userId → saveAll một lô) — throughput tăng hàng chục lần trên đúng 6 partition hiện có; nếu sau đó vẫn thiếu mới cân nhắc tăng partition", s: 2, why: "Đánh vào thừa số 'cost-per-message' — rẻ, thuận chiều (không đụng ordering/partition), và tấn công đúng phần lớn nhất (25ms HTTP thành ~0.3ms/message khi batch 100). Cache theo userId còn cứu cả addressService khỏi 600 req/s vô ích. Nguyên tắc: tối ưu đơn giá trước khi mua thêm làn đường." },
      { t: "Tăng partition 6 → 24 và scale consumer lên 24", s: 1, why: "Giải được về số học (24 × 30 = 720 > 600) nhưng đắt và một chiều: partition không giảm lại được, rebalance/ordering phải tính, và bạn vừa nhân 4 số connection tới addressService — 600 call/s vẫn nguyên, giờ dồn từ 24 nguồn. Hợp lệ làm bước SAU khi đơn giá đã tối ưu mà tải thật vẫn vượt." },
      { t: "Rollback release, bỏ bước enrich địa chỉ", s: 1, why: "Hotfix chính đáng để cứu SLA downstream NGAY (và nên làm nếu lag đang gây thiệt hại) — nhưng là nút tạm dừng, không phải lời giải: yêu cầu nghiệp vụ 'có địa chỉ trong event' vẫn còn đó và sẽ quay lại. Điểm trọn thuộc về phương án vừa giữ tính năng vừa giữ throughput." },
      { t: "Tăng max.poll.records lên 2000 để mỗi lần poll được nhiều hơn", s: 0, why: "Poll to hơn không đổi CHI PHÍ XỬ LÝ mỗi message — bạn xếp hàng dài hơn vào cùng một quầy chậm. Còn rủi ro mới: xử lý 2000 × 33ms = 66 giây > max.poll.interval.ms mặc định (5 phút thì chưa, nhưng đủ gần để rung chuông) → nguy cơ bị đá khỏi group, rebalance, lặp lại từ đầu." }
      ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> release thêm HTTP call sync 25ms/message → đơn giá xử lý ×8 → throughput trần 6 partition tụt dưới produce rate → lag tăng tuyến tính → thêm consumer 7-8 vô dụng (trần partition) → downstream trễ 50 phút.</p><p><b>Ba bài học mang đi:</b> (1) Công thức capacity của consumer group: <em>partitions × (1/cost-per-message)</em> — mọi chẩn đoán lag bắt đầu bằng phép chia này. (2) Lag ĐỀU = vấn đề throughput; lag LỆCH một partition = hot key/poison — dạng phân bố lag là manh mối đầu tiên. (3) Call sync từng-message trong consumer là N+1 của thế giới messaging — batch/cache/async là bộ ba thuốc chuẩn.</p>",
  prove: "<p>Mô phỏng không cần Kafka (~15 phút):</p><pre>// Gia lap: queue 10.000 item, produce 600/s\n// Consumer A: xu ly tung item, Thread.sleep(33)   ← call sync gia lap\n// Consumer B: lay 200 item/luot, sleep(30) MOT lan cho ca lo (batch call), roi xu ly ca lo\n// Do: sau 60 giay, queue con lai bao nhieu?\n//  A: queue PHINH ~26k (vao 36k, ra ~10k)  ← lag tang vo han\n//  B: queue ~0                              ← van 1 'consumer', chi doi pattern</pre><p><b>Cách xác minh:</b> in kích thước queue mỗi giây — đường cong của A leo dốc tuyến tính y như Grafana trong sự cố; của B đi ngang quanh 0. Cùng tài nguyên, khác đơn giá mỗi message — khác số phận.</p>",
  refs: [["Confluent – Consumer lag & scaling", "https://docs.confluent.io/platform/current/kafka/monitoring.html#consumer-group-lag"], ["Spring Kafka – Batch listeners", "https://docs.spring.io/spring-kafka/reference/kafka/receiving-messages/listener-annotation.html#batch-listeners"]]
});

INCIDENTS.register({
  id: "inc-gc-pressure", sev: "P2", domain: "JVM / Performance", skills: ["jvm-gc", "resil-perf"], time: "15–20 phút",
  title: "p99 răng cưa, CPU 78% — nhưng không hề có memory leak",
  teaser: "Young GC chạy 2 giây một lần, mỗi lần khựng 200ms. Heap sau GC vẫn đẹp. Đây là bệnh gì nếu không phải leak?",
  alert: "<b>[Grafana]</b> 11:05 — <code>report-service</code> p99 latency dao động răng cưa 40ms → 280ms chu kỳ ~2-3 giây; CPU node 78% (baseline 30%). Bắt đầu từ khi bật tính năng 'export báo cáo realtime' (chạy nền mỗi 10 giây). Memory alert: KHÔNG kêu — heap usage trong ngưỡng.",
  artifacts: [
    { label: "🗑 GC log (G1, trích 10 giây)", body: "<pre>[11:05:01] Pause Young (Normal) (<span class='hl'>G1 Evacuation Pause</span>)  2048M-&gt;310M(4096M)  <span class='hl'>212ms</span>\n[11:05:03] Pause Young (Normal)                         2050M-&gt;312M(4096M)  198ms\n[11:05:06] Pause Young (Normal)                         2047M-&gt;309M(4096M)  221ms\n[11:05:08] Pause Young (Normal)                         2051M-&gt;311M(4096M)  205ms\n...\nAllocation rate (tính từ log): <span class='hl'>~1.1 GB/giây</span>\nHeap SAU mỗi GC: ~310M — <span class='hl'>ổn định suốt 3 giờ, KHÔNG tăng dần</span>\nFull GC: 0 lần · Promotion vào Old gen: không đáng kể</pre>" },
    { label: "📈 Tương quan metrics", body: "<pre>p99 latency:  ▂█▂▂█▂▂█▂  (đỉnh 280ms — khớp TỪNG THỜI ĐIỂM với Pause Young trong GC log)\nCPU:          78%  (jstat: GC threads chiếm ~35% CPU)\nHeap used:    răng cưa 310M ↗ 2GB ↘ 310M mỗi ~2 giây\nRelease gần nhất: bật ReportExportJob — chạy mỗi 10 giây, xử lý ~200k dòng</pre>" },
    { label: "📄 Code hot path (ReportExportJob)", body: "<pre>String csv = \"\";\nfor (ReportRow row : rows) {                     // ~200.000 dòng mỗi lượt\n    <span class='hl'>csv += row.getId() + \",\" + row.getName() + \",\" + row.getTotal() + \"\\n\";</span>  // ①\n}\nList&lt;ReportRow&gt; filtered = rows.stream().filter(r -&gt; r.getTotal() &gt; 0).toList();\nList&lt;ReportRow&gt; sorted   = filtered.stream().sorted(...).toList();              // ② bản sao trung gian\n<span class='hl'>Long sum = rows.stream().map(ReportRow::getTotal).reduce(0L, Long::sum);</span>       // ③ boxing 200k lần</pre>" }
  ],
  steps: [
    { q: "Câu hỏi phân loại quan trọng nhất: đây là memory LEAK hay bệnh gì khác? Dựa vào đâu?",
      opts: [
        { t: "Không phải leak — heap SAU mỗi GC ổn định ~310M suốt 3 giờ. Đây là GC PRESSURE: allocation rate ~1.1GB/s làm young gen đầy mỗi 2 giây → GC chạy liên tục → pause tích luỹ vào tail latency + GC threads ăn CPU", s: 2, why: "Cặp chẩn đoán phân biệt phải thuộc lòng: leak = ĐÁY heap sau GC tăng dần (giữ được ít dần); pressure = đáy ổn định nhưng TỐC ĐỘ LẤP ĐẦY khủng khiếp (rác sinh quá nhanh). Thuốc hai bệnh khác hẳn nhau — chẩn sai là uống nhầm." },
        { t: "Là leak giai đoạn sớm — cần chụp heap dump theo dõi thêm 24h", s: 1, why: "Thận trọng không thừa, nhưng 3 giờ dữ liệu với đáy heap PHẲNG TUYỆT ĐỐI + 0 Full GC + promotion không đáng kể đã đủ bác leak với độ tin cao. Chờ 24h là trả thêm một ngày p99 xấu cho sự chắc chắn không cần thiết — bằng chứng hiện có đã chỉ hướng khác." },
        { t: "GC của JVM bị lỗi, cần nâng version JDK", s: 0, why: "GC đang làm ĐÚNG và XUẤT SẮC việc của nó: dọn 1.7GB rác mỗi 2 giây không sót giọt nào. Nó là nạn nhân phải dọn hậu quả, không phải thủ phạm. Đổ lỗi cho công cụ đang gồng mình là hướng điều tra ngược." },
        { t: "Heap 4GB quá nhỏ so với nhu cầu, cần 16GB", s: 0, why: "'Nhu cầu' thật (live data) chỉ 310M — heap 4GB đang thừa 12 lần. Vấn đề là tốc độ XẢ RÁC, không phải sức chứa. Heap to hơn chỉ làm chu kỳ GC thưa hơn nhưng mỗi lần dọn nhiều hơn — tail latency có khi còn tệ." }
      ] },
    { q: "Cần tìm NGUỒN cấp phát 1.1GB/s. Cách hiệu quả nhất?",
      opts: [
        { t: "Allocation profiler: async-profiler chế độ alloc (hoặc JFR + TLAB events) chạy 60 giây — ra thẳng flame graph 'method nào cấp phát bao nhiêu'", s: 2, why: "Câu hỏi 'AI cấp phát' có công cụ đo trực tiếp, overhead thấp, chạy được trên production. 60 giây profiling thay cho nhiều giờ đọc code đoán mò — và kết quả là bằng chứng thuyết phục được cả đội (flame graph chỉ thẳng ReportExportJob chiếm 90% allocation)." },
        { t: "Đọc GC log kỹ hơn để tìm tên class đang cấp phát", s: 0, why: "GC log nói CÓ BAO NHIÊU rác và dọn mất bao lâu — không bao giờ nói AI xả. Đào thêm ở nguồn dữ liệu không chứa câu trả lời là siêng năng đặt sai chỗ; biết mỗi công cụ trả lời được câu gì là kỹ năng điều tra lõi." },
        { t: "Review toàn bộ code các release gần đây tìm chỗ tạo object nhiều", s: 1, why: "Ở đây MAY là có manh mối release (ReportExportJob) nên khoanh vùng nhanh — nhưng phương pháp 'đọc code tìm allocation' không scale và hay trượt (allocation ẩn trong autoboxing, varargs, lambda capture mắt thường khó thấy). Dùng được như bước phụ; profiler vẫn là chuẩn." },
        { t: "Thêm log đếm object trong các vòng lặp nghi vấn", s: 0, why: "Tự chế máy đo kém chính xác trong khi máy đo xịn (profiler) cắm vào là chạy — và chính việc thêm log/String concat trong vòng lặp nóng lại... cấp phát thêm. Đo lường can thiệp làm nhiễu chính thứ đang đo." }
      ] }
  ],
  cause: { q: "Nguyên nhân gốc?",
    opts: [
      { t: "Hot path mới (job mỗi 10s, 200k dòng) xả rác với tốc độ ~1.1GB/s: String += trong vòng lặp (mỗi vòng một String + char[] mới), hai bản sao List trung gian, boxing Long 200k lần — young gen đầy mỗi 2s → GC liên tục: pause đội p99, GC threads chiếm CPU", ok: true, why: "GC pressure là 'thuế rác': từng dòng code trông vô hại, nhân với 200k × mỗi-10-giây thành nhà máy rác công nghiệp. String += trong loop là ví dụ giáo khoa (đã học ở câu String/StringBuilder) — mỗi vòng copy toàn bộ chuỗi cũ: O(n²) cả CPU lẫn allocation." },
      { t: "Máy thiếu CPU cho job report chạy song song với traffic", ok: false, why: "CPU cao là HỆ QUẢ: 35% CPU đang nuôi GC threads dọn rác. Giảm allocation là CPU tự về — thêm core chỉ là thuê thêm người quét dọn cho bữa tiệc xả rác vô hạn." },
      { t: "G1 không hợp workload này, chuyển ZGC là hết khựng", ok: false, why: "ZGC giảm PAUSE thật (sub-ms) — nhưng 1.1GB/s rác vẫn phải có người dọn: CPU cho GC còn nguyên, và allocation rate này ở scale lớn hơn sẽ vượt cả tốc độ dọn của ZGC. Đổi GC là giảm đau; nguồn rác vẫn là khối u. (Là mitigation hợp lệ NẾU không sửa được code — nhưng ở đây code sửa được và rẻ.)" },
      { t: "Job chạy quá dày (10 giây một lần), giãn ra 5 phút là hết", ok: false, why: "Giãn lịch làm cơn đau THƯA hơn chứ không NHẸ hơn — mỗi lần chạy vẫn xả từng ấy rác, p99 vẫn răng cưa trong lúc chạy, và 'realtime' trong tên tính năng vừa chết. Đây là đàm phán yêu cầu trá hình, không phải sửa kỹ thuật." }
    ] },
  fix: { q: "Sửa thế nào cho đúng tầng?",
    opts: [
      { t: "Giảm allocation ở hot path: StringBuilder (pre-size) thay String +=, gộp filter+sort+sum vào MỘT lượt duyệt không tạo list trung gian, primitive stream (mapToLong) thay boxing — rồi chạy lại profiler xác nhận allocation rate giảm", s: 2, why: "Đánh thẳng nguồn rác — allocation rate từ 1.1GB/s xuống vài chục MB/s là GC thưa hẳn, pause biến khỏi p99, CPU về baseline. Bước 'đo lại sau sửa' quan trọng ngang bước sửa: tối ưu không có số trước/sau là tối ưu bằng niềm tin." },
      { t: "Tăng young gen (-Xmn) để GC thưa hơn", s: 1, why: "Toán học đúng: young to gấp đôi → chu kỳ GC giãn gấp đôi — mua được thời gian thật và đôi khi là đủ. Nhưng mỗi pause có thể DÀI hơn (evacuate nhiều hơn), CPU cho GC không giảm, và bạn đang tinh chỉnh thùng rác thay vì bớt xả. Hợp lệ làm bước kèm, không phải lời giải chính." },
      { t: "Chuyển job export sang một service riêng để cách ly", s: 1, why: "Bulkhead đúng triết lý (rác của báo cáo không đội p99 của API) và đáng làm về DÀI HẠN nếu job còn phình. Nhưng hôm nay nó là giải pháp đắt (service mới, deploy, vận hành) cho bug 10 dòng code — và service mới vẫn đốt CPU cho GC vô ích. Cách ly không thay được sửa lãng phí." },
      { t: "Gọi System.gc() định kỳ vào lúc vắng để 'dọn chủ động'", s: 0, why: "Hiểu ngược cơ chế hai lần: (1) rác sinh liên tục 1.1GB/s — dọn 'lúc vắng' không giảm được lần dọn lúc đông; (2) System.gc() gợi ý FULL GC — pause còn dài hơn Young GC đang than phiền. Đây là đề xuất tăng liều đúng thứ thuốc đang gây tác dụng phụ." }
      ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> bật job export 200k dòng/10s → String += / list trung gian / boxing xả ~1.1GB/s → young gen 2GB đầy mỗi 2 giây → Pause Young 200ms liên tục → p99 răng cưa khớp từng nhịp GC + GC threads chiếm 35% CPU → memory alert im lặng vì... memory không hề 'đầy'.</p><p><b>Ba bài học mang đi:</b> (1) Phân biệt <em>leak</em> (đáy heap sau GC tăng dần) với <em>pressure</em> (đáy phẳng, allocation rate khủng) — hai bệnh, hai thuốc. (2) Câu 'ai cấp phát' trả lời bằng <em>allocation profiler</em>, không phải GC log (nó chỉ nói bao nhiêu, không nói ai). (3) Rác rẻ từng cục, đắt theo nhịp: code chạy 200k lần × mỗi 10 giây phải được viết theo chuẩn khác code chạy 1 lần — 'hot path' là khái niệm vật lý, không phải khẩu hiệu.</p>",
  prove: "<p>Tự đo trên máy (~10 phút, chỉ cần JDK):</p><pre>// Churn.java — chay:  java -Xmx512m -Xlog:gc Churn a   (roi thu lai voi 'b')\npublic class Churn {\n  public static void main(String[] args) {\n    boolean bad = args[0].equals(\"a\");\n    long t = System.currentTimeMillis();\n    for (int round = 0; round &lt; 50; round++) {\n      if (bad) { String s = \"\"; for (int i = 0; i &lt; 20_000; i++) s += i; }        // a\n      else { StringBuilder sb = new StringBuilder(200_000); for (int i = 0; i &lt; 20_000; i++) sb.append(i); } // b\n    }\n    System.out.println((System.currentTimeMillis() - t) + \" ms\");\n  }\n}</pre><p><b>Cách xác minh:</b> bản (a) chậm hơn bản (b) <b>hàng chục tới hàng trăm lần</b>, và <code>-Xlog:gc</code> in ra hàng loạt dòng Pause Young ở bản (a) so với lác đác ở (b) — cùng một 'kết quả nghiệp vụ', khác allocation là khác cả thời gian lẫn số lần GC. Nâng cao: cắm async-profiler chế độ <code>-e alloc</code> vào bản (a) để thấy flame graph chỉ thẳng dòng String concat.</p>",
  refs: [["async-profiler", "https://github.com/async-profiler/async-profiler"], ["Oracle – G1 tuning", "https://docs.oracle.com/en/java/javase/17/gctuning/garbage-first-g1-garbage-collector1.html"]]
});
