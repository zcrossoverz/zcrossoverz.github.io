/* ============================================================
   DESIGN LAB — các bài thiết kế hệ thống
   Schema: { id, lv, title, teaser, domain, skills:[node id], time,
     brief(HTML — yêu cầu + con số), steps:[{q, opts:[{t, s:0|1|2, why}]}],
     debrief(HTML — kiến trúc tham chiếu + what-if), refs }
   Quy tắc: mỗi step có đúng 1 phương án s:2; MỌI option phải có why
   (why = bài giảng trade-off — phần giá trị nhất của module này).
   ============================================================ */

DESIGNS.register({
  id: "dsg-rate-limiter", lv: "middle", domain: "API / Resilience", skills: ["resil-patterns", "cache-redis", "api-evolution"], time: "15–20 phút",
  title: "Thiết kế rate limiting cho public API",
  teaser: "100 req/phút mỗi user, 6 instance sau load balancer, không được biến sự cố Redis thành outage. Bốn quyết định — mỗi cái một trade-off.",
  brief: "<p>Public API của bạn đang bị vài client viết script gọi dồn dập làm ảnh hưởng người khác. Yêu cầu:</p><ul><li>Giới hạn <b>100 request/phút cho mỗi user</b> (theo API key), cho phép <b>burst ngắn hợp lý</b> (client retry, mở app dồn 5-10 call).</li><li>Hệ chạy <b>6 instance</b> sau load balancer, round-robin — user không dính máy nào cố định.</li><li>Traffic ~2.000 req/giây toàn hệ. Thêm latency cho mỗi request phải <b>&lt; 2ms</b>.</li><li>Khi thành phần đếm (nếu có) gặp sự cố: <b>API không được sập theo</b>.</li></ul>",
  steps: [
    { q: "Quyết định 1 — Chọn thuật toán đếm giới hạn:",
      opts: [
        { t: "Token bucket: mỗi user một 'xô' 100 token, nạp lại ~1.67 token/giây; request tiêu 1 token, hết token → chặn", s: 2, why: "Khớp cả hai yêu cầu: tốc độ trung bình bị ghìm ở 100/phút NHƯNG burst ngắn được phép tiêu phần token để dành — đúng hành vi client thật (mở app dồn 8 call vẫn qua). State chỉ 2 số/user (số token + timestamp cuối) — rẻ. Đây là lựa chọn của đa số hệ lớn (Stripe, AWS)." },
        { t: "Fixed window: đếm số request trong từng phút lịch (10:00:00–10:00:59), sang phút mới reset về 0", s: 1, why: "Đơn giản nhất (1 counter + TTL 60s) và đủ tốt cho nhiều hệ — nhưng có lỗ hổng ranh giới: user bắn 100 call lúc 10:00:59 + 100 call lúc 10:01:00 = 200 call trong 2 giây mà không vi phạm. Chấp nhận được nếu limit chỉ mang tính 'chống lạm dụng thô'; biết rõ nhược điểm này thì dùng vẫn hợp lệ." },
        { t: "Sliding window log: lưu timestamp của TỪNG request trong 60 giây gần nhất, đếm chính xác tuyệt đối", s: 0, why: "Chính xác nhất nhưng trả giá vô lý ở scale này: 2.000 req/s × 60s = 120.000 timestamp phải lưu và dọn LIÊN TỤC, mỗi request một lần ghi + đếm sorted set. Độ chính xác tuyệt đối không phải yêu cầu — 'không sập, không tốn' mới là. Chọn giải pháp đắt nhất cho yêu cầu không ai đặt ra là lỗi thiết kế kinh điển." },
        { t: "Đếm bằng cột counter trong bảng users của PostgreSQL, UPDATE mỗi request", s: 0, why: "2.000 UPDATE/giây dồn vào DB nghiệp vụ — bạn vừa thiết kế một máy tạo lock contention và kéo DB (tài nguyên quý nhất) vào con đường của MỌI request. Rate limiter phải rẻ hơn thứ nó bảo vệ." }
      ] },
    { q: "Quyết định 2 — State đếm đặt ở đâu (nhớ: 6 instance, user rơi máy nào cũng được)?",
      opts: [
        { t: "Redis tập trung: bucket của mỗi user là 1 key, logic tiêu-token viết bằng Lua script để nguyên tử", s: 2, why: "6 instance nhìn chung MỘT bộ đếm → limit đúng nghĩa 100/phút toàn hệ. Redis ở đường nóng: ~0.5ms/lệnh — nằm trong ngân sách 2ms. Lua gói 'đọc token → tính nạp lại → trừ → ghi' thành một thao tác nguyên tử — không race giữa các instance (bài học từ check-then-act)." },
        { t: "Đếm local trong memory từng instance, limit đặt 100/6 ≈ 16 req/phút mỗi máy", s: 1, why: "Không phụ thuộc hệ ngoài, latency ~0 — hướng suy nghĩ tốt. Nhưng chia đều 100/6 chỉ đúng khi LB chia tải hoàn hảo từng user (round-robin không hứa thế): user có thể bị chặn oan ở máy này khi quota máy khác còn thừa. Là phương án fallback tốt, không phải phương án chính." },
        { t: "Đếm local + sticky session để user luôn vào một máy", s: 0, why: "Sticky session để phục vụ rate limiter là đuôi vẫy chó: mất khả năng chịu lỗi (máy chết là user 'của máy đó' văng), khó scale, và mọi thứ khác của hệ đang stateless bỗng bị trói. Trả giá kiến trúc lớn cho một bài toán đã có lời giải rẻ (Redis)." },
        { t: "Ghi từng request vào Kafka, một consumer tổng hợp số đếm rồi phát ngược lại các instance", s: 0, why: "Đếm qua pipeline bất đồng bộ nghĩa là con số luôn TRỄ vài giây — kẻ spam bắn hết đợt tấn công trước khi số đếm kịp cập nhật. Rate limiting là bài toán quyết-định-tức-thì; kiến trúc streaming dành cho analytics, không dành cho cái phanh." }
      ] },
    { q: "Quyết định 3 — Redis chết/timeout. Request đang tới, xử lý sao?",
      opts: [
        { t: "Fail-open có phanh phụ: cho request đi qua + đếm tạm bằng limiter local trong lúc Redis chết + alert ngay lập tức", s: 2, why: "Nguyên tắc: sự cố của bộ ĐẾM không được nghiêm trọng hơn sự cố nó phòng ngừa. Chặn 100% user vì Redis chết = tự tạo outage. Fail-open trần trụi thì mất phanh đúng lúc dễ bị lạm dụng — nên có lưới local thô (dù không chính xác toàn cục) + alert để người xử lý sớm. Ba lớp: chính xác (Redis) → thô (local) → con người (alert)." },
        { t: "Fail-closed: không xác nhận được quota thì từ chối request cho an toàn", s: 1, why: "Đúng cho ngữ cảnh KHÁC: nếu rate limit là hàng rào BẢO MẬT (chống brute-force đăng nhập, chống dò OTP) thì fail-closed là bắt buộc — thà chặn oan còn hơn mở toang. Nhưng đề bài là chống lạm dụng public API: chặn toàn bộ khách vì Redis hắt hơi là chọn sai chi phí. Trả lời 'tuỳ vai trò của limiter' là câu trả lời senior — ở đây vai trò nghiêng fail-open." },
        { t: "Retry lệnh Redis 3 lần (mỗi lần timeout 1s) trước khi quyết định", s: 0, why: "Bạn vừa cộng tối đa 3 GIÂY vào latency của MỌI request trong lúc Redis bệnh — ngân sách là 2ms. Chưa kể 2.000 req/s × 3 retry = đấm kẻ ốm túi bụi (bài retry storm nguyên văn). Đường nóng không có chỗ cho chờ đợi kiểu này: timeout phải vài chục ms và quyết ngay." },
        { t: "Chuyển sang đếm bằng database trong lúc Redis chết", s: 0, why: "Failover sang hệ CHẬM HƠN NHIỀU ngay lúc traffic vẫn nguyên = dồn 2.000 update/giây vào DB — bạn cứu bộ đếm bằng cách giết DB nghiệp vụ. Fallback phải rẻ hơn hoặc bằng hệ chính, không bao giờ đắt hơn." }
      ] },
    { q: "Quyết định 4 — Client chạm limit. API trả về gì?",
      opts: [
        { t: "429 Too Many Requests + header Retry-After + bộ X-RateLimit-Limit/-Remaining/-Reset trên mọi response", s: 2, why: "429 là mã đúng ngữ nghĩa (client hiểu 'lỗi do gọi nhiều, không phải server hỏng'). Retry-After biến client từ kẻ phá hoại thành công dân tốt — nó biết chờ bao lâu thay vì retry mù. Bộ X-RateLimit-* trên MỌI response giúp client tự điều tiết TRƯỚC khi chạm trần — giảm cả support ticket. Đây là chuẩn de-facto (GitHub, Stripe)." },
        { t: "429 với body JSON báo lỗi, không kèm header hướng dẫn", s: 1, why: "Đúng mã, thiếu hướng dẫn — client biết bị chặn nhưng không biết chờ bao lâu → đa số sẽ retry theo cấu hình mặc định của chúng (thường là ngay lập tức) → tự tạo mưa 429. Thiếu vài cái header là thiếu một nửa giá trị của limiter." },
        { t: "503 Service Unavailable — server đang không phục vụ được mà", s: 0, why: "Sai ngữ nghĩa có hậu quả dây chuyền: 503 nói 'SERVER hỏng' → monitoring phía client tính là lỗi hệ thống của BẠN, SLA dashboard đỏ, và nhiều client mặc định retry 5xx hăng hơn — đúng điều limiter muốn dập. Chọn status code là giao tiếp máy-với-máy, sai một mã là sai cả chuỗi phản ứng." },
        { t: "Âm thầm drop connection để tiết kiệm tài nguyên trả response", s: 0, why: "Client không nhận được gì sẽ coi là lỗi mạng → retry ngay và mạnh hơn (kèm connection mới — còn đắt hơn cái response bạn vừa tiết kiệm). Im lặng là câu trả lời đắt nhất trong giao thức: mọi bên đối xử với im lặng bằng kịch bản xấu nhất." }
      ] }
  ],
  debrief: "<p><b>Kiến trúc tham chiếu:</b></p><pre>Client → LB → [Gateway/Filter]\n              │  key = api_key (hoặc user_id)\n              ▼\n        Redis (Lua token bucket)     ← nguyên tử, ~0.5ms\n        │  ok → đi tiếp (kèm X-RateLimit-*)\n        │  hết token → 429 + Retry-After\n        └─ Redis lỗi → fail-open + local coarse limiter + ALERT</pre><p><b>Điểm hay bị quên:</b> đặt limiter ở <em>gateway/filter chung</em> (một chỗ, mọi endpoint hưởng) chứ không rải trong từng service; phân TẦNG limit (per-user + per-IP cho user chưa đăng nhập + global làm cầu chì tổng); và <em>đo</em> — dashboard tỉ lệ 429 theo client để phân biệt 'kẻ lạm dụng' với 'client viết sai'.</p><p><b>Nếu yêu cầu đổi:</b> scale ×100 (200k req/s) → Redis cluster shard theo user key là đủ vì bài toán shard hoàn hảo theo key; latency ngặt hơn nữa → đếm local + đồng bộ quota qua Redis theo chu kỳ (đánh đổi chính xác lấy tốc độ); còn nếu limiter phục vụ BẢO MẬT (login) → lật quyết định 3 sang fail-closed, và limit theo IP + username kết hợp.</p>",
  refs: [["Stripe – Scaling rate limiters", "https://stripe.com/blog/rate-limiters"], ["Cloudflare – Rate limiting algorithms", "https://blog.cloudflare.com/counting-things-a-lot-of-different-things/"]]
});

DESIGNS.register({
  id: "dsg-notification", lv: "senior", domain: "Messaging / EDA", skills: ["msg-eda", "msg-delivery", "resil-idem"], time: "20 phút",
  title: "Hệ thống thông báo đa kênh (email / push / in-app)",
  teaser: "Sự kiện nghiệp vụ phải thành thông báo — không được mất, không được gửi đôi, kênh chậm không được kéo kênh nhanh. Kinh điển của event-driven.",
  brief: "<p>Sàn thương mại điện tử ~5 triệu user cần gửi thông báo khi có sự kiện (đơn xác nhận, giao hàng, khuyến mãi):</p><ul><li>Ba kênh: <b>email</b> (provider ngoài, chậm ~500ms, hay chập chờn), <b>push</b> (FCM, nhanh), <b>in-app</b> (ghi DB, đọc khi mở app).</li><li>Thông báo giao dịch (đơn hàng) <b>không được mất</b>; gửi <b>trùng là sự cố</b> (khách nhận 2 email xác nhận cùng đơn → mất uy tín).</li><li>Đỉnh tải: chiến dịch khuyến mãi bắn <b>500k thông báo trong 10 phút</b> — không được làm chậm luồng đặt hàng.</li><li>User có preferences: tắt kênh, không nhận khuyến mãi ban đêm.</li></ul>",
  steps: [
    { q: "Quyết định 1 — OrderService có sự kiện 'đơn xác nhận'. Đưa yêu cầu thông báo vào hệ thống thế nào?",
      opts: [
        { t: "Outbox: ghi notification-request vào bảng outbox CÙNG transaction với đơn hàng; relay đẩy sang Kafka topic notifications", s: 2, why: "Yêu cầu 'không được mất' + nguồn phát là transaction nghiệp vụ → outbox là câu trả lời sách giáo khoa (đã học ở câu Kafka-trong-transaction): đơn commit thì yêu cầu thông báo CHẮC CHẮN tồn tại, relay lỗi thì retry — không ma, không mất. Luồng đặt hàng chỉ tốn thêm 1 INSERT — không đợi provider nào." },
        { t: "OrderService gọi thẳng NotificationService qua HTTP, service này gửi luôn 3 kênh rồi mới trả lời", s: 0, why: "Bạn vừa nối số phận luồng ĐẶT HÀNG (doanh thu) vào email provider chập chờn (~500ms, hay lỗi): provider chậm → đặt hàng chậm; provider chết → tuỳ code, có thể đặt hàng lỗi theo. Nghiệp vụ chính không bao giờ được chờ nghiệp vụ phụ — đây là bài học cascading số 1." },
        { t: "OrderService bắn event vào Kafka trực tiếp ngay trong method @Transactional", s: 1, why: "Đúng hướng bất đồng bộ, nhưng dính đúng cái bẫy đã học: publish trước commit → rollback là consumer nhận event về đơn không tồn tại; publish sau commit mà fail → mất thông báo giao dịch (vi phạm yêu cầu). Với thông báo khuyến mãi thì chấp nhận được — với thông báo giao dịch thì outbox mới đủ chặt." },
        { t: "Cron job quét bảng orders mỗi phút tìm đơn mới rồi gửi thông báo", s: 1, why: "Chạy được và bền (không mất — quét lại là thấy), nhưng: trễ tới 60s cho thông báo cần tức thì, quét bảng nghiệp vụ tạo tải đọc vô ích, và cần cột trạng thái 'đã thông báo' — lúc đó bạn đã tự chế một outbox kém: cùng độ phức tạp, thua độ trễ. Nếu đằng nào cũng vậy, làm outbox đúng bài." }
      ] },
    { q: "Quyết định 2 — Ba kênh tốc độ khác nhau (email chậm & hay lỗi, push nhanh, in-app rẻ). Tổ chức consumer thế nào?",
      opts: [
        { t: "Mỗi kênh một topic (hoặc consumer group) riêng + worker riêng — bulkhead: email sập không đụng push/in-app", s: 2, why: "Cách ly theo đặc tính: email chậm/lỗi chỉ làm lag hàng đợi email — push vẫn tới trong ms, in-app vẫn ghi. Scale độc lập (chiến dịch 500k → tăng riêng worker email), retry policy riêng từng kênh (email retry 3 lần backoff, push bỏ qua nhanh). Đây là bulkhead áp vào messaging." },
        { t: "Một consumer đọc topic chung, gửi lần lượt email → push → in-app cho từng thông báo", s: 1, why: "Đơn giản, ít bộ phận — hợp lý cho MVP tải nhỏ. Nhưng ở đề bài này: kênh chậm nhất (email 500ms) quyết định throughput cả chuỗi (~2 thông báo/giây/consumer — 500k cái là 3 ngày!); email lỗi còn chặn push của cùng message. Head-of-line blocking nguyên văn." },
        { t: "Một consumer nhưng bắn 3 kênh song song bằng CompletableFuture rồi chờ cả 3 xong mới commit offset", s: 1, why: "Throughput khá hơn phương án tuần tự, nhưng vẫn buộc chung số phận: email timeout là cả message chưa commit — redelivery làm push/in-app (đã thành công) chạy LẠI → trùng ở kênh nhanh vì kênh chậm. Tách topic sạch hơn nhiều so với vá bằng thread." },
        { t: "In-app xử lý trước bằng code sync, email/push 'để sau tính'", s: 0, why: "Đây không phải phương án kiến trúc mà là né đề bài — yêu cầu rõ ba kênh với đặc tính khác nhau. Trong design review thật, 'để sau tính' cho phần khó nhất (email chập chờn) là mời sự cố đặt lịch trước." }
      ] },
    { q: "Quyết định 3 — Kafka at-least-once + retry nghĩa là worker CÓ THỂ xử lý một thông báo hai lần. Chống 'khách nhận 2 email' thế nào?",
      opts: [
        { t: "Idempotency theo dữ liệu: bảng notification_log với UNIQUE (notification_id, channel); worker insert-trước-gửi-sau, đụng conflict là bỏ qua êm", s: 2, why: "Trọng tài nằm ở DB — đúng bài duplicate-message đã học: constraint chặn cả redelivery lẫn hai worker chạy song song. Insert TRƯỚC khi gửi tạo 'claim' — kẻ đến sau thấy conflict là rút lui. (Trade-off nhỏ phải biết: crash giữa insert và gửi → thông báo có claim mà chưa gửi — cần job quét dòng 'claimed quá hạn chưa confirm' để gửi bù.)" },
        { t: "Redis SETNX notification_id:channel với TTL 24h trước khi gửi", s: 1, why: "Chạy được đa số trường hợp và nhanh — nhưng dấu-đã-gửi nằm ở hệ thống KHÁC hệ ghi nhận (Redis restart/evict là thủng), TTL hết mà message cũ quay lại (replay từ offset cũ) là gửi trùng. Khi đã có DB trong luồng, unique constraint rẻ và tuyệt đối hơn — Redis chỉ đáng khi luồng hoàn toàn không chạm DB." },
        { t: "Cấu hình Kafka exactly-once (EOS) là hết trùng, khỏi làm gì thêm", s: 0, why: "Hiểu sai phạm vi EOS (đã học ở diagnostic): nó bảo đảm exactly-once cho chuỗi ĐỌC-XỬ LÝ-GHI TRONG KAFKA; còn 'gọi API email provider' là side-effect bên ngoài — Kafka không thể rút lại email đã bay. Với sink ngoài, câu trả lời luôn là at-least-once + idempotent consumer." },
        { t: "Giảm maxAttempts về 1, không retry nữa là không trùng", s: 0, why: "Đổi 'không trùng' lấy 'mất thông báo' — vi phạm thẳng yêu cầu số 1 (không được mất). Chưa kể trùng còn đến từ rebalance/crash chứ không chỉ retry. Trị triệu chứng bằng cách tắt hệ miễn dịch." }
      ] },
    { q: "Quyết định 4 — Preferences (tắt kênh, im lặng ban đêm với khuyến mãi) kiểm tra Ở ĐÂU?",
      opts: [
        { t: "Kiểm tại WORKER ngay trước khi gửi (đọc preferences có cache) + thông báo khuyến mãi ban đêm thì reschedule vào hàng chờ sáng", s: 2, why: "Worker là điểm cuối trước khi chạm user — kiểm ở đây thì preferences LUÔN là bản mới nhất (user tắt kênh lúc 20:00, thông báo trong queue lúc 20:05 vẫn được tôn trọng). Reschedule (thay vì drop) giữ đúng ngữ nghĩa 'im lặng ban đêm' ≠ 'huỷ'. Cache preferences (TTL ngắn) để không đấm DB 500k lần trong chiến dịch." },
        { t: "Kiểm tại PRODUCER: đọc preferences lúc tạo yêu cầu, user tắt kênh thì không ghi outbox kênh đó", s: 1, why: "Giảm được message vô ích trong queue (tiết kiệm thật trong chiến dịch lớn) — nhưng preferences là dữ liệu SỐNG: giữa lúc enqueue và lúc gửi (có thể hàng giờ nếu lag), user đổi ý thì quyết định đã đóng băng sai. Hợp làm tầng lọc THÔ bổ sung, không thay được kiểm tra cuối tại worker." },
        { t: "Không cần kiểm — user không muốn thì tự unsubscribe qua link trong email", s: 0, why: "Đẩy chi phí sang user và pháp lý: gửi khi người ta đã tắt là vi phạm cả trải nghiệm lẫn quy định chống spam (CAN-SPAM/GDPR tuỳ thị trường) — với push thì là đường thẳng tới gỡ app. Preferences là YÊU CẦU, không phải tính năng phụ." },
        { t: "Kiểm bằng job dọn queue mỗi 10 phút: xoá message của user đã tắt kênh", s: 0, why: "Queue (Kafka) không phải chỗ 'xoá message theo điều kiện' — mô hình log không hỗ trợ tự nhiên; job quét-xoá vừa phức tạp vừa luôn trễ 10 phút. Chọn giải pháp đánh nhau với công cụ thay vì đặt kiểm tra ở nơi tự nhiên (worker)." }
      ] }
  ],
  debrief: "<p><b>Kiến trúc tham chiếu:</b></p><pre>OrderService ──(cùng transaction)──▶ outbox ──relay──▶ Kafka \"notifications\"\n                                                        │ (router theo kênh + preferences thô)\n                                        ┌───────────────┼────────────────┐\n                                        ▼               ▼                ▼\n                                  topic email     topic push       topic in-app\n                                  worker email    worker push      worker in-app\n                                  (retry+backoff, (nhanh, bỏ qua   (INSERT bảng\n                                   DLQ khi bó tay) lỗi nhanh)       notifications)\n                     mỗi worker: check preferences (cache) → claim UNIQUE(id,channel) → gửi → confirm</pre><p><b>Các mảnh thường bị quên:</b> DLQ cho message chết (điều tra thay vì retry vô hạn); bảng trạng thái gửi (đối soát 'khách bảo không nhận được'); partition theo <code>user_id</code> để thông báo một user giữ thứ tự; và rate limit RIÊNG với provider (FCM/email có quota — chiến dịch 500k phải trải, không bắn thẳng).</p><p><b>Nếu yêu cầu đổi:</b> thêm kênh SMS đắt tiền → thêm topic + worker, KHÔNG đụng producer (đây là phần thưởng của thiết kế tách kênh); yêu cầu 'đúng thứ tự tuyệt đối giữa các kênh' → gom lại một consumer có trạng thái (đổi throughput lấy ordering — nói được cái giá là đạt); scale ×10 chiến dịch → tách luồng transactional và marketing thành hai cụm topic/worker với ưu tiên khác nhau, marketing không bao giờ được lấn tài nguyên của transactional.</p>",
  refs: [["microservices.io – Transactional outbox", "https://microservices.io/patterns/data/transactional-outbox.html"], ["Uber – Reliable reprocessing & DLQ", "https://www.uber.com/blog/reliable-reprocessing/"]]
});

DESIGNS.register({
  id: "dsg-file-upload", lv: "middle", domain: "System Design / Storage", skills: ["api-evolution", "resil-patterns", "des-sysdes"], time: "15–20 phút",
  title: "Upload & xử lý file lớn (ảnh, video tới 2GB)",
  teaser: "Backend Java của bạn không sinh ra để làm ống dẫn 2GB. Ai nhận file, ai xử lý, client biết trạng thái kiểu gì — ba câu hỏi tách hệ này thành hay/dở.",
  brief: "<p>Nền tảng khoá học cần cho giảng viên upload video bài giảng:</p><ul><li>File tới <b>2GB</b>; ~10.000 upload/ngày, giờ cao điểm dồn cục.</li><li>Sau upload phải <b>transcode</b> (đổi định dạng/độ phân giải — CPU nặng, 5–30 phút/video) rồi mới xem được.</li><li>Mạng người dùng Việt Nam: <b>rớt giữa chừng là chuyện thường</b> — upload lại từ đầu file 2GB là trải nghiệm không chấp nhận được.</li><li>Hạ tầng: app Java (4 instance × 2GB heap), có dùng object storage (S3-compatible).</li></ul>",
  steps: [
    { q: "Quyết định 1 — Đường đi của bytes: file 2GB từ máy giảng viên vào storage bằng đường nào?",
      opts: [
        { t: "Backend cấp presigned URL (có hạn dùng, chỉ định key + giới hạn) — client upload THẲNG lên object storage, không đi qua app Java", s: 2, why: "Bytes đi thẳng tới nơi chuyên trị bytes: app Java không phải làm ống dẫn 2GB (nhớ heap 2GB!), băng thông không nhân đôi, 4 instance rảnh tay làm nghiệp vụ. Backend vẫn giữ toàn quyền kiểm soát QUA việc ký URL: ai được upload, vào key nào, hạn bao lâu, giới hạn kích thước. Đây là pattern chuẩn ngành cho file lớn." },
        { t: "Client POST multipart lên backend, backend stream tiếp lên storage", s: 1, why: "Kiểm soát tại chỗ tốt (validate ngay, không lộ storage) và OK với file NHỎ. Nhưng ở 2GB: mỗi upload chiếm một connection + băng thông app suốt nhiều phút, giờ cao điểm vài chục upload đồng thời là nghẽn instance (chưa kể ai đó lỡ buffer thay vì stream là OOM tức thì). Trả giá tài nguyên đắt cho quyền kiểm soát mà presigned URL vẫn cho bạn theo cách rẻ hơn." },
        { t: "Lưu file vào cột BYTEA/BLOB trong PostgreSQL cho 'dữ liệu ở một chỗ'", s: 0, why: "DB là nơi đắt nhất để chứa bytes câm: 2GB/dòng làm backup phình nổ, replication lê lết, buffer cache bị chiếm bởi thứ không bao giờ được query theo điều kiện. Object storage rẻ hơn ~10 lần và sinh ra đúng cho việc này. DB giữ METADATA (đường dẫn, trạng thái, owner) — bytes ở storage." },
        { t: "Dựng FTP/SFTP server cho giảng viên upload", s: 0, why: "Đẩy độ phức tạp sang người dùng (cần client FTP, tài khoản riêng) và sang vận hành (một server stateful phải nuôi, auth riêng, không CDN, không presign). Năm 2026 câu này chỉ đúng khi đối tác doanh nghiệp CŨ bắt buộc — với người dùng cuối là trải nghiệm thời tiền sử." }
      ] },
    { q: "Quyết định 2 — Transcode 5–30 phút/video chạy ở đâu, kích hoạt kiểu gì?",
      opts: [
        { t: "Upload xong (storage event/client confirm) → đẩy job vào queue → cụm worker riêng chuyên transcode, autoscale theo độ sâu queue", s: 2, why: "Ba tính chất của bài toán — nặng CPU, chạy lâu, dồn cục — đều chỉ về queue + worker: cách ly (transcode ngốn CPU không đụng API), hấp thụ đỉnh (queue phình thay vì hệ sập), scale đúng chỗ (thêm worker khi queue sâu, co về 0 lúc vắng). Job lâu 5-30 phút cần visibility timeout/heartbeat để job của worker chết được giao lại." },
        { t: "Transcode ngay trong request upload, xong hết mới trả 200", s: 0, why: "Request HTTP sống 5–30 phút: timeout ở mọi tầng (LB, proxy, client) sẽ chém nó trước khi xong; user nhìn spinner nửa tiếng; retry của client tạo job trùng. Quy tắc bất di bất dịch: việc tính bằng PHÚT không bao giờ sống trong vòng đời một request." },
        { t: "Cron mỗi 5 phút quét bảng video trạng thái UPLOADED rồi transcode tuần tự", s: 1, why: "Bền và đơn giản (crash thì lượt quét sau nhặt lại) — mô hình poller này chạy được thật. Nhược: trễ trung bình 2.5 phút vô ích, quét bảng liên tục, tuần tự một máy không hấp thụ được đỉnh (100 video/giờ cao điểm × 15 phút = tồn 25 giờ). Là bản 'queue tự chế bằng DB' — chấp nhận được ở quy mô nhỏ, đuối ở đề bài này." },
        { t: "Transcode ngay trên instance API bằng thread pool nền sau khi nhận notify", s: 1, why: "Bất đồng bộ rồi (hơn phương án sync) nhưng thiếu CÁCH LY: transcode ăn CPU của chính máy đang phục vụ API — giờ cao điểm là latency API tăng theo số video (bài noisy neighbor). Và job nền trong process chết theo deploy/restart — mất job nếu không có cơ chế nhặt lại. Đúng hướng async, sai chỗ đặt tải." }
      ] },
    { q: "Quyết định 3 — Giảng viên cần biết 'video của tôi xong chưa?'. Cơ chế nào?",
      opts: [
        { t: "Bảng trạng thái (UPLOADED → PROCESSING → READY/FAILED) + client polling endpoint status với khoảng cách tăng dần", s: 2, why: "Đơn giản, bền, đủ: trạng thái là DỮ LIỆU (sống sót qua restart, đối soát được), polling vài giây một lần cho tác vụ tính-bằng-phút là chi phí không đáng kể, hoạt động mọi client. Với UX 'chờ 5–30 phút', realtime từng giây không mua thêm giá trị gì — chọn công cụ đơn giản nhất đạt yêu cầu là kỹ năng, không phải lười." },
        { t: "Giữ nguyên kết nối HTTP của upload mở cho tới khi transcode xong", s: 0, why: "Lặp lại lỗi của quyết định 2 dưới lốt khác: connection sống 30 phút xuyên qua LB/proxy/mobile network là ảo tưởng — sẽ bị cắt, và client không biết job vẫn chạy hay đã chết. Trạng thái của việc-chạy-lâu phải nằm ở server-side data, không nằm trong một kết nối." },
        { t: "WebSocket/SSE push trạng thái realtime cho client", s: 1, why: "UX mượt hơn polling và ĐÚNG khi cần realtime thật (giá cổ phiếu, chat, tiến độ % từng giây). Ở đây trả giá hạ tầng (quản kết nối, reconnect, scale-out sticky/pubsub) cho một sự kiện xảy ra MỘT LẦN sau nhiều phút — over-engineering kinh điển. Nâng cấp sau được nếu sản phẩm cần hiển thị % tiến độ." },
        { t: "Gửi email khi xong, không cần trạng thái trong app", s: 1, why: "Email là kênh BỔ SUNG tốt (user đóng tab vẫn biết) — nhưng làm kênh DUY NHẤT thì dở: vào lại app không thấy trạng thái đâu, email trễ/spam là mù hoàn toàn, và hệ vẫn cần bảng trạng thái nội bộ để vận hành/đối soát. Kết luận: bảng trạng thái là lõi bắt buộc, email là topping." }
      ] },
    { q: "Quyết định 4 — Mạng rớt giữa file 2GB + upload phải an toàn. Chốt những gì?",
      opts: [
        { t: "Multipart upload (chia part ~10MB, rớt part nào up lại part đó) + validate magic bytes/size + quét virus & transcode là hàng rào sau upload", s: 2, why: "Multipart giải đúng nỗi đau số 1 (resume theo part — rớt mạng ở 90% không phải làm lại từ 0; S3 hỗ trợ sẵn, presigned từng part). Validate không tin Content-Type client khai — đọc magic bytes; size chặn từ presign policy. Quét virus/độc hại chạy ASYNC sau upload (file 2GB quét sync là treo) — file chỉ chuyển READY khi qua hết cổng. Phòng thủ nhiều lớp nhưng lớp nào cũng đặt đúng thời điểm rẻ nhất." },
        { t: "Tin Content-Type client gửi lên, chỉ chặn danh sách đuôi file nguy hiểm (.exe, .sh)", s: 0, why: "Cả hai đều là dữ liệu do CLIENT tự khai — đổi tên virus.exe thành bai-giang.mp4 mất 2 giây. Kiểm tra bảo mật dựa trên lời tự khai của đối tượng bị kiểm tra là không kiểm tra. Nội dung thật nằm trong bytes (magic bytes/probe bằng ffprobe) — kiểm cái đó." },
        { t: "Quét virus đồng bộ toàn bộ file TRƯỚC khi cho ghi vào storage", s: 1, why: "Ý thức bảo mật đúng, vị trí sai: quét 2GB mất nhiều phút — làm sync là quay lại request-sống-30-phút. Kiến trúc lành: nhận vào khu 'quarantine' (chưa ai xem được) → quét async → đạt thì mới publish. An toàn tương đương, không treo ai." },
        { t: "Không cần resume — hiện progress bar và dặn user giữ mạng ổn định", s: 0, why: "'Dặn user giữ mạng' không phải cơ chế kỹ thuật. Đề bài đã nói thẳng mạng rớt là chuyện thường — bỏ resume là chọn tỉ lệ upload fail cao + user điên tiết làm đặc tính sản phẩm. Yêu cầu phi chức năng bị nêu đích danh mà thiết kế lờ đi là fail design review từ vòng đọc đề." }
      ] }
  ],
  debrief: "<p><b>Kiến trúc tham chiếu:</b></p><pre>Client ──① xin phép──▶ API: tạo record (PENDING) + presigned multipart URLs\n  │\n  └──② upload từng part thẳng lên Object Storage (rớt part nào up lại part đó)\n            │ ③ complete → storage event / client confirm\n            ▼\n     API đổi trạng thái UPLOADED ──▶ queue transcode ──▶ worker farm (autoscale theo queue)\n            │                              │ quarantine → scan → transcode → thumbnail\n            ▼                              ▼\n     client polling /status …      READY (CDN url) hoặc FAILED (+lý do)</pre><p><b>Các mảnh thường bị quên:</b> dọn rác upload dở (multipart bỏ ngang chiếm chỗ — lifecycle rule tự huỷ sau N ngày); idempotency khi client retry bước 'complete'; giới hạn số upload đồng thời mỗi user (chống lạm dụng presign); và phát video qua <b>CDN</b> chứ không stream từ storage gốc.</p><p><b>Nếu yêu cầu đổi:</b> user toàn cầu → multipart upload qua transfer acceleration/edge; cần xem NGAY khi đang xử lý → transcode ra HLS theo segment, cho xem phần đã xong (đổi độ phức tạp lấy time-to-first-view); ảnh nhỏ &lt;5MB chiếm 90% lượng upload → đường riêng đơn giản (single PUT presign, xử lý sync nhẹ) — <em>đừng bắt ảnh selfie đi chung pipeline của video 2GB</em>: phân luồng theo đặc tính tải cũng là thiết kế.</p>",
  refs: [["AWS – Multipart upload", "https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html"], ["AWS – Presigned URLs", "https://docs.aws.amazon.com/AmazonS3/latest/userguide/PresignedUrlUploadObject.html"]]
});

DESIGNS.register({
  id: "dsg-url-shortener", lv: "middle", domain: "System Design / Read-heavy", skills: ["des-sysdes", "cache-patterns", "data-scale"], time: "15–20 phút",
  title: "Thiết kế URL shortener (kiểu bit.ly)",
  teaser: "Bài mở màn kinh điển của mọi vòng system design — trông đơn giản, nhưng 4 quyết định bên dưới tách người học thuộc khỏi người hiểu: sinh mã, đường đọc, ngữ nghĩa redirect, và đếm click.",
  brief: "<p>Xây dịch vụ rút gọn link cho nền tảng social/marketing:</p><ul><li>Hiện có ~<b>100 triệu link</b>, tạo mới ~<b>50 link/giây</b>; redirect ~<b>5.000 req/giây</b>, đỉnh viral <b>20.000 req/giây</b> (tỉ lệ đọc:ghi ~100:1).</li><li>Mã rút gọn <b>≤ 8 ký tự</b> (URL càng ngắn càng có giá trị).</li><li>Redirect p99 <b>&lt; 20ms</b> (chưa tính network của user).</li><li>Cần <b>đếm click theo link theo ngày</b> cho dashboard marketing — số liệu trễ vài phút chấp nhận được.</li><li>Link tạo xong <b>không đổi đích</b> (trừ tính năng admin hiếm khi dùng), không xoá hàng loạt.</li></ul>",
  steps: [
    { q: "Quyết định 1 — Sinh mã rút gọn (short code) bằng cách nào?",
      opts: [
        { t: "Bộ đếm tăng dần (DB sequence / Redis INCR, cấp theo DẢI cho từng instance) rồi encode base62 — 'abc123' là số 62-tiến-phân đội lốt chữ", s: 2, why: "Không bao giờ đụng độ (mỗi số một mã — khỏi check tồn tại), mã ngắn tối ưu (62⁷ ≈ 3.5 nghìn tỉ — 7 ký tự dùng cả trăm năm), và cấp theo dải (instance A giữ 1–100k, B giữ 100k–200k) để không chèn ép một bộ đếm trung tâm mỗi request. Trade-off phải NÓI RA khi phỏng vấn: mã đoán được một phần (tuần tự) → trộn thêm bước xáo (bijective shuffle) hoặc chấp nhận nếu link không nhạy cảm." },
        { t: "Hash URL gốc (SHA-256) lấy 7 ký tự đầu làm mã", s: 1, why: "Có điểm cộng riêng: cùng URL cho cùng mã (khử trùng lặp tự nhiên — tiết kiệm storage khi user rút gọn lặp lại). Nhưng cắt hash ngắn là mời birthday paradox: ở trăm triệu link, xác suất đụng độ thành chuyện phải xử lý (check tồn tại + salt re-hash khi trùng) — thêm một round-trip DB cho MỌI lần tạo. Hợp lệ nếu dedup là yêu cầu quan trọng; ở đề bài này (50 write/s, không nhắc dedup) là mua phức tạp không ai đặt." },
        { t: "Random 7 ký tự, check DB nếu trùng thì random lại", s: 1, why: "Chạy được và mã không đoán được (điểm cộng bảo mật). Giá: mỗi lần tạo cần ít nhất một lượt check-tồn-tại; khi không gian mã đầy dần, số lần 'trúng mã đã dùng' tăng — tạo link chậm dần theo thời gian một cách âm thầm. So với counter+base62 (không cần check gì), bạn trả thêm IO để mua tính ngẫu nhiên — chỉ đáng khi 'không đoán được' là yêu cầu thật." },
        { t: "Dùng thẳng ID tự tăng làm mã: short.ly/10482913", s: 0, why: "Ba vấn đề: (1) mã dài dần theo số lượng (10 chữ số ở tỉ link — phá yêu cầu ≤8 ký tự trong khi base62 chỉ cần 6); (2) LỘ business metrics — đối thủ tạo 2 link cách nhau 1 ngày là biết bạn tạo bao nhiêu link/ngày; (3) crawl được toàn bộ link chỉ bằng vòng for tăng dần — nếu có link 'bán riêng tư' là lộ sạch. Encode base62 gần như miễn phí mà chặn được (1); shuffle chặn thêm (2)(3) — không có lý do bỏ qua." }
      ] },
    { q: "Quyết định 2 — Đường ĐỌC (redirect) chịu 5–20k req/s với p99 < 20ms. Tra mã → URL thế nào?",
      opts: [
        { t: "Cache-aside Redis trước DB: hit trả ngay (~0.5ms), miss thì đọc DB rồi nạp cache TTL dài + NEGATIVE cache ngắn cho mã không tồn tại", s: 2, why: "Dữ liệu mapping code→URL là BẤT BIẾN (đề bài cho sẵn) — giấc mơ của cache: không lo invalidation, TTL đặt hàng ngày/tuần thoải mái, hit rate rất cao vì traffic dồn vào link mới/viral (working set nhỏ so với 100M). Negative cache (lưu 'mã này KHÔNG tồn tại' TTL ngắn) chặn kẻ quét mã rác đấm thẳng DB — cache penetration đã học. DB vẫn là source of truth, Redis chết thì chậm chứ không sai." },
        { t: "Chỉ DB với index/PK trên cột code — khỏi cache cho đỡ một tầng", s: 1, why: "Đơn giản là một đức tính, và PK lookup của Postgres/MySQL đủ nhanh (~1-5ms) cho 5k req/s nếu connection pool khoẻ. Nhưng đỉnh viral 20k req/s dồn vào VÀI link nóng — chính xác kịch bản cache giải rẻ nhất mà DB chịu đắt nhất (connection cạn, latency đuôi phình). Bỏ cache ở hệ đọc:ghi 100:1 với data bất biến là bỏ điểm dễ nhất của đề." },
        { t: "Nạp toàn bộ mapping vào HashMap trong memory mỗi instance lúc khởi động", s: 0, why: "100M entry × (mã + URL trung bình ~100 byte) ≈ chục GB — không vừa heap, GC khóc, cold start tính bằng phút, và link tạo MỚI sau lúc nạp thì instance không biết (phải thêm cơ chế sync — bạn đang tự chế một cache phân tán kém). 'Cache all' chỉ hợp khi dataset nhỏ và đóng; đề bài này cần cache CHỌN LỌC theo độ nóng — việc của Redis + TTL." },
        { t: "Đọc DB và UPDATE click_count = click_count + 1 trong cùng transaction cho tiện đếm luôn", s: 0, why: "Bạn vừa trói đường ĐỌC nóng nhất hệ vào một thao tác GHI: 20k UPDATE/s lên vài dòng viral = lock contention trên hot row, redirect xếp hàng sau nhau — p99 20ms thành mơ. Đếm click là bài toán KHÁC với ngân sách KHÁC (trễ vài phút được!) — quyết định 4 xử nó riêng. Trộn hai bài toán khác ngân sách vào một transaction là lỗi thiết kế gốc." }
      ] },
    { q: "Quyết định 3 — Redirect trả HTTP status nào: 301 hay 302?",
      opts: [
        { t: "302/307 (temporary): browser KHÔNG cache vĩnh viễn — mọi click đều quay về server, giữ được đếm click và khả năng admin đổi/khoá đích", s: 2, why: "Nhìn ngữ nghĩa thuần thì 'link rút gọn trỏ cố định' giống 301 — nhưng nghiệp vụ của bạn SỐNG bằng analytics (yêu cầu đếm click) và cần khoá link xấu. 301 bị browser cache vĩnh viễn: từ click thứ hai, browser đi thẳng đích — server mù hoàn toàn, và link malware đã phát tán thì KHÔNG THU HỒI ĐƯỢC với user cũ. Chọn status code ở đây là quyết định kinh doanh đội lốt kỹ thuật — bit.ly, TinyURL đều dùng 301/302 có cân nhắc, và các dịch vụ cần analytics chọn 302. (Nói được trade-off tải: 302 = server ăn mọi click — đã có cache ở quyết định 2 gánh.)" },
        { t: "301 (permanent): đúng ngữ nghĩa 'chuyển hướng vĩnh viễn', browser cache giúp giảm hẳn tải server", s: 1, why: "Lập luận ngữ nghĩa + giảm tải là ĐÚNG — và nếu đề bài KHÔNG có yêu cầu analytics thì 301 thắng. Nhưng đề ghi rõ 'đếm click cho dashboard marketing': 301 làm mất dấu từ lượt ghé thứ hai của mỗi browser — số liệu thủng không sửa được (cache của user, bạn không xoá được). Chọn phương án phá một yêu cầu được ghi tường minh = trừ điểm đọc đề, dù kiến thức HTTP đúng." },
        { t: "Trả 200 + trang HTML có meta-refresh/JavaScript tự chuyển trang", s: 0, why: "Chậm hơn hẳn (parse HTML rồi mới nhảy), phá chuỗi referrer, SEO coi là nội dung mỏng, client không phải browser (curl, app, bot hợp lệ) đứng hình. Kỹ thuật này là tàn dư thời chưa hiểu HTTP — redirect là việc của status code 3xx, đừng bắt HTML làm việc của giao thức." },
        { t: "Trả JSON {\"url\": \"...\"} để client tự xử lý chuyển hướng", s: 0, why: "Link rút gọn được share vào tin nhắn/email/QR — người mở bằng BROWSER TRẦN, và browser không tự hiểu JSON của bạn: user nhìn thấy một cục text thay vì tới đích. API-first chỉ đúng khi bạn kiểm soát client; sản phẩm này bản chất là dịch vụ cho browser công cộng. Nhầm bối cảnh sử dụng là lỗi nặng hơn nhầm cú pháp." }
      ] },
    { q: "Quyết định 4 — Đếm click theo link theo ngày (trễ vài phút OK) mà không làm chậm redirect. Kiến trúc ghi nhận?",
      opts: [
        { t: "Fire-and-forget: redirect trả 302 NGAY, đồng thời bắn event click (async) vào queue/log — consumer gộp theo (link, ngày) rồi ghi bảng aggregate định kỳ", s: 2, why: "Ngân sách hai bài toán khác nhau được tôn trọng tuyệt đối: đường redirect không chờ BẤT KỲ thao tác đếm nào (mất event lẻ tẻ khi crash? — đề cho phép, số marketing không phải sổ cái tiền). Consumer gộp 20k event/s thành vài trăm UPDATE aggregate/phút — DB nhẹ tênh. Đây là mẫu chuẩn 'tách hot path khỏi analytics' dùng được ở mọi hệ đọc nhiều." },
        { t: "Redis INCR key link:ngày ngay trong đường redirect (sub-ms), job định kỳ quét flush xuống DB", s: 1, why: "Latency ổn (INCR ~0.2ms, trong ngân sách) và đơn giản hơn queue — phương án hoàn toàn dùng được ở quy mô này. Điểm trừ so với queue: thêm một lệnh ĐỒNG BỘ vào hot path (Redis chập chờn là redirect chậm theo — cần timeout ngắn + bỏ qua lỗi đếm), và mất trắng số đếm giữa hai lần flush nếu Redis restart (đề chấp nhận được). Trả lời này + nói rõ hai điểm trừ = vẫn là câu trả lời tốt; im lặng về chúng mới là vấn đề." },
        { t: "INSERT một dòng vào bảng click_events trong DB, đồng bộ, mỗi redirect", s: 0, why: "20k INSERT/s đồng bộ trong hot path: redirect đứng chờ DB (p99 20ms tan vỡ ngay đỉnh viral), bảng events phình hàng tỉ dòng/tháng đè backup và vacuum. Vi phạm cùng lúc hai ngân sách (latency của redirect, throughput của DB) để mua độ chính xác từng-event mà dashboard marketing KHÔNG yêu cầu." },
        { t: "Bỏ đếm ở server — nhúng Google Analytics vào trang đích là có số liệu", s: 0, why: "Trang ĐÍCH không thuộc quyền bạn (link rút gọn trỏ đi khắp nơi) — không nhúng gì được vào đó; GA của chủ trang đích (nếu có) không chia số cho bạn theo link rút gọn. Phương án này không sai kỹ thuật — nó bất khả thi về QUYỀN SỞ HỮU: dữ liệu click chỉ tồn tại ở điểm duy nhất bạn kiểm soát — chính cú redirect. Bỏ nó là bỏ yêu cầu." }
      ] }
  ],
  debrief: "<p><b>Kiến trúc tham chiếu:</b></p><pre>Tạo link:  API ──▶ cấp dải counter ──▶ base62(id) ──▶ INSERT (code, url, owner, ts)\n\nRedirect:  GET /{code}\n           ├─ Redis GET code        ── hit  → 302 Location: url  (~1ms)\n           │                        ── miss → DB PK lookup → SET cache (TTL dài)\n           │                        ── không tồn tại → negative cache TTL ngắn → 404\n           └─ bắn event {code, ts} async ──▶ queue ──▶ aggregator ──▶ bảng clicks(code, ngày, count)</pre><p><b>Các mảnh thường bị quên:</b> chống abuse (rate limit tạo link theo user/IP — chính là đề dsg-rate-limiter; quét URL độc hại trước khi phát hành + trang cảnh báo khi link bị báo xấu); custom alias (nhánh riêng: check tồn tại + reserve từ khoá nhạy cảm); và <em>bài toán này gần như stateless theo code</em> — scale ngang thoải mái vì mọi quyết định đặt state đúng chỗ (counter theo dải, cache, aggregate).</p><p><b>Nếu yêu cầu đổi:</b> scale đọc ×10 nữa (200k/s toàn cầu) → CDN/edge cache đứng trước (redirect cache được ở edge với TTL ngắn!) + replica đọc theo region — dữ liệu bất biến nên nhân bản thoải mái, không lo consistency; DB một node hết chứa nổi 100M→10 tỉ link → shard theo code (hash/range trên chính short code — key tự nhiên, không bao giờ query xuyên link; xem bài <a href='67-consensus-consistent-hashing.html'>#67 consistent hashing</a> để thêm node không phải xáo lại toàn bộ); link có HẠN DÙNG (campaign 30 ngày) → thêm cột expire + lazy check lúc redirect + job dọn — đừng tin mỗi TTL của cache, nguồn sự thật là DB.</p>",
  refs: [["System Design Primer – Pastebin/URL shortener", "https://github.com/donnemartin/system-design-primer"], ["Stripe – Rate limiters (chống abuse tạo link)", "https://stripe.com/blog/rate-limiters"], ["Cloudflare – Counting things", "https://blog.cloudflare.com/counting-things-a-lot-of-different-things/"]]
});
