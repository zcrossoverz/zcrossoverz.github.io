/* INCIDENTS — Messaging & Kafka. Schema: xem i1. */

INCIDENTS.register({
  id: "inc-duplicate-message", sev: "P2", domain: "Messaging / Kafka", skills: ["msg-delivery", "resil-idem"], time: "20 phút",
  title: "Khách được cộng điểm thưởng 2 lần — 'mà code em đâu có bug'",
  teaser: "Consumer xử lý xong thì crash trước khi commit offset. Kafka làm đúng phận sự: giao lại. Ai chịu trách nhiệm cho 2 dòng điểm thưởng?",
  alert: "<b>[CSKH escalate]</b> 10:40 — 37 khách báo được cộng điểm thưởng ×2 cho đơn hàng sáng nay. Đối chiếu: các đơn này đều nằm trong khung <b>09:58–10:02</b>, trùng thời điểm <code>loyalty-consumer</code> có một đợt <b>rolling deploy</b>.",
  artifacts: [
    { label: "📜 Log consumer (pod cũ, trước khi bị kill)", body: "<pre>09:59:41 INFO  poll() → 48 records (partition 2, offset 15200-15247)\n09:59:52 INFO  processed offset 15200..15238  (đã INSERT điểm cho 39 đơn)\n<span class='hl'>09:59:53 INFO  Received SIGTERM (rolling deploy), shutting down...\n09:59:53 WARN  Consumer closed BEFORE commitSync() — last committed offset: 15199</span>\n\n--- pod mới ---\n10:00:11 INFO  partition 2 assigned (rebalance), resuming from <span class='hl'>offset 15200</span>\n10:00:14 INFO  processed offset 15200..15247   ← <span class='hl'>15200-15238 xử lý LẦN HAI</span></pre>" },
    { label: "🗄 Bảng loyalty_points", body: "<pre>SELECT order_id, points, created_at FROM loyalty_points\nWHERE order_id = 'ORD-88123';\n\n order_id   | points | created_at\n------------+--------+--------------------\n ORD-88123  |  150   | 09:59:47.112\n <span class='hl'>ORD-88123  |  150   | 10:00:12.508</span>   ← dòng thứ hai, sau rebalance\n\n-- Schema hiện tại: id BIGSERIAL PK, KHÔNG có unique constraint nào trên order_id</pre>" },
    { label: "📄 Code consumer", body: "<pre>@KafkaListener(topics = \"order-completed\")\npublic void onOrderCompleted(OrderEvent evt) {\n    int points = calc(evt.getTotal());\n    <span class='hl'>pointsRepo.insert(evt.getOrderId(), points);</span>   // insert trần — gọi 2 lần là 2 dòng\n    notifyUser(evt.getUserId(), points);\n}\n// enable.auto.commit=false; commit thủ công theo batch sau khi xử lý xong\n// (crash giữa 'xử lý xong' và 'commit' → giao lại: at-least-once)</pre>" }
  ],
  steps: [
    { q: "Đọc log: pod cũ xử lý tới offset 15238 nhưng offset đã commit là 15199, pod mới resume từ 15200. Đây là hành vi gì của Kafka?",
      opts: [
        { t: "Hành vi ĐÚNG của at-least-once: với broker, 'đã xử lý' = 'offset đã commit'; chưa commit thì giao lại — trách nhiệm chống trùng thuộc về consumer", s: 2, why: "Điểm mấu chốt: Kafka không hề sai và không thể biết bạn 'đã insert DB xong'. Cửa sổ giữa side-effect và commit offset là nơi at-least-once đẻ ra duplicate — theo THIẾT KẾ, không phải theo lỗi." },
        { t: "Bug của rolling deploy: SIGTERM lẽ ra phải chờ consumer commit xong", s: 1, why: "Graceful shutdown tử tế (chờ xử lý + commit trong grace period) LÀM HẸP cửa sổ này đáng kể — quan sát tốt, đáng làm. Nhưng không đóng kín được: crash cứng, OOMKilled, mất mạng vẫn tạo đúng kịch bản cũ. Không được phép dựa vào 'shutdown luôn êm'." },
        { t: "Offset bị hỏng do rebalance — cần reset lại consumer group", s: 0, why: "Offset 15199 là con số ĐÚNG (lần commit thành công cuối). Reset offset còn gây xử lý lại nhiều hơn hoặc mất message. Chẩn 'dữ liệu hỏng' cho một hệ đang hành xử đúng spec là hướng nguy hiểm." },
        { t: "Hai pod cùng đọc một partition trong lúc deploy nên xử lý trùng", s: 0, why: "Trong một consumer group, một partition chỉ gán cho MỘT consumer tại một thời điểm — rebalance chuyển quyền chứ không nhân đôi. Log cũng cho thấy tuần tự: pod cũ dừng 09:59:53, pod mới nhận 10:00:11." }
      ] },
    { q: "Vì sao 2 dòng điểm thưởng vào được database êm ru, không lỗi nào?",
      opts: [
        { t: "Bảng không có ràng buộc duy nhất trên order_id — INSERT trần lần hai hợp lệ về mặt schema; DB chỉ giữ được luật mà bạn đã KHAI với nó", s: 2, why: "Điểm chốt tầng dữ liệu: idempotency phải được VẬT CHẤT HOÁ thành constraint. Không có unique thì mọi phòng thủ nằm ở code — mà code, như vừa thấy, có thể chạy hai lần." },
        { t: "Vì transaction của lần hai không biết gì về lần một", s: 1, why: "Mô tả hiện tượng đúng (hai transaction độc lập, cách nhau 25s) nhưng chưa chạm nguyên nhân sửa được: chúng ĐƯỢC PHÉP cùng thành công vì thiếu ràng buộc duy nhất. Diễn đạt lại theo hướng 'thiếu constraint' mới ra hành động." },
        { t: "Do dùng BIGSERIAL làm primary key nên không chặn được trùng", s: 0, why: "PK surrogate không có lỗi — nó làm việc của nó. Thiếu là một UNIQUE BỔ SUNG trên khoá nghiệp vụ (order_id). Hiểu 'PK kỹ thuật' và 'khoá nghiệp vụ' là hai tầng khác nhau rất quan trọng khi thiết kế bảng." },
        { t: "Isolation level quá thấp, nâng lên SERIALIZABLE sẽ chặn", s: 0, why: "Hai INSERT cách nhau 25 giây — không hề chồng lấn thời gian. Isolation điều phối các transaction ĐỒNG THỜI; nó không có ký ức chặn một insert 'giống insert hôm qua'. Chỉ constraint mới nhớ giùm bạn." }
      ] }
  ],
  cause: { q: "Nguyên nhân gốc của điểm thưởng ×2?",
    opts: [
      { t: "Consumer KHÔNG idempotent (insert trần) trong một hệ giao hàng at-least-once — redelivery hợp lệ sau rebalance gặp side-effect không chống trùng, và schema thiếu unique constraint làm lưới cuối", ok: true, why: "Công thức sự cố: at-least-once (bản chất) + side-effect không idempotent (lỗi thiết kế) = duplicate lộ ra bất cứ khi nào có crash/rebalance/retry. Deploy chỉ là dịp — không sửa gốc thì network blip tuần sau lại 37 khách nữa." },
      { t: "Rolling deploy sai quy trình — cấm deploy giờ cao điểm là hết", ok: false, why: "Deploy là sự kiện HỢP LỆ xảy ra hằng ngày (cùng họ với crash, OOM, rebalance tự nhiên). Hệ phải đúng XUYÊN QUA các sự kiện đó. Né deploy giờ cao điểm là kiêng gió trong khi cửa sổ thủng." },
      { t: "Kafka cấu hình sai — bật exactly-once là xong chuyện", ok: false, why: "EOS của Kafka bao trọn chuỗi Kafka→Kafka (transactions, đọc-xử lý-ghi trong Kafka). Side-effect ra NGOÀI (INSERT Postgres, gửi notification) nằm ngoài phạm vi bảo đảm đó. Với sink ngoài, chuẩn vẫn là at-least-once + idempotent consumer." },
      { t: "Thiếu message key nên Kafka không tự khử trùng được", ok: false, why: "Key quyết định message vào PARTITION nào (ordering), không phải cơ chế dedup. Broker không khử trùng cho consumer — gán thêm chức năng tưởng tượng cho key là hiểu sai vai các mảnh ghép." }
    ] },
  fix: { q: "Sửa để 'chạy hai lần vẫn đúng'?",
    opts: [
      { t: "Hai lớp: (1) UNIQUE constraint trên order_id (hoặc bảng processed_message theo message id) + INSERT ... ON CONFLICT DO NOTHING / bắt DuplicateKeyException bỏ qua êm; (2) notifyUser cũng phải sau chốt dedup — rồi mới commit offset", s: 2, why: "Constraint là trọng tài tuyệt đối (kể cả 2 instance chạy song song), code xử lý conflict là đường êm cho lần chạy thứ hai. Nhớ luật vàng: cứ tưởng tượng message NÀO cũng có thể đến 2 lần — code phải đúng trong thế giới đó." },
      { t: "Kiểm tra SELECT trước khi INSERT: có rồi thì thôi", s: 1, why: "Chặn được ca redelivery cách nhau 25s, nhưng check-then-insert là race khi hai consumer/hai retry chạy SÁT nhau (cùng SELECT thấy trống, cùng INSERT). Đi được 80% đường — 20% còn lại vẫn phải là unique constraint; vậy sao không đặt constraint ngay từ đầu." },
      { t: "Chuyển sang commit offset TRƯỚC khi xử lý (at-most-once)", s: 0, why: "Hết trùng thật — đổi bằng MẤT: crash sau commit trước khi insert là điểm thưởng của khách bay màu vĩnh viễn, không dấu vết. Với nghiệp vụ tiền/điểm, mất im lặng tệ hơn trùng có thể phát hiện. Trade-off sai chiều." },
      { t: "Thêm Redis SETNX theo order_id để đánh dấu đã xử lý", s: 1, why: "Hoạt động trong đa số ca, nhưng đưa thêm một hệ thống (Redis) vào chuỗi đúng-đắn: Redis restart/evict key là thủng, và dấu-đã-xử-lý với INSERT nằm ở HAI nơi không nguyên tử. Khi sự thật cuối nằm ở Postgres, hãy để Postgres làm trọng tài." }
    ] },
  debrief: "<p><b>Chuỗi nhân quả:</b> deploy gửi SIGTERM giữa batch → consumer chết sau khi insert 39 đơn nhưng TRƯỚC commitSync → offset ghi nhận vẫn là 15199 → pod mới resume 15200, xử lý lại 39 đơn → insert trần không ai chặn → 37 khách ×2 điểm (2 khách may mắn: notify fail nên transaction rollback — sự ngẫu nhiên không phải chiến lược).</p><p><b>Ba bài học mang đi:</b> (1) <em>At-least-once là mặc định của thế giới thật</em> — mọi consumer phải viết như thể message sẽ đến ít nhất hai lần. (2) Idempotency phải được <em>vật chất hoá bằng unique constraint</em> — code check được, nhưng DB mới là trọng tài không ngủ quên. (3) Sự cố kiểu này <em>điều tra bằng đối chiếu offset log với timeline deploy</em> — học cách đọc 'last committed offset' là kỹ năng on-call messaging cơ bản.</p>",
  prove: "<p>Tái hiện không cần Kafka thật (~15 phút):</p><pre>-- 1. Tao bang:  CREATE TABLE pts(id bigserial PK, order_id text, points int);\n-- 2. Viet ham xuLy(orderId): INSERT INTO pts(order_id, points) VALUES (?, 150);\n-- 3. Goi xuLy('ORD-1') HAI lan (gia lap redelivery) → SELECT: 2 dong. Bug tai hien.\n-- 4. Sua:  ALTER TABLE pts ADD CONSTRAINT uq_order UNIQUE (order_id);\n--          INSERT ... ON CONFLICT (order_id) DO NOTHING;\n-- 5. Goi lai 2 lan → 1 dong. Chay 2 luong song song goi cung luc → van 1 dong.</pre><p><b>Cách xác minh:</b> bước 3 ra 2 dòng (lỗi), bước 5 luôn ra đúng 1 dòng kể cả gọi đồng thời — chứng minh constraint thắng cả race mà SELECT-trước-INSERT thua. Có Kafka local thì làm bản full: kill -9 consumer giữa batch và xem offset resume y như log sự cố.</p>",
  refs: [["Kafka – delivery semantics", "https://kafka.apache.org/documentation/#semantics"], ["PostgreSQL – INSERT ON CONFLICT", "https://www.postgresql.org/docs/current/sql-insert.html#SQL-ON-CONFLICT"]]
});
