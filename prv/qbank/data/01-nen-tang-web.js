/* ============================================================
   QBANK DATA — Nền tảng & Web   (prefix id: web-)
   Trạng thái: REWRITTEN — đáp án viết bằng ngôn ngữ đơn giản cho junior,
   giải thích thuật ngữ cặn kẽ, không giả định biết trước.
   Thêm câu mới: copy 1 khối { ... }, dán ngay TRƯỚC dòng đánh dấu DATA_END
   (gần cuối file), đổi id tăng dần (web-025, web-026...).
   Schema + quy tắc viết đáp án: xem qbank/GUIDE.md
   ============================================================ */
QB.register({
  group: "Nền tảng & Web",
  order: 1,
  prefix: "web",
  status: "rewritten",
  questions: [
/* DATA_START */

{ id:"web-001", legacy:1, t:"Web & HTTP", lv:"junior", core:true,
  tags:["http","stateless","giao-thuc"],
  q:"HTTP là gì? Vì sao người ta nói HTTP là giao thức <em>stateless</em>?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Giao thức (protocol)</strong> = bộ quy tắc mà hai máy tính thống nhất trước để nói chuyện được với nhau — giống như hai người phải nói cùng một ngôn ngữ. <strong>Stateless</strong> = không trạng thái, tức là không ghi nhớ gì giữa các lần nói chuyện.</div><p><strong>HTTP</strong> (HyperText Transfer Protocol) là bộ quy tắc để trình duyệt/app (gọi là <strong>client</strong>) nói chuyện với máy chủ (<strong>server</strong>). Cuộc nói chuyện luôn theo một khuôn: client gửi một <strong>request</strong> (yêu cầu), server trả một <strong>response</strong> (phản hồi). Request gồm: hành động muốn làm (method như GET/POST), địa chỉ tài nguyên (URL), thông tin kèm theo (headers) và có thể có dữ liệu (body). Response gồm: mã kết quả (status code như 200, 404), headers và dữ liệu trả về.</p><p><strong>Stateless</strong> nghĩa là: xử lý xong một request, server <em>quên luôn</em>, không tự nhớ bạn là ai ở request trước. Hai request liên tiếp của cùng một người, với server, là hai người lạ. Vì sao thiết kế vậy? Để server <em>đơn giản và dễ nhân bản</em>: không phải nhớ gì thì request nào rơi vào server nào trong cụm cũng xử lý được — muốn chịu tải gấp đôi chỉ cần thêm máy.</p><p>Cái giá phải trả: muốn server biết 'vẫn là tôi đây' thì <em>mỗi request phải tự mang theo giấy tờ</em> — đó là lý do cookie, session, token tồn tại (xem câu về cookie/session/token).</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gọi lên tổng đài, mỗi lần gặp một nhân viên khác nhau và họ không biết bạn đã gọi hôm qua. Muốn được phục vụ tiếp, bạn phải đọc lại mã khách hàng mỗi cuộc gọi — mã đó chính là cookie/token.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> Đừng trả lời 'stateless nghĩa là web không lưu được trạng thái'. Ứng dụng vẫn nhớ được người dùng (qua cookie, session, database) — chỉ là <em>bản thân giao thức HTTP</em> không nhớ giúp, app phải tự lo.</div>",
  refs:[["MDN – HTTP overview","https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview"]] },

{ id:"web-002", t:"Web & HTTP", lv:"junior", core:true,
  tags:["dns","tcp","tls","tong-quan"],
  q:"Gõ <code>google.com</code> vào trình duyệt và nhấn Enter — chuyện gì xảy ra? (câu hỏi kinh điển)",
  a:"<p>Câu này interviewer dùng để xem bạn hiểu <em>bức tranh tổng thể</em> đến đâu. Trả lời theo trình tự sau, mỗi bước một câu là đủ:</p><ol><li><strong>Tìm địa chỉ (DNS):</strong> máy tính chỉ gửi dữ liệu được tới địa chỉ IP dạng số (vd <code>142.250.4.100</code>), không gửi tới 'google.com' được. Nên việc đầu tiên là tra sổ danh bạ DNS: tên miền → IP. (Chi tiết ở câu DNS.)</li><li><strong>Mở đường dây (TCP):</strong> có IP rồi, trình duyệt 'bắt tay' với server qua giao thức TCP — hai bên chào hỏi 3 bước (three-way handshake) để chắc chắn đường truyền hai chiều hoạt động.</li><li><strong>Niêm phong đường dây (TLS):</strong> vì là <code>https</code>, hai bên thoả thuận khoá mã hoá để không ai đọc lén được. Server trình 'chứng minh thư' (certificate) để chứng minh nó đúng là google.com thật.</li><li><strong>Hỏi và đáp (HTTP):</strong> trình duyệt gửi request <code>GET /</code>. Ở phía server, request thường đi qua reverse proxy (nginx) → ứng dụng backend → có thể hỏi thêm database → trả về response HTML.</li><li><strong>Vẽ trang (render):</strong> trình duyệt đọc HTML, gặp thẻ nào cần CSS/JS/ảnh thì gửi tiếp các request khác để tải, rồi dựng trang lên màn hình.</li></ol><div class='qb-eli5'><b>🌱 Nhớ bằng hình ảnh:</b> Gửi thư cho người lạ: tra địa chỉ nhà (DNS) → gọi điện xác nhận có người nhận (TCP) → thống nhất viết bằng mật mã riêng (TLS) → gửi thư và chờ hồi âm (HTTP) → đọc thư (render).</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> Lỗi hay gặp là sa đà vào một bước (kể 10 phút về DNS) mà quên bức tranh. Hãy kể đủ 5 bước trước, rồi hỏi lại: 'anh/chị muốn em đào sâu bước nào?' — vừa đúng ý vừa ghi điểm giao tiếp.</div>",
  refs:[["web.dev – What happens when you navigate to a URL","https://web.dev/articles/howbrowserswork"]] },

{ id:"web-003", t:"Web & HTTP", lv:"junior", core:true,
  tags:["dns","udp","cache"],
  q:"DNS là gì? Quá trình phân giải tên miền diễn ra thế nào?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Phân giải (resolve)</strong> = quá trình đổi tên miền thành địa chỉ IP. <strong>TTL</strong> (time-to-live) = thời hạn một kết quả tra cứu được phép lưu tạm (cache) trước khi phải tra lại.</div><p><strong>DNS</strong> (Domain Name System) là 'danh bạ điện thoại' của Internet: con người nhớ tên (<code>shopee.vn</code>), máy tính cần số (IP như <code>103.245.1.9</code>) — DNS đổi tên thành số.</p><p>Khi bạn truy cập một tên miền, máy tính tìm IP theo thứ tự <em>từ gần đến xa</em>, gặp đâu có sẵn thì dừng:</p><ol><li><strong>Cache trình duyệt</strong> — vừa tra lúc nãy thì khỏi tra lại.</li><li><strong>Cache hệ điều hành</strong>.</li><li><strong>DNS resolver</strong> (thường của nhà mạng, hoặc 8.8.8.8 của Google) — 'tổng đài tra cứu' hỏi giúp bạn.</li><li>Resolver chưa biết thì nó hỏi lần lượt: <strong>root server</strong> (cho biết ai quản đuôi <code>.vn</code>) → <strong>TLD server</strong> (cho biết ai quản <code>shopee.vn</code>) → <strong>authoritative server</strong> (nơi giữ đáp án chính thức: IP của shopee.vn).</li></ol><p>Kết quả được cache ở mọi tầng theo TTL, nên lần sau nhanh hơn nhiều.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hỏi số điện thoại của một người: xem danh bạ máy mình → chưa có thì hỏi bạn thân (resolver) → bạn thân đi hỏi tổ trưởng khu phố (root) → trưởng toà nhà (TLD) → chính chủ (authoritative), xong lưu lại để lần sau khỏi hỏi.</div><div class='qb-gotcha'><b>⚠ Hay bị hỏi thêm:</b> (1) DNS chạy chủ yếu trên <strong>UDP cổng 53</strong> — tra cứu nhỏ, cần nhanh, mất gói thì hỏi lại. (2) Vì cache nhiều tầng, khi đổi IP của tên miền, thay đổi <em>không ăn ngay lập tức</em> — phải chờ cache cũ hết TTL. Đó là lý do 'đổi DNS phải chờ'.</div>",
  refs:[["Cloudflare – What is DNS?","https://www.cloudflare.com/learning/dns/what-is-dns/"]] },

{ id:"web-004", legacy:2, t:"Web & HTTP", lv:"junior", core:true,
  tags:["http","idempotent","rest"],
  q:"Phân biệt GET, POST, PUT, PATCH, DELETE. <em>Idempotent</em> và <em>safe</em> nghĩa là gì?",
  a:"<p>Năm 'động từ' của HTTP, ví dụ trên resource đơn hàng <code>/orders</code>:</p><ul><li><strong>GET</strong> <code>/orders/42</code> — <em>đọc</em> đơn 42, không thay đổi gì.</li><li><strong>POST</strong> <code>/orders</code> — <em>tạo mới</em> một đơn (server sinh id).</li><li><strong>PUT</strong> <code>/orders/42</code> — <em>thay thế toàn bộ</em> đơn 42 bằng dữ liệu bạn gửi.</li><li><strong>PATCH</strong> <code>/orders/42</code> — <em>sửa một phần</em> (vd chỉ đổi địa chỉ giao).</li><li><strong>DELETE</strong> <code>/orders/42</code> — <em>xoá</em> đơn 42.</li></ul><div class='qb-term'><b>📖 Hai từ khoá quan trọng:</b> <strong>Safe</strong> (an toàn) = request chỉ <em>đọc</em>, không làm thay đổi dữ liệu trên server → GET là safe. <strong>Idempotent</strong> (đọc: i-đem-pô-tần) = gọi 1 lần hay gọi 10 lần thì <em>trạng thái cuối cùng của server giống nhau</em>.</div><p>Suy từ định nghĩa: GET idempotent (đọc bao nhiêu lần cũng vậy). PUT idempotent (ghi đè cùng một nội dung 10 lần → kết quả vẫn là nội dung đó). DELETE idempotent (xoá rồi, xoá lại vẫn là 'không còn'). <strong>POST thì KHÔNG</strong>: mỗi lần gọi tạo thêm một bản ghi mới — gọi 3 lần được 3 đơn hàng.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Idempotent giống nút bấm thang máy: bấm tầng 5 một lần hay dí liên tục, thang vẫn chỉ lên tầng 5. POST giống đặt thêm ly trà sữa: mỗi lần bấm là thêm một ly.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> Vì POST không idempotent, khi mạng chập chờn mà client tự động gửi lại (retry) thì dễ tạo <em>bản ghi trùng</em>. Cách chống: client gửi kèm <strong>Idempotency-Key</strong> (một mã duy nhất cho mỗi thao tác) để server nhận ra 'à, cái này xử lý rồi'. Nói được ý này là điểm cộng lớn.</div>",
  refs:[["MDN – HTTP methods","https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods"]] },

{ id:"web-005", legacy:3, t:"Web & HTTP", lv:"junior", core:true,
  tags:["http","status-code"],
  q:"Các nhóm HTTP status code (2xx, 3xx, 4xx, 5xx) nghĩa là gì? Cho vài mã hay gặp.",
  a:"<p>Status code là con số 3 chữ số server trả về để nói kết quả request. Chữ số đầu cho biết <em>chuyện gì đã xảy ra và lỗi (nếu có) thuộc về ai</em>:</p><ul><li><strong>2xx — Thành công.</strong> <code>200 OK</code> (xong, có dữ liệu trả về), <code>201 Created</code> (tạo mới thành công — chuẩn cho POST), <code>204 No Content</code> (xong nhưng không có gì để trả — hay dùng cho DELETE).</li><li><strong>3xx — Đi chỗ khác.</strong> <code>301</code> (chuyển nhà vĩnh viễn, lần sau đi thẳng địa chỉ mới), <code>302</code> (chuyển tạm thời), <code>304 Not Modified</code> (nội dung không đổi, cứ dùng bản cache của bạn).</li><li><strong>4xx — Lỗi của phía gọi (client).</strong> <code>400 Bad Request</code> (dữ liệu gửi lên sai/thiếu), <code>401 Unauthorized</code> (chưa đăng nhập hoặc token sai), <code>403 Forbidden</code> (biết bạn là ai nhưng không đủ quyền), <code>404 Not Found</code> (không có tài nguyên này), <code>409 Conflict</code> (đụng độ trạng thái, vd tạo trùng), <code>429 Too Many Requests</code> (gọi dồn dập quá, bị chặn bớt).</li><li><strong>5xx — Lỗi của server.</strong> <code>500 Internal Server Error</code> (code server văng lỗi), <code>502 Bad Gateway</code> (proxy gọi tiếp vào trong nhưng bên trong chết), <code>503 Service Unavailable</code> (quá tải/bảo trì), <code>504 Gateway Timeout</code> (bên trong xử lý quá lâu).</li></ul><div class='qb-eli5'><b>🌱 Mẹo nhớ 401 vs 403:</b> 401 = chưa qua cổng soát vé (chưa chứng minh bạn là ai). 403 = qua cổng rồi nhưng khu VIP không cho vào (biết bạn là ai, nhưng không đủ quyền).</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> API trả <code>200</code> nhưng trong body ghi <code>{\"success\": false}</code> là anti-pattern — monitoring và client không phân biệt được thành công/thất bại bằng status code nữa. Lỗi phải trả đúng mã 4xx/5xx.</div>",
  refs:[["MDN – HTTP status","https://developer.mozilla.org/en-US/docs/Web/HTTP/Status"]] },

{ id:"web-006", legacy:4, t:"Web & HTTP", lv:"junior", core:true,
  tags:["cookie","session","token","jwt"],
  q:"Cookie, Session và Token khác nhau thế nào?",
  a:"<p>Cả ba đều giải cùng một bài toán: HTTP stateless — server không tự nhớ bạn — nên phải có cách 'nhớ giùm'. Khác nhau ở chỗ <em>dữ liệu nằm ở đâu</em>.</p><p><strong>1. Cookie</strong> — nền tảng của hai cái còn lại. Là mẩu dữ liệu nhỏ server nhờ trình duyệt lưu (qua header <code>Set-Cookie</code>). Từ đó trở đi, trình duyệt <em>tự động gửi kèm</em> cookie trong mọi request tới đúng website đó — bạn không phải làm gì.</p><p><strong>2. Session</strong> — dữ liệu để ở <em>phía server</em>. Đăng nhập xong, server tạo một bản ghi 'phiên làm việc' (trong RAM/Redis/DB) chứa thông tin của bạn, rồi đưa cho trình duyệt một <em>mã số phiên</em> (session id) đựng trong cookie. Mỗi request sau, server cầm mã số này tra ngược lại bản ghi. Ưu: server toàn quyền kiểm soát (thu hồi phiên = xoá bản ghi). Nhược: server phải <em>lưu và tra</em> cho hàng triệu người dùng — nhiều server thì phải share chỗ lưu chung (vd Redis).</p><p><strong>3. Token (tiêu biểu là JWT)</strong> — dữ liệu để ở <em>phía client</em>. Server đóng gói thông tin của bạn (id, quyền, hạn dùng) vào một chuỗi, <em>ký chữ ký số</em> lên rồi đưa client giữ. Request sau client gửi kèm token (thường trong header <code>Authorization</code>); server chỉ cần <em>kiểm chữ ký</em> là tin được nội dung, khỏi tra DB. Ưu: server không phải lưu gì — scale dễ, hợp microservices/mobile. Nhược: đã phát ra thì <em>khó thu hồi từng cái</em> trước khi hết hạn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gửi xe có 2 kiểu vé. <strong>Session</strong> = vé chỉ ghi số '42', thông tin xe nằm trong sổ của bảo vệ — mất sổ là toi, nhưng bảo vệ muốn huỷ vé nào cũng được. <strong>JWT</strong> = vé ghi đầy đủ biển số + đóng mộc chống giả — bảo vệ nhìn mộc là cho ra, khỏi tra sổ, nhưng vé đã phát thì khó huỷ nửa chừng.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> 'JWT lưu ở đâu an toàn?' — để trong cookie <code>HttpOnly</code> thì JS không đọc trộm được (chống XSS) nhưng cần chống CSRF; để trong localStorage thì tiện nhưng XSS lấy được. Trả lời được trade-off này là điểm cộng.</div>",
  refs:[["MDN – Cookies","https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies"],["jwt.io – Introduction","https://jwt.io/introduction"]] },

{ id:"web-007", legacy:6, t:"Web & HTTP", lv:"junior", core:false,
  tags:["tcp","udp","network"],
  q:"TCP khác UDP thế nào? Backend hay dùng cái nào?",
  a:"<p>TCP và UDP là hai cách <em>vận chuyển</em> dữ liệu qua mạng — nằm dưới HTTP một tầng. Dữ liệu đi trên mạng bị cắt thành từng gói nhỏ (packet), và gói có thể <em>đến trễ, đến lộn thứ tự, hoặc mất dọc đường</em>. Hai giao thức này xử lý chuyện đó theo hai triết lý ngược nhau:</p><p><strong>TCP — chắc chắn trước đã.</strong> Trước khi gửi, hai bên 'bắt tay' 3 bước để mở kết nối. Mỗi gói gửi đi đều được đánh số và bên nhận phải xác nhận (ACK); gói nào mất thì gửi lại, gói đến lộn thứ tự thì xếp lại cho đúng. Kết quả: dữ liệu <em>đến đủ và đúng thứ tự</em> — đổi lại tốn thời gian chào hỏi và xác nhận.</p><p><strong>UDP — nhanh trước đã.</strong> Không bắt tay, không xác nhận, không gửi lại. Bắn gói đi là xong, mất thì thôi. Nhanh và nhẹ hơn hẳn, nhưng không hứa hẹn gì.</p><p><strong>Backend web dùng TCP gần như toàn bộ:</strong> HTTP, kết nối database, gRPC, Kafka... đều chạy trên TCP — vì mất một mẩu dữ liệu giao dịch là không chấp nhận được. UDP hợp với thứ 'mất một tí không sao nhưng phải nhanh': video call (rớt 1 khung hình chẳng ai để ý), game online, và DNS.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> TCP như gửi hàng có mã vận đơn — từng kiện được theo dõi, mất là gửi bù. UDP như phát tờ rơi — phát thật nhanh, ai hụt tờ nào thì chịu.</div><div class='qb-gotcha'><b>⚠ Câu hỏi nối tiếp hay gặp:</b> 'HTTP/3 chạy trên gì?' — trên <strong>QUIC</strong>, một giao thức xây trên UDP nhưng tự thêm lớp tin cậy riêng. Nghe ngược đời nhưng mục đích là né các hạn chế cố hữu của TCP (bắt tay chậm, nghẽn kiểu đoàn tàu).</div>",
  refs:[["Cloudflare – TCP vs UDP","https://www.cloudflare.com/learning/ddos/glossary/user-datagram-protocol-udp/"]] },

{ id:"web-008", legacy:7, t:"Web & HTTP", lv:"middle", core:false,
  tags:["https","tls","certificate","ma-hoa"],
  q:"HTTPS/TLS bảo vệ điều gì? 'Bắt tay TLS' làm gì?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Mã hoá đối xứng</strong> = hai bên dùng <em>chung một chìa khoá</em> để khoá/mở dữ liệu — nhanh, nhưng phải làm sao trao chìa cho nhau mà không bị nghe lén. <strong>Mã hoá bất đối xứng</strong> = có <em>cặp</em> khoá: khoá công khai (public, ai cũng biết) để khoá, khoá bí mật (private, chỉ chủ giữ) để mở — chậm hơn nhưng trao đổi an toàn. <strong>Certificate (chứng chỉ)</strong> = 'chứng minh thư' của server, được một tổ chức uy tín (CA — Certificate Authority) ký xác nhận.</div><p><strong>HTTPS = HTTP chạy bên trong TLS.</strong> TLS cho bạn 3 lớp bảo vệ: <strong>bí mật</strong> (kẻ đứng giữa nghe lén chỉ thấy dữ liệu loạn xạ), <strong>toàn vẹn</strong> (dữ liệu bị sửa giữa đường sẽ bị phát hiện), <strong>xác thực server</strong> (chắc chắn đang nói chuyện với shopee.vn thật chứ không phải trang giả).</p><p><strong>Bắt tay TLS</strong> (TLS handshake) — diễn ra ngay sau khi mở kết nối TCP, đơn giản hoá gồm 3 việc:</p><ol><li>Hai bên <strong>thoả thuận thuật toán</strong> mã hoá sẽ dùng.</li><li>Server <strong>trình certificate</strong>; trình duyệt kiểm tra chữ ký của CA — đây là bước chống trang giả mạo.</li><li>Hai bên dùng mã hoá bất đối xứng để <strong>thống nhất một khoá phiên</strong> (khoá đối xứng), rồi từ đó về sau nói chuyện hoàn toàn bằng khoá phiên này — vì nó nhanh.</li></ol><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Như gặp đối tác lần đầu: xem CMND có công chứng (certificate) → hai bên kín đáo thống nhất một 'ám hiệu riêng' (khoá phiên) → từ đó nói chuyện bằng ám hiệu cho nhanh.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> TLS chỉ bảo vệ dữ liệu <em>trên đường truyền</em>. Dữ liệu nằm trong database không được TLS mã hoá — muốn thì phải mã hoá lúc lưu (encryption at rest). Và ổ khoá HTTPS không có nghĩa website 'đáng tin' — trang lừa đảo cũng mua được certificate.</div>",
  refs:[["Cloudflare – TLS","https://www.cloudflare.com/learning/ssl/transport-layer-security-tls/"]] },

{ id:"web-009", t:"Web & HTTP", lv:"junior", core:true,
  tags:["cors","browser","same-origin"],
  q:"CORS là gì? Vì sao FE gọi API cứ bị lỗi CORS, và sửa ở đâu?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Origin</strong> (nguồn gốc) = bộ ba <em>giao thức + tên miền + cổng</em>, vd <code>https://shop.vn:443</code>. Chỉ cần lệch một trong ba (khác domain, khác port, http vs https) là bị coi là 'khác origin'.</div><p>Chuyện bắt đầu từ một luật an ninh có sẵn trong mọi trình duyệt — <strong>Same-Origin Policy</strong>: JavaScript của trang A <em>không được đọc</em> dữ liệu trả về từ origin B khác. Luật này chống kiểu tấn công: bạn đang đăng nhập ngân hàng, mở nhầm trang xấu, JS của trang xấu lặng lẽ gọi API ngân hàng bằng cookie của bạn và đọc số dư.</p><p>Nhưng có nhu cầu chính đáng: FE ở <code>localhost:3000</code> gọi API ở <code>localhost:8080</code> — khác port nên là khác origin! <strong>CORS</strong> (Cross-Origin Resource Sharing) là cơ chế để <em>server tuyên bố nới luật</em>: trong response, server thêm header <code>Access-Control-Allow-Origin: http://localhost:3000</code>, nghĩa là 'origin này là bạn tôi, trình duyệt cho nó đọc nhé'.</p><p>Với request 'không đơn giản' (vd có header <code>Authorization</code>, hoặc method PUT/DELETE), trình duyệt còn cẩn thận gửi trước một request thăm dò — <strong>preflight</strong> (method OPTIONS) — hỏi server 'tôi sắp gửi cái này, được không?', server gật thì request thật mới được gửi.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Chung cư (trình duyệt) mặc định không cho khách lạ (JS trang khác) lên căn hộ (API). Muốn cho lên, <em>chủ hộ</em> (server) phải đăng ký tên khách với bảo vệ trước — đó là header CORS.</div><div class='qb-gotcha'><b>⚠ Hiểu sai kinh điển:</b> (1) Lỗi CORS <strong>sửa ở server</strong> (thêm config cho phép origin của FE), không phải sửa ở FE. (2) CORS là luật <em>của trình duyệt</em> — gọi bằng Postman/curl không bao giờ dính CORS. Nên 'Postman gọi được mà web không gọi được' chính là dấu hiệu thiếu config CORS.</div>",
  refs:[["MDN – CORS","https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS"]] },

{ id:"web-010", t:"Web & HTTP", lv:"middle", core:false,
  tags:["cache","etag","performance"],
  q:"HTTP caching hoạt động thế nào? <code>Cache-Control</code>, <code>ETag</code>, mã 304 liên quan gì nhau?",
  a:"<p>Ý tưởng: thứ gì không đổi thì đừng tải lại — đỡ chậm, đỡ tốn băng thông, đỡ tải cho server. HTTP có hai chiến lược cache, dùng phối hợp:</p><p><strong>1. Cache theo thời hạn — khỏi hỏi luôn.</strong> Server trả header <code>Cache-Control: max-age=3600</code> nghĩa là 'trong 1 giờ tới, cứ dùng bản này, khỏi hỏi tôi'. Trong 1 giờ đó trình duyệt lấy từ cache local, <em>không gửi request nào cả</em> — nhanh nhất có thể.</p><p><strong>2. Cache có kiểm tra lại — hỏi nhưng hỏi rẻ.</strong> Hết hạn rồi thì sao, tải lại từ đầu? Chưa cần. Lần đầu server trả kèm <code>ETag: \"abc123\"</code> — như 'mã vân tay' của nội dung. Lần sau trình duyệt hỏi kèm <code>If-None-Match: \"abc123\"</code>, nghĩa là 'tôi đang giữ bản vân tay abc123, còn xài được không?'. Nội dung chưa đổi → server chỉ trả <strong><code>304 Not Modified</code></strong> với body <em>rỗng</em> — 'xài tiếp đi' — thay vì gửi lại cả file. Nội dung đổi rồi → trả <code>200</code> kèm bản mới + ETag mới.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hỏi thời khoá biểu: cách 1 — cô giáo dặn 'tuần này không đổi, đừng hỏi nữa' (max-age). Cách 2 — bạn nhắn 'lịch của em bản ngày 12, có mới hơn không?', cô chỉ nhắn lại 'vẫn thế' (304) thay vì chép lại cả bảng.</div><div class='qb-gotcha'><b>⚠ Đáng nói trong phỏng vấn:</b> 304 tiết kiệm băng thông nhưng <em>vẫn tốn một vòng hỏi-đáp</em>. Nên thực tế frontend hay dùng chiêu: đặt tên file có mã hash (<code>app.9f8e2.js</code>) + <code>max-age</code> một năm — nội dung đổi thì <em>tên file đổi</em>, coi như cache tự vô hiệu, không cần hỏi ai bao giờ.</div>",
  refs:[["MDN – HTTP caching","https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching"]] },

{ id:"web-011", t:"Web & HTTP", lv:"middle", core:false,
  tags:["http2","performance","multiplexing"],
  q:"HTTP/2 cải tiến gì so với HTTP/1.1?",
  a:"<p>Vấn đề của <strong>HTTP/1.1</strong>: trên một kết nối TCP, tại một thời điểm chỉ 'chở' được <em>một</em> request-response. Trang web hiện đại cần tải hàng chục file (CSS, JS, ảnh...) — xếp hàng lần lượt thì quá chậm, nên trình duyệt phải 'chữa cháy' bằng cách mở ~6 kết nối song song tới mỗi domain. Vẫn chậm và tốn kém.</p><p><strong>HTTP/2</strong> sửa tận gốc bằng <strong>multiplexing</strong> (ghép kênh): trên <em>một</em> kết nối TCP duy nhất, nhiều request/response được cắt nhỏ thành khung (frame), đánh số luồng, gửi <em>xen kẽ đồng thời</em> rồi ráp lại ở đầu kia. Hàng chục file tải song song trên một kết nối — không phải xếp hàng, không phải mở 6 kết nối.</p><p>Kèm theo: <strong>nén header</strong> (HPACK — headers lặp đi lặp lại giữa các request được nén thay vì gửi nguyên văn mỗi lần) và chuyển từ định dạng chữ (text) sang <strong>nhị phân</strong> (binary) — máy parse nhanh và ít lỗi hơn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> HTTP/1.1 như quầy có một nhân viên phục vụ xong khách này mới tới khách kia. HTTP/2 như nhân viên nhận cùng lúc nhiều đơn, làm xen kẽ, đơn nào xong phần nào giao phần đó.</div><div class='qb-gotcha'><b>⚠ Điểm cộng khi trả lời:</b> HTTP/2 chưa hết nghẽn hoàn toàn — nếu <em>một gói TCP</em> bị mất, cả đoàn frame phía sau phải chờ (head-of-line blocking ở tầng TCP, vì TCP bắt buộc đúng thứ tự). <strong>HTTP/3</strong> giải quyết bằng cách bỏ TCP, chạy trên QUIC/UDP — luồng nào mất gói chỉ luồng đó chờ.</div>",
  refs:[["web.dev – HTTP/2","https://web.dev/articles/performance-http2"]] },

{ id:"web-012", t:"REST API", lv:"junior", core:true,
  tags:["api","co-ban"],
  q:"API là gì? Nói 'viết API' trong công việc backend nghĩa là làm gì?",
  a:"<p><strong>API</strong> (Application Programming Interface) là <em>bản hợp đồng cho phần mềm gọi phần mềm</em>: quy định rõ 'muốn tôi làm việc X thì gọi thế này, đưa dữ liệu dạng này, tôi trả về dạng này'. Người gọi <em>không cần biết bên trong làm sao</em> — chỉ cần biết hợp đồng.</p><p>Từ 'API' rất rộng: một class Java phơi ra các method public — đó cũng là API; thư viện bạn import cũng có API của nó. Nhưng trong công việc backend, 'viết API' thường mang nghĩa hẹp: <strong>Web API</strong> — điểm truy cập qua mạng để frontend, app mobile, hay hệ thống khác gọi vào bằng HTTP.</p><p>Cụ thể khi bạn 'viết một API' trong Spring, bạn làm các việc: định nghĩa <strong>endpoint</strong> (đường dẫn + method, vd <code>GET /api/orders/42</code>), nhận và <strong>kiểm tra dữ liệu vào</strong> (validate), gọi tầng nghiệp vụ xử lý, rồi trả <strong>response</strong> (thường là JSON + status code). Tập hợp nhiều endpoint như vậy tạo thành API của hệ thống.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> API như <em>menu nhà hàng</em>: liệt kê rõ món gì gọi được và cần báo gì (size, topping). Bạn gọi món theo menu, bếp làm sao mặc bếp — bạn không cần (và không được) vào bếp. Menu đổi món = API đổi hợp đồng = mọi người gọi theo cách cũ sẽ gãy, nên hợp đồng phải ổn định.</div><div class='qb-gotcha'><b>⚠ Phân biệt cho rõ:</b> API là <em>hợp đồng</em>, REST là <em>một phong cách thiết kế</em> hợp đồng đó (dùng HTTP method + URL danh từ), còn JSON là <em>định dạng dữ liệu</em> trao đổi. Ba khái niệm ba tầng khác nhau — trộn lẫn khi trả lời là lộ ngay người học vẹt.</div>",
  refs:[["AWS – What is an API?","https://aws.amazon.com/what-is/api/"]] },

{ id:"web-013", t:"REST API", lv:"junior", core:true,
  tags:["json","data-format"],
  q:"JSON là gì? Vì sao API hầu như đều dùng JSON?",
  a:"<p><strong>JSON</strong> (JavaScript Object Notation) là định dạng <em>văn bản thuần</em> để biểu diễn dữ liệu có cấu trúc. Toàn bộ JSON chỉ có 6 loại giá trị: <strong>object</strong> <code>{\"ten\": \"An\"}</code> (tập hợp cặp khoá–giá trị), <strong>array</strong> <code>[1, 2, 3]</code> (danh sách), <strong>string</strong> (chuỗi trong nháy kép), <strong>number</strong>, <strong>true/false</strong>, và <strong>null</strong>. Lồng nhau thoải mái:</p><pre><code class='language-text'>{\n  \"id\": 42,\n  \"ten\": \"Nguyễn Văn An\",\n  \"daKichHoat\": true,\n  \"soThich\": [\"đá bóng\", \"đọc sách\"]\n}</code></pre><p>Vì sao thống trị? <strong>Người đọc hiểu ngay</strong> (debug dễ), <strong>ngôn ngữ nào cũng parse được</strong> (Java, Python, JS... đều có sẵn thư viện), <strong>gọn hơn hẳn XML</strong> (định dạng phổ biến trước đó — cùng dữ liệu, XML dài gấp rưỡi vì thẻ đóng mở). Trong Java/Spring, thư viện Jackson tự động chuyển JSON ↔ object Java cho bạn (quá trình này gọi là serialize/deserialize).</p><div class='qb-eli5'><b>🌱 Hình dung:</b> JSON như tờ khai thông tin viết theo mẫu chuẩn quốc tế — cơ quan nào (ngôn ngữ nào) cũng đọc được, không cần phiên dịch riêng.</div><div class='qb-gotcha'><b>⚠ Hai bẫy thực chiến:</b> (1) JSON <em>không có kiểu ngày tháng</em> — mọi người quy ước truyền string chuẩn ISO-8601 (<code>\"2026-07-02T10:30:00Z\"</code>); tự chế format ngày là mầm hoạ. (2) JavaScript chỉ biểu diễn chính xác số nguyên tới 2^53 — id kiểu <code>Long</code> của Java to hơn mức đó gửi xuống FE sẽ bị <em>sai lệch âm thầm</em> → id lớn nên trả về dạng string.</div>",
  refs:[["json.org","https://www.json.org/json-en.html"]] },

{ id:"web-014", legacy:5, t:"REST API", lv:"junior", core:true,
  tags:["rest","api-design"],
  q:"REST là gì? Kể vài nguyên tắc thiết kế REST API tốt.",
  a:"<p><strong>REST</strong> là một <em>phong cách thiết kế</em> Web API (không phải luật hay công nghệ), với tư tưởng cốt lõi: mô hình hoá hệ thống thành các <strong>resource</strong> (tài nguyên — đơn hàng, người dùng, sản phẩm...), mỗi resource có địa chỉ URL riêng, và thao tác lên nó bằng <em>đúng nghĩa</em> của HTTP method.</p><p>Các nguyên tắc thực dụng nhất khi thiết kế:</p><ul><li><strong>URL là danh từ, số nhiều</strong> — <code>/orders</code>, <code>/orders/42</code>. Hành động nằm ở method, không nằm trong URL: dùng <code>DELETE /orders/42</code> chứ không phải <code>POST /deleteOrder?id=42</code>.</li><li><strong>Dùng method đúng vai</strong>: GET đọc, POST tạo, PUT/PATCH sửa, DELETE xoá (chi tiết ở câu về method).</li><li><strong>Status code đúng nghĩa</strong>: tạo xong trả 201, không tìm thấy trả 404 — đừng cái gì cũng 200.</li><li><strong>Quan hệ lồng nhau thể hiện qua đường dẫn</strong>: <code>/orders/42/items</code> — các item của đơn 42.</li><li><strong>Đọc danh sách phải có phân trang, lọc, sắp xếp</strong> qua query param: <code>/orders?status=paid&amp;page=2&amp;sort=-createdAt</code>.</li><li><strong>Version API</strong> để nâng cấp không làm gãy client cũ: <code>/api/v1/orders</code>.</li><li><strong>Lỗi trả về có cấu trúc nhất quán</strong> (mã lỗi, thông điệp, chi tiết field nào sai) — client xử lý được bằng code thay vì đoán.</li></ul><div class='qb-eli5'><b>🌱 Ghi nhớ:</b> Thiết kế REST tốt giống hệ thống biển báo trong toà nhà lớn: quy tắc đặt tên nhất quán đến mức người mới nhìn địa chỉ là đoán được bên trong có gì và cư xử thế nào.</div><div class='qb-gotcha'><b>⚠ Bẫy phỏng vấn:</b> REST không có 'cảnh sát' — không tuân cũng chẳng ai bắt. Giá trị thật là <strong>tính nhất quán và dễ đoán</strong>. Nếu bị hỏi 'API thế nào là RESTful?', nói thêm được REST nguyên bản của Roy Fielding còn có các ràng buộc như stateless, cacheable, uniform interface là điểm cộng.</div>",
  refs:[["restfulapi.net","https://restfulapi.net/"]] },

{ id:"web-015", t:"REST API", lv:"junior", core:true,
  tags:["api-design","path","query-param"],
  q:"Path variable, query parameter, body, header — dữ liệu nào nên đặt ở đâu?",
  a:"<p>Một request HTTP có 4 'ngăn' chứa dữ liệu, mỗi ngăn một vai:</p><ul><li><strong>Path variable</strong> — nằm trong đường dẫn: <code>/orders/<b>42</b></code>. Dành cho thứ <em>định danh</em> resource: id, mã. Thiếu nó thì URL không trỏ vào đâu cả.</li><li><strong>Query parameter</strong> — sau dấu <code>?</code>: <code>/orders?<b>status=paid&amp;page=2</b></code>. Dành cho <em>tuỳ chọn khi đọc</em>: lọc, tìm kiếm, phân trang, sắp xếp. Đặc điểm: bỏ đi vẫn hợp lệ (có giá trị mặc định).</li><li><strong>Body</strong> — khối dữ liệu đính kèm (thường JSON). Dành cho <em>nội dung</em> cần tạo/sửa trong POST, PUT, PATCH — dữ liệu nhiều, có cấu trúc lồng nhau.</li><li><strong>Header</strong> — thông tin <em>về</em> request chứ không phải nội dung nghiệp vụ: xác thực (<code>Authorization</code>), định dạng dữ liệu (<code>Content-Type</code>), mã theo dõi request (trace id)...</li></ul><p>Ví dụ tổng hợp — sửa địa chỉ giao của đơn 42:</p><pre><code class='language-text'>PATCH /api/v1/orders/42        ← path: đơn nào\nAuthorization: Bearer eyJhb... ← header: tôi là ai\nContent-Type: application/json\n\n{ \"diaChiGiao\": \"12 Lý Thường Kiệt\" }   ← body: sửa thành gì</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Gửi bưu phẩm: địa chỉ nhà (path) — thiếu là không giao được; ghi chú 'giao giờ hành chính' (query) — không ghi cũng chẳng sao; món đồ trong hộp (body); thông tin người gửi ngoài vỏ hộp (header).</div><div class='qb-gotcha'><b>⚠ Hai lỗi hay gặp:</b> (1) Nhét token/password vào query param — query <em>bị ghi vào access log, lịch sử trình duyệt</em> → lộ bí mật. Thông tin nhạy cảm đi vào header hoặc body. (2) GET có body — chuẩn HTTP không cấm hẳn nhưng nhiều proxy/thư viện bỏ qua body của GET → tìm kiếm phức tạp quá thì đổi sang <code>POST /search</code>.</div>",
  refs:[["MDN – URL structure","https://developer.mozilla.org/en-US/docs/Learn_web_development/Howto/Web_mechanics/What_is_a_URL"]] },

{ id:"web-016", t:"Auth cơ bản", lv:"junior", core:true,
  tags:["authentication","authorization","security"],
  q:"Authentication khác Authorization thế nào? Liên hệ với mã 401 và 403?",
  a:"<p>Hai từ tiếng Anh na ná nhau nhưng là hai câu hỏi hoàn toàn khác nhau, xảy ra theo thứ tự:</p><p><strong>Authentication (xác thực — 'AuthN') = Bạn LÀ AI?</strong> Chứng minh danh tính: nhập đúng username/password, quét vân tay, gửi token hợp lệ. Đây là bước <em>đăng nhập</em>.</p><p><strong>Authorization (phân quyền — 'AuthZ') = Bạn ĐƯỢC LÀM GÌ?</strong> Biết bạn là ai rồi, giờ xem bạn có quyền làm việc đang xin không: user thường được xem đơn <em>của mình</em>, nhưng xoá user khác thì phải là admin.</p><p>Map thẳng vào status code:</p><ul><li><strong>401 Unauthorized</strong> = trượt <em>authentication</em> — chưa đăng nhập, token thiếu/sai/hết hạn. (Tên mã hơi gây nhiễu — bản chất nó là 'Unauthenticated'.)</li><li><strong>403 Forbidden</strong> = trượt <em>authorization</em> — server biết rõ bạn là ai, nhưng vai của bạn không đủ quyền làm việc này. Đăng nhập lại cũng vô ích.</li></ul><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Đi concert: soát vé ở cổng = authentication (vé thật hay giả?). Vào rồi nhưng khu VIP yêu cầu vòng tay VIP = authorization (vé thường thì đứng khu thường). Không vé → 401. Có vé thường đòi vào VIP → 403.</div><div class='qb-gotcha'><b>⚠ Điểm cộng phỏng vấn:</b> Trong Spring Security, hai bước này tách rõ: authentication do <code>AuthenticationManager</code>/filter lo (kiểm token, đăng nhập); authorization do rule trên endpoint hoặc <code>@PreAuthorize(\"hasRole('ADMIN')\")</code> trên method. Nói được mapping này chứng tỏ bạn không chỉ thuộc định nghĩa.</div>",
  refs:[["Auth0 – AuthN vs AuthZ","https://auth0.com/docs/get-started/identity-fundamentals/authentication-and-authorization"]] },

{ id:"web-017", t:"Auth cơ bản", lv:"junior", core:true,
  tags:["jwt","token","security"],
  q:"JWT trông như thế nào bên trong? Vì sao server xác minh được mà không cần lưu gì?",
  a:"<p><strong>JWT</strong> (JSON Web Token, đọc là 'jot') là một chuỗi gồm <strong>3 phần, ngăn bằng dấu chấm</strong>: <code>xxxxx.yyyyy.zzzzz</code></p><ol><li><strong>Header</strong> — JSON nhỏ ghi loại token và thuật toán ký (vd <code>HS256</code>), được encode base64.</li><li><strong>Payload</strong> — JSON chứa các <strong>claim</strong> (thông tin khẳng định): <code>sub</code> (id người dùng), <code>exp</code> (hết hạn lúc nào), <code>roles</code> (quyền)... cũng encode base64.</li><li><strong>Signature (chữ ký)</strong> — phần quyết định: lấy hai phần trên + một <strong>khoá bí mật chỉ server biết</strong>, đưa qua thuật toán ký, ra chữ ký. Ai sửa dù 1 ký tự của payload thì chữ ký tính lại sẽ <em>không khớp</em>.</li></ol><p><strong>Vì sao không cần lưu?</strong> Khi token quay lại, server chỉ làm phép tính: ký lại header+payload bằng khoá bí mật của mình rồi so với chữ ký đính kèm. Khớp → token do chính mình phát ra, nội dung nguyên vẹn → <em>tin được payload</em>, lấy user id từ đó ra dùng luôn. Không khớp → giả mạo, chặn. Toàn bộ là phép tính, không cần tra bảng session nào cả — đó là tính <strong>stateless</strong> của JWT.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> JWT như giấy giới thiệu có <em>mộc đỏ</em>: nội dung ghi rõ bạn là ai, được làm gì; mộc chống sửa chữa. Bảo vệ chỉ cần soi mộc thật/giả là xong, khỏi gọi điện về công ty xác minh (khỏi tra DB).</div><div class='qb-gotcha'><b>⚠ Ba bẫy kinh điển:</b> (1) Payload chỉ được <em>encode base64</em> — ai cũng decode đọc được, KHÔNG phải mã hoá → đừng nhét thông tin nhạy cảm. (2) Chữ ký chống <em>sửa</em>, không chống <em>hết hạn</em> — server phải tự check claim <code>exp</code>. (3) JWT phát ra rồi thì <em>khó thu hồi từng cái</em> (server có lưu đâu mà xoá) → thực tế dùng access token sống ngắn (5–15 phút) + refresh token sống dài có thể thu hồi.</div>",
  refs:[["jwt.io – Introduction","https://jwt.io/introduction"]] },

{ id:"web-018", t:"Hạ tầng web", lv:"junior", core:false,
  tags:["nginx","tomcat","reverse-proxy"],
  q:"Nginx và Tomcat khác nhau vai trò gì? Reverse proxy là gì?",
  a:"<p>Hai anh này hay đứng cạnh nhau trong một hệ thống nhưng làm hai việc khác nhau:</p><p><strong>Tomcat</strong> là <em>application server</em> (máy chủ ứng dụng) cho Java: nó chạy <em>code của bạn</em> — nhận request, gọi vào servlet/Spring controller, thực thi logic, trả kết quả. Spring Boot nhúng sẵn Tomcat bên trong, nên <code>java -jar app.jar</code> là có web server chạy code bạn luôn.</p><p><strong>Nginx</strong> là <em>web server / reverse proxy</em> đứng <em>trước</em> ứng dụng: nhận mọi request từ Internet trước, rồi mới chuyển tiếp vào trong. Nó không chạy logic nghiệp vụ của bạn, nhưng làm cực tốt các việc: trả file tĩnh (ảnh, css, js), <strong>TLS termination</strong> (gánh phần giải mã HTTPS), <strong>load balancing</strong> (chia request cho nhiều instance app), nén dữ liệu, chặn bớt request rác.</p><div class='qb-term'><b>📖 Từ khó:</b> <strong>Reverse proxy</strong> = 'người đại diện phía server': client tưởng đang nói chuyện với hệ thống, thực ra chỉ nói chuyện với proxy; proxy chuyển tiếp vào các server thật giấu phía sau. (Ngược với forward proxy — đại diện phía <em>client</em>, vd proxy công ty để nhân viên ra Internet.)</div><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Nginx là <em>lễ tân + bảo vệ toà nhà</em>: đón mọi khách, hỏi giấy tờ, chỉ đường lên đúng phòng ban (và cân bằng khách giữa các quầy). Tomcat là <em>nhân viên trong phòng</em> thật sự xử lý hồ sơ.</div><div class='qb-gotcha'><b>⚠ Câu hay hỏi nối:</b> 'Vì sao không cho Tomcat ra thẳng Internet?' — được về kỹ thuật, nhưng nginx phía trước cho: một chỗ quản TLS/certificate, load balance nhiều instance, trả static file nhanh hơn, và thêm một lớp đệm an ninh. Mã 502 Bad Gateway chính là 'nginx gọi vào trong nhưng app chết' — hiểu chuỗi này giúp bạn debug production.</div>",
  refs:[["Nginx – Reverse proxy","https://docs.nginx.com/nginx/admin-guide/web-server/reverse-proxy/"]] },

{ id:"web-019", legacy:12, t:"Git", lv:"junior", core:true,
  tags:["git","staging","commit"],
  q:"Giải thích working directory, staging area (index) và commit. <code>git add</code> khác <code>git commit</code> ra sao?",
  a:"<p>Git chia chỗ chứa code của bạn thành 3 'vùng', và hiểu 3 vùng này thì mọi lệnh git đột nhiên có nghĩa:</p><ol><li><strong>Working directory</strong> — thư mục thật trên máy, nơi bạn đang gõ code. Sửa file tức là đang thay đổi vùng này.</li><li><strong>Staging area</strong> (còn gọi là <em>index</em>) — 'khay chờ': nơi bạn <em>gom những thay đổi sẽ đưa vào lần chụp tới</em>. Nó tồn tại để bạn được <em>chọn lọc</em> — sửa 5 file nhưng chỉ muốn commit 2 file liên quan nhau? Chỉ add 2 file đó.</li><li><strong>Repository</strong> (kho <code>.git</code>) — nơi lưu lịch sử: mỗi <strong>commit</strong> là một 'ảnh chụp' toàn bộ dự án tại một thời điểm, kèm thông điệp mô tả, tạo thành chuỗi lịch sử quay lại được.</li></ol><p>Vậy: <code>git add file</code> = đặt thay đổi của file đó lên <em>khay chờ</em>. <code>git commit</code> = <em>chụp ảnh những gì đang ở trên khay</em> và lưu vào lịch sử. Thay đổi chưa add thì commit không dính — đây chính là điều làm người mới bối rối 'sao sửa rồi mà commit không có gì?'.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Chụp ảnh kỷ yếu: cả lớp đứng lộn xộn trong sân (working directory) → thầy gọi những bạn sẽ vào khung hình xếp vào hàng (git add — staging) → bấm máy (git commit). Ai chưa được gọi vào hàng thì không có mặt trong tấm ảnh.</div><div class='qb-gotcha'><b>⚠ Lệnh soi 3 vùng:</b> <code>git status</code> cho biết file nào đang ở vùng nào; <code>git diff</code> xem khác biệt working vs staging; <code>git diff --staged</code> xem khay chờ vs commit gần nhất. Phỏng vấn hỏi 'commit một phần thay đổi thế nào?' → trả lời <code>git add -p</code> (chọn từng đoạn) là điểm cộng.</div>",
  refs:[["Git – Recording changes","https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository"]] },

{ id:"web-020", t:"Git", lv:"junior", core:true,
  tags:["git","pull","fetch"],
  q:"<code>git pull</code> khác <code>git fetch</code> thế nào? Vì sao nhiều người khuyên fetch trước?",
  a:"<p>Cả hai đều 'lấy code mới từ remote (vd GitHub) về', khác nhau ở mức độ can thiệp vào code của bạn:</p><p><strong><code>git fetch</code></strong> — chỉ <em>tải thông tin về</em>: cập nhật bản sao của các nhánh remote (vd <code>origin/main</code>) trên máy bạn. <strong>Không đụng gì</strong> vào nhánh và working directory của bạn. Sau khi fetch, bạn có thể ung dung xem trước: <code>git log main..origin/main</code> (họ có commit gì mới), <code>git diff main origin/main</code> (khác gì code mình).</p><p><strong><code>git pull</code></strong> = <code>git fetch</code> + <strong>gộp luôn</strong> vào nhánh hiện tại (mặc định bằng merge). Tiện, nhưng là hành động 'mua hàng không xem trước': nếu remote có thay đổi xung đột, bạn dính conflict ngay giữa lúc đang dở tay.</p><p>Vì thế lời khuyên phổ biến: <strong>fetch để nhìn trước, rồi chủ động quyết định</strong> merge hay rebase. Còn khi bạn hiểu rõ nhánh của mình, <code>git pull --rebase</code> là thói quen được nhiều team chuộng: kéo commit mới về rồi 'đặt lại' các commit local của bạn lên trên, giữ lịch sử thẳng, không sinh merge commit vặt.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> fetch = xem trước menu món mới qua cửa kính; pull = gọi món và bị dọn thẳng lên bàn ăn dở của bạn — hên thì ngon, xui thì đổ lẫn vào đĩa đang ăn (conflict).</div><div class='qb-gotcha'><b>⚠ Hiểu nhầm hay gặp:</b> 'fetch xong sao code chưa đổi?' — đúng thiết kế đấy! Fetch chỉ cập nhật <em>bản theo dõi</em> <code>origin/main</code>, còn nhánh <code>main</code> của bạn đứng yên cho tới khi bạn merge/rebase. Hai cái tên giống nhau nhưng là hai nhánh khác nhau.</div>",
  refs:[["Atlassian – git fetch","https://www.atlassian.com/git/tutorials/syncing/git-fetch"]] },

{ id:"web-021", legacy:13, t:"Git", lv:"junior", core:false,
  tags:["git","merge","rebase"],
  q:"<code>git merge</code> và <code>git rebase</code> khác gì? Khi nào dùng cái nào?",
  a:"<p>Tình huống: bạn tách nhánh <code>feature</code> từ <code>main</code>, code vài commit; trong lúc đó <code>main</code> cũng có commit mới. Giờ muốn gom hai dòng lịch sử lại — có hai cách:</p><p><strong>Merge</strong> — 'gộp và ghi nhận sự thật': tạo một <strong>merge commit</strong> có 2 cha, nối hai nhánh lại. Lịch sử giữ nguyên vẹn <em>chuyện đã xảy ra</em>: thấy rõ chỗ rẽ nhánh, chỗ nhập lại. An toàn tuyệt đối vì <em>không viết lại gì</em> — nhược điểm là lịch sử nhiều nhánh chằng chịt như sơ đồ tàu điện.</p><p><strong>Rebase</strong> — 'viết lại cho đẹp': nhấc <em>từng commit</em> của bạn ra, đặt lần lượt lên <em>đỉnh</em> của <code>main</code> mới, như thể bạn bắt đầu code từ hôm nay. Lịch sử thành <em>một đường thẳng</em> dễ đọc. Nhưng chú ý: các commit được 'đặt lại' là <strong>commit mới tinh</strong> (hash đổi hết) — bản chất là viết lại lịch sử.</p><p><strong>Quy tắc vàng: KHÔNG rebase nhánh mà người khác đang dùng chung.</strong> Viết lại lịch sử đã đẩy lên chung = lịch sử máy đồng đội với remote lệch nhau = một buổi sáng gỡ rối. Công thức thực dụng của đa số team: <em>rebase nhánh feature cá nhân</em> cho gọn trước khi mở PR; <em>merge (hoặc squash-merge) vào main</em>.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Merge như nộp nhật ký công việc nguyên bản — kể cả những đoạn chồng chéo. Rebase như chép lại nhật ký cho mạch lạc trước khi nộp — dễ đọc hơn, nhưng đừng chép lại cuốn sổ mà cả phòng đang cùng ghi.</div><div class='qb-gotcha'><b>⚠ Câu bẫy phỏng vấn:</b> 'Rebase xong push bị từ chối, làm sao?' — vì hash đổi nên phải <code>git push --force-with-lease</code> (an toàn hơn <code>--force</code> vì nó từ chối đè nếu remote có commit mới của người khác). Trả lời '--force' trống không dễ bị trừ điểm.</div>",
  refs:[["Atlassian – Merging vs Rebasing","https://www.atlassian.com/git/tutorials/merging-vs-rebasing"]] },

{ id:"web-022", t:"Git", lv:"junior", core:false,
  tags:["git","reset","revert","undo"],
  q:"Lỡ commit sai — <code>git revert</code> khác <code>git reset</code> thế nào? Khi nào dùng cái nào?",
  a:"<p>Hai cách 'hoàn tác' với hai triết lý ngược nhau:</p><p><strong><code>git revert &lt;commit&gt;</code> — sửa sai bằng cách ghi thêm.</strong> Git tạo một <em>commit mới</em> có nội dung ngược lại commit sai (bên kia thêm dòng nào, bên này xoá dòng đó). Lịch sử cũ còn nguyên, chỉ dài thêm một mắt xích 'undo'. Vì <em>không viết lại lịch sử</em>, nó an toàn trên nhánh chung — đây là cách chuẩn để gỡ một thay đổi đã đẩy lên <code>main</code>.</p><p><strong><code>git reset &lt;commit&gt;</code> — sửa sai bằng cách xoá sổ.</strong> Kéo con trỏ nhánh lùi về commit chỉ định, những commit sau đó <em>biến khỏi lịch sử nhánh</em>. Ba mức độ, khác nhau ở chỗ 'giữ lại được gì':</p><ul><li><code>--soft</code>: chỉ lùi lịch sử; thay đổi của các commit bị gỡ được <em>giữ nguyên trên khay staging</em> — hay dùng để gom nhiều commit vụn thành một.</li><li><code>--mixed</code> (mặc định): lùi lịch sử; thay đổi giữ trong working directory nhưng rời khỏi staging.</li><li><code>--hard</code>: lùi lịch sử và <em>vứt sạch thay đổi</em> — code chưa commit mất luôn. Lệnh nguy hiểm nhất trong bộ.</li></ul><p><strong>Chọn thế nào:</strong> commit đã đẩy lên nhánh chung → <code>revert</code>. Commit còn nằm trên máy mình/nhánh cá nhân → <code>reset</code> thoải mái, lịch sử sạch hơn.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Ghi sổ kế toán: revert = ghi thêm một dòng bút toán điều chỉnh (sổ sách minh bạch, ai cũng thấy); reset = xé trang — chỉ được xé sổ nháp của mình, không được xé sổ cái công ty.</div><div class='qb-gotcha'><b>⚠ Cứu mạng khi lỡ tay:</b> lỡ <code>reset --hard</code> nhầm? <code>git reflog</code> ghi lại <em>mọi</em> vị trí HEAD từng đứng — tìm hash cũ rồi <code>git reset --hard &lt;hash&gt;</code> để quay về. Biết reflog trong phỏng vấn là điểm cộng 'đã từng trầy trật thực chiến'.</div>",
  refs:[["Atlassian – Resetting, checking out & reverting","https://www.atlassian.com/git/tutorials/resetting-checking-out-and-reverting"]] },

{ id:"web-023", legacy:14, t:"Git", lv:"junior", core:false,
  tags:["git","merge-conflict"],
  q:"Merge conflict xảy ra khi nào? Xử lý theo các bước ra sao?",
  a:"<p><strong>Conflict xảy ra khi</strong> hai nhánh cùng sửa <em>một vùng dòng của cùng một file</em> (hoặc một bên sửa, một bên xoá file). Git tự gộp được khi các thay đổi nằm chỗ khác nhau; đụng cùng chỗ thì nó không dám đoán ai đúng — nó dừng lại và hỏi bạn.</p><p>Khi đó file có vùng đánh dấu:</p><pre><code class='language-text'>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD\ngiá = tính_giá_khuyến_mãi(đơn);     ← bản của nhánh bạn đang đứng\n=======\ngiá = tính_giá_theo_hạng_thành_viên(đơn);   ← bản của nhánh kia\n&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature/membership</code></pre><p><strong>Các bước xử lý bình tĩnh:</strong></p><ol><li><code>git status</code> — xem danh sách file đang conflict.</li><li>Mở từng file, với mỗi vùng: chọn bản đúng, hoặc <em>tự viết bản gộp cả hai ý</em> (trường hợp thường gặp nhất — đây là lúc phải hiểu nghiệp vụ, không phải chọn bừa một bên). Xoá sạch 3 dòng marker.</li><li>Chạy thử/compile lại — gộp xong mà không chạy thì gộp vô nghĩa.</li><li><code>git add</code> file đã sửa (báo 'tôi giải quyết xong file này').</li><li><code>git commit</code> (merge) hoặc <code>git rebase --continue</code> (rebase). Rối quá muốn làm lại từ đầu: <code>git merge --abort</code>.</li></ol><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Hai biên tập viên cùng sửa một câu trong bài báo theo hai hướng — tổng biên tập (bạn) phải đọc cả hai và quyết định câu cuối cùng, không máy nào quyết thay được.</div><div class='qb-gotcha'><b>⚠ Phòng hơn chữa:</b> conflict to là triệu chứng của nhánh sống quá lâu. Giảm bằng: thường xuyên kéo <code>main</code> mới nhất về nhánh mình, PR nhỏ, và tách file 'ai cũng đụng' (config khổng lồ) thành nhiều file nhỏ. Nói được ý phòng ngừa này là vượt mức junior.</div>",
  refs:[["Git – Basic branching & merging","https://git-scm.com/book/en/v2/Git-Branching-Basic-Branching-and-Merging"]] },

{ id:"web-024", legacy:15, t:"Linux", lv:"junior", core:true,
  tags:["linux","port","process","debug"],
  q:"Service không lên được vì 'port đã bị chiếm'. Bạn dùng lệnh Linux nào để tìm và xử lý?",
  a:"<div class='qb-term'><b>📖 Từ khó:</b> <strong>Process (tiến trình)</strong> = một chương trình đang chạy; mỗi process có mã số <strong>PID</strong>. <strong>Port</strong> = 'số cửa' trên máy để các kết nối mạng đi vào — mỗi port chỉ một process được 'đứng nghe' (listen). Lỗi <code>Address already in use</code> nghĩa là cửa 8080 đã có người đứng.</div><p><strong>Bước 1 — tìm ai đang chiếm port:</strong></p><pre><code class='language-text'>ss -ltnp | grep :8080\n# l = đang listen, t = TCP, n = hiện số, p = hiện process\n# hoặc:  lsof -i :8080</code></pre><p>Kết quả cho bạn tên process và PID, vd <code>java, pid=12345</code>.</p><p><strong>Bước 2 — xem nó là cái gì trước khi giết:</strong></p><pre><code class='language-text'>ps -fp 12345    # xem lệnh đầy đủ đang chạy — app cũ của mình treo? hay app của người khác?</code></pre><p><strong>Bước 3 — dừng đúng cách:</strong></p><pre><code class='language-text'>kill 12345      # gửi SIGTERM: 'xin anh dừng lại' — app được dọn dẹp (đóng connection, flush log) rồi thoát\nkill -9 12345   # SIGKILL: hệ điều hành khai tử ngay — chỉ dùng khi kill thường không ăn</code></pre><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Chỗ đậu xe của bạn có xe lạ: nhìn biển số (ss/lsof) → hỏi bảo vệ xe của ai (ps) → nhờ chủ xe đánh ra (kill) → cẩu xe đi (kill -9) chỉ khi chủ xe mất tích.</div><div class='qb-gotcha'><b>⚠ Vì sao đừng vung kill -9:</b> process không có cơ hội dọn dẹp — transaction dở dang, file ghi nửa chừng, connection không đóng. Luôn thử SIGTERM trước, chờ vài giây, bất đắc dĩ mới -9. Trả lời có trình tự (tìm → xem → dừng lịch sự → ép) ăn điểm hơn nhiều so với 'em kill -9'.</div>",
  refs:[["man ss","https://man7.org/linux/man-pages/man8/ss.8.html"]] },

{ id:"web-025", t:"Linux", lv:"junior", core:true,
  tags:["linux","log","grep","debug"],
  q:"File log nặng 2GB trên server. Dùng lệnh gì để tìm lỗi mà không làm treo máy?",
  a:"<p>Nguyên tắc số một: <strong>đừng mở cả file vào RAM</strong> — quên <code>vim</code>/<code>nano</code> với file 2GB đi. Bộ đồ nghề chuẩn:</p><p><strong>Xem đuôi file / theo dõi trực tiếp</strong> (lỗi vừa xảy ra thì nằm cuối file):</p><pre><code class='language-text'>tail -n 200 app.log          # 200 dòng cuối\ntail -f app.log              # bám theo log đang chạy (Ctrl+C để thoát)\ntail -f app.log | grep ERROR # chỉ hiện dòng có ERROR khi nó xuất hiện</code></pre><p><strong>Tìm kiếm trong cả file</strong> — <code>grep</code> đọc tuần tự từng dòng nên file to mấy cũng không tốn RAM:</p><pre><code class='language-text'>grep -n \"NullPointerException\" app.log   # -n kèm số dòng\ngrep -C 5 \"OutOfMemory\" app.log          # kèm 5 dòng trước/sau để có ngữ cảnh\ngrep -c ERROR app.log                     # đếm số dòng lỗi</code></pre><p><strong>Duyệt thong thả có cuộn</strong>: <code>less app.log</code> — chỉ nạp phần đang xem nên mở tức thì; phím <code>G</code> nhảy xuống cuối, <code>/tuKhoa</code> tìm, <code>q</code> thoát. Log đã nén? <code>zgrep ERROR app.log.gz</code> tìm thẳng khỏi giải nén.</p><div class='qb-eli5'><b>🌱 Ví dụ đời thường:</b> Tra một cuốn từ điển 2000 trang: không ai bê cả cuốn photo ra đọc (vim); người khôn lật đúng trang cần (less), hoặc nhờ người dò từng trang tìm đúng từ khoá (grep).</div><div class='qb-gotcha'><b>⚠ Combo thực chiến hay bị hỏi:</b> 'Đếm lỗi nào xuất hiện nhiều nhất?' — nối ống (pipe) các lệnh nhỏ: <code>grep ERROR app.log | awk '{print $5}' | sort | uniq -c | sort -rn | head</code>. Không cần thuộc lòng từng cờ, nhưng nói được tư duy 'ghép lệnh nhỏ thành dây chuyền' là đúng chất Linux.</div>",
  refs:[["man grep","https://man7.org/linux/man-pages/man1/grep.1.html"]] },

/* DATA_END */
] });
